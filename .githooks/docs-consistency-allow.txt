# 文档一致性门禁的白名单（AGENTS.md 规则 17）
#
# 只在"这条引用是**正当的历史陈述**"时才加进来，并且必须写明理由。
# 格式：每行 `<文件或目录前缀>::<正则片段>`，两者都匹配才放行。
# 不要用本文件掩盖"改一份漏一份"的漂移——那正是这个门禁要抓的东西。
#
# 示例（不要照抄）：
#   docs/features/F12-图库助手档3工具与联调.md::TerminalOperationTool
#
# 已有条目：
