# Handoff 0023 — 2026-09-15 — T8-review 收口 + AI 打标（T14–T16）

> 上一份：`0022`（T8-hard 循环收敛完成）。**本文件只记 0022 之后的增量**——档 3 的整体口径、真机命令、踩过的坑都在 0020/0021/0022，不在这里重复。
> 本会话 16 笔提交：`cd5ecc7` → `fdbcab7` → `b179452` → `29e8ee6` → `e12f929` → `ff7f65c` → `d0932c0` → `54562b4` → `7d9ea52` → `db41f30` → `69ac1e1` → `177439d` → `4bb6f63` → `c841fb7` → `b376d7c` → `19b7ad9`（`86a01d9..HEAD` 实测 16 笔）。

## 当前状态（一句话）

四件事：① **T8-hard 独立 review 收口**（有条件通过：1 条 P2 + 3 条 P3，全部登记在案）；② **T13 的"顺带"决策点**收口（删掉两个不注册的教学遗留工具——0022 曾把已完成的 T13 误列为待办，本会话接手时又重复了一次，已勘误并在快照里记了教训）；③ **T14** = review 的那条 P2（助手页面与 F11 的"只读不改"错误声明回填）；④ **AI 打标落地**（档 3 之后的新增能力：字据 → T15 后端服务 → T16 管理端入口）。工作区干净（HEAD `4bb6f63`）；**backend 8123 与 frontend 5173 仍在跑**（用户正在点 AI 打标）。

## 刚完成（按时间）

1. **T8-hard 独立 review**（`cd5ecc7`，规则 13 只读会话）：**有条件通过**。本体成立（三处协议缺口读旧代码确认为真；测试迁移是加强不是削弱——`ReActAgentTest` 两条"吞异常"用例改成"必须上抛"；越界为零）；独立复跑 `ai/agent` 全模块 **152 例全绿**（门禁排除 9 类 ⇒ 143，与文档口径一致）、前端 `type-check` 138 与基线持平且改动文件 0 命中。待办：**P2**（页面/F11 的"只读"声明）、**P3×3**（`runLoop` 上限提示未判 `state`；`AgentEvent.Error` 的 javadoc"本轮唯一"措辞过强；计划 §5 设计（`AgentExecutionException` + `state=FINISHED`）与实现（`state=ERROR`）不一致、"实例只跑一次"宜提到 `BaseAgent`）。
2. **T13 顺带项**（`fdbcab7` 代码 + `b179452` 文档，用户拍板"两个都删"）：删 `TerminalOperationTool`（可执行本机 shell）与 `ParallelSearchTool`（给本仓不存在的 DeepResearchAgent 写的）+ 各自测试共 4 文件；删前核实**零引用**（不在 `ToolRegistration`/`HuoshanToolFactory`）⇒ 行为零影响；`.githooks` 排除名单同步。门禁 **143 → 137 例**。**T13 至此真正收口**（主体 2026-09-14 就完成了，是 0022 的判断有误）。
3. **T14**（`29e8ee6` + `e12f929`，规则 3 先立条目）：`/assistant` 页面 subtitle 与空状态提示从"只读不改"改为如实描述（可读可改、改动须用户明确要求、不支持删除）；F11 补档 3 能力、核心流程图与已知限制 12 收口。`type-check` 净增 0、改动文件 eslint 干净。
4. **AI 打标**（本会话主体）：字据 `ff7f65c`（spec 关键三行增补 + plan T15-T17 + 两个待拍板项）→ 用户拍板"**只管理员手动触发**（不做定时）→ **两步式**（先建议、确认后落库）→ 普通用户助手**摘掉**只读打标"→ **T15 后端**（`d0932c0`：`api/vision/AiVisionTagApi` + `manager/ai/PictureAiTagManager` + 两个 `@AuthCheck(mustRole=ADMIN)` 端点 + `app.ai.vision.*` 配置；**只写 tags/category**，审核四字段结构性不写；看图有界并发=层次一）→ **T16 前端**（`db41f30`：`AiTagPictureModal.vue` + 管理页勾选与按钮）。文档：`54562b4`/`7d9ea52`/`69ac1e1` + 功能讲解 `docs/features/F14-AI打标.md`。
5. **现场真机验收时抓出并修掉一个真 bug**（`b376d7c` 代码 + `19b7ad9` 文档）：用户实测点「AI 打标」报"看图失败：系统错误"⇒ 根因是**管理页的 `a-table` 没配 `row-key`**（ant-design-vue 默认 `rowKey='key'`，图库记录没这个字段）⇒ 勾选给出的 key 是 `undefined` ⇒ 发出 `{"pictureIdList":["undefined"]}` ⇒ 后端 Jackson 解析 `List<Long>` 失败（日志原文 `Cannot deserialize ... from String "undefined"`）⇒ 落到 `GlobalExceptionHandler` 的泛 `RuntimeException` 兜底 ⇒ `50000 系统错误`。**修复**：表格补 `row-key="id"`；新增纯函数 `frontend/src/utils/pictureSelection.ts`（`toPictureIdList` 丢弃 `undefined`/空串/超安全整数范围的 number；`isPictureIdList` 校验），页面与弹窗两侧接入。**规则 12 先红后绿**：仓外脚本 `%TEMP%\t16-selection\check.mjs`（esbuild 转译真实纯函数、10 例）修复前红、修复后 10/10 绿；后端契约双向实测（数字串 id → 40100；字面量 `"undefined"` → 50000）。
6. **两条新发现**：`npm run openapi` **不可用**（按 tag 拼音改名 + 清空 `src/api` 目录 + 删手写文件；机制与恢复路径见 `decisions.md`，`177439d`）；**前端社交模块残留**（`/square`、`/post/:id`、`/user/:id` 三个路由 + 四个文件引用已停用的 `/post/**`，而 `postController.ts` 里**手工追加的通知函数仍在服务**、**不可整文件删**）——已挂进 plan.md「存量遗留」（`4bb6f63`）。

## 进行中 / 下一步

1. **T17（已拍板未做，小）**：普通用户的助手不再挂 `visionTagger`；管理员若保留则工具改为调 T15 的接口（避免提示词两处维护）。文件范围：backend 代理（透传角色）+ `HuoshanAssistantController` + `HuoshanToolFactory`。
2. **review 的 3 条 P3（未做，各"一行 + 一例测试"）**：`runLoop` 上限提示加 `state == RUNNING` 判断（附一例 `maxSteps=1 + finishOnStep` 契约测试）；`AgentEvent.Error` javadoc 措辞；计划 §5 与实现对齐 + 实例只跑一次的不变量提到 `BaseAgent`。
3. **AI 打标的收尾**：**配置这一环已就位**——用户把 `app.ai.vision.{base-url,api-key,model}` 放在本机 `backend/src/main/resources/application-local.yaml` 的 **`app:` 前缀下**（值已核对：base-url = `https://api.xiaomimimo.com` 协议根不带 `/v1`、model = `mimo-v2.5`、api-key 51 字符），并且**已重新打包 + 用新 jar 重启**；**只剩"点一次出建议"这最后一步没见证**（用户最后尚未复跑成功）。F14 里 8+1 条已知限制（最值得留意：本链路**无重试**、并发度是**每请求 4**、请求体解析失败与真故障同文案）。
4. **未拍板**：前端社交残留清理（plan「存量遗留」，涉及删路由/迁通知函数，需先立字据）。
5. 用户已明确"等 T8 之后再做"的那批：R5 的 6 条 P3、上线必办项（nginx SSE 块、旧端点 `/ai/manus/chat` 未鉴权、双端密钥一致、MCP 与引擎同机）、探针清理、push/tag。

## 未决问题

- **push / tag**：`main` 领先 `origin/main` **157 笔**未 push（`git rev-list --count origin/main..main` 实测）；仓库**仍无任何 tag**（档 3 已收口 + AI 打标落地，是打第一个 tag 的时机，用户未决）。
- **AI 打标的现场验收差最后一步**：配置已就位 + 已重打包并用新 jar 重启，但"点一次出建议"没有被见证（用户最后一次操作是在配置生效前）。
- **现场还开着**：backend 8123 + frontend 5173（用户试 AI 打标用；引擎 8124 与 MCP 8127 未起——AI 打标不需要它们）。要停就按端口找进程杀。
- **探针残留**：本会话集成测试自建唯一词条并**硬删收尾**（picture 行用 SQL 物理删，因 `@TableLogic` 只做逻辑删）；`ai/agent/tmp` 已清。**未新增**探针数据；0020 记的那批（t12 空间 9 张图、`t8-*` 记忆行）仍未清，由用户定。
- 沿用 0020-0022 的其它挂账（F12/F13 已知限制、多实例与浏览器兼容未验、门禁余量、T6 遗留 P3、`provider 不可达`重试预算等）。

## spec 偏差（认真轨）

- **越界**：**无**。AI 打标是本会话的**新增需求**，已按规则 3 先立字据（`docs/spec.md` 关键三行增补 + `docs/plan.md` T15-T17）并经用户确认（不做定时 / 两步式 / 普通用户摘掉），代码随后落地。其余改动都在已批范围内（review 的 P2 = T14、T13 顺带项由用户拍板）。
- **缺口**：**T17**（已拍板）；**AI 打标的浏览器人工验收**（无登录态）；review 的 3 条 P3。
- **关键三行检视**：**成立**——引擎仍是独立 Maven 应用/独立进程（8124）；headless 端点仍只认服务间 API key；凭据与密钥仍只在内存或**不入库**的本地配置里（AI 打标由图库 backend 自调多模态模型，**不引入任何新凭据通道**，`app.ai.vision.*` 只走环境变量/本地 yaml）。
- **过程教训（重要）**：0022 把已完成的 T13 列成待办，本会话接手时**又重复了一次**（review 结论里也写了"建议做 T13"）。**下一步必须以 `docs/plan.md` 的勾选状态与实施记录为准**，不要照抄上一份快照的待办清单——已写进 0022 的勘误。

## 线上状态（已上线项目才填）

- 仍是存量版本（同 0020-0022）：线上无 `/assistant` 入口，`ai/` 引擎与 MCP 均未部署，**AI 打标也未上线**（前端入口刚做，且线上跑的是旧 jar）。本会话无线上操作（只有本地真机）。

## 环境备注

- 分支 `main`；**工作区干净**；ahead `origin/main` **157 笔**；**无 tag**。
- **本机服务**：MySQL 3306 / Redis 6379 常驻；**backend 8123（用"含用户修正后的 application-local.yaml"重新打包的 jar）与 frontend 5173 仍开着**（留给用户点 AI 打标）；引擎 8124 / MCP 8127 **未起**。要停：按端口找进程（`Get-NetTCPConnection -LocalPort 8123 -State Listen` 取 OwningProcess 后 `Stop-Process`）。
- **一个这次踩到的启动坑（用户拍板不写进 AGENTS/decisions，只在此记一笔以免下个会话重踩）**：`application-local.yaml` 在 `src/main/resources` 下，**会被打进 jar**；运行时应用读的是 **jar 里那份快照**，不是源码目录里的文件 ⇒ **改了它必须 `mvn -B -o package -DskipTests` 重打包再启动**（只重启无效）；另外**打包前必须先停服务**，否则 Windows 锁住 jar，报 `Unable to rename ... .jar to ... .jar.original`。本次因此白跑了两轮。
- **配置核对方法（这次的前车之鉴）**：核对"配置是否生效"要 ① **打印完整层级**（只看 `vision:` 键名会把 `spring.ai.vision` 误判成 `app.ai.vision`）；② **核对运行时真正读的产物（jar 内那份）**，不是源码文件；③ 报"未配置"时先看值是不是空串/未解析的 `${...}` 占位符。
- **起服务的正确姿势（本会话实测）**：① `cd backend && mvn -B -o package -DskipTests`——**必须重打包**，`target/` 里那份 jar 曾停在 9/14，缺本会话的新端点；② `set "SPRING_PROFILES_ACTIVE=local,test" && set "AI_ENGINE_INTERNAL_API_KEY=smoke-internal-key" && java -jar backend\target\yun-picture-base-0.0.1-SNAPSHOT.jar`（`test` profile 关验证码与登录限流）；③ 前端 `cd frontend && npm run dev` → 5173；④ 健康检查 `http://127.0.0.1:8125/actuator/health`；⑤ 公开探活 `POST http://localhost:8123/api/picture/list/page/vo`（`{"current":1,"pageSize":1}`，无鉴权）。
- **AI 打标的配置与试法**：用户已把 `app.ai.vision.{base-url,api-key,model}` 填进本机 `backend/src/main/resources/application-local.yaml`（不入库；`.example` 有模板）；入口 `/admin/pictureManage` → 勾选 → 「AI 打标」→ 建议可编辑 → 确认写入。接口为 `POST /api/picture/ai_tag/{suggest,apply}`（管理员；单次 ≤ 8 张；MiMo 单张 10~15s，并发 4 约 30~40s）。
- **本会话又踩的坑**：① `mvn -Dtest='!A,!B'` 在 **cmd** 里必须**去掉单引号**（否则 surefire 报 "No tests matching pattern" —— 我第一次跑门禁就因此红了一次）；② cmd 不支持 `;` 链接（用 `&`），我用 PowerShell 语法写 `;` 报了 "此时不应有 git"；③ `npm run openapi` **不要跑**（见 `decisions.md`）；④ 单测里用 `LambdaUpdateWrapper` 需要 MyBatis-Plus 的 `TableInfo` 缓存（Spring 上下文里才有），纯单测要 `TableInfoHelper.initTableInfo(...)` 手工初始化一次。
- **探针与产物**：集成测试 `PictureAiTagIntegrationTest`（本机 MySQL + 桩模型，11.8s）自建唯一词条并硬删收尾；`ai/agent/tmp` 已清理。沿用 0020 记的 `%TEMP%\t12\jar.txt`（**含凭据的 cookie 罐，用完请删**）与 `%TEMP%\t8\*`。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**。要 AI 打标的细节看 `docs/features/F14-AI打标.md`；要档 3 全貌看 `0020`；要循环与协议细节看 `F13`。
2. 口径全文：`docs/decisions.md` **2026-09-15 各行**（T8-hard review / T13 顺带项 / AI 打标 / openapi 生成器不可用 / provider 重试挂账）。
3. **下一件事建议 T17**（已拍板、小），或 review 的 3 条 P3（各一行 + 一例测试，可穿插）；再往后按用户原话办：R5 的 P3、上线必办项、前端社交残留（未拍板）、探针清理、push/tag。
