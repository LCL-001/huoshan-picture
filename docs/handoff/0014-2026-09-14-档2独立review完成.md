# Handoff 0014 — 档 2 里程碑独立 review 完成（2026-09-14）

## 当前状态（一句话）

**档 2（用户可见 MVP）的规则 13 里程碑独立 review 已完成并留档：有条件通过（无 P1、4 条 P2、7 条 P3）**，交付本体逐条实测成立（凭据组透传、凭据零落日志、引擎不可达不挂死、门禁真绿、`ai/` 零改动）；工作区干净、本机服务已全部停止；**下一会话第一件事是让用户拍板 R4/R5/R6（见"未决问题"），然后进档 3**。

## 刚完成（本会话，按时间）

1. **只读 review（规则 13）**：通读 AGENTS → spec 关键三行 → `docs/features/F11-AI助手MVP.md` → 档 2 实施计划 → handoff 0013 → decisions 最后 6 条 → 实现文件（`AiAssistantController` / `AiAssistantProxyManager` / `AiAssistantProperties` / `ThreadPoolConfig` / `AssistantPage.vue` / assistant 组件与两个 utils / 引擎 `HuoshanAssistantController` + `HeadlessApiKeyInterceptor` + `BaseAgent` + 工具集），必审清单 11 条逐条给结论。

2. **独立复跑门禁（不采信自述）**：
   - backend `32/32` 绿；`AiAssistantProxyIntegrationTest` `3/3` 绿（真实登录产出的凭据链）。
   - **ai/agent `87/87` 绿——但只有用 `.githooks/pre-commit:54` 的排除名单才绿**；AGENTS/简报那条通用过滤命令直接套 `ai/agent` 是 **97 例 1 错误**（`MyManusTest` `@Resource MyManus` 注入不存在的 bean，遗留教学测试，非本档回归）。这个口径差异已记进 F11 已知限制 11 与 plan.md。
   - 前端 `npm run type-check` 138 例（存量），**assistant 相关文件 0 命中**；8 个改动文件 eslint 无输出 → "净增 0" 属实。
   - `git diff 46d266d^..HEAD -- ai/` 为空 → "引擎侧零改动"属实。

3. **自建验证设施（不依赖作者自述）**：三个探针后端实例（8126→真引擎 / 8128→死引擎 / 8131→桩引擎）+ 自写桩引擎（8130，7 个 SSE 场景）+ 并发脚本（70 并发）+ 跨站探针页（8135，`127.0.0.1` → `localhost`）+ 真浏览器加载真实 `assistantSse.ts`。证据与脚本在 `%TEMP%\t11-review\`（`README.md` 索引）。

4. **关键实测结论**（细节见 F11「已知限制」7-11 与 decisions 2026-09-14 review 行）：
   - 凭据组自建桩收到与浏览器逐字一致的头/Cookie；**三处实例日志 + 引擎日志对真 token/会话值 0 命中**（含异常与拒绝路径）。
   - 代理**放行任意非空白 satoken**（真会话 + 假 token `review-bogus-token` 仍读到真实空间）→ R4。
   - **CSRF 实弹成立**：跨站顶层导航带齐两把 Cookie 并跑完一次 agent（同页 no-cors 子资源请求被 Lax 拦下回 40100）→ R5。
   - 线程池 70 并发 → 64 通 6 拒（上界属实）；拒绝给浏览器的是**空体 HTTP 500**。
   - 真浏览器：断流后 15s 零重连（"不 close 就会重跑 agent"的前提与"close 杜绝重连"的结论都成立）。
   - SSE 中继四个边界缺陷（多行 data 成半帧 / 缺 `[DONE]` 时"答案+意外中断"同屏 / 上游 HTML 错误体进回答气泡 / 拒绝只给空体 500）。
   - `chatId` 可换行注入后端日志（实测伪造出整行）。
   - 全仓 `v-html`/`innerHTML` 0 处、链接只认 http(s) → XSS 面无 sink，口径成立。

5. **落库**：提交 `625f294`（`docs: 档 2 里程碑独立 review 结论…`）——`decisions.md` 新增 review 行 + `F11` 已知限制 7-11；本快照与 `plan.md`（档 2 review 记录 + 新增挂账两行）同批提交。

6. **环境**：为跑负向/边界探针，停自己的后台任务时把常驻的 8123/8124/5173 一并带停了 → 已按 0013 的原样命令重启并验证（engine `/api/health` ok、代理无 Cookie 回 40100、真图库一问一答跑通、actuator 8125 正常）；**收尾时又全部停掉**，与 0013 修正后的"已全部停止"一致。探针端口（8126/8128/8129/8130/8131/8132/8133/8135）已全部释放。

## 进行中 / 下一步

1. **等用户拍板 R4/R5/R6**（建议：R6 最便宜可立即做；R4 需你同意"代理加一次身份一致性校验"；R5 与档 3 写类工具上线绑定，属届时的 P1）——方案见本快照"未决问题"与最终 review 报告。
2. **档 3 完整一期**：visionTagger 看图打标、`batchEditPictures` / `batchUploadByUrl`、T9 MCP 搜图、T8-hard 循环合并（`run()` 退役）、T12 两条业务场景联调、T13 清理。
3. **引擎部署前必办**（一期要真上线才碰）：旧端点 `/ai/manus/chat` 未鉴权（本会话实测：匿名 `HTTP 200 + text/event-stream` 就能进 agent）、引擎端口/内网约束、双端密钥一致（引擎 `HUOSHAN_HEADLESS_API_KEY` = backend `AI_ENGINE_INTERNAL_API_KEY`）、`ai/` 应用打包与启动方式、**nginx 的 SSE 配置（本会话新发现，见未决问题）**。
4. **挂账收口（小颗粒、可穿插）**：引擎侧"超时转显式错误事件 + 埋点"、R1（引擎侧取消不关上游）、前端门禁存量清理、P3 速修清单（`chatId` 净化 / relay 逐行 emit / emitter 超时 330s / 错误文案不贴原文 / 拒绝先发可见文案）。

## 未决问题

- **R4（P2，待拍板；涉推翻"代理只判存在性"已决口径）**：实测代理放行**任意非空白 satoken**（真会话 + 假 token 仍读到真实空间）；decisions 2026-09-14 那条"不一致时回 `SPACE_NOT_LOGIN`"只对空间维度读图成立，`listSpaces` 一类路径实际只由 Spring Session 授权 → 建议代理加一次 `StpKit.SPACE.getLoginIdByToken(satoken)` 与 `loginUser.getId()` 的一致性比对（只判身份一致性、不判权限），或先更正记录表述。
- **R5（P2，档 3 写类工具上线前必办，届时 P1）**：`GET /api/ai/assistant/chat` 只认 Cookie + 有 LLM 副作用 + 无 nonce，实测跨站顶层导航即带 Cookie 跑完一次 agent → 加一次性 ticket（页面先用 axios 取、EventSource URL 带 `&ticket=`）或改 POST + `fetch` 流式。
- **R6（P2）**：`/ai/**` 不在 `SaTokenConfigure`，Sa-Token 注解在未注册路径会**静默失效**；现仅注释 + 文档 → 补反射守护测试。
- **上线前必办（P2-部署，本会话新增）**：仓库只有 `nginx-security.conf`，**没有 server 块** → `proxy_buffering off` / `proxy_read_timeout` **未能证实**；nginx 默认缓冲会让折叠条不逐步显示、默认 60s read timeout 会掐断长对话。档 2 上线前必须确认并实测。
- **P3 六条**：`chatId` 可换行注入后端日志；relay 多行 `data:` 产出半个 JSON 帧（前端静默丢弃）；上游缺 `[DONE]` 时"完整答案 + 响应意外中断"同屏；上游非 JSON 错误体（网关 502 的 HTML）原文进回答气泡；线程池拒绝只给空体 HTTP 500；代理 emitter 超时早于引擎数十毫秒 → 超时形态退化成裸断连。
- **本会话未能证实**：生产 nginx 的 SSE 配置（见上）；引擎日志是否记录**用户提问原文**（本样本未命中，但工具返回原文命中 4 次——含空间名等用户数据，`MyLoggerAdvisor` 存量行为）；生产 CORS 实际放行源（`application-prod.yaml` 未入库）。
- **前端门禁存量红**：`npm run type-check` 138 例 / `eslint .` 73 例既有错误（本会话实测 138，assistant 文件 0 命中）；AGENTS/handoff 0001"前端以 type-check + lint 为准"目前不成立，需单独立任务。
- **push / tag**：`main` 领先 `origin/main` **99 笔**未 push（含本快照一笔）；仓库**无任何 tag**（用户未决）。
- **门禁余量**：看门用例段间隔 300ms vs `CONTROL_TIMEOUT` 1000ms = 3.3×，2 核 CI 上偏紧（加固方案 1500~2000ms，未做）。
- **T6 遗留 P3**：③ `@DurationUnit(SECONDS)`、⑥ `OpenAiChatModelsTest` 夹具去 `/v1`；①②⑤⑦继续挂。
- **档 3 之后仍挂**：visionTagger / 两个批量工具 / T9 搜图 / T8-hard / T12 联调 / T13 清理。

## spec 偏差（认真轨）

- **越界（做了 spec 没写的）**：**无**。本会话只新增了一笔文档（`decisions.md` review 行 + `F11` 已知限制 + `plan.md` review 记录/挂账）与本快照，均为规则 13/7 要求；未动任何实现代码。
- **缺口（spec 承诺但没做的）**：**无未挂账缺口**——档 2 承诺三件套（backend 代理 / 前端界面 / 步骤折叠条）与验收标准 1 均已达成；"超时转显式错误事件""前端 type-check+lint 过"仍挂账；本会话新增的 R4/R5/R6 与 nginx 必办项已同时记入 plan.md 与 F11。
- **关键三行检视**：**成立**——`ai/` 仍是独立 Maven 应用独立进程（档 2 全程 `git diff -- ai/` 为空）；引擎端点只接受服务间 API key、不注册登录体系（`/ai/huoshan/**` 拦截器 fail-closed 实测 401）；**"凭据只在请求生命周期内存流转、不落库、不进日志"首次拿到双向实测证据**（后端三实例 + 引擎日志对真凭据 0 命中）。

## 线上状态（已上线项目才填）

- 当前线上版本：https://www.lincode.online 仍是 backend / frontend **存量版本**；档 2 的所有改动未上线（线上还没有 `/assistant` 入口），`ai/` 引擎仍未部署。
- 未处理反馈（[线上] 任务）：**无**（本会话无线上反馈项）。

## 环境备注

- 分支 `main`；**工作区干净**；ahead `origin/main` **99 笔**未 push（含本快照一笔）；**无 tag**。
- **本会话提交链（1 笔）**：`625f294`（docs: 档 2 里程碑独立 review 结论）+ 本快照与 `plan.md` 两行（同批）。另：用户侧在本会话期间提交了 `100fdb5`（修正 handoff 0013 的本机服务状态为"已全部停止"）。
- **门禁口径（本会话实跑）**：
  - backend：`cd backend && mvn -B -o test "-Dtest=!*IntegrationTest,!RedisStringTest,!YunPictureBaseApplicationTests" -DfailIfNoTests=false` → **32 例绿**。
  - 本地集成测试：`-Dtest=AiAssistantProxyIntegrationTest` → **3 例绿**（需 MySQL/Redis）。
  - ai/agent：**必须用 `.githooks/pre-commit` 的排除名单**（`-Dtest=!MyManusTest,!MyAiAgentApplicationTests,!ProductScopeTest,!PgVectorVectorStoreConfigTest,!FileOperationToolTest,!PDFGenerationToolTest,!ResourceDownloadToolTest,!TerminalOperationToolTest,!WebScrapingToolTest,!WebSearchToolTest`）→ **87 例绿**；换成 AGENTS 那条通用过滤命令会红（`MyManusTest`）。
  - 前端：`npm run type-check` → 138 例存量错误（净增 0）；改动文件 eslint 干净。
- **本机服务状态：当前全部停止**（8123 / 8124 / 8125 / 5173 均已释放）。重启命令见 handoff 0013「本机服务状态」节（本会话实测可用：引擎 `/api/health` → `ok`、代理无 Cookie → `40100`、真图库一问一答跑通）。
- **本会话新踩的坑（记下备用）**：
  1. **停后台任务会连带收掉本会话先前启动的其它常驻进程**——我 `TaskStop` 自己的探针实例时，把 8123/8124/5173 一起停了。停之前先确认哪些进程是"别人的"；发现被误停后按 0013 命令重启即可恢复。
  2. 启动**第二个 backend 实例**必须同时覆盖 `--management.server.port`，否则 8125 冲突、应用直接起不来（`Web server failed to start. Port 8125 was already in use`）。
  3. cmd 里 `for %S in (...) do curl ...` 这种单行复合命令展开不可靠（变量可能整行未替换），批量探针改写成脚本或用单独命令。
  4. 从 cmd 单行调 PowerShell 传中文/竖线易被拆散（`Select-String 不是内部或外部命令`），写成 `.ps1` 用 `powershell -File` 跑。
  5. 用 `Test-NetConnection` 批量查端口比 `netstat | findstr` 更省心；PowerShell 读 UTF-8 日志要显式 `-Encoding UTF8`（否则中文乱码、误判数据错）。
- **可复用资产**：`%TEMP%\t11-review\`（review 证据 + 脚本：`stub-sse.js` 桩引擎七场景、`saturate.js` 并发、`csrf-page.js` 跨站探针、`README.md` 索引）；`%TEMP%\t11-smoke\`（档 2 冒烟资产与截图）。含凭据的抓取文件已删除。
- **探针残留（本地开发库）**：账号 `tier1probe0914140822` / 空间 `tier1-probe-space`（同 0013）；集成测试临时账号自建自删。另：本会话的 review 对话在引擎库留下 `chat_message` 记录（chatId 形如 `review-*` / `csrf-nav-1` / `ui-*` / `rev-*`），需要清理时按 chatId 识别。
- **版本锚点**：backend SB 2.7.6 / Java 17；引擎 SB 3.5.13 / Java 21 / Spring AI 1.1.2；Ollama 本机 11434（`qwen3:4b` 带 tools）；后端 context-path `/api`、actuator 8125。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**；
2. 先看用户对 **R4/R5/R6** 是否已拍板（`docs/decisions.md` 2026-09-14 review 行 + 本快照"未决问题"），拍板了就先修它们，没拍板就直接进档 3；
3. 需要 review 的全部证据与反驳过程：`docs/features/F11-AI助手MVP.md`「已知限制」7-11 + `docs/plan.md` 档 2 review 记录两行 + `%TEMP%\t11-review\README.md`；
4. 档 3 开工前读 `docs/plan.md` 的「MVP 档位与推进顺序」档 3 行与 `docs/decisions.md`（B 口径、id 字符串、R1 延后、review 行）。
