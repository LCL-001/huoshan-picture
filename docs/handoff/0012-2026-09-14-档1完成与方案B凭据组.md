# Handoff 0012 — 档 1 headless 打通完成（T7-lite + T8-lite）与方案 B 凭据组落地（2026-09-14）

## 当前状态（一句话）

**档 1（headless 打通）已完成并端到端验证**——图库助手会话类型、headless SSE 端点、三个只读工具全部落地，中途因冒烟实测发现"空间列表不吃 sa-token"，用户当日拍板**方案 B（代理与工具透传一组凭据 satoken + Spring Session 会话标识）**并已实现、正样本跑通；工作区干净、门禁 87 用例全绿；**下一会话从档 2（T10 后端代理 + T11 前端助手界面）开工**。

## 刚完成（本会话，按时间）

1. **接手与立项执行**：读完 AGENTS/spec 关键三行/handoff 0011，按 0011 指定的档 1 顺序执行（写即时计划 → 验风险 1 → T7-lite → T8-lite → 冒烟 → 文档收口）。**backend / frontend 源码零改动**（只重建过 backend 的 jar，见"环境备注"）。
2. **风险 1（工具挂载能否用于 OpenAI 协议模型）结论：不成立、无需改代码**。三层证据，全在档 1 计划文档"风险 1 结论"节：
   - 字节码：`OpenAiChatModel.buildRequestPrompt` 对 `ToolCallingChatOptions` 走**接口拷贝**分支并回设 `internalToolExecutionEnabled`；`DefaultChatClientUtils` 先把 provider options 统一转成 provider 中立的 `DefaultToolCallingChatOptions`；
   - 桩测试（进库、永久守卫）：`HuoshanAssistantToolOptionsTest` 用记录型 `ChatModel` 断言 options 是 `ToolCallingChatOptions`、`internalToolExecutionEnabled=false`、三个工具回调都在，**外加减向控制**（设 true 必须得到 true）证明断言非空过；
   - 真机探针（仓库外 `%TEMP%\t71-risk1\`）：真模型（Ollama `qwen3:4b`）+ 真工具 + 桩图库 HTTP，`listSpaces_http_hits=1`（**恰好一次即证明内部执行标志存活、无双跑**），耗时 18.6s，`agent.run()` 返回"Step 1 工具结果 / Step 2 您当前有1个空间：团队空间"。
3. **端点鉴权定为 A 口径（服务间 API key）**：`HeadlessApiKeyInterceptor` 只拦 `/ai/huoshan/**`，`app.huoshan.headless-api-key` 为空时**一律拒绝**（fail-closed），比对用 `MessageDigest.isEqual`。三条负向实测：无 key → HTTP 401 + code 40100；有 key 无 satoken → 40000；userId 非数字 → 40000。
4. **两个实现层面的发现（已记 decisions）**：① 图库助手会话**不建 `conversation` 行**——`ai/agent` 库 `conversation.user_id` 有外键指向引擎 `user` 表（`V1__init.sql:26`），外部身份插不进去，多轮记忆改由无外键的 `chat_message` / `chat_summary` 承载；② 会话 id 用 `UUID.nameUUIDFromBytes("huoshan:<userId>:<chatId>")` 派生（定长 36 且跨用户不可读；直接拼接最长 77 字符会超 varchar(64)）。
5. **真图库冒烟（无效 satoken）**：越权不可达达成——图库回 40100、工具转结构化错误 `{"error":true,"code":40100}`、模型明确告知需登录；`getTagCategory` 读到真库词表。**踩到一个环境坑并记档**：本地 `backend/target` 的 jar 曾陈旧于源码（09-12 构建 vs 09-13 的 T3.2 收权提交），首轮冒烟出现"匿名可枚举空间/图片元数据与属主"的**修复前**行为——**真图库冒烟前必须先按当前源码重建后端**。
6. **用户提示 `application-test.yaml`（叠加 `local,test` 关验证码与登录限流）后，脚本可自注册/自登录取真凭据**，据此做对照实验，暴露真正缺陷：`POST /space/list/page/vo` **只认 Spring Session、不吃 sa-token**（新签发的有效 satoken 单独调用仍 40100），空间维度读图还有更严的 `checkSpaceViewPermission`（`PictureServiceImpl:768-786`）要求两把凭据的 loginId 一致。**用户当日拍板方案 B**（A 只解锁"入口"、覆盖不到空间维度读图，档 3 仍要回来补；B 一次到位且图库 backend 业务代码零改动）。
7. **方案 B 落地 + 正样本达成**：引擎改成透传凭据组——`HuoshanApiClient` 同时带 `satoken` 头与 `Cookie: SESSION=…`，端点用 `@CookieValue("SESSION")` 取会话标识且**缺任一把在入口即拒 40000**（不再拖到工具调用阶段才炸）。实测：
   ```
   NEG 仅带 satoken                 → 40000（缺 SESSION）
   POS satoken + SESSION：
       listSpaces                   → {"total":1,"items":[{"id":"2099389400592543745","spaceName":"tier1-probe-space",...}]}
       listPictures(spaceId=上面的id) → {"total":0,"items":[]}   ← code=0，通过加强校验
   ```
8. **顺带修掉冒烟暴露的真 bug（id 精度）**：工具 DTO 把 id 收成 `Long` 又吐回 JSON 数字，抵消了图库"Long 统一序列化成字符串"（`JsonConfig` 的 `ToStringSerializer`）的防精度丢失设计；模型读 `…745`、写回 `…700`（IEEE754 双精度截断）导致 `40400 空间不存在`。已改为 **id 一律字符串透传**（`SpaceItem.id` / `PictureItem.id` / `PictureItem.spaceId` 与 `listPictures` 的 `spaceId` 入参都是 String），工具描述与系统提示词明示"id 是字符串、必须原样复制"，测试桩也换成真实 19 位字符串 id 并加断言钉住。
9. **文档同步**：decisions.md 新增/改 5 条（B 决策、凭据组口径、id 精度口径、旧端点不锁、超时错误事件归档 2）；设计文档加 **R3 修订**（"透传 satoken"→"透传凭据组"，含原因与落地）并同步正文身份/端点两处；plan.md 档 1 实施记录 + T7/T8 档 1 标注 + 档 2 前置项已决策 + T10 条目补"会话 Cookie 透传"与负向验收；spec.md F11 与档 2 不放宽清单同步；档 1 计划文档补"冒烟追加发现"与"方案 B 补跑"两节。

## 进行中 / 下一步

**下一会话第一件事：档 2（用户可见 MVP，本轮终点）= T10 backend 代理 + T11 前端助手界面与步骤折叠条。** 收口时做一次里程碑独立 review（规则 13）+ 一篇合并的功能讲解（规则 9）。

**T10 契约要点（已由本会话确定，开工即用）**：

- 代理端点（backend，SB 2.7）接收浏览器请求，转发到引擎 `GET /api/ai/huoshan/chat?message=&userId=&chatId=`；
- **必须同时转发一组凭据**：`satoken` 头（原样）+ `SESSION` 会话 Cookie（B 口径；缺任一把引擎回 40000）；`X-Internal-Api-Key` 走配置（密钥不入库）；
- 用户标识 `userId` 由后端从自己的登录态解析后透传（引擎侧只用于会话归属，权限由图库按凭据判）；
- SSE 流转发：设计选型仍"实施时定"（SseEmitter / OkHttp EventSource）；**引擎事件协议（step / answer / metrics / [DONE]）本会话零改动**，前端折叠条按同一语义消费即可；
- 凭据只在请求生命周期内存流转、不落库、**不进日志**（与引擎侧同口径）；
- 验收要含负向用例："只带 satoken 不带会话 Cookie → 引擎拒绝"。

**T11 要点**：助手入口 + 对话面板 + SSE 步骤折叠条（`step` 事件里 `kind=think` / `kind=tool` 分别对应折叠区的思考与工具步骤，`answer` 进气泡）；复用现有 Axios 实例（已 `withCredentials: true`，凭据自动带上）。

**建议顺序**：读 spec 关键三行 + 本快照 + decisions 最后 4 行 → 写 `docs/plans/` 档 2 即时计划（范本：档 1 计划文档）→ T10（含集成测试）→ T11 → 联调冒烟（本地 local,test 起后端 + 引擎，探针账号可直接复用）。

## 未决问题

- **旧端点 `/ai/manus/chat` 未鉴权**——**引擎部署前必办**（公网暴露面 + 无鉴权时 TerminalOperationTool 等工具的可用性）。档 1 刻意未动它（属改变既有行为、不在档 1 条目内），已记 decisions + plan 挂账。
- **超时转显式错误事件 + 超时/取消埋点**——顺延档 2（与前端错误渲染同批验收）；要改 `BaseAgent` 共享循环，会同时影响旧端点，注意口径（R3：流式超时到达调用方时是**状态码 200** 的 `WebClientResponseException` 包裹，须沿 cause 链判定）。
- **R1（客户端取消不关闭上游请求，P1）**：用户已拍板延后到档 2 之后，只做超时转显式错误事件、不承诺关闭上游。
- **正样本依赖 `application-test.yaml` 取 token**：图库登录有验证码、程序化登录被挡死，联调/冒烟时用 `SPRING_PROFILES_ACTIVE=local,test` 起后端（叠加在 local 之上，**用完要去掉**）。线上/生产环境不能带 test profile。
- **探针残留（本地开发库）**：探针账号 `tier1probe0914140822`（密码 `Probe12345`，邮箱 `tier1probe0914140822@example.com`）与其名下空间 `tier1-probe-space`（id `2099389400592543745`，无图片）。可复用可删。
- **push / tag**：`main` 领先 `origin/main` **86 笔**、仓库**仍无任何 tag**（用户未决，本会话未擅自 push 或打 tag）。
- **门禁余量**：稳态看门用例段间隔 300ms vs `CONTROL_TIMEOUT` 1000ms = 3.3×，2 核 CI 上偏紧（加固方案：提到 1500~2000ms，未做）。
- **T6 遗留 P3**：③ `@DurationUnit(SECONDS)`、⑥ `OpenAiChatModelsTest` 夹具去 `/v1` 建议并入 T7 首次触碰时同做（T7 已触碰但未做，仍挂）；①②⑤⑦继续挂。
- **档 3 之后仍挂**：visionTagger / 两个批量工具 / T9 MCP 搜图 / T8-hard 循环合并 / T12 联调 / T13 清理。

## spec 偏差（认真轨）

- **越界（做了 spec 没写的）**：本会话唯一的"越界"是**凭据口径变化**（satoken → 凭据组），属用户当轮拍板的修订，已同步 spec.md F11、设计文档 R3 修订、decisions.md、plan.md，并顺手清掉了 spec 里残留的"satoken 透传 / token 只在内存流转"旧表述。实现侧新增文件全部落在 `ai/agent/src/{main,test}`。
- **缺口（spec 承诺但没做的）**：无新增——一期后续项仍按档 1/2/3 显式排序，档 2 之前不做项写在 spec.md 第 8 行；"超时转显式错误事件"原属 T8，本会话显式再切分到档 2 并记因（decisions 2026-09-14 条）。
- **关键三行检视**：**成立**——`ai/` 仍是独立 Maven 应用与独立进程，本会话 backend / frontend **源码零改动**；"不做的事"里"引擎端点只接受服务间 API key、不注册登录体系"继续成立（引擎仍未接任何登录体系）；凭据口径变化已被用户拍板并记因，不属翻案。

## 线上状态（已上线项目才填）

- 当前线上版本：https://www.lincode.online 仍是 backend / frontend 存量版本；**`ai/` 引擎尚未部署**（档 2 之后才涉及部署），本会话所有改动均未上线。
- 未处理反馈（[线上] 任务）：**无**（本会话无线上反馈项）。

## 环境备注

- 分支 `main`；**工作区干净**；ahead `origin/main` **86 笔**未 push；**无 tag**。门禁 `core.hooksPath=.githooks` 已启用（pre-commit 跑测试，docs-only commit 自动跳过）。
- **本会话提交链（10 笔，按序）**：`01379be`（档 1 计划）→ `775d1e6`（T7 工具集与客户端）→ `c995c88`（会话类型/身份映射/agent）→ `fbcfe2e`（风险 1 钉桩测试）→ `74921ec`（headless 端点与鉴权）→ `f0a8bb2`（docs 档 1 实施记录）→ `f881987`（docs 冒烟发现）→ `a2d0f41`（feat 凭据组/B）→ `1f28ae0`（fix id 精度）→ `2de4558`（docs 方案 B 决策与设计 R3）→ 本快照一笔。
- **门禁口径（本会话实跑 87 用例全绿）**：
  `cd ai/agent && mvn -B -o test -Dtest="!MyManusTest,!MyAiAgentApplicationTests,!ProductScopeTest,!PgVectorVectorStoreConfigTest,!FileOperationToolTest,!PDFGenerationToolTest,!ResourceDownloadToolTest,!TerminalOperationToolTest,!WebScrapingToolTest,!WebSearchToolTest" -DfailIfNoTests=false`
  （门禁同时跑 backend 的无 MySQL/Redis 单测 13 例；`.githooks` 排除名单本会话无需改动。）
- **本机服务**：MySQL 3306 / Redis 6379 / Ollama 11434（`qwen3:4b`，带 tools 能力）在跑；backend 8123 与引擎 8124 冒烟时按需起停（冒烟脚本会自动起停，用完不留进程）。
- **可复用的资产（都在仓库外）**：
  - `%TEMP%\t71-risk1\`：`HuoshanRisk1Probe.java` + `run-probe.ps1`（风险 1 真机探针）、`run-endpoint-smoke.ps1`（端点 401/40000 负向）、`run-tier1-smoke.ps1`（真图库冒烟）、`run-smoke-b.ps1`（方案 B 正/负样本）、`run-authz-probe{2,3,4}.ps1`（鉴权对照实验）、`message*.txt`（中文消息，脚本因此可保持纯 ASCII）、`cp.txt`（classpath）；
  - `%TEMP%\t71-smoke\`：各轮日志与 SSE 原始片段（`sse-negative.txt`、`sse-b-positive.txt`、`backend-authz*-result.txt`）。
  - **两个必记的坑**：① PowerShell 5.1 以 **ANSI** 读取无 BOM 的 `.ps1`，脚本里写中文会把自身语法带崩——中文放独立 UTF-8 文件、脚本保持纯 ASCII；② 给原生程序（curl.exe）传含双引号的 JSON 会被 PowerShell 吃掉引号，请求体必须是写文件 + `--data-binary "@file"`。
- **版本锚点（读框架源码前先锚）**：spring-ai **1.1.2**、spring-ai-alibaba-dashscope **1.1.2.0**、spring-webflux 6.2.17、reactor-core 3.7.17、JDK 21。sb 3.5.13。本地 Maven 仓 `D:\develop\Maven\apache-maven-3.9.11\maven_repo`（无 `sa-token-spring-boot3-starter`，如需方案 B 的备选路线要联网下载）。
- **backend jar 已按当前源码重建**（`mvn -B -o package -DskipTests`，源码未改动），现在冒烟跑的是修复后行为。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**；
2. 开工前读 `docs/plan.md` 的「MVP 档位与推进顺序」（含档 2 前置项与 T10 条目）、`docs/decisions.md` 最后 4 行（B 决策、凭据组口径、id 精度口径、旧端点与超时归档）；
3. 需要档 1 的实现细节/证据时读 `docs/plans/2026-09-14-档1-headless打通实施计划.md`（含风险 1 三层证据、冒烟追加发现、方案 B 补跑与正样本原始片段）。
