# Handoff 0013 — 档 2 用户可见 MVP 完成（T10 代理 + T11 助手界面，2026-09-14）

## 当前状态（一句话）

**档 2（用户可见 MVP）已完成并端到端验证**——图库 backend AI 代理端点（凭据组透传 + SSE 转发）与前端助手界面（对话面板 + SSE 步骤折叠条）全部落地，真图库正/负样本冒烟与浏览器人工验收双双通过；**冒烟暴露并修掉一个真 bug**（代理把 `session.getId()` 当会话 Cookie 转发，导致图库回 40100）；工作区干净、门禁 backend 32 例 + ai/agent 87 例全绿；**下一会话第一件事是按规则 13 做里程碑独立 review（建议新开只读会话）**，随后进档 3。

## 刚完成（本会话，按时间）

1. **接手与三份并行调研**：读完 AGENTS/spec 关键三行/handoff 0012/plan/decisions/设计文档；派三个只读子代理分别摸清「引擎 headless 端点契约」「backend 分层与配置惯例」「frontend 结构与惯例」，结论直接支撑了实现（例如：引擎端点是 GET + 无 `produces`、`EventSource` 只能 GET、`/ai/**` 不在 SaTokenConfigure 拦截路径内、`request.ts` 的 axios 有 10s 超时不能用）。
2. **写档 2 即时计划**（提交 `46d266d`，`docs/plans/2026-09-14-档2-用户可见MVP实施计划.md`）：含 10 条实现口径说明、7 个 Task 的 TDD 步骤、验收清单；设计文档留的"SSE 转发选型实施时定"在计划里定为 `SseEmitter` + JDK `HttpURLConnection`（零新增依赖），理由写在口径说明 4。
3. **T10 落地**：`68ded79`（`AiAssistantController` 端点 + `AiAssistantProxyManager` 转发 + `AiAssistantProperties` 配置 + `aiAssistantExecutor` 线程池 + 19 条新单测，门禁 13→32 例）、`e329a48`（凭据链集成测试 3 例，真实登录产出的 `satoken` Cookie 与真实会话 id 打到桩引擎）。期间做了负向探针（摘掉 `satoken` 头 → 恰好 1 例转红）。
4. **前端原型先给用户过目**（用户要求"改前端先生成原型图"）：按仓库既有设计令牌手写静态原型页 + headless Chrome 渲成 PNG（空状态 / 对话中两态），用户确认"可以，没问题"后才落代码。
5. **T11 落地**（`6d6e4f2`）：`AssistantPage.vue` + `StepTimeline.vue` + `assistantSse.ts` + `assistantController.ts`；改 `router`（`/assistant`）、`GlobalHeader`/`BasicLayout` 两处菜单入口、`request.ts` 导出 `BASE_URL`。前端检查：`type-check`/`eslint` 净增 0（但**基线本已红**，见未决问题）。
6. **联调冒烟（真引擎 + 真图库）**：`local,test` 起 backend + 引擎（Ollama qwen3:4b 走 OpenAI 兼容端点）。首轮工具回 `40100 未登录` → 三步对照定位（原样 Base64 Cookie 打空间接口回 `code=0`、解码 UUID 回 `40100`）→ **根因：代理转发的是 `session.getId()`（解码后 id），而浏览器 Cookie 是 Base64 形态**。按规则 12 先写复现测试（6 失败 2 错误）再修（`7988542`），并把参数名改成 `sessionCookie` 防再犯。修后正样本读到真实空间 `tier1-probe-space`、回答正确、`[DONE]` 收尾；停掉引擎再打 → 合成"引擎暂时不可用"+`[DONE]`，客户端不挂死。
7. **浏览器端到端**（IAB）：登录后进 `/assistant` 发送"我有几个空间？"，界面依次呈现用户气泡 → 折叠条「正在执行（0 步）」+转圈+输入禁用+「停止」→「执行步骤（1 步）」含 `工具 · listSpaces` 与真实返回 → 回答气泡；截图 `%TEMP%\t11-smoke\ui-answer.png`。
8. **文档收口**：新增 `docs/features/F11-AI助手MVP.md`（规则 9，含已知限制 6 条）；`plan.md` 补档 2/T10/T11 三处实施记录与新增挂账；`decisions.md` 新增 4 条（T10 实现口径、会话 Cookie 原样透传、前端 `EventSource` 口径、前端门禁存量红）；`spec.md` F11 补"档 2 完成"；档 2 计划文档补"执行记录"（含冒烟原始片段与三步对照）。

## 追加（同日 T11.1：用户实机反馈后的界面打磨）

用户在会话末尾拿真实浏览器试了助手（截图 `%TEMP%\t11-smoke\ui-answer.png` 就是那次），当场提两条：**回答里的 Markdown 没适配**（`**加粗**`、`- `/`1. ` 列表都字面显示）、**工具步骤平铺原始 JSON 对普通用户没必要**。当日完成并提交 `5fac9d2`（计划条目 plan.md T11.1）：

- **回答渲染 Markdown 子集**：新增 `utils/assistantFormat.ts`（纯函数解析）+ `components/assistant/AssistantText.vue` 与 `AssistantInline.vue`（块层 + 行内层，全部走 Vue 模板插值）。**不引 markdown 库、不用 `v-html`**——回答内容会被工具数据与提问影响，只有"永不产出 HTML 字符串"才不留 XSS 面（引库还得再引 sanitizer）。
- **工具步骤改「别名 + 摘要 + 详情」**：新增 `ToolStepRow.vue`，默认只显示「工具 · 读取标签词表 / 标签 13 个 · 分类 5 个」，原始 JSON 点「详情」才展开（默认收起）；摘要直接由工具返回载荷算出（共 N 个空间 / 共 N 张图片 / 标签 N 个·分类 M 个 / 失败：原因），解析失败退化为"已完成"，永不抛错。`StepTimeline.vue` 里思考步走 `AssistantText`、工具步走 `ToolStepRow`。
- **验证**：项目无前端测试框架，用 esbuild 把真实 `assistantFormat.ts` 转成 mjs 后在 node 跑 **19 条断言**（载荷直接取自真机 SSE 原文与回答原文）→ 19/19 通过（脚本 `%TEMP%\t11-smoke\format-check.mjs`）；改动文件 eslint 干净、`vue-tsc --build --force` 计数仍 **138（净增 0）**；浏览器实机复跑并截图 `ui-polish-default.png`（摘要态）/ `ui-polish-detail.png`（展开详情态）。
- **顺带观察（值得记）**：摘要里的数字比模型口播准——同一轮工具返回 13 个标签，模型口播"10个标签"，摘要直接读载荷所以是 13。工具摘要客观上给用户加了一层"事实锚"。

## 进行中 / 下一步

**下一会话第一件事：按规则 13 做档 2 里程碑独立 review——建议新开一个只读会话**，只读 AGENTS.md、spec 关键三行、`docs/features/F11-AI助手MVP.md` 与档 2 计划文档，专挑"做得对不对"（凭据组是否真的只在内存流转、`/ai/**` 不进拦截器的边界、SSE 中继语义与 `[DONE]` 处理、错误转 `answer` 是否够用、线程池上界与拒绝路径、与 B 口径的一致性）。review 结论记入 decisions.md 或该功能文档"已知限制"。

**review 之后的三条路（按价值排序）**：

1. **引擎部署前的必办项**（一期要真上线才碰）：旧端点 `/ai/manus/chat` 未鉴权、引擎端口/内网约束、双端密钥一致（引擎 `HUOSHAN_HEADLESS_API_KEY` 与 backend `AI_ENGINE_INTERNAL_API_KEY`）、`ai/` 应用的打包与启动方式。
2. **档 3 完整一期**：visionTagger 看图打标、`batchEditPictures` / `batchUploadByUrl`、T9 MCP 搜图、T8-hard 循环合并（统一 run/runStream、`run()` 退役）、T12 两条业务场景联调、T13 清理。
3. **挂账收口**（小颗粒、可穿插）：引擎侧"超时转显式错误事件"+埋点（顺延档 2 后仍未做）、R1（取消不关上游）、前端门禁存量清理、回答 Markdown 渲染。

## 未决问题

- **前端门禁存量红（本会话新发现，已记 decisions）**：`npm run type-check` **138 例**、`eslint .` **73 例**既有错误，全在本次未触碰文件里（`LocationQueryValue` 类型不兼容、`any`、未用变量一类，疑似 vue-tsc/eslint 版本升级后的债）；本次改动净增 0。AGENTS 与 handoff 0001 记的"前端以 type-check + lint 为准"**目前不成立**，需用户拍板是否单独立任务清理（本会话按规则 1 未顺手改）。
- **Markdown 只支持子集**（T11.1 已从"纯文本"升级）：粗体、行内代码、http(s) 链接、`- `/`1. ` 列表、`> ` 引用可用；**表格、嵌套列表、图片、标题、单星号斜体仍会字面显示**（要全覆盖需引 markdown 库 + sanitizer，取舍见 decisions.md 2026-09-14 呈现层口径）。
- **错误以 `answer` 形式呈现**：服务端失败看起来像模型在说话，没有独立错误视觉/事件（引擎侧"超时转显式错误事件"仍挂账）。
- **R1（客户端取消不关闭上游，P1）**：用户已拍板延后到档 2 之后。**档 2 的代理侧做了对照处理**（客户端断流即 `disconnect()` 代理→引擎那段），但**引擎→provider 那段仍不关**（`Flux.cache(0)`）。
- **旧端点 `/ai/manus/chat` 未鉴权**——引擎部署前必办。
- **超时转显式错误事件 + 超时/取消埋点**：原定顺延档 2，本会话未做（档 2 定义里没有它，只在代理侧做了超时兜底文案），仍挂在档 2 之后。
- **push / tag**：`main` 领先 `origin/main` **96 笔**（本快照与配套 docs 计入）未 push；仓库**仍无任何 tag**（用户未决，本会话未擅自 push 或打 tag）。
- **门禁余量**：稳态看门用例段间隔 300ms vs `CONTROL_TIMEOUT` 1000ms = 3.3×，2 核 CI 上偏紧（加固方案：提到 1500~2000ms，未做）。
- **T6 遗留 P3**：③ `@DurationUnit(SECONDS)`、⑥ `OpenAiChatModelsTest` 夹具去 `/v1`（T7 已触碰但未做，仍挂）；①②⑤⑦继续挂。
- **档 3 之后仍挂**：visionTagger / 两个批量工具 / T9 搜图 / T8-hard / T12 联调 / T13 清理。

## spec 偏差（认真轨）

- **越界（做了 spec 没写的）**：**无**。T10/T11 都在 plan.md 里有条目、在 spec 关键三行里有"档 2"定位；新增的只有 `docs/features/F11-AI助手MVP.md`（规则 9 要求）、四份文档同步与一条 spec F11 的"档 2 完成"补记。
- **缺口（spec 承诺但没做的）**：T11 的验收项"`npm run type-check` + lint 过"**未能达成**（存量红，本次净增 0），已在 plan/decisions/features 三处显式记录并挂账；"超时转显式错误事件"仍未做（档 2 定义外）。
- **关键三行检视**：**成立**——`ai/` 仍是独立 Maven 应用与独立进程（本会话 `ai/agent` 源码**零改动**，只重建过 jar）；"引擎端点只接受服务间 API key、不注册登录体系"继续成立（代理侧只做存在性判定，身份与授权仍全在图库）；"凭据只在请求生命周期内存流转、不落库、不进日志"落实为代码（`log.info` 只记 `userId`/`chatId`）+ 单测断言（凭据不进任何落库路径，仓库无落库代码）。

## 线上状态（已上线项目才填）

- 当前线上版本：https://www.lincode.online 仍是 backend / frontend 存量版本；**本会话所有改动均未上线、`ai/` 引擎仍未部署**（档 2 的界面在本地跑通，线上还没有 `/assistant` 入口）。
- 未处理反馈（[线上] 任务）：**无**（本会话无线上反馈项）。

## 环境备注

- 分支 `main`；**工作区干净**；ahead `origin/main` **96 笔**未 push；**无 tag**。
- **本会话提交链（8 笔，按序）**：`46d266d`（档 2 计划）→ `68ded79`（T10 端点+转发+配置+19 单测）→ `e329a48`（凭据链集成测试）→ `6d6e4f2`（T11 前端界面）→ `7988542`（fix 会话 Cookie 原样值）→ `7b62c00`（档 2 收口 docs）→ `7b9ebad`（本快照）→ `5fac9d2`（T11.1 界面打磨）+ 配套 docs 一笔。
- **门禁口径（本会话实跑）**：
  - backend：`cd backend && mvn -B -o test "-Dtest=!*IntegrationTest,!RedisStringTest,!YunPictureBaseApplicationTests" -DfailIfNoTests=false` → **32 例绿**（13 存量 + 10 `AiAssistantControllerTest` + 9 `AiAssistantProxyManagerTest`）。
  - ai/agent：87 例绿（`.githooks` 与 CI 口径不变）。
  - **cmd 坑**：上面那条 `-Dtest` 的值在 cmd 里必须用**双引号**；单引号是字面量，会让 surefire 报 "No tests matching pattern"（AGENTS 里那条命令是给 sh 门禁用的）。
  - 本地集成测试：`-Dtest=AiAssistantProxyIntegrationTest` → 3 例绿（需 MySQL/Redis 在跑）。
- **本机服务状态（会话末尾已全部停止：8123 / 8124 / 5173 均已释放，不占端口）**。需要复测时按下面重启（三个都要，AI 助手链路缺一不可）：
  - backend `8123`：`set "SPRING_PROFILES_ACTIVE=local,test"` + `set "AI_ENGINE_INTERNAL_API_KEY=smoke-internal-key"` + `java -jar backend/target/yun-picture-base-0.0.1-SNAPSHOT.jar`（日志 `%TEMP%\t11-smoke\backend2.log`）；
  - 引擎 `8124`：`HUOSHAN_BASE_URL=http://localhost:8123/api`、`HUOSHAN_HEADLESS_API_KEY=smoke-internal-key`、`AI_OPENAI_BASE_URL=http://localhost:11434`、`AI_OPENAI_API_KEY=ollama`、`AI_OPENAI_MODEL=qwen3:4b`（日志 `engine2.log`）——两端密钥必须一致；
  - 前端 `5173`：`cd frontend && npm run dev`（日志 `frontend.log`）；
  - 改动源码后**先重建 jar 再冒烟**（档 1 踩过"jar 陈旧于源码"的坑）。
  - **收尾提醒**：`local,test` profile 只为冒烟取真凭据，**线上/生产绝不能带 test**。
- **可复用的冒烟资产（都在仓库外 `%TEMP%\t11-smoke\`）**：`login.json`（探针账号登录体，纯 ASCII）、`msg.txt`（中文提问，独立 UTF-8 文件）、`cookies.txt`/`cookies2.txt`、`sse.txt`/`sse2.txt`/`sse-neg.txt`（三轮 SSE 原始输出）、`headers.txt`、`backend*.log`/`engine*.log`/`frontend.log`、`ui-answer.png`（浏览器验收截图）。原型资产在 `%TEMP%\assistant-ui-proto\`（`prototype.html` + `prototype.png`）。
- **两个新踩的坑（记下备用）**：① 浏览器自动化点不动这个前端——Playwright 的 `click` 一直卡在可操作性检查（元素可见可用、`elementFromPoint` 也确认没被遮挡），`tab.cua.click` 同坐标也不生效；**改用 `playwright.evaluate` 里 `btn.click()` 驱动 Vue 处理链**即可（fill 一直正常），截图第一次报 `browser screenshot activity capture failed for guest`、重试即成功；② 用 `type`/`findstr` 看 UTF-8 日志/SSE 会显示成 GBK 乱码，**别据此判断数据错**（用 Read 工具或浏览器看原文）。
- **版本锚点**：backend SB 2.7.6 / Java 17；引擎 SB 3.5.13 / Java 21 / Spring AI 1.1.2；Ollama 本机 11434（`qwen3:4b` 带 tools）；后端 context-path `/api`、actuator 在 8125。
- **探针残留（本地开发库）**：账号 `tier1probe0914140822`（密码 `Probe12345`）与空间 `tier1-probe-space`（id `2099389400592543745`，无图片）；另有集成测试自建自删的临时账号（`ai-proxy-*`）。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**；
2. **第一件事按规则 13 做里程碑独立 review**（建议新开只读会话），范围与重点见本快照"进行中 / 下一步"第一段；
3. review 后读 `docs/plan.md` 的「MVP 档位与推进顺序」（档 3 行）+ `docs/decisions.md` 最后 5 条（档 2 四条 + 前端门禁存量）决定方向；
4. 需要实现细节时：T10/T11 看 `docs/features/F11-AI助手MVP.md` 与 `docs/plans/2026-09-14-档2-用户可见MVP实施计划.md`（含冒烟原始片段、三步对照、执行偏差）；档 1 引擎侧看 `docs/plans/2026-09-14-档1-headless打通实施计划.md`。
