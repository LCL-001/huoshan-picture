# Handoff 0009 — T6 独立 review 完成（2026-09-14）

## 当前状态（一句话）

T6（OpenAI 协议模型接入）的独立只读 review 完成，结论**有条件通过**——交付本体经字节码反查与实机实测成立，唯一 P1 是**流式路径无任何超时**（已立 T6.1，T7/T8 前必做）；本会话零代码改动，只落两份文档（`9a780a7` / `26b3ce4`）。下一步按序：**T6.1 → T7 图库工具集**。

## 刚完成（本会话，按时间）

- **只读复核 T6 六个方面**（核心设计取舍 / 流式超时 / 配置守卫 / 配置语义 / 测试有效性 / spec 一致性），全部落到字节码或自跑命令输出，未采信任何既有叙述。
- **落盘**：
  - `9a780a7`：decisions.md 追加"T6 独立 review：有条件通过"一行；plan.md T6 条目补"独立 review"记录（七项复核证据 + P1/P2/P3 全量口径），并在 T6 与 T7 之间新增 **T6.1** 任务条目。
  - `26b3ce4`：T6.1 口径细分——从"给 WebClient 接超时"扩为"**首包超时 + 流空闲超时**两件事缺一不可"（附三形态实测），并记明**不可用"保留 `BaseAgent.run()`"当兜底**（用户当次追问）。
- 提交前已确认仓库无代码/配置改动；探针改过的 `target/classes/application.yaml` 已还原并校验哈希与 src 一致。

## 实测证据索引（新会话可直接复用，不必重跑）

- **守卫必要性**：`spring-ai-autoconfigure-model-openai:1.1.2` 的 imports 恰好六条（chat/embedding/image/audio.speech/audio.transcription/moderation），六条全部 `@ConditionalOnProperty(name="spring.ai.model.<type>", havingValue="openai", matchIfMissing=true)`（逐类 `javap -v`）。运行时 `--debug` 条件报告：六条全落 Negative matches，理由 `found different value in property`。
- **守卫充分性（全量扫描）**：classpath 上 16 个 autoconfigure jar 的常量池里 `spring.ai.model.*` 键**总共 8 个** = OpenAI 六类 + DashScope 独有 `rerank`/`video`。T6 守卫正好覆盖 OpenAI 六类，无遗漏；`rerank`/`video` 在 ollama profile 下被 `spring.ai.dashscope.enabled=false` 关掉（既有状态，与 T6 无关）。`none` 不是被特殊识别的值，任何非 `openai` 的值都能关掉。
- **升级风险**：1.1.8 与 1.1.2 的 OpenAI imports 清单相同（均六条），暂无漂移；守卫是清单式，上游新增第七类会默认启用。
- **持有器取舍**：`ChatClientAutoConfiguration#chatClientBuilder(…, ChatModel, …)` 形参即单个 `ChatModel`（`@Bean @Scope("prototype") @ConditionalOnMissingBean`）；容器实有 `ollamaChatModel`（`@ConditionalOnMissingBean(types: OllamaChatModel)`），另有 4 处按类型注入 `ChatModel`（`AiController.java:33`、`ConversationTitleService.java:15`、`FlowWindowBasedChatMemory.java:34`、`MyManus.java:45`）→ 再注册一个 `ChatModel` Bean 即启动崩（响亮失败，非静默）。
- **流式/非流式客户端（P1 根因）**：`OpenAiApi.chatCompletionEntity` 取 `restClient`、`chatCompletionStream` 取 `webClient`（字节码）；`OpenAiApi$Builder` 无参构造兜底 `WebClient.builder()`，而 `OpenAiChatModels#build` 只调 `restClientBuilder(...)`。反射实测装配结果：`connector=JdkClientHttpConnector`、`readTimeout=null`、JDK `HttpClient.connectTimeout=Optional.empty`（classpath 无 reactor-netty/jetty/HC5，`DefaultWebClientBuilder#initConnector` 按序落到 Jdk）。
- **超时三形态（`readTimeout=3s` 实测，决定 T6.1 的做法）**：稳态吐字的长流 **3.4s 正常完成、6 段全收**（不是整响应死线，不会误杀长回答）｜完全不响应 **3020 ms 抛** `WebClientRequestException: java.net.http.HttpTimeoutException: request timed out`（等价 JDK `HttpRequest.timeout`，只覆盖到响应头到达）｜吐 2 段后静默 **20s 仍 pending、无任何异常**（必须靠流侧空闲超时兜）。
- **base-url 语义**：不带 `/v1` → `/v1/chat/completions`；带 `/v1` → `/v1/v1/chat/completions`；留空 → 生效 `https://api.openai.com`。实现里没有"防"，只有配置注释 + 启动装配日志，误配后果是 404（响亮）。
- **配置边界实测**：半配置在真实上下文**启动即失败**（`BeanCreationException: … Factory method 'openAiChatModels' threw exception with message: app.ai.openai.vision 配置不完整：api-key 与 model 必须同时提供`）；只配 base-url → `configured=false partial=false`，静默忽略；`timeout` 裸数字按**毫秒**（`60` → `PT0.06S`，`60s` → `PT1M`）。
- **测试有效性**：门禁口径 62 用例含新增 7 条（实测跑通）；守卫测试负向探针两失效面都实测红（删 `image: none` 行 / 置 `image: openai`），pristine 基线绿，还原后哈希一致。缺口：**7 条单测无一断言"超时真的接到 HTTP 客户端上"**（P2，也正是 P1 漏网原因）。
- **密钥**：T6 三笔代码提交的 diff 只有占位符 `sk-your-openai-protocol-key`；`git ls-files` + `git check-ignore -v` 确认 `ai/**/.env`、`ai/**/application-local.yaml` 仍在 .gitignore 内（:15-16）。

## 进行中 / 下一步

- 无进行中任务。**下一步按序：T6.1（P1，必做）→ T7 图库工具集。**
- **T6.1 口径（全文见 docs/plan.md，含证据与验收）**：① 装配 `OpenAiApi` 时补 `webClientBuilder(...)`，connector 设连接/首包超时（覆盖"连不上/首包不来"，顺带修掉 JDK client 默认无连接超时）；② 流上补**空闲超时**（`Flux.timeout(Duration)` 逐信号，或 T8 事件循环的看门狗），覆盖"吐一半卡死"。验收含"稳态长流不被误杀"。文件范围 `ai/agent` 的 `config/OpenAiChatModels.java` + 测试（②若落在 T8 循环里，范围随之加）。**不要用保留 `run()` 兜底**（差异在 HTTP 客户端不在同步/异步；SSE 是产品必需；且与 R2 拍板相反）。
- **T7 开工口径不变**：模型入口 `OpenAiChatModels.vision()`；文件范围 `tools/huoshan/` + 必要 config/DTO；粗粒度工具、一期不注册删除类工具；开工前按 writing-plans 写即时计划（范本 T5/T6 计划文档）。

## 未决问题

- **P1**（流式超时）与 **P2**（超时接线无测试）已登记为 T6.1；**P3 七项**见 docs/plan.md T6 条目"review 发现"，其中三处是"一行级 hardening"，可顺手做：③ `timeout` 加 `@DurationUnit(SECONDS)` 或注释写明单位；④ 只配 base-url 被静默忽略——文档写明或纳入半配置拒绝；⑥ `OpenAiChatModelsTest` 夹具 base-url 仍带 `/v1`（:21、:26）与 b5edf74 口径相反，建议改成不带 `/v1` 的形态。另 ①（守卫测试看不到 profile 覆盖）②（升级检查项）⑤（守卫属性与 DashScope 同名共享，yaml 注释宜补一句）⑦（半配置 fail-fast 会拒掉"只注入一半变量"的姿势）记已知限制。
- **视觉多模态路径仍未实测**：T6 只验了文本调用（本机 Ollama 兼容端点返回「收到」）；`image_url` 多模态与云端视觉模型（qwen-vl 等）要等 T7 的 visionTagger。
- **本地无已验证可用的 OpenAI 协议云端 key**（application-local.yaml 里那个 dashscope key 未验证可用）。
- 待实施期确认项不变：T10 的 SSE 转发 SB 2.7 选型、T9 的 Pexels key 实测、T7/T12 的视觉模型读 COS 私有空间签名 URL 可用性。
- main 落后 origin/main **69 笔**未 push；仓库**无任何 tag**。
- 依赖审计口径：Maven 侧不在门禁依赖审计范围内（AGENTS 规则 14 只覆盖 npm 栈）。
- 小挂账：`docs/features/F10-标签词表.md` 首节名仍是旧模板的"是什么"（不强制返工）；T6 未写 features 讲解文档（使能型任务，F11 讲解按 plan 归 T12）。

## spec 偏差（认真轨）

- **越界（做了 spec 没写的）**：无。本会话零代码/配置改动（只改 docs），T6 原交付的文件范围与 plan 声明一致。
- **缺口（spec 承诺但没做的）**：T6 范围内唯一实质欠账是 P1（流式超时）——配置项承诺"单次请求超时"，实际只兑现了阻塞路径那一半；已从"已知限制"升级为 T6.1 前置任务。F11 其余项分属 T7-T12，非 T6 缺口。
- **关键三行检视**：成立。实测传参启动 `ai/agent` → `OpenAI 协议模型就绪` 日志 + `/api/health=ok` 同时成立，普通会话仍走 ollama profile 的 `ollamaChatModel`；backend/frontend 零改动，`ai/` 仍为独立应用独立进程。

## 线上状态（已上线项目才填）

- 当前线上版本：https://www.lincode.online 为存量图库；仓内无 tag 可与线上版本对应。
- 未处理反馈（`[线上]` 任务）：docs/ 下无 `[线上]` 前缀任务（T4-T6 与本次 review 都在 `ai/` 与文档层，尚未部署）。

## 环境备注

- 分支：main（ahead origin/main 69 笔，未 push）；未提交改动：无，工作区干净；本会话提交：`9a780a7`、`26b3ce4`。
- 本机：Maven 3.9.11 + JDK 21（本地仓库在 `D:\develop\Maven\apache-maven-3.9.11\maven_repo`，不是 `%USERPROFILE%\.m2\repository`）；MySQL 3306 / Redis 6379 / Ollama 11434 均在跑。
- 门禁：密钥扫描（always）→ 纯文档豁免 → lint/typecheck/依赖审计（存在才跑）→ 测试（backend 13 + ai/agent 62）；ai/agent 侧是排除名单式（`!MyManusTest,!MyAiAgentApplicationTests,!ProductScopeTest,!PgVectorVectorStoreConfigTest,!FileOperationToolTest,!PDFGenerationToolTest,!ResourceDownloadToolTest,!TerminalOperationToolTest,!WebScrapingToolTest,!WebSearchToolTest`）。
- **复核探针与日志全在 `%TEMP%\t6rev\`（仓库外，可随时删）**：`T6TimeoutProbe.java`（超时/base-url）、`T6StreamTimeoutProbe2.java`（三形态）、`T6EdgeProbe.java`（配置边界）、`T6ConnectorProbe2.java`（反射 connector）、`rev-run.log`（`--debug` 条件报告全文）、`run-*.ps1`（脚本，含 classpath 拼接方式：目标/日志路径都在里面）。classpath 复用 `%TEMP%\t6-smoke\cp.txt`。
- **两个可复用的小坑**：① 跑探针需 PowerShell（classpath 长度 27k 超 cmd 限制），照 `run-stream2.ps1` 抄即可；② 临时改 `target/classes/application.yaml` 做负向探针后要用 `mvn surefire:test`（不要再跑 `test`，会 process-resources 覆盖回去），改完必须还原——本会话已还原并校验哈希与 src 一致。
