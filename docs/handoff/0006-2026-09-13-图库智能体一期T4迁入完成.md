# Handoff 0006 — 图库智能体一期立项与 T4 引擎迁入完成（2026-09-13）

## 当前状态（一句话）

图库智能体一期立项并完成 T4：MyManus 引擎与 image-search-mcp-server 已迁入本仓 `ai/`（独立应用、独立进程），门禁覆盖双套件，源仓库 yu-ai-agent 冻结归档；下一步 T5 标签词表（backend）与 T6 OpenAI 协议模型接入（ai/agent）可并行。

## 刚完成（本会话，按时间）

- **一期立项头脑风暴**（决策链全程逐项用户拍板）：目标=图库真实功能优先 → 场景=整理管家+选图入库代理（审核助手后续立项、周报不做）→ 形态=图库内嵌助手（图库前端界面 + backend 代理调引擎）→ 身份=代理透传 satoken 不落库（无绑定向导）→ 模型=OpenAI 协议可插拔（多 ChatModel 按会话类型装配）→ **R2：引擎代码整体迁入本仓**（Spring AI 无法运行于 SB 2.7 进程，故同仓不同应用）→ **对话循环收敛为仅 SSE 模式**（同步 run() 退役，挂 T8 执行）。全部记入 docs/decisions.md 两条 + docs/designs/2026-09-13-图库智能体一期设计.md（R2 修订记录）。
- **文档落盘**（双仓）：spec 增补 F10 标签词表 / F11 AI 助手一期；plan 一期任务 T4-T12 + T13 可选；关键事实核实——标签词表现状为 Controller 硬编码 9 词（PictureController.java:250-256），编辑不校验词表（PictureServiceImpl:1152 直接 toJsonStr），搜索走 LIKE；`/picture/edit/batch`、`/picture/upload/url` 现成可用。
- **T4 完成**（实施计划 docs/plans/2026-09-13-T4-AI引擎迁入实施计划.md）：
  - 88b50bf 迁入 ai/agent（原样迁移，端口 8123→8124，55 用例基线在新位置全绿；密钥件 application-local.yaml/.env 手动复制不入库，.gitignore 已覆盖）
  - f1c4a1a 迁入 ai/image-search-mcp-server（8127，顺带修复 ImageSearchTool.java UTF-8 BOM 编译错误——源仓遗留缺陷）
  - 4560fd2 门禁纳入 ai/agent 单测（沿用源仓排除清单；负向探针被拦 + 正向双套件绿，两方向均实测）
  - 286629e AGENTS/README 速览补 ai/ 说明，plan 勾选 T4；本次提交门禁首跑双套件全绿（backend 13 + ai/agent 55）
  - 双服务冒烟通过：8124 /api/health → ok；8127 Tomcat 启动正常
- **源仓库 yu-ai-agent 归档**：用户 README 手改已代提交（8a5f450）+ 归档 handoff（6828310）+ tag `archive/engine-migrated-to-huoshan`；此后只读，勿在那开发。

## 进行中 / 下一步

- 无进行中任务。plan T5-T13 待开工（T5 标签词表 / T6 OpenAI 协议模型接入 可并行；随后 T7 图库工具集 → T8 会话类型+headless 端点（含仅 SSE 循环收敛）→ T9 MCP client 接入 → T10 backend 代理 → T11 前端助手界面 → T12 联调验收）。
- 每任务开工前按 writing-plans 写即时实施计划（范本见 T4）。

## 未决问题

- 无阻塞决策。已知待实施期确认：SSE 转发的 SB 2.7 选型（SseEmitter/OkHttp，T10）；Pexels key 有效性实测（T9）；视觉模型读 COS 私有空间签名 URL 可用性（T7/T12）；image-search-mcp-server 的 PEXELS_API_KEY 目前走本地 yaml。
- dev/main 均未 push（两仓），何时推远程由用户决定。

## spec 偏差（认真轨）

- 越界（做了 spec 没写的）：无——ImageSearchTool BOM 修复属迁移验收内的编译必需修复，已在 commit 注明。
- 缺口（spec 承诺但没做的）：F10/F11 未实现（T5-T12 挂账，属进行中非漂移）；"目标/不做的事/边界"三行经本轮检视仍成立。

## 环境备注

- 分支：main；未提交改动：无；本会话产出 88b50bf…286629e 共 8 笔 + 前置文档 5 笔。
- 本机：Maven 3.9.11 + JDK 21；MySQL(3306)/Redis(6379) 在跑；agent 本地库 yu-ai-agent 沿用。服务端口：backend 8123 / ai/agent 8124 / MCP 8127 / 前端 5173。
- 门禁：pre-commit 现跑 backend 无 MySQL/Redis 单测（13 用例）+ ai/agent 纯单测（55 用例，排除 10 类清单见钩子）；`.githooks/`-only 提交按 docs 豁免跳过。
- 密钥件（不入库，已就位）：ai/agent/application-local.yaml、ai/agent/.env、ai/image-search-mcp-server/src/main/resources/application-local.yaml。
- 源仓 yu-ai-agent（D:\develop\codes_claudecode\yu-ai-agent）已归档：tag archive/engine-migrated-to-huoshan，dev 领先远程未 push，完整历史与 doc/ 本地材料在彼仓。
- Windows 工具链坑（0004/0005 记录仍有效），本会话新增：robocopy 退出码 0-7 均为成功（勿与 && 链接）；Bash 工具里 cmd 单 `&` 链接会被转义拆坏（用 `&&` 且注意同样断链）；长日志用 PowerShell `Get-Content -Tail`/`Select-String` 读。
