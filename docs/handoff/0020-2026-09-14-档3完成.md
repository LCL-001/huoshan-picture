# Handoff 0020 — 档 3 完成（工具 / 搜图 / DeepSeek 接入 / 联调）（2026-09-14）

> 上一份：`0019`（R5 验收 ⑤ 复跑收口）。本文件是**档 3 实施会话**的快照。
> 本会话共 **12 笔提交**（R5 那两份快照之外）：档 3 计划 → T7 写类工具 → T7 看图打标 → T9 搜图 MCP → T13 清理 → DeepSeek 接入（含 extra-body 能力）→ T12 两条场景联调（含修掉一个真 bug）。

## 当前状态（一句话）

**档 3 除 T8-hard 外全部完成**：图库助手新增 `visionTagger` / `batchEditPictures` / `batchUploadByUrl` 三个工具（+ 档 1 的三个只读工具 + MCP `searchImage`），模型接入 **DeepSeek `deepseek-flash`**，**两条业务场景已在真机链路上跑通并核对落库**（智能整理 12.6s、选图入库 9.3s），联调抓到并修掉一个真 bug（记忆回放里的孤立 tool_calls 让 DeepSeek 第二轮必 400）；门禁 **113 例**绿。**只剩 T8-hard**（统一 run/runStream、`run()` 退役、事件协议固化为显式契约）。

## 刚完成（本会话，按时间）

1. **立计划**（`a3414fb`）：`docs/plans/2026-09-14-档3-完整一期实施计划.md`——六个任务的顺序与理由、文件范围、验收、阻塞项、风险（含"List 参数能否生成数组 schema"这条先手风险）。
2. **T7-a 写类工具**（`c003eea`，门禁 99 例）：`BatchEditPicturesTool` / `BatchUploadByUrlTool` + 三个结果 DTO + 客户端 `batchEditPictures` / `uploadByUrl` + `HuoshanToolFactory.assistantTools`（取代 `readOnlyTools`）+ 提示词放开写操作。**schema 契约测试**：`pictureIdList` / `fileUrls` 必须发布成 JSON `array`（第一次写还把参数名猜错成 `pictureIdList` vs 实际 `pictureIds`，正好证明断言在生效）。
3. **T7-b 看图打标**（`e896af0`，门禁 107 例）：`VisionTaggerTool`——只收图片 id，自己经 `GET /picture/get/vo?id=` 取地址再逐张调多模态模型（**不让模型搬 URL**），提示词注入当前词表，**只给建议不落库**；单次上限 8 张；未配视觉模型时工厂**不挂**这个工具。
4. **T9 搜图 MCP**（`58ba4cc`，门禁 108 例）：`spring.ai.mcp.client` 接 `ai/image-search-mcp-server`（**SSE**，8127），`initialized: false`（**MCP 没起也不拖垮引擎启动**），`AI_MCP_CLIENT_ENABLED` 总开关；工具**只并进图库助手**（`ObjectProvider<ToolCallbackProvider>`），`MyManus` 的 `ToolRegistration` 一字未动。**双进程冒烟**：`tools/list`→`["searchImage"]`、`tools/call`→15 条 Pexels 地址；引擎侧一次性上下文冒烟确认装配并调用成功。
5. **T13 教学遗留清理**（`05db19e`）：删 8 个文件（假 `AuthAdvisor` 328 行、`FileBasedChatMemory` 147 行、两个 20 行空壳 `@RestController`、`demo/invoke/` 四个类）。**纠正计划里一处误导**：`ChatMessage`/`ChatSummary` 的 PO/Mapper/Repository/Service **不是死代码**（是 `FlowWindowBasedChatMemory` 的落地表），只删那两个空壳 Controller。
6. **DeepSeek 接入**（`691ff7f` + `c35b6f2` + `a451cb4`）：用户点名 `deepseek-flash`。查官方文档确认 `base-url=https://api.deepseek.com`、`deepseek-flash` **支持 Tool Calls 与图像理解**（`v4-pro` 不支持图片）。**关键发现**：DeepSeek 思考模式默认开启，而**带 `tools` 的请求每轮必须回传 `reasoning_content`**，Spring AI 的 OpenAI 客户端不认这个私有字段 ⇒ **第二轮必 400**；故显式关闭思考模式——为此**新增 `extra-body` 配置能力**（`ModelEntry.extraBody` → `OpenAiChatOptions.Builder.extraBody`），厂商私有参数只写环境配置。配置落在不入库的 `application-local.yaml`（顺带确认**引擎不读 `.env`**：没有 dotenv 加载器）。**真机冒烟三项全通**：`/v1/chat/completions` 可用、带工具多轮通过（`TOOL CALLS 1`）、图像输入对**图库 COS 图**可用；Pexels CDN 地址被 DeepSeek 拒（不影响链路——它只是入库的输入）。
7. **T12 两条场景真机联调**（`c02309f` + `f6f2fd5` + `b008330`）：真机链 = 脚本 → backend 8123（`local,test`）→ 引擎 8124（`profile local`，DeepSeek）→ 图库 API + MCP 8127，探针账号真登录。
   - **抓到真 bug 并修复**：第一次 `listSpaces` 成功，**第二次模型调用直接 400**（`assistant message with 'tool_calls' must be followed by tool messages`）。查 `chat_message` 表确证根因：**记忆只持久化 assistant 的 toolCall 决策行、不持久化工具响应行**，回放出来的 assistant 消息天生"带 tool_calls 却没有配对响应"。修复 = 新增 `MessageConverter.toReplayMessages` 作为**给模型的那条路**专用转换（工具响应行丢弃、assistant 只留文本去 toolCalls、纯决策行整条丢弃），`FlowWindowBasedChatMemory` 6 处回放全换；折叠条读库里原样元数据，不受影响。**以前没暴露是因为 DashScope 兼容模式宽松不校验**——换厂商必须重跑联调的实证。
   - **加固**：工具入参剥掉包裹引号（MCP 返回的是带引号的 JSON 字符串字面量）。
   - **场景一（智能整理）12.6s 通过**：列空间+词表 → 拉清单（3 张无标图）→ **看图建议（全部复用词表已有标签）** → 逐张批编 → **模型自发回读清单复核** → 汇总回答。落库核对：3 张 `tags` 写入、2 张 `category=素材`、`tag.usageCount` 增量符合 T5 口径（一次调用计 1）。
   - **场景二（选图入库）9.3s 通过**：搜图（15 条）→ 批量入库（提交 3 / 成功 3 / 失败 0）→ 回读确认。落库核对：空间 3→6 张、`totalCount=6` / `totalSize=87108`。
   - 讲解文档 `docs/features/F12-图库助手档3工具与联调.md`（含 8 条已知限制）。

## 进行中 / 下一步

1. **T8-hard（档 3 唯一剩余项）**：抽事件消费者接口统一 `run`/`runStream`、**`run()` 退役**（存量调用方迁到事件流收集口径）、`lastStepKind` 等受保护字段收编进事件载荷、**SSE 事件协议固化为显式契约测试**。**本会话刻意没起头**：它动的是 `BaseAgent`/`ReActAgent`/`ToolCallAgent` 这段共享循环，还要迁移 6 个测试类，不想在没法收尾的情况下动。
2. **R5 review 的 6 条 P3**（可穿插）：前端取票判 `data.code`、`getAndDelete`/javadoc 订正（**必须改代码**）、"票不转发给引擎"补 1 行断言、拒绝路径可观测性、`/ticket` 频控与长度校验。
3. **上线前必办 / 部署前必办**：nginx server 块与 SSE 配置、引擎旧端点 `/ai/manus/chat` 未鉴权、双端密钥一致（引擎 `HUOSHAN_HEADLESS_API_KEY` = backend `AI_ENGINE_INTERNAL_API_KEY`）、`application-prod.yaml` 的 apex/WS 改动要**重新打包**才生效、**MCP 服务要与引擎同机/内网**（不部署则设 `AI_MCP_CLIENT_ENABLED=false`）。
4. **apex 尾巴**：定唯一主机名（www）+ 另一个 301（nginx，不在仓库）。

## 未决问题

- **F12 已知限制 8 条**（详见该文档）：记忆里同一条用户消息被 advisor 重复写入（实测 3 次，浪费 token）、MCP 返回是"逗号拼接整串"（不按逗号拆分）、看图逐张串行（一步数十秒）、打标不自动落库、**前端摘要认不出新载荷**（`assistantFormat.ts` 会退化成"已完成"）、批编逐张结果不可知、`/v1` 路径换厂商须重测、T8-hard 未做。
- **多实例部署未验**、浏览器兼容未验（只在 Chromium）、`EventSource` 自动重连在真实 403 下的行为未验（同 0018）。
- **push / tag**：`main` 领先 `origin/main` **130 笔**未 push；仓库**无任何 tag**（用户未决）。
- 沿用 0018/0019 的其它挂账：门禁余量、T6 遗留 P3 ①②⑤⑦、R1（取消不关上游）、"用户提问原文进 nginx access log"。

## spec 偏差（认真轨）

- **越界（做了 spec/plan 没写的）**：**两处小的越界加固，均已记账**——① T9 计划里"传输方式实施时定"，我定为 SSE（记 decisions）；② T12 联调发现"工具入参带引号"的既有上游形态，顺手在 `cleanList` 剥引号（记 decisions + 测试）。其余全部落在 T7/T9/T12/T13 条目内。
- **缺口（spec/plan 承诺但没做的）**：**T8-hard 一条**（见上，本会话未起头，已挂在 plan.md 与 F12 已知限制 8）。
- **关键三行检视**：**成立**——引擎仍是独立 Maven 应用、独立进程（本会话所有改动都在 `ai/` 与 `docs/`，**backend/ 一行未改**）；"引擎端点只接受服务间 API key、不注册登录体系"未变；"凭据只在请求生命周期内存流转、不落库、不进日志"未变（本会话新增的 DeepSeek 密钥写在**不入库**的 `application-local.yaml`，未进任何提交）。

## 线上状态（已上线项目才填）

- 当前线上版本：https://www.lincode.online 仍是 backend / frontend **存量版本**；档 2/3 全部改动（含 R4/R6/R5 与本次工具）未上线，`ai/` 引擎与 MCP 服务均未部署。本会话无线上操作。
- 未处理反馈（[线上] 任务）：**无**。

## 环境备注

- 分支 `main`；**工作区干净**；**本会话 12 笔提交**（a3414fb / c003eea / 449fe75 / e896af0 / 58ba4cc / 87e02fd / 05db19e / 23388b0 / 691ff7f / c35b6f2 / a451cb4 / c02309f / f6f2fd5 / b008330，其中 5 笔为 docs）。ahead `origin/main` **130 笔**；**无 tag**。
- **本机服务状态**：MySQL 3306 / Redis 6379 常驻；**本会话起过的 8123 / 8124 / 8125 / 8127 全部已停**（`netstat` 复核无监听）。
- **启动三件套（真机联调用）**：
  - backend：`set "SPRING_PROFILES_ACTIVE=local,test" && set "AI_ENGINE_INTERNAL_API_KEY=smoke-internal-key" && java -jar backend\target\yun-picture-base-0.0.1-SNAPSHOT.jar`
  - MCP：`cd ai/image-search-mcp-server && java -jar target\image-search-mcp-server-0.0.1-SNAPSHOT.jar`（8127，sse profile 默认）
  - 引擎：`set "SPRING_PROFILES_ACTIVE=local" && set "HUOSHAN_HEADLESS_API_KEY=smoke-internal-key" && set "HUOSHAN_BASE_URL=http://localhost:8123/api" && java -jar ai\agent\target\my-ai-agent-0.0.1-SNAPSHOT.jar`（8124；**改代码后要 `mvn -B -o package -DskipTests` 重打包**）
- **本会话新踩的坑**：
  1. **Maven 本地仓库不在 `~/.m2`**，在 `D:\develop\Maven\apache-maven-3.9.11\maven_repo`（要找 jar 用 `mvn help:evaluate -Dexpression=settings.localRepository`；`~/.m2` 里那份是别的项目留下的、版本对不上）。
  2. **cmd 不认单引号**：`mvn -Dtest='!A,!B'` 会把引号整串传进去 → `No tests matching pattern`；必须用**双引号**。含 `|` 的 PowerShell 也不能直接塞进 cmd 单引号里（会被当管道），要写成 `.ps1` 再 `-File` 跑。
  3. **重定向目标目录必须先存在**：`> "%TEMP%\t12\x.log"` 在目录不存在时让整个命令 exit 1（现象像"服务起不来"）。
  4. **`URI.create(url).toURL()` 抛受检异常**，不能写在 lambda 里（`spec.media(...)`）——先在外面构造 URL，两处都踩过。
  5. **Spring AI 的 `Media(MimeType, URL)` 形状是对的**（不用自己 base64）：COS 图直接可用；`MimeTypeUtils` 没有 webp 常量，用 `MimeType.valueOf("image/webp")`。
- **仓库外资产（无凭据，可复用）**：`%TEMP%\t12\` —— `chat.js`（真登录→取票→带票建流，打印 SSE 帧）、`upload.js`（按 URL 批量入库）、`msg-s1.txt` / `msg-s2.txt`（两条场景的提问原文）、`jar.txt`（**cookie 罐，含凭据，用完请删**）、`pexels-urls.txt`、`engine.log` / `engine2.log` / `backend.log` / `mcp.log`；`%TEMP%\t9-mcp\mcp-smoke.js`（手写 MCP-over-SSE 客户端，`URL_OUT` 可落 URL 列表）。
- **探针残留（本次未清理，供用户核对结果）**：`yu_picture` 库里探针账号 `t12probe01`（id `2099522489805303810`）、空间 `t12-organize`（id `2099522943394115585`，**6 张图**，含 3 张已打标 + 3 张场景二新入库的）+ COS 上对应对象；引擎库 `yu-ai-agent` 的 `chat_message` / `chat_summary` 里有会话 `e10ada56-…`（t12-s1）与 t12-s2 的记录。**Redis**：票键 60s 自灭，satoken/会话键随账号删除才失效。要清干净：删该账号的 space/picture 行（或直接 `DELETE FROM user WHERE userAccount='t12probe01'` 后按 spaceId 清 picture）+ `DELETE FROM chat_message WHERE conversation_id IN (...)`。
- **版本锚点**：backend SB 2.7.6 / Java 17 / sa-token 1.39.0；引擎 SB 3.5.13 / Java 21 / Spring AI 1.1.2；后端 context-path `/api`、actuator 8125；引擎 8124；MCP 8127；**本地 Redis 5.0.14.1**；主脑与 vision = DeepSeek `deepseek-flash`（思考模式关闭）。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**；档 3 的口径与证据看 `docs/features/F12-图库助手档3工具与联调.md` + `docs/decisions.md` 2026-09-14 的七条（B 口径、id 字符串、Redis 5.x、R5、T7 工具集、DeepSeek 接入、T12 修复×2）。
2. **下一件事建议 T8-hard**（档 3 唯一剩余项，见上"进行中"）；动它之前先把 `BaseAgent`/`ReActAgent`/`ToolCallAgent` 与 4 个相关测试类读透——本会话的 `fix: 记忆回放…` 已经证明**这段循环对消息序列很敏感**。
3. 要复现两条业务场景：照"环境备注"起三件套 → 用 `%TEMP%\t12\chat.js` 跑 `msg-s1.txt` / `msg-s2.txt`（**注意 cookie 罐已含凭据、且探针账号还在**）；改代码后先重打包引擎。
4. 坑与命令口径看本文件「环境备注」（Maven 仓库位置、cmd 引号、重定向目录、lambda 受检异常、webp MIME）；更早的口径看 0016（`set "VAR=value"` 不留尾随空格、cookie jar 的 `#HttpOnly_`、Redis 5.x）与本会话 0019。
