# Handoff 0028 — 2026-09-16 08:10（含会话 2 增量）— 部署产物入库 / 探针数据清理 / DashScope 实测 / 引擎侧摘掉 DashScope

> 上一份：`0027-2026-09-15-T18补记与T21-T24收口.md`（含 09-15 当天的会话 2/3/4）。
> 本快照按规则 6 **只写增量**，且**一天一份**：下面是本日两次会话的增量，09-15 之前的上下文读 0027，不复述。
> 工作区干净；分支 `main`；ahead `origin/main` **211**（origin 停在 2026-09-09）；**仍无 tag、仍未 push**。

## 当前状态（一句话）

**物料齐了、模型耦合也摘干净了，但还差一次"排练"**：D3/D4 部署产物已入库、探针数据已清干净、引擎启动的必要条件已实测（**结论已反转：不再需要 DashScope key**）；**没有任何东西被部署**，线上仍是修复前版本。

## 本次增量（会话 1：三笔；会话 2：两笔）

### 1. D3/D4 部署产物入库（`bf4435f`）

新建**入库**的 `deploy/`（`backend/docs/deploy/` 被 gitignore 排除、不在版本控制内，已改用这个位置，AGENTS.md 的部署指向也改了）：

- `deploy/nginx/ai-assistant-sse.conf` —— SSE location 片段。`proxy_buffering off` 是折叠条逐步显示的前提；`proxy_read_timeout 360s` 必须**长于**应用侧两个 300s 的 `SseEmitter`（否则 nginx 先断、error 帧发不出来）。附三步验法。
- `deploy/prod-checklist.md` —— 三进程逐项配置 + 启动顺序 + 9 项自检表 + **三个陷阱**。

### 2. 历史探针数据清理（`c68eb71`）

活跃数据：用户 38→**8**、空间 37→**6**、图片 178→**156**、词条 26→**17**、悬空成员 2→0；Redis db1 303→**292**（删 11 个属主已不存在的死会话）。**用户自己在用的 8 个账号一行未动**（`1234` 有 144 图、`admin` 有 10 图）。可复跑脚本入库：`backend/sql/cleanup/2026-09-15-purge-probe-data.sql`。

**两个坑（都写进脚本注释了，值得记住）**：
- MySQL 的 `LIKE` **不支持 `[_]` 字符类**（那是 SQL Server 语法）。写 `'sec[_]%'` 会匹配 **0 条**、把 15 个账号静默漏删，而脚本仍然"成功"退出。**精确前缀一律用 `left(col, N) = 'x'`。**
- **不能照账号名猜是不是探针**：我一开始把 `1234`～`1234567`、`admin` 都列成"历史探针"，盘点内容足迹后发现 `1234` 有 144 张图、`admin` 是管理员。**删之前必须看属主的真实足迹。**

### 3. 引擎启动必要条件实测（`9a263a9`）—— 本次最有价值的一条

把清单里原先标着"本机没验证过"的那条验掉了，结果**它真的会让首次部署失败**：

| 做法 | 结果 |
|---|---|
| `AI_DASHSCOPE_API_KEY` 为空 | 启动失败：`DashScope API key must be set` |
| 把 `spring.ai.model.chat` 关成 `none` | 启动失败：`Parameter 1 of constructor in FlowWindowBasedChatMemory required a bean of type ChatModel that could not be found` |

**结论：prod 必须给一个真实可用的 DashScope key**——哪怕助手的大脑是 MiMo（OpenAI 协议）。而且**它不是摆设**：`FlowWindowBasedChatMemory.summarize()` 里真的会 `chatModel.call(...)`，**长对话的摘要压缩走的就是这条链路**，给假 key 能启动、会话一长摘要就失败。想摆脱这个耦合得改代码（摘要改走 MiMo / 让该注入可选），属独立任务。

## 新产生的决定

会话 1 的规则变更与口径都已入 `docs/decisions.md` 与 `docs/decisions/`：流程损耗治理（`2026-09-15-process-overhead.md`）、spec 三行缩短（`2026-09-15-spec-key-three-lines.md`）、D1 回环绑定（`2026-09-15-D1-loopback.md`）、助手三处可见缺陷（`2026-09-15-assistant-visible-defects.md`）、A1 已接受暴露。会话 2 新增一条**「引擎侧摘掉 DashScope」**（`docs/decisions/2026-09-16-dashscope-decoupling.md`）。**两次会话都没有推翻既有决定**。

## 未决 / 下一步（按建议顺序）

1. **先在本地把 prod 拓扑整条排练一遍**（我提过、用户尚未表态）。这是没有服务器时唯一能验证清单的办法，也能消掉"引擎从未以 prod 形态从空库起过"这个未知：新建空库 `yu-ai-agent-rehearsal` → 引擎按清单 prod 参数起（空库走 Flyway + `thinking` 开关）→ 后端 + MCP 一起起 → 跑自检表 → 把每条自检的**实际输出**记下来，对不上就改清单。**注意**：会话 2 之后这条不再需要 DashScope key（这是本次变化对排练的直接影响）。
2. **`push` 必须由用户定**：`origin/main` 停在 **2026-09-09**，本地领先 **211**。若部署路径是"服务器上 clone/pull 再打包"，拿到的就是两个月前的代码（既无 AI 助手也无安全修复）。两条路：push（仓库**公开**，注意"文档里写着线上仍未修"这层含义）／scp 产物（那就必须守陷阱 1 的 profile 纪律）。
3. **部署素材**：D3/D4 已就绪，按 `deploy/prod-checklist.md` 执行。部署后勾 `spec.md` 的 F11（D5）。**顺带**：升级后端会把 A1（匿名可枚举空间与属主）一并修掉。
4. **会话 2 留下的唯一未验证项**：摘要 / 会话标题 / MyManus 对话这三条 MiMo 调用**只有单测+桩服务+真机启动**，没跑过真实调用。**在下一次真实对话（本地手动起服务，或部署后）顺手看一眼**：长对话应出现 `摘要模型调用完成`、不应出现 `会话摘要压缩失败，降级为硬裁剪`（清单自检表里有这一条）。
5. **仍开着的（按规则 13 判为已知限制，不挂账）**：D2 引擎 `CorsConfig`（缓解未修，本机进程仍可利用）；SSE 中继的三个边界（多行 `data:` 半帧 / 缺 `[DONE]` 时多一句"响应意外中断" / 线程池拒绝给空体 500）；`/ticket` 无频控、`message`/`chatId` 无长度校验；`/ai/manus/chat` 零鉴权（靠 D1 回环让它不可达，**未修鉴权**）。
6. **数据卫生的第二笔账（需用户点头）**：逻辑删除的历史残留 `user` 377 行 / `space` 206 行 / `picture` 362 行（`isDelete=1`）。对应用不可见，但占库且**唯一索引仍被占用**（同名词条/账号不可再注册）。清理要跨表物理删除，属独立任务。
7. **另一笔代价**：清掉的 22 张图是 SQL 物理删除，**COS 对象未回收**（绕过了应用的异步清理），都是测试图。

## spec 偏差自检（认真轨必做）

- **越界**：无。会话 1 新增的 `deploy/`、`backend/sql/cleanup/`、`frontend/scripts/` 与 `.githooks` 门禁、会话 2 改的引擎配置，都属**工程产物**，不触及 spec 的关键三行（那三行讲的是产品范围与三应用边界，不是仓库目录或模型厂商）。
- **缺口**：只有 F11 仍未勾（等部署，用户已拍板）；无新增承诺未兑现项。
- **关键三行仍成立**：目标 / 不做的事 / 边界**一字未改**（会话 1 之前缩短过三行措辞，内容等价且用户已确认，见 `2026-09-15-spec-key-three-lines.md`）。会话 2 把"模型厂商"从 DashScope 换成 MiMo、并删掉一个提供方，**不涉及**"边界"行里的三应用边界。
- **线上状态**：仍是存量版本，无 `/assistant` 入口；**无 `[线上]` 反馈任务**（本日两次会话都没做任何线上操作）。

## 环境残留

- **服务全停并核实**：8123 / 8124 / 5173 / 8127 无 LISTENING，无残留 java 进程；MySQL 3306 / Redis 6379 常驻（非本日会话启动）。
- 会话 1 的两次启动尝试（DashScope 那两次）失败退出；**会话 2 起停引擎 4 次**（都在 8124），每次停后都核实无监听、无 java 残留，最后一次核实为"空"。
- **未创建任何业务数据**：会话 2 刻意没跑真实对话/摘要，避免又制造探针数据。
- `ai/agent/target/` 下有 `mvn package` 的产物（jar，未入库）。
- **工作区干净**；`git status` 无输出。
- **门禁基线（会话 2 实测）**：backend **75 例**、`ai/agent` **177 例**、前端 `type-check` **124**（= 基线）、`npm run check:assistant-format` **21 断言**——全绿。门禁外的 `MyAiAgentApplicationTests`（需本机 MySQL/Redis）也是 1 例绿。
- 一次性脚本仍在 `%TEMP%`（`db-query.mjs`、盘点/清理 SQL、redis 脚本）：**未入库**。其中**清库脚本已入库**，断言性质的数字也都在本快照与 records 里，可按库当前状态直接复核。这是规则 16 那条"证据不能只留 `%TEMP%`"的现状——**部分满足**。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**（含两段增量）。
2. 要细节时**按指针打开**，别顺序读索引：`docs/plan.md` 的「未决与非任务事项」节（D 表 / A1 / 数据卫生 / 已关闭项）；`deploy/prod-checklist.md`；`docs/decisions/2026-09-16-dashscope-decoupling.md`（会话 2 的口径与全部证据）；`docs/plans/records/` 下 `acceptance-2026-09-15-T17-T22.md`、`assistant-visible-fixes.md`、`probe-data-purge-2026-09-15.md`、`D1.md`。
3. **下一件事**：等用户就"是否先做本地 prod 排练"表态，以及 push 的决策（两者都属"需要用户定"，不要自己动）。**其余都可直接推进**：清单自检表那类人工验收没法自己做，但第 4 条那个 MiMo 观测点，只要本地起一次真服务就能顺手验。

## 增量 2 —— 引擎侧摘掉 DashScope（2026-09-16 会话 2，用户两轮指示）

- 第一轮"**把摘要也接 mimo**"（`e4892e9`）：`FlowWindowBasedChatMemory` 的摘要改走 `OpenAiChatModels.assistant()`（prod 上是 MiMo）。
- 第二轮"**仍用默认模型的两处也接 mimo**"：`AiController`→`MyManus`（`/ai/manus/chat`）与 `ConversationTitleService`（会话标题）也改走主脑；同时**去掉跨厂商回退**（摘要拿不到主脑即降级硬裁剪、标题跳过、manus 端点入口 fail-closed），并把入库配置改成 `spring.ai.model.chat: none` + `model.embedding: none` + `dashscope.enabled: false`。
- **口径与全部证据**（红→绿、桩服务验法、真机启动四形态、残余清单）在 `docs/decisions/2026-09-16-dashscope-decoupling.md`，不复述。一句话结论：**prod 不再需要 `AI_DASHSCOPE_API_KEY`**；`deploy/prod-checklist.md` 第 3 节该行、第 5 节排错提示与自检表、`docs/plan.md` D4 行都已同步。
- **踩到的一个非显然点**：`spring.ai.model.chat: none` **管不到** DashScope 的 agent 自动配置（`dashScopeAgent` 缺 key 照样让启动失败），必须显式 `dashscope.enabled: false`。
- 本增量的提交：`e4892e9`（摘要改走主脑）、`6a46a87`（两处调用点 + 入库配置）、`47fefdf`（文档）。
- spec 偏差自检、环境残留、门禁基线与"下一步"都已并入**上面的公共小节**，此处不重复。唯一只在这里说的：三条 MiMo 调用（摘要 / 标题 / MyManus 对话）**没有真机跑过**，只有单测 + 桩服务 + 真机启动。

## 增量 3 —— 引擎移除 Flyway（2026-09-16 会话 3，用户指示）

用户原话：「这个项目暂时不用 flyway」→「我不使用 Flyway，你可以把它相关的删除了」。

- **移除的是实体，不是开关**：`db/migration` 下 V1–V4 四个迁移脚本（`git rm` 整目录）、`ai/agent/pom.xml` 的 `flyway-mysql` 依赖、`application.yaml` 的 `spring.flyway` 块，三处全删。
- **补上了随之而来的缺口**：新增**入库**脚本 `deploy/sql/engine-schema.sql`（生产建库口径）。内容取自本机在跑的 `yu-ai-agent` 库的 `show create table`，只建引擎代码真正会碰的四张表（`user` / `conversation` / `chat_message` / `chat_summary`）。
- **为什么这条路本来就是坏的**（供后来人理解，不是"关了能用的东西"）：V1 的 `create index if not exists` 是 MariaDB 语法，MySQL 8 报 ERROR 1064，实测复现；且本机库的 Flyway 记录里 V1 是 `<< Flyway Baseline >>`（checksum NULL）——**V1 从未真正执行过**。V2–V4 建的那批 interview/study/knowledge/agent_run 表引擎代码零引用。
- **验证**（全绿）：建表脚本在空库实跑 → 四张表建出、与在跑的库**逐列（26 列，四项属性无 DIFF）逐索引一致**、连跑两遍幂等；`ai/agent` 门禁 **177 例**（删除前后各跑一次）；全上下文装配 `MyAiAgentApplicationTests` **1 例**（`Started in 6.6 s`）。
- **踩到的一个非显然点**：中文 Windows 上 mysql 客户端默认字符集不是 utf8mb4，直接跑脚本会报 `ERROR 1067 Invalid default value for 'title'`；脚本首行已加 `set names utf8mb4;`（文档命令仍建议带 `--default-character-set=utf8mb4`）。
- **口径与完整证据**：`docs/decisions/2026-09-16-flyway-removed.md`；`deploy/prod-checklist.md` 第 3 节该行与第 5 节第 1 步、`docs/plan.md` D4 行、`docs/decisions.md` 均已同步。
- **本会话另一件事（用户指示）**：`.playwright-mcp/`（浏览器工具的工作目录）加进 `.gitignore`；`git stash` 里存着用户要求回滚的那批未提交改动（V1 补 chat_summary、`ImageSearchTool` 加固 + 测试）——**Flyway 移除后，V1 那部分已无意义，Stash 可按需丢弃**。
- **用户明确不做的**：本地 prod 排练（增量 2 之后不再需要 DashScope key 的那条也一并搁置）；**push 等用户通知**。
- **环境残留（2026-09-16 会话 3 收尾）**：下午未记录会话留下的四个项目进程——后端 8123、引擎 8124、MCP 8127、前端 vite 5173——**已按用户指示全部停止并核实**（四个端口无 LISTENING，无残留 java/node；剩下的两个 java 是 IntelliJ 的 RemoteJdbcServer，非本项目）。引擎进程停之前连着 `yu_ai_agent_rehearsal_manual_20260916` 库、启动参数含 `--spring.flyway.enabled=false`——说明"空库手工建表 → 引擎起"这条路径被私下走过一遍，但没留记录、未逐项核对自检表。**5 个 `yu_ai_agent_rehearsal_*` / `schema_fix_check_*` 排练库仍在（未删，属用户数据）**；MySQL 3306 / Redis 6379 常驻未动。
- 本增量提交：`719105b`（Flyway 移除 + 建表脚本）、`36c6266`（.gitignore）、`197bf96`（Flyway 口径文档）、`be56ee2`（停进程后的文档订正）。

## 增量 4 —— 上线前盘点（2026-09-16 会话 3 末，回答"上线前还要准备什么"）

逐条实测后，**三个新缺口**（都属"清单没覆盖"，本次已补进 `deploy/prod-checklist.md`）：

| # | 缺口 | 为什么现在才看见 |
|---|---|---|
| 1 | **服务器要同时有 JDK 17 与 21** | 后端是 17，**引擎与 MCP 的 pom 都是 `<java.version>21</java.version>`**；清单原来只写"依赖 MySQL/Redis"，没写 JDK。用 17 起引擎会直接报 class 版本不支持 |
| 2 | **三个进程怎么常驻没定** | 第 5 节只有"起 MCP → 起引擎 → 起后端"的命令，**没有守护方式**：开机自启、崩溃拉起、日志落盘与轮转。老后端是拿老办法跑的、做法未入库。不定下来的话，机器一重启三个进程都不会回来 |
| 3 | **前端怎么发布没定** | 全仓（`docs/` 与 `deploy/`）`grep npm run build` **零命中**——从来没人写过。且 `frontend/package.json` 的 `build` = `run-p type-check build-only`，而 type-check 有 **124 例存量错误基线**，这条命令很可能非 0 退出；`pure-build` 才是纯 vite build。要用哪条、`dist/` 怎么上服务器、静态目录在哪，都得先定 |

**已就绪的**（无需再准备）：Flyway 已移除且建表脚本实测可用（四张表、逐列逐索引与在跑库一致、幂等）；prod 不再需要 DashScope key；D3/D4 产物（nginx 片段 + 清单）已入库。

**等你定的**：① push 时机（你说会通知，没说就不推——注意 `origin/main` 停在 2026-09-09，走 clone/pull 路径会拿到两个月前的代码）；② 那 5 个排练库清不清；③ 数据卫生第二笔（逻辑删除残留 `user` 377 / `space` 206 / `picture` 362 行）要不要做——跨表物理删除，需你点头。

**部署后要做的**：勾 `docs/spec.md` 的 F11（D5）、跑清单第 5 节那 9 项自检、顺手把后端也升上去（会一并带上 A1 的匿名枚举修复）。

**没做但已明确**：本地 prod 排练（你指示不做）。因此"空库 → 手工建表 → 引擎起 → 助手能对话"这条链路**仍没有逐项验收记录**，只有那次私下排练留下的痕迹。

## 增量 5 —— AI 助手后端安全 review 与 T27 修复计划（2026-09-16）

用户要求再次 review 后端，本轮按风险导向只读检查了 `backend` 的助手入口/ticket/SSE 代理与 `ai/agent` 的工具、日志和写操作边界；未改业务代码。review 结论已经按规则 13 收敛为两类：

- **现在修：** 两项 P1（代理自动跟随重定向可能泄露内部 API Key 与 satoken；写工具只有提示词确认、没有确定性执行门）+ 六项 P2（query satoken、早取消竞态、INFO 原文日志、异常原文外泄、错误体单行无界读取、凭据名配置与转发不一致）。
- **已知限制并关闭：** F11 已记录的多行 SSE、缺 `[DONE]`、线程池拒绝空 500、300 秒超时、GET message 访问日志、频控/长度等不重复挂账。

用户随后要求“生成修复计划文档，我新开会话去修复”。已提交 `8b57493 docs: 规划助手后端安全加固`：

- `docs/plans/2026-09-16-T27-AI助手后端安全加固实施计划.md`：T27.1~T27.8，含文件范围、先红测试、负向控制、提交拆分和停止条件；
- `docs/plan.md`：新增未完成任务 T27；
- `docs/decisions/2026-09-16-assistant-backend-security-review.md` + `docs/decisions.md`：落定 review 结论。

**新会话建议顺序：** 先做不改变产品交互的 T27.1（凭据/重定向）→ T27.2（取消与错误边界）→ T27.3（日志/工具异常脱敏）；T27.4 是用户可见行为字据——“后端持有待执行计划 + 前端显式确认 + 单次原子执行”，必须先让用户确认，再做 T27.5~T27.8。不要把确定性确认门降级成“提示词里再强调一次”。

### spec 偏差

- 当前只新增计划，**没有实现越界**；spec 关键三行仍成立。
- T27.4 将补充“写操作必须经过确定性确认门，模型自然语言判断不构成授权”，属于规则 3 的用户可见行为变更；目前尚未写入 spec，等新会话展示具体协议并获用户确认后再改。
- F11 仍待部署后勾选，线上状态未变化。

### 环境与工作区残留

- 四个项目服务当前仍在监听：backend 8123（PID 36116）、agent 8124（PID 43584）、MCP 8127（PID 38392）、frontend 5173（PID 34548）。用户此前要求启动查看效果，本轮未停止。
- `git status` 唯一未跟踪项：`backend/src/main/java/graphify-out/`。这是本轮结构分析工具生成的目录，不是业务产物；按 AGENTS 删除规则未直接删除，需用户明确允许后移入回收站。
- `stash@{0}` 仍是用户要求回滚时保留的 V1/chat_summary + ImageSearchTool 加固改动；未触碰。
- 本地 `main` 当前领先 `origin/main` **225** 笔；未 push。

## 增量 6 —— T27 首次上线最小安全收口

用户放行的“第一阶段前 4 项”已完成：代理拒绝 query Sa-Token、禁止自动跟随重定向并统一凭据配置名；助手暂时不装配两个直接写工具；完整 Prompt、工具参数/结果和未知异常不再向 INFO/用户原文暴露；页面与功能文档同步为“查询 + 搜图候选 + 建议”。详细红绿证据只放在 `docs/plans/records/T27-first-release.md`。

**仍未完成**：SSE 早取消竞态、错误体单行有界读取，以及 T27.4～T27.7 的确定性写确认门；因此不能把 T27 整体勾成完成，也不能恢复两个写工具。

**环境残留**：backend 与 agent 已重新打包并替换为新进程：8123 PID 36536、8124 PID 38780；`/api/health` 均 HTTP 200。MCP 8127 PID 38392、frontend 5173 PID 34548 继续复用；匿名浏览器访问 `/assistant` 正确跳转登录页，登录后的文案/写入拒绝路径仍需用户账号做人工验收。运行日志在本机 `.codex-runtime/`（已忽略）。`backend/src/main/java/graphify-out/` 和 `stash@{0}` 仍未触碰。

## 增量 7 —— 交接并进入上线执行

### 上线候选与证据

- 运行时代码候选截至 `91ede81`；之后的 `7202d4a`、`2023e6e` 及本次交接只改文档，不改变产物行为。
- 本轮提交门禁已验证 backend **77** 例、`ai/agent` **186** 例全绿；助手 copy/format 常驻检查再次 PASS。
- backend 与 agent 已重新打包并以新 jar 启动，本地 8123/8124 `/api/health` 均 HTTP 200；MCP 8127、frontend 5173 仍在监听。
- 前端生产构建已补跑：`npm run pure-build` 成功，4015 modules，约 32.5s；主 bundle 约 2.95 MB（gzip 约 941 KB），只有大 chunk 警告，不阻断发布。
- 线上 `https://www.lincode.online/api/health` 于 2026-09-16 实测 HTTP 200；匿名 `POST /api/space/list/page/vo` 仍返回 `code:0` 与空间记录，证明线上仍是 A1 所述的旧后端，尚未部署本地安全修复。

### Git / 版本状态

- `main` 相对 `origin/main`：behind 0；提交本交接前 ahead **232**，本交接提交后应为 **233**。远端仍停在 2026-09-09。
- 仓库当前没有版本 tag；本次若作为首个规范发布，建议在用户明确放行 push 后使用 `v0.1.0`，并让 tag 指向实际部署提交。
- **未 push、未打 tag、未改线上。** 下一会话不得把“准备上线”误读为已经发布。

### 下一会话执行顺序

1. 先读 `deploy/prod-checklist.md`，以它作为唯一部署清单；本次已补上助手只读边界与日志脱敏三条冒烟项，并确定前端构建命令为 `npm ci && npm run pure-build`。
2. 用户明确说可以 push 后，推送 `main`；不要在未确认时自行 push。若走服务器 `clone/pull`，这一步是硬前置。
3. 在服务器确认 JDK 17 + 21、生产环境变量、MySQL/Redis、`deploy/sql/engine-schema.sql`、nginx SSE 片段，以及三个 Java 进程的守护方式。**进程守护目前仍未定**，必须在实际部署时选择现有方案或 systemd，并确认自动拉起与日志轮转。
4. 服务器侧构建，避免把本机 `application-local.yaml` 和本机密钥烤进 jar；启动顺序为 MCP 8127 → agent 8124 → backend 8123，再发布 `frontend/dist/` 并 reload nginx。
5. 跑清单第 5 节全部自检；尤其验证普通用户只读工具集、两种写入要求均零写入、INFO 日志不含原文、8124/8127 仅绑定 127.0.0.1。
6. 全部通过后再把 `docs/spec.md` 的 F11 勾上、记录线上版本并打/push tag；失败优先回滚旧产物，不在服务器临时改源码。

### spec 偏差与未决

- 关键三行仍成立：当前实现正是“确定性确认门完成前不直接批量改图或按 URL 入库”，无实现越界。
- F11 仍未勾选，符合“部署上线后再勾”的既定口径。
- T27 整体仍未完成：SSE 早取消竞态、错误体单行有界读取、确定性写确认门继续保留；这些不阻止本次按只读助手边界上线，但不能恢复写工具。

### 环境备注

- 本地监听：frontend 5173 PID 34548、backend 8123 PID 36536、agent 8124 PID 38780、MCP 8127 PID 38392。
- 唯一未跟踪项仍是 `backend/src/main/java/graphify-out/`，属结构分析工具残留；按删除规则未处理，也不会随 push 上传。`stash@{0}` 继续保留，未触碰。
- 下次任何工具接手，先说“读 AGENTS.md 和最新交接”，然后从本节“下一会话执行顺序”开始。

