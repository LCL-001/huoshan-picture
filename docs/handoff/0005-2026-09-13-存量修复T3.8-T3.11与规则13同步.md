# Handoff 0005 — 存量修复 T3.8-T3.11 与规则 13 同步（2026-09-13）

## 当前状态（一句话）

T3 批次 10 项全部完成（T3.8-T3.11 为本会话用户点名新增的存量修复并全部收口），工作区干净，AGENTS.md 工作规则已同步至 vibe 最新 13 条。

## 刚完成

- **T3.8 满员空间替换图片被额度预检误拒**（4d37b45）：额度预检移到 oldPicture 解析之后、仅对新增图片按新增口径生效；替换图片不增条数、大小按净差值由事务内原子校验兜底（T3.4 已有，未动）。测试：PictureReplacePrecheckOwnershipIntegrationTest 3 用例（满员替换成功记净差值 / 满员新增仍被拒守卫 / admin 替换归属不变）
- **T3.9 admin 替换他人图片归属被改**（4d37b45，与 T3.8 同 commit）：getPicture 签名 `Long pictureId` → `Picture oldPicture`，更新分支保留原归属 userId，仅新增归属上传人
- **独立 review**（规则 13 首次实践，全新上下文 agent 只读评审，结论登记 c0365f7）：T3.8/T3.9 **通过**（8 用例实测绿 + 回退即红推演）；发现既有 P2 权限缺口，用户拍板立 T3.10
- **T3.10 spaceId 反推路径缺空间上传权限校验**（8b29f46）：uploadPicture 在 spaceId 由原图反推（请求只传 pictureId）时补查目标空间并要求 PICTURE_UPLOAD（抽 checkSpaceUploadPermission 私有方法，显式传参路径复用），与 checkPictureAuth（deletePicture）判权同源。**口径收紧**：站点管理员非团队空间成员时替换/上传该空间图片被拒（与显式传参路径及 deletePicture 现状一致）；私有空间属主/站点管理员不受影响；团队空间属主由 createSpace 自动建 admin 成员行（SpaceServiceImpl.java:120-127）不受影响。测试：PictureReplaceSpaceAuthIntegrationTest 4 用例（移出成员被拒 / 降权 viewer 被拒 / 非成员站点 admin 被拒 / editor 正例守卫）
- **T3.11 上传事务失败补偿删除孤儿 COS 文件**（5f44ecc，用户点名选项①）：transactionTemplate.execute 包 try-catch，回滚后按引用计数（count == 0 才删）异步清理刚上传的新文件，原异常照常重抛；覆盖"替换超额"与"新增过预检但事务内原子校验失败"两条路径；COS 上传在事务前的时序不变（picSize 上传后才知道）；cleanupPictureFile javadoc 补第三类调用前提（记录从未提交）。测试：PictureReplaceQuotaIntegrationTest 扩至 6 用例（超额替换：旧文件按 URL 精确断言绝不动 + 新文件被补偿清理；新增超额：补偿清理 + 额度不变）
- **工作规则 13 条同步**（bf9416f）：AGENTS.md 新增规则 13"独立 review"；与 vibe-start 模板逐字符机械比对验证，规则 7 项目特有豁免清单（.gitattributes / .githooks/**，与 pre-commit 脚本对齐）有意保留
- **CI 口径拍板**（decisions.md 已记）：维持"CI/门禁排除集成测试"现状，集成测试靠本地全量手动回归，不采纳 service 容器/nightly 方案
- 测试基线：全量 62 绿（13 门禁单测 + 49 集成）

## 进行中 / 下一步

- 无进行中任务，plan.md 任务清单（T3.1-T3.11）全部勾完。接手后等用户指派方向，候选（均需用户点名，未登记 plan）：
  - 低危清单（handoff 0002 的 2026-09-12 审查报告，仍未修）：IP 限流头伪造（nginx 未强制覆写 X-Real-IP）、picture 索引与 `LIKE '%kw%'` 不匹配、admin 接口返回密码哈希、字段长度口径（url 1024 vs 列 512）、协同编辑双页签互踢、前端 WS 无重连
  - 0004 提及的两个存量 bug 已在本会话修完（T3.8/T3.9），勿重复立项

## 未决问题

- 无阻塞决策。2026-09-13 拍板均已记 decisions.md：CI 维持排除集成测试（本会话）；ShardingSphere 注释停用、通知接口保留（上会话，防翻案仍有效）
- 满员空间的超额替换仍会"先传 COS、事务内拒绝"（T3.8 后替换路径进入该失败面），但孤儿文件已被 T3.11 补偿清理，不再是问题

## spec 偏差（认真轨）

- 越界（做了 spec 没写的）：无——T3.8-T3.11 均为缺陷修复/安全口径对齐，spec F1-F9 功能面无变化
- 缺口（spec 承诺但没做的）：无；"目标/不做的事/边界"三行经本轮检视仍成立

## 环境备注

- 分支：main，未提交改动：无；本会话产出 bf9416f…d5e7b93 共 8 笔（3 fix + 5 docs）
- 本机环境：Maven 3.9.11 + JDK 21（编译目标 17），本地 MySQL 8（3306）与 Redis（6379）可用；全量含集成 `mvn -B test` 在本机可跑；门禁/CI 同口径 13 单测
- 集成测试夹具坑（0004 的 ①-⑤ 仍有效，新增两条）：⑥ space_user 表有 (spaceId, userId) 唯一键，同一用户同一空间只能一行——反复授权/改角色会 DuplicateKey，测试须"每用例自建用户 + 成员行"；⑦ JUnit 方法执行顺序不定，可变夹具（成员行删除/降权）必须用例自包含
- Windows 工具链坑（0004 仍有效），新增：git log 中文经 PowerShell 管道会 GBK 假乱码（存储无损、仅显示错）→ 验证提交信息用 cmd 直接重定向 `git log -1 --format=%s > 文件` 再用工具读文件；PowerShell 内联 -Command 含 $ 与管道会被 cmd 拆坏 → 写 .ps1 文件执行，且 ps1 内避免中文字面量（Write 工具产出无 BOM UTF-8，PowerShell 5.1 会按 GBK 解析中文字面量）
- 后端起停命令见 AGENTS.md"命令"节；本会话未启动过后端/前端
