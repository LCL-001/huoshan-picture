# Handoff 0015 — R4/R6 收口（2026-09-14）

## 当前状态（一句话）

**档 2 独立 review 挂的 R4（代理侧身份一致性校验）与 R6（Sa-Token 注解作用域守护测试）已按用户拍板"R4 同意加校验、R6 做"完成并留档**：门禁 **37 例**、本地全量集成 **55 例**全绿，`ai/` 引擎侧零改动，工作区干净、本机服务全停；**下一会话第一件事是进档 3（完整一期）——若档 3 先做写类工具，动手前必须先办 R5（跨站 CSRF）**。

## 刚完成（本会话，按时间）

1. **接手**：读 AGENTS → spec 关键三行 → handoff 0014 → decisions 2026-09-14 各行 → `docs/features/F11-AI助手MVP.md` → plan.md 档 2 实施/review 记录 → 现有单测与集成测试。

2. **动手前的前置核查（四项，都影响方案成立与否）**：
   - 登录走的是 `StpKit.SPACE.login(user.getId())`（`UserServiceImpl.java:202`）→ `StpKit.SPACE.getLoginIdByToken(satoken)` 能拿到与会话同一套账号 id。**若登录用默认 `StpUtil`，这个校验会把所有正常用户拒掉**——动手前必须先确认这一点。
   - sa-token **1.39.0** 的 `getLoginIdByToken(String)` 语义：解出 `sources jar` 读源码确认——token 空/未找到/非有效值一律返回 `null`、**不抛异常**（不走活跃超时校验），实现里可直接判 null。
   - `application.yaml` 的 sa-token 块**没有配 `token-prefix`**（141 行起的配置只有 token-name/timeout/cookie）；若配了前缀，`splicingKeyTokenValue` 用的是未剪切的原值、键名会对不上。
   - MySQL 3306 / Redis 6379 在跑，集成测试可跑红绿。

3. **R4 实现（先红后绿，规则 12）**：`AiAssistantController` 加**门槛三** `checkCredentialsBelongToSameUser` —— `StpKit.SPACE.getLoginIdByToken(satoken)` 必须非空且与 `loginUser.getId()` 同 id，否则在返回 `SseEmitter` 之前就拒、零上游请求：**无效/过期 → 40100，有效但不属于同一人 → 40102**（与 `checkSpaceViewPermission` 同口径）。**只判身份一致性、不判权限**，权限仍只由图库服务端判。
   - **先写复现测试并看到红**：单测 `AiAssistantControllerTest` 12 跑 **2 失败**、集成测试 `AiAssistantProxyIntegrationTest` 5 跑 **2 失败**。另有意外旁证：修复前那版连既有的 `rejectsWhenNotLoggedIn` 的 `REQUESTS==0` 断言都被带红——原因是新用例真把请求转给了引擎、异步计数残留到下一个用例，正好实证"假 token 也能打通"。
   - 修完：单测 12/12、集成类 5/5 绿。
   - 单测**没有引入替身**：用 Sa-Token 内存 DAO 播种真 token（无 Spring 时 `SaManager` 缺省即 `SaTokenDaoDefaultImpl`，键名用 `StpKit.SPACE.splicingKeyTokenValue`），验的是真实 `getLoginIdByToken` 语义。
   - 集成用例两条：① 真会话 + 随手编的 token（`review-bogus-token`）→ 40100 且零上游；② **第二个真实账号**的真 satoken 配本账号会话 → 40102 且零上游（两把各自都真实有效，这才是"身份一致性"的核心语义）。

4. **R6 实现**：新增 `backend/.../config/AiPathSaTokenGuardTest.java`，三条不变量——① `/ai/**` 下控制器不得出现任何 Sa-Token 注解；② 全仓凡用了这类注解的控制器路径必须落在拦截清单覆盖内；③ 拦截清单不得含 `/ai/**`。为可断言，`SaTokenConfigure` 的路径数组提取为常量 `ANNOTATION_GUARDED_PATTERNS`（行为不变）。扫描用 `ClassPathScanningCandidateComponentProvider` + `@RestController/@Controller` 过滤（**类注解被注释掉的停用模块天然不入扫**），注解判定用 `AnnotatedElementUtils.hasAnnotation` 以识别 `@SaSpaceCheckPermission` 这类组合注解。
   - **两轮负向控制（证明不是空转）**：给助手端点临时挂 `@SaCheckLogin` → 2 条不变量红并点名 `AiAssistantController#chat`；把 `/ai/**` 加进拦截路径 → 第 3 条红。两者都已还原。

5. **回归与提交**：
   - 门禁 **37 例**全绿（原 32 + 新增 5：`AiAssistantControllerTest` +2、`AiPathSaTokenGuardTest` +3）。
   - 本地全量集成套件 **55 例 / 14 个类**全绿（`mvn -B -o test -Dtest=*IntegrationTest`）。
   - 提交 `fd2d3b2`（`fix: 助手代理补 satoken 与会话身份一致性校验（R4）及 Sa-Token 注解作用域守护测试（R6）`）——走 pre-commit hook（backend + ai/agent 两套都跑，落地即证明双绿）。
   - 提交 `b888f5c`（`docs: R4 身份一致性校验与 R6 注解作用域守护收口（decisions/F11/plan 同步）`）。规则 7 的代码/文档分离已遵守。

6. **文档同步**：decisions.md 新增 R4/R6 收口行（含**推翻**"只判存在性"的翻案记录、以及更正 2026-09-14"B 落地"条里"不一致时回 `SPACE_NOT_LOGIN`"的适用范围）；F11 改了「关键设计与理由」3/7、「核心流程」图、怎么验证的例数、「已知限制」8；plan.md 档 2 后加了"R4/R6 收口记录"一条。

## 进行中 / 下一步

1. **档 3 完整一期**（本轮真正的功能增量）：visionTagger 看图打标、`batchEditPictures` / `batchUploadByUrl`、T9 MCP 搜图（`ai/image-search-mcp-server`）、T8-hard 循环合并（统一 `run`/`runStream`、`run()` 退役、事件协议固化）、T12 两条业务场景联调、T13 清理。
2. **档 3 若先做写类工具 → 必须先办 R5**（见"未决问题"；届时是 P1）。
3. **上线前必办**（档 2 上线才碰）：nginx **server 块**（仓库只有 `nginx-security.conf`，`proxy_buffering off` / `proxy_read_timeout` 未证实）；引擎部署前的旧端点未鉴权、双端密钥一致、`ai/` 打包启动方式。
4. **挂账收口（小颗粒、可穿插）**：P3 六条（`chatId` 净化 / relay 逐行 emit / emitter 超时对齐 / 错误文案不贴原文 / 拒绝先发可见文案）、引擎侧"超时转显式错误事件 + 埋点"、前端门禁存量清理、T6 遗留 P3（③⑥）。

## 未决问题

- **R5（P2；档 3 写类工具上线前必办，届时 P1）**：`GET /api/ai/assistant/chat` 只认 Cookie + 有 LLM 副作用 + 无 nonce，实测跨站顶层导航即带齐两把 Cookie 跑完一次 agent。修法二选一：一次性 ticket（页面先用 axios 取、EventSource URL 带 `&ticket=`）或改 POST + `fetch` 流式。
- **新增残留（本会话发现并记入 F11 已知限制 8②，未做）**：两把凭据不一致（40100/40102）发生在返回 `SseEmitter` 之前，走 `GlobalExceptionHandler` 回 HTTP 200 + JSON，而前端 `EventSource` **读不到响应体** → 用户只看到"助手连接中断，请重试"，实际得重新登录。要不要做"登录态失效 → 跳登录页"是产品选择，需单独立任务（前端还背着存量门禁债）。
- **P3 六条**（同 0014）：`chatId` 可换行注入后端日志；relay 多行 `data:` 产出半个 JSON 帧（前端静默丢弃）；上游缺 `[DONE]` 时"完整答案 + 响应意外中断"同屏；上游非 JSON 错误体（网关 502 的 HTML）原文进回答气泡；线程池拒绝只给空体 HTTP 500；代理 emitter 超时早于引擎数十毫秒使超时形态退化成裸断连。
- **上线前必办（P2-部署）**：仓库缺 nginx server 块（同上）。
- **引擎部署前必办**：旧端点 `/ai/manus/chat` 未鉴权（实测匿名 `HTTP 200 + text/event-stream` 就能进 agent）、端口/内网约束、双端密钥一致（引擎 `HUOSHAN_HEADLESS_API_KEY` = backend `AI_ENGINE_INTERNAL_API_KEY`）。
- **前端门禁存量红**：`npm run type-check` 138 例 / `eslint .` 73 例既有错误（本会话未触碰前端，净增 0）；AGENTS/handoff 0001"前端以 type-check + lint 为准"目前不成立，需单独立任务。
- **push / tag**：`main` 领先 `origin/main` **101 笔**未 push（含本会话两笔）；仓库**无任何 tag**（用户未决）。
- **门禁余量、T6 遗留 P3 ①②⑤⑦**：同 0014，继续挂。
- **未证实**（同 0014）：生产 nginx SSE 配置；引擎日志是否记录用户提问原文；生产 CORS 实际放行源。

## spec 偏差（认真轨）

- **越界（做了 spec 没写的）**：**无**。R4 是 review 挂账项、经用户逐条拍板后才动（规则 1/2）；R6 同属用户批的范围内；`SaTokenConfigure` 只把字面量提成常量、行为未变；本会话新增文件只有 1 个测试类 + 1 份快照。
- **缺口（spec 承诺但没做的）**：**无未挂账缺口**。R4/R6 已收口；R5、nginx 必办、P3 六条、引擎部署前必办均已同时记入 plan.md / F11 / 本快照。
- **关键三行检视**：**成立**——本会话 `git diff 649e439..HEAD -- ai/` **空**（引擎仍是独立 Maven 应用、本会话零改动）；"引擎端点只接受服务间 API key、不注册登录体系"未变；"凭据只在请求生命周期内存流转、不落库、不进日志"未变——新增的校验只是**读**了一次 Sa-Token 的 Redis 键做身份比对，既不落库也不进日志（`log.info` 仍只记 `userId` 与 `chatId`）。

## 线上状态（已上线项目才填）

- 当前线上版本：https://www.lincode.online 仍是 backend / frontend **存量版本**；档 2 全部改动（含本会话 R4/R6）未上线（线上还没有 `/assistant` 入口），`ai/` 引擎仍未部署。
- 未处理反馈（[线上] 任务）：**无**（本会话无线上反馈项）。

## 环境备注

- 分支 `main`；**工作区干净**；ahead `origin/main` **101 笔**未 push（含本会话两笔）；**无 tag**。
- **本会话提交链（2 笔）**：`fd2d3b2`（fix 代码，hook 实跑 backend 37 + ai/agent）+ `b888f5c`（docs，门禁按纯文档跳过）。
- **门禁口径（本会话实跑）**：
  - backend：`cd backend && mvn -B -o test "-Dtest=!*IntegrationTest,!RedisStringTest,!YunPictureBaseApplicationTests" -DfailIfNoTests=false` → **37 例绿**。
  - 本地全量集成：`-Dtest=*IntegrationTest` → **55 例绿**（14 个类，需 MySQL/Redis；本次一并验证了 R4 没有伤到空间/会话鉴权那些类）。
  - ai/agent：沿用 `.githooks/pre-commit:54` 的排除名单口径（0014 实测 87 例绿）；**AGENTS 那条通用过滤命令直接套 `ai/agent` 会红**（`MyManusTest`），别把红当回归。
  - 前端：本会话未触碰，未跑（存量 138/73 例口径同 0014）。
- **本机服务状态：当前全部停止**（8123 / 8124 / 8125 / 5173 实测均不可连；MySQL 3306、Redis 6379 作为常驻服务在跑，集成测试依赖它们）。重启命令见 handoff 0013「本机服务状态」节。
- **本会话新踩的坑（记下备用）**：
  1. **PowerShell 的 `Set-Content -Encoding UTF8` 在 PS 5.1 下会写 BOM**，改完 `SaTokenConfigure.java` 后 javac 直接报 `非法字符: '\ufeff'`（AGENTS 规则 10 说的就是这个坑）。**改仓库里的源码/文档一律用编辑工具，别用 PowerShell 管道回写**；已从备份还原并确认首字节 `112,97,99`（无 BOM）。
  2. 表格型 md（decisions.md）做 Edit 时**别拿"行首短片段"当锚点**——会把那一行的行头吃掉、与前一行粘连。本会话发生过一次，已按 `| 生效 |` 接缝修回并逐行校验"每行 4 列"。
  3. `git log`/`git commit` 的中文在 GBK 控制台显示为乱码，**不代表入库是乱码**：用 `git log --format=%s > file` 再按 UTF-8 读，看到字节 `232,186,171` 即正常。
  4. 集成测试里"先转发的用例"会让下一个用例的 `REQUESTS==0` 断言假红（异步计数残留）——修完 R4 后不再有转发，但**新增会转发的前置用例时要留意**。
- **可复用资产**：`%TEMP%\t11-review\`（review 证据 + 桩引擎七场景、并发、跨站探针）、`%TEMP%\t11-smoke\`（档 2 冒烟资产与截图）——同 0014，本次未新增。
- **探针残留**：本会话集成测试的临时账号由 `@AfterAll` 自建自删（未留用户行）；Redis 里可能残留这些临时账号的 satoken 会话键（7 天 TTL，无实害）。
- **版本锚点**：backend SB 2.7.6 / Java 17 / sa-token 1.39.0；引擎 SB 3.5.13 / Java 21 / Spring AI 1.1.2；Ollama 本机 11434；后端 context-path `/api`、actuator 8125。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**；
2. **R4/R6 已做完**，不必再等拍板：直接读 `docs/plan.md` 的「MVP 档位与推进顺序」档 3 行与刚才那条"R4/R6 收口记录"，以及 `docs/decisions.md`（R4/R6 收口行、B 口径、id 字符串、R1 延后、review 行）开工；
3. **档 3 若从写类工具起手，先办 R5**（跨站 CSRF，见"未决问题"第一条）；
4. R4/R6 的全部判据与反证：`docs/features/F11-AI助手MVP.md`「关键设计与理由」3/7 +「已知限制」8 + plan.md 的"R4/R6 收口记录" + decisions.md 最后一行的 R4/R6 条；
5. 坑与门禁口径看本快照"环境备注"——尤其"用编辑工具改文件，别用 PowerShell 回写"。
