# Handoff 0022 — T8-hard 循环收敛完成（2026-09-15）

> 上一份：`0021`（记忆去重修复 + MiMo 切换）。**本文件只记 0021 之后的增量**——档 3 的整体口径、真机命令、踩过的坑都在 0020/0021，不在这里重复。
> 本会话 5 笔提交：`fb46859`（计划）→ `97079e6`（T8-a 统一循环）→ `5e11c7b`（T8-b 协议契约）→ `da61141`（T8-c error 事件）→ `0dd8e56`（docs 收口）。

## 当前状态（一句话）

**T8-hard 做完了**——引擎对话循环从"两套循环 + 受保护字段旁路"收敛成"一套循环 + 事件消费者接口"，`run()` 退役，SSE 事件协议由显式契约测试（16 例）钉住，失败/超时改发**一条 `error` 事件**（不再把厂商原文重复当回答）；门禁 `ai/agent` **143 例**、backend **48 例**绿，前端 `type-check` 净增 0，真机做过改造前/后逐帧比对。**档 3 至此只剩 T13（教学遗留清理）**；工作区干净（HEAD `0dd8e56`）。

## 刚完成（按时间）

1. **立计划**（`fb46859`，`docs/plans/2026-09-15-T8hard-循环收敛实施计划.md`）：开工前实测 `ai/agent` 门禁 118 例绿、`run()` **全仓无生产调用方**，并读代码找出四处协议缺口（受保护字段旁路 / 卡死路径**双发 `[DONE]`** / 校验失败流**不以 `[DONE]` 收尾**（代理会再补一条 answer）/ 失败文本**重复多轮**进气泡）。列出三个待拍板项。
2. **用户拍板**：T8-c 纳入本轮、`MyManusTest` 直接删、`runSummary()` 保留。
3. **T8-a 统一循环**（`97079e6`）：新增 `agent/event` 包（`AgentEvent` 密封接口 `Step/Answer/Metrics/Done` + `AgentEventListener` + `SseAgentEventListener`）；`BaseAgent.step()` 由 `String` 改为 `List<AgentEvent>`，删掉 `lastStepKind`/`lastThinkText`/`lastToolNames` 三个受保护字段（think 文本改由 `ToolCallAgent.act()` 从自己的 `toolCallChatResponse` 取）；`run()` 退役、两套循环合并为 `runLoop(prompt, listener)`；新增 `protected SseEmitter createEmitter()` 缝（照后端 `AiAssistantProxyManager` 的先例）；删 `MyManusTest`（它注入的 `MyManus` 从不注册为 Bean、从未真正跑过）；四个测试类迁移到"事件流 + 收集"口径。门禁 118 → 120。
4. **T8-b 协议契约**（`5e11c7b`）：`SseEventProtocolContractTest` 16 例（帧级 7 / 循环级 7 / runStream 级 3）+ 测试替身 `RecordingAgentEventListener` / `RecordingSseEmitter`；**负向控制三种改法各红一次**（删 `Done` → 7 例红；`content` 改名 `text` → 3 例红；恢复双 `[DONE]` → 循环级 `Done==1` 断言红）。门禁 136。
5. **T8-c 失败/超时改发 `error`**（`da61141`，规则 12 先红后绿）：复现测试 `AgentFailureEventTest` 修复前 **5/5 全红**（红输出 `expected: 1 but was: 5`——失败循环真的跑了 5 步）；修复后 `think()`/`step()` 不再吞异常、循环 `catch` 统一发**一条** `Error`（文案固定，原文只进日志，超时沿 cause 链判 `TimeoutException`/`HttpTimeoutException`）；前端两处（`assistantSse.ts` 认 `error`、`AssistantPage.vue` 的 `onError` 改为"有回答则追加"）；代理补 1 例 `error` 帧透传守护。门禁引擎 143 / backend 48。
6. **真机比对（T8-d）**：用 `git worktree` 检出 `9daa286` 单独打包做"改造前"，与 HEAD 跑同一句只读提问：**帧形态与字段集合逐个一致**、终止帧各恰好 1 个；故障注入（provider 恒返 400）对照——改造前 `frames=7`（**6 条内容逐字相同**的 `answer`，即厂商原文重复 6 次）+ `[DONE]`，改造后 `frames=2`（一条 `error` + `[DONE]`）。证据 `%TEMP%\t8\summary.txt`。
7. **docs 收口**（`0dd8e56`）：新增 `docs/features/F13-对话循环与SSE事件协议.md`（协议表 + 设计理由 + 已知限制）、`decisions.md` 三条 2026-09-15、`plan.md` T8 勾选 + 实施记录、`F12` 已知限制 8 改写、门禁排除名单去掉已删的 `MyManusTest`。

## 进行中 / 下一步

1. **档 3 只剩 T13 教学遗留清理**（`AuthAdvisor` 假实现、`demo` 包、空壳控制器 `ChatMessage`/`ChatSummary`、`FileBasedChatMemory` 死代码、空目录；`TerminalOperationTool`/`ParallelSearchTool` 是否删是 T13 的决策点）。纯删除、低风险，**建议下一件事就做它**（做完档 3 收口，可以考虑打第一个 tag）。
2. 新挂账（本会话实测发现，需用户决策）：**"provider 不可达"这一族失败仍表现为连接中断**——Spring AI 重试模板对 `ResourceAccessException` 指数退避重试（10 次、单次上限 180s），总时长 > 引擎 `SseEmitter` 300s ⇒ 客户端先被掐断，`error` 事件没机会发（`decisions.md` 2026-09-15 挂账行）。
3. **MCP 服务不在时，图库助手端点直接 500**（本会话踩到：`ToolCallbackProvider.getToolCallbacks()` 真去连、连不上就抛 `Client failed to initialize listing tools`）——T9 记录的"不部署就设 `AI_MCP_CLIENT_ENABLED=false`"是从这个失败形态出发的必要项；**建议单独立项**把"少一个工具照常对话"做成真降级。
4. R5 的 6 条 P3（含 `getAndDelete`/javadoc 订正）、上线/部署必办项（nginx SSE 块、旧端点 `/ai/manus/chat` 未鉴权、双端密钥一致、`application-prod.yaml` 改动需重打包、MCP 与引擎同机/内网）、apex 尾巴：**用户已明确"等 T8 之后再做"**，现在正是那个时间点。
5. MiMo 偏慢（同场景 53.4s vs deepseek-flash 12.6s）是否优化：用户未表态。

## 未决问题

- **探针残留**：本会话真机跑动有**数据写入**——用 `msg-s1`（智能整理）跑了改造前引擎一次，对 `t12-organize` 的 3 张图又提交了一轮打标（`batchEditPictures`，值与 0021 记的应一致）；该空间现有 9 张图、`yu-ai-agent` 里新增若干会话记忆行（`t8-*`）。清理方式见 0020「环境备注」，**清不清仍由用户定**。
- **push / tag**：`main` 领先 `origin/main` **140 笔**未 push；仓库**仍无任何 tag**（档 3 收口后是打第一个 tag 的自然时机，用户未决）。
- 沿用 0020/0021 的其它挂账（F12 已知限制其余条目、多实例与浏览器兼容未验、门禁余量、T6 遗留 P3 等）。
- **未做的人工验收**：浏览器端 `error` 事件渲染没跑（服务端帧与代理透传都已实测，前端改动只过了 type-check/lint），已记 F13「已知限制 5」。

## spec 偏差（认真轨）

- **越界**：**无**。改动落在 `ai/agent`（引擎循环与事件层）、`frontend/` 两处（用户拍板 T8-c 纳入）、`backend/` 仅 1 例测试；`.githooks` 与 docs 按规则 7 走 `docs:` 提交。R1、R5 的 P3、T13 其余清理、代理侧错误语义统一**均未触碰**（按用户"等 T8 之后"）。
- **缺口**：**T13 一条**（档 3 最后一个任务）；另有两条新挂账（重试预算、MCP 缺失时的 500）见上。
- **关键三行检视**：**成立**——引擎仍是独立 Maven 应用/独立进程（8124）；headless 端点仍只认服务间 API key；凭据与密钥仍只在内存或**不入库**的本地配置里（本会话未改任何配置文件；真机实验用的临时 local 配置副本放在仓外 worktree，已随手删除）。

## 线上状态（已上线项目才填）

- 仍是存量版本（同 0020/0021）：线上无 `/assistant` 入口、`ai/` 引擎与 MCP 均未部署。本会话无线上操作（只有本地真机联调）。

## 环境备注

- 分支 `main`；**工作区干净**；ahead `origin/main` **140 笔**；**无 tag**。
- **本机服务**：MySQL 3306 / Redis 6379 常驻；本会话起过的 8123 / 8124 / 8127 / 8199（故障注入桩）**全部已停**（`netstat` 复核无监听）；`git worktree`（改造前引擎）已 `remove`，`git worktree list` 只剩主仓。
- **启动三件套**（本会话实测可用，命令同 0020）：backend `set "SPRING_PROFILES_ACTIVE=local,test" && set "AI_ENGINE_INTERNAL_API_KEY=smoke-internal-key" && java -jar backend\target\yun-picture-base-0.0.1-SNAPSHOT.jar`；MCP `cd ai/image-search-mcp-server && java -jar target\image-search-mcp-server-0.0.1-SNAPSHOT.jar`（8127，**必须起**：不起则助手端点 500）；引擎 `set "SPRING_PROFILES_ACTIVE=local" && set "HUOSHAN_HEADLESS_API_KEY=smoke-internal-key" && set "HUOSHAN_BASE_URL=http://localhost:8123/api" && java -jar ai\agent\target\my-ai-agent-0.0.1-SNAPSHOT.jar`（**改代码后必须 `mvn -B -o package -DskipTests` 重打包**）。
- **模型密钥位置**（都不入库）：`ai/agent/src/main/resources/application-local.yaml` → `app.ai.openai.{assistant,vision}.api-key`（当前 MiMo 形态）。**注意**：该文件的 `base-url` 是**写死的**值，环境变量 `AI_OPENAI_BASE_URL` 覆盖不了它；要做故障注入请用 **JVM 系统属性**（`-Dapp.ai.openai.assistant.base-url=…`，优先级高于 yaml，本会话实测有效）。
- **本会话新增的仓外资产**（`%TEMP%\t8\`）：`raw.js`（原样抓 SSE 帧 + 归一化 shape 行的探针，比 `t12\chat.js` 更适合做帧比对）、`stub400.js`（恒返 400 的 provider 桩，用于注入"终止性失败"）、`summary.txt`（改造前/后比对汇总）、`msg-readonly.txt`、以及 `{before,after}-{raw,ro,error400}.txt`、`engine-*.log`、`backend.log`、`neg{1,2,3}.log`（三次负向控制）、`red-c.log`/`green-c.log`（T8-c 红绿）。**要复现改造前引擎**：`git worktree add %TEMP%\t8\before 9daa286` → 把 `application-local.yaml` 拷进去 → 在该目录 `mvn -B -o package -DskipTests`。
- **凭据注意**：`%TEMP%\t12\jar.txt` 仍是**含凭据的 cookie 罐**（0020 已标"用完请删"），本会话继续沿用它做真机；本会话新建的 `%TEMP%\t8\` 产物**不含凭据**（已 grep 复核，命中项只是测试类名与未解析的配置占位符）。
- **本会话新踩的坑**：① `timeout /t N` 在非交互 shell 里报"不支持输入重新定向"——**等就用 `powershell Start-Sleep`**；② PowerShell 的 `-replace` 替换串拼 3 段会报"只允许后面包含 2 个元素"（0021 记过，本会话又踩一次），要拆成多次或用 Edit 工具；③ `mvn -Dtest='!A,!B'` 在 **cmd** 里必须去掉单引号（0020 记过）；④ 引擎日志里 MCP 连不上是**每请求**失败，不是启动失败——现象是助手端点 500 而不是起不来。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**（要档 3 全貌再读 `0020`；要循环与协议细节读 `docs/features/F13-对话循环与SSE事件协议.md`）。
2. 口径全文：`docs/decisions.md` **2026-09-15 三条**（T8-hard 收敛 / T8-c error 事件 / 重试预算挂账）；功能与限制：`F13`（循环与协议）与 `F12`（档 3 工具）。
3. **下一件事建议 T13**（档 3 收尾，纯删除、低风险）；再往后按用户原话办：R5 的 P3、上线必办项、探针清理、push/tag。
