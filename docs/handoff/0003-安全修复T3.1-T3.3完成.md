# Handoff 0003 — 安全修复批次 T3.1-T3.3 完成（2026-09-13）

## 当前状态（一句话）

功能审查产出的 7 项修复已完成 3 项（T3.1 越权、T3.2 匿名枚举、T3.3 会话生命周期），工作区干净，下一项是 T3.4 重复上传额度虚增。

## 刚完成

- **T3.1 图片读接口权限绑定目标资源**（5768cc9）：`/picture/get/vo` 与两个列表接口改为按目标资源显式判权；`StpInterfaceImpl` pictureId 分支提前防 spaceUserId 走私；Content-Type 判断改 startsWith（与 HttpRequestWrapperFilter 必须成对改）。测试：HttpRequestWrapperFilterTest 4 个 + PictureSpaceViewAuthIntegrationTest 8 个
- **T3.2 空间列表/详情收权**（c2f61e6）：`/space/list/page/vo` 仅返回本人空间+已加入团队空间（admin 不受限，新方法 `listMyRelatedSpaceByPage`）；`/space/get/vo` 要求与空间有归属关系。前端合法用法已逐一核实不受影响。测试：SpaceListAuthIntegrationTest 9 个
- **T3.3 会话生命周期**（5501642）：改密/删号/角色变更踢 Sa-Token 会话；`getLoginUser` 匿名请求不再创建 Redis 空会话。测试：UserSessionLifecycleIntegrationTest 4 个（走真实登录流程，验收"改密后旧 satoken 失效 + 新密码可登录"闭环）
- 全量回归：单测 13 + 集成 32 全绿；每笔 fix 提交均经门禁实跑放行
- 三条架构口径决定已补记 docs/decisions.md（目标绑定鉴权、会话踢出口径、接受的 Spring Session 残余）

## 进行中 / 下一步

- **T3.4 重复上传额度虚增**（下一项）：`PictureServiceImpl#uploadPicture` 事务段（约 :294-310）对空间额度无条件 `totalSize + 新大小、totalCount + 1`，更新分支（pictureId != null）不扣旧值——每次替换图片额度虚增一份。方案：更新分支改为 `totalSize = totalSize - oldSize + newSize`（count 不变）+ 更新成功后复用 `cleanupPictureFile` 清理旧 COS 对象。注意旧图三件套（原图/压缩/缩略）与共享 URL 引用计数逻辑
- T3.5 删空间/删用户级联 → T3.6 ShardingSphere 二选一 → T3.7 社交通知口径（后两项需用户拍板方向）
- 低危清单未登记 plan：IP 限流头伪造（nginx 未强制覆写 X-Real-IP）、picture 索引与 `LIKE '%kw%'` 不匹配、admin 接口返回密码哈希、字段长度口径（url 1024 vs 列 512）、协同编辑双页签互踢、前端 WS 无重连等，见会话 2026-09-12 审查报告

## 未决问题

- Spring Session 在改密后仍有效（已评估接受为低残余，记 decisions.md；如需彻底收敛：会话版本号机制，getLoginUser 比对 Redis epoch）
- T3.6 方向待拍板：修复启用 ShardingSphere（需修库名不一致/空 range 静默丢数据/动态建表回退三个坑）还是删除死代码死配置并改 README——当前线上实为单表
- T3.7 方向待拍板：SocialController 的 /notification/* 三接口还在服务（与 spec"社交停用"冲突），停掉还是改 spec

## 环境备注

- 分支：main，未提交改动：无
- 本机环境：Maven 3.9.11 + JDK 21（编译目标 17），本地 MySQL 8（3306）与 Redis（6379）可用，集成测试全量可跑
- 测试口径：门禁/CI 跑 `mvn -B test -Dtest='!*IntegrationTest,!RedisStringTest,!YunPictureBaseApplicationTests'`；集成测试命名以 IntegrationTest 结尾
- **集成测试夹具坑**（新会话写测试前必读 handoff 0002 + 本条）：① `StpKit.SPACE.login()` 后必须 `getSession().set(USER_LOGIN_STATE, user)` 补快照；② viewer 正例必须用团队空间（私有空间不认成员行）；③ 绕过验证码：种子 Redis 键 `huoshantuku:auth:captcha:img:{uuid}`（值 abcd）与 `huoshantuku:auth:captcha:email:{email}`；④ 直接调带 @AuthCheck 的控制器方法可行——切面走 RequestContextHolder，先 bindRequest(admin) 再调用
- 后端/前端本地起停命令见 AGENTS.md"命令"节；本轮验证时短暂起过后端 8123 + 前端 5173，已停净
