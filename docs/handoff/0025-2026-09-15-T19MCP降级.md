# Handoff 0025 — 2026-09-15 — T19 搜图 MCP 降级收口（附快照数字口径核实）

> 上一份：`0024`（T17 角色裁剪 + T18 失败时限收口）。**本文件只记 0024 之后的增量**——档 3 全貌、真机命令、更早的坑在 0020–0024，不重复。
> 本会话 4 笔提交：`731a481`（字据）→ `669794a`（代码）→ `fd4464c`（记录）→ 本快照自身。会话起点 HEAD `7ca74f0`。
> **快照里的 ahead 一律不含快照自身那笔**（同 0024 口径）：ahead `origin/main` **166 → 169**，含本快照即 **170**（口径见"未决问题"末条）。

## 当前状态（一句话）

**T19 落地并真机验证**：MCP 服务（8127）不可达时不再让整条对话失败——解析工具回调失败即降级为空数组（沿用 T9"少一个工具照常开对话"）+ **60s 冷却**（不每次白等一次 20s 的连接超时），反向对照证明 8127 起来后不误降级、`searchImage` 照常可用。工作区干净；**四个端口全空**（本会话起的引擎与 MCP 服务都已停）；MySQL/Redis 仍常驻。

## 刚完成（按时间）

1. **立字据 T19**（`731a481`，只改 `docs/plan.md`）：按规则 3 把条目连同文件范围、验收、两条参数（60s 冷却、不动 `request-timeout`）与"不做"边界写清；同条记下"20.1s 这个上界来自 `spring.ai.mcp.client.request-timeout` 默认值 20s"的核实来源（spring-ai-autoconfigure-mcp-client-common **1.1.2** 的 `spring-configuration-metadata.json`）。
2. **取红基线（一手复跑，先红后绿的第一手证据）**：8127 不启、用**修复前**的 jar 打 headless 端点 → HTTP **200** 但体是 `{"code":50000,"data":null,"message":"系统内部异常"}`、耗时 **20.13s**、**无 SSE**，栈顶 `HuoshanAssistantController.java:107`，异常 `CompletionException(ConnectException)`（SDK 文案 `Client failed to initialize listing tools`）。比 0024 记的"回 HTTP 50000"更准：**HTTP 状态是 200**，50000 是业务码。
3. **实现**（`669794a`）：新增 `ai/agent/.../tools/mcp/McpToolCallbackResolver`（`@Component`，唯一的 `resolve(Supplier<ToolCallbackProvider>)` **不抛异常**；Bean 不存在 ⇒ 空数组且**不进冷却**；取 Bean / 列工具任一步失败 ⇒ 空数组 + 一条 warn + 60s 冷却；成功 ⇒ 清冷却；时钟用 `LongSupplier` 注入，生产走 `System::nanoTime`）；控制器第 106-107 行收成一行 `resolve(mcpToolCallbackProvider::getIfAvailable)`；`application.yaml` **只补注释**（键值未动）。**门禁**：ai/agent 147 → **155 例**、backend 74 例不变。
4. **负向控制（规则 12 同款，实测会红）**：catch 里强制重抛 ⇒ **4 例红**（两条降级用例 + 冷却两条，因它们都以"首次解析失败"开头）；`isCoolingDown()` 恒 false ⇒ **恰好 1 例红**（`Expecting AtomicInteger(2) to have value: 1`，正是"冷却期内不该再尝试"）。
5. **真机复跑（修复后，三个形态都亲自跑过）**：① 8127 不启 → 首次对话 **24.1s** 收 `event=error` + `[DONE]`（约 20s MCP 超时 + 约 4s 黑洞 provider，**不再 50000**），**紧接着第二次 3.62s**（冷却生效）；日志依次一条 WARN（`RuntimeException: Client failed to initialize listing tools`）+ 一条 INFO（冷却期）。② 8127 起来 + 黑洞模型 → **4.35s**、**零降级日志**。③ 8127 起来 + **真模型（MiMo `mimo-v2.5`）**：问"帮我从网上搜 3 张日落的图片" → **15.15s**，SSE `tool(searchImage)` + `answer` + `[DONE]`，回答里是真实 `images.pexels.com` 地址、无 error 帧。
6. **记录**（`fd4464c`）：plan T19 勾选 + 实施记录 + 未做/已知代价 + 一条顺带发现；`decisions.md` 新增 T19 行；`F12` 补覆盖任务、验证口径、门禁数（140 → 155）、已知限制 10 改写为**已收口**（含四条残留）、关键设计与理由新增第 8 条。
7. **后续 review 订正（T20，2026-09-15）**：发现原冷却检查不是原子的，并发会话可能在首个失败写入冷却前一起通过检查，随后依次支付连接超时。`f14a7c9` 用非阻塞 single-flight 收口：一个会话探测，其余立即按空工具降级；并发回归在旧实现稳定红（provider 调用 2 次而期望 1），修复后目标类 9/9、门禁 backend 74 / ai 156 全绿。另订正两处文字：旧故障响应是 **HTTP 200 + 业务码 50000**；60s 是从“失败被确认”起算，不是从“服务恢复”起算。

## 进行中 / 下一步

1. **T18 的 `OpenAiChatModelsRetryTest` 时间敏感、满载偶发红（本会话顺带实测发现，未修，建议优先处理）**：`totalBudgetTruncatesRetriesEvenWhenAttemptsRemain` 的预算 800ms 与首次退避 500ms 太近——**全量并行跑**时偶发红一次（`expected: 2 but was: 3`，该用例自身耗 2.399s），而**单类隔离跑 4 次全绿**（每次 9.4s）。含义：**门禁会随机拦提交**（本会话手动全量跑就撞上一次红，重跑才绿；提交时代码门禁又跑了一遍，那次是绿的）。收口二选一：断言放宽成"远小于 10 次"（如 `isLessThanOrEqualTo(3)`），或用可控时钟/更宽预算-退避比把时序钉死。属 T18 文件范围，按规则 1 未在 T19 内顺手改。
2. **MCP 降级的四条残留**（记在 `F12` 已知限制 10）：静默降级（用户与模型都不知道本轮少了搜图工具）；降级粒度是**整批** MCP 工具（将来接第二个 MCP 服务会连带）；冷却状态在**进程内**（多实例各自计时；失败被确认后各实例最多再等 60s 重试，从服务恢复时刻计算还需叠加在途探测的剩余超时）；每个故障窗口第一次尝试仍要 ≤20s。**若要让用户看见**，需要一次"前端提示 + 引擎事件"的小任务（未拍板）。
3. 沿用 0024 的挂账：**review 的 3 条 P3**（`runLoop` 上限提示未判 `state`（附一例 `maxSteps=1 + finishOnStep` 契约测试即先红后绿）/ `AgentEvent.Error` javadoc 措辞 / 计划 §5 与实现对齐 + 实例只跑一次的不变量提到 `BaseAgent`）；**T17 剩余口径**（管理员侧工具是否改调 T15、提示词按角色补话术、两种角色的人工验收）；**R1 埋点**；引擎 emitter 仍 300s；AI 打标的现场验收最后一步；R5 的五条 P3（前端取票不判业务码 / 后端解析失败与真故障共用"系统错误"文案 / `POST /ticket` 无频控 / 无票被拒不记 `userId` / 集成用例少一条断言）；前端社交残留（未拍板）；前端 `type-check` 138 / `eslint` 73 存量红；上线必办四项（nginx SSE 块 / 旧端点 `/ai/manus/chat` 未鉴权 / 双端密钥一致 / MCP 与引擎同机——**注意 T19 让"MCP 没起"不再致命，但真要用搜图仍须同机可达**）；探针清理（0020 记的 t12 空间 9 张图 + `t8-*` 记忆行）；push/tag。

## 未决问题

- **push / tag**：`main` 领先 `origin/main` **169 笔（不含本快照自身；含它 170）**未 push（本会话新增 3 笔代码/文档）；仓库**仍无任何 tag**（档 3 + AI 打标 + T17/T18/T19 都已收口，是打第一个 tag 的时机，用户未决）。
- **`docs/decisions.md` 有一行历史遗留多一个 `|`**（非本次引入，渲染会多出一列）——仍未动，属可穿插的一行级清理。
- **spec 的 F11 仍未勾选**（一期 T4–T19 全部收口，是否勾选或是否等到上线，待用户确认）。
- **快照数字口径（本会话核实并订正了自己的一个误判）**：每份快照的 `ahead` **都不含快照自身那笔**——0024 写"158 → 165、7 笔提交"是 158 + 7，它自己那笔 `7ca74f0` 不计，**内部自洽、不是漏数**；我一开始判成"0024 漏数"，属误读，已就地订正（本份同理：166 + 3 = 169，含本快照为 170）。**教训仍是同一条**：要真实值就自己跑一次 `git rev-list --count origin/main..HEAD`，别照抄快照、也别照抄别人对快照的解读。

## spec 偏差（认真轨）

- **越界**：**无**。T19 属一期范围内（T9 的 MCP 接入的健壮性收口），用户原话"做 MCP 降级"即授权；口径未变 ⇒ **spec 关键三行无需改动**（已在字据里写明）。两处**已声明的 AI 判定**（用户只拍板"做降级"，未逐项拍参数）：① **60s 冷却**（不做成配置项）；② **不做显式健康探测**（plan 上一轮列的另一方向）——两条的理由与代价都写进了 `decisions.md` T19 行，用户可推翻。
- **缺口**：MCP 降级四条残留（上方"下一步"2）；`OpenAiChatModelsRetryTest` 的时间敏感（1）；沿用 0024 的 review 三条 P3、T17 三条口径、R1 埋点。
- **关键三行检视**：**成立**——引擎仍是独立 Maven 应用/独立进程（8124）；headless 端点仍只认服务间 API key（本轮没碰鉴权、没新增信道）；凭据与密钥仍只在内存或不入库的本地配置里（本轮只动了 MCP 工具的**装配**，没碰任何凭据路径；新增的 warn 日志只可能带 MCP 服务地址，不含用户凭据）。

## 线上状态

- 仍是存量版本（同 0020–0024）：线上无 `/assistant` 入口，`ai/` 引擎与 MCP 均未部署，AI 打标也未上线。本会话无线上操作，plan.md 无 `[线上]` 任务。

## 环境备注

- 分支 `main`；**工作区干净**；ahead `origin/main` **169 笔（不含本快照自身；含它 170）**；**无 tag**。
- **本机服务**：MySQL 3306 / Redis 6379 常驻；**8123 / 8124 / 8127 / 5173 全空**（交接前实测）。
- **本会话新的可复用做法**：
  1. **起引擎用 `--spring.profiles.active=local` 就自带 MiMo 配置**（`application-local.yaml` 里那组 `app.ai.openai.assistant/vision`），不必手工传 base-url/model/api-key；黑洞 provider 复现仍用 `--app.ai.openai.assistant.base-url=http://127.0.0.1:9 --app.ai.openai.assistant.api-key=smoke-key --app.ai.openai.assistant.model=smoke-model`。
  2. **起 MCP 服务（8127）要带 `--spring.profiles.active=sse,local`**——`local` 那份才有 Pexels 密钥（只带 `sse` 时 `tools/list` 仍返回 `searchImage`，但真调用会因缺密钥失败）。
  3. **本地起引擎不再需要 `--spring.ai.mcp.client.enabled=false`**（T19 降级已生效）；想彻底不连 MCP 时仍可用它。
  4. PowerShell 跑中文查询：把话术写成 UTF-8 文件（Write 工具），脚本里 `[System.IO.File]::ReadAllText($f,[Text.Encoding]::UTF8)` + `EscapeDataString`——避免 .ps1 被按 GBK 解码导致乱码（本会话就是这么发的中文提问）。
- **探针与产物**：`%TEMP%\t19\`——`baseline-red.ps1` + `base-engine.log` + `base-resp.txt`（红基线）、`after-degrade.ps1` + `after-resp-{a,b}.txt` + `after-degrade-engine.log`（降级两连击）、`after-healthy.ps1` + `healthy-{blackhole,real}-engine.log` + `healthy-real-resp.txt` + `mcp-server.log`（反向对照）、`msg-zh.txt`。**都用后即弃的桩凭据（`satoken: dummy`），无非临时凭据落盘**；引擎 jar 已按 T19 代码重新打包（`ai/agent/target/my-ai-agent-0.0.1-SNAPSHOT.jar`）。沿用 0020 的 `%TEMP%\t12\jar.txt`（**含凭据的 cookie 罐，用完请删**）与 `%TEMP%\t8\*`。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**。T19 细节看 `docs/features/F12`（工具表"工具（搜图）"行、关键设计与理由 8、已知限制 10、怎么验证）+ `docs/decisions.md` 2026-09-15 T19 行。
2. **下一件事建议**：先处理 `OpenAiChatModelsRetryTest` 的时间敏感（它会随机拦提交，属规则 5 的"红绿灯不可靠"）→ 再 review 的 3 条 P3（可穿插）→ 再按用户原话办：MCP 降级的用户可见性、T17 剩余口径、R1 埋点、前端社交残留、探针清理、push/tag。
