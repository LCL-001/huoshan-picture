# Handoff 0029 — 2026-09-17 — AI 助手首次生产部署完成

> 上一份：`0028-2026-09-16-部署产物与探针清理.md`。本快照按规则 6 只写 09-17 增量，不复述既有实现史。

## 当前状态（一句话）

**AI 助手一期已首次部署到 `https://www.lincode.online`，F11 已勾选**：backend 8123、agent 8124、MCP 8127 与前端均已上服务器，agent 的 MySQL 用户名错误修正后，登录用户可通过助手列出自己的空间；AI 打标可用，nginx SSE / WebSocket / API 片段已合并。

## 本次增量

- Git 已通过 SSH 推送，部署前仓库 `main` 与 `origin/main` 同指 `10571ea`；本次使用本地打包产物上传服务器，不是在服务器构建。
- agent 生产库定为 `huoshan_ai_agent`，`deploy/sql/engine-schema.sql` 的四张表已在真库确认存在。
- 真机故障已定位并修复：旧示例写 `MYSQL_USERNAME=huoshan_agent`，宝塔实际创建的用户是 `huoshan_ai_agent`，日志报 `Access denied for user 'huoshan_agent'@'localhost'`；修改环境变量并重启后成功。模板已同步订正。
- 三个 Java 进程采用宝塔 Java 项目管理；backend 用 JDK 17，agent / MCP 用 JDK 21。agent JVM 参数顺序已明确为 `java -Xms... -Xmx... -jar ... --spring.profiles.active=prod`。
- nginx 已按 `deploy/nginx/ai-assistant-sse.conf` 思路补充 AI 助手 SSE、WebSocket 与普通 API 代理，8124 / 8127 不对公网开放。
- `docs/spec.md` F11 与 `docs/plan.md` D3–D5 / A1 已按上线事实更新；`deploy/prod-checklist.md` 记录真机踩坑与实际部署方式。

## 线上状态

- 当前线上部署候选：`10571ea`（服务器产物未做哈希回读，仓库尚无发布 tag）。
- 已确认：助手页面可访问、backend → agent 联通、登录态可用于列出自己的空间、AI 打标可用、生产数据库四张表存在。
- 未处理线上故障：0 个已知阻断故障。
- 尚未保存为可复跑证据的检查：8124/8127 运行时监听地址、`nginx -t` 输出、SSE 长对话逐帧/360s、长对话摘要、MCP 搜图、首发只读负向控制、INFO 日志脱敏、宝塔重启恢复。它们不是本次已知故障，但不能写成已验证。

## 未决 / 下一步

1. 服务器下次维护时补一次重启恢复演练，确认宝塔对三个 Java 项目的开机自启、崩溃重拉和日志轮转。
2. 按 `deploy/prod-checklist.md` 尚未勾选的项目补验；若准备打 `v0.1.0`，先完成这些检查并回读服务器 JAR/前端产物哈希。
3. 本次会话中生产数据库、Redis、模型与内部调用密钥曾以明文贴入聊天；**密钥轮换未确认**。上线后应轮换，并保证 backend 的 `AI_ENGINE_INTERNAL_API_KEY` 与 agent 的 `HUOSHAN_HEADLESS_API_KEY` 同值。
4. T27 仍未整体完成；首发继续维持助手只读/建议能力，不能因“已上线”而恢复两个写工具。

## spec 偏差

- 越界：无；线上仍遵守首发只读/建议边界。
- 缺口：F11 已上线并勾选；完整写确认门不属于当前已上线边界，仍由 T27 后续承接。
- 关键三行仍成立。

## 证据说明

- 用户提供了宝塔项目配置截图、生产库四表截图、IDEA 远程 MySQL 连接截图和 agent 异常日志；这些属于**不可复跑、证据已过期**的会话附件，没有入库。
- 可复现数据库认证检查：`mysql -h127.0.0.1 -P3306 -u<实际用户> -p huoshan_ai_agent -e "SHOW TABLES;"`。
- 可复现 agent 健康检查：`curl -i http://127.0.0.1:8124/api/health`。
- 可复现 nginx 检查：`nginx -t`；随后登录线上 `/assistant` 发送“列出我的空间”。

## 环境备注

- 分支：`main`；线上部署候选为 `10571ea`。本次另有数据库用户名模板修复与本快照两笔收尾提交，收尾流程会一并 push；下次接手以 `git status -sb` 和 `git log -3` 复核远端状态。
- 未提交业务改动：无。唯一未跟踪项仍为 `backend/src/main/java/graphify-out/`（结构分析工具残留）；按删除规则未处理。
- `stash@{0}` 仍为 2026-09-16 用户要求保留的 V1 / `chat_summary` + MCP `ImageSearchTool` 加固改动，未触碰。
- 无发布 tag。
