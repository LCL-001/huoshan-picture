# Handoff 0007 — 标签词表 T5 完成（2026-09-14）

## 当前状态（一句话）

一期 T5 完成：`tag` 全局词表落库（种子 9 标签 + 5 分类）、/tag_category 查表动态化（PictureTagCategory 结构逐字段不变）、单编/批编同事务 upsert 累加 usageCount；全量回归 65 用例绿；下一步 T6（OpenAI 协议模型接入）可开工。

## 刚完成（本会话，按时间）

- **T5 实施计划**（be1c006，docs/plans/2026-09-13-T5-标签词表实施计划.md）：按 writing-plans 四任务拆解——域模型/词表服务/接线/收口；范围外显式声明 /picture/update 不接词表、picture.tags 与 LIKE 逻辑不动、词表无删除入口、/tag_category 不加缓存。
- **a68d8cc Task 1**：backend/sql/create_tag_table.sql（独立脚本不动 create_table.sql；SET NAMES utf8mb4 防 GBK 客户端；INSERT IGNORE 幂等种子）+ Tag（@TableLogic、IdType.AUTO）/ TagMapper（+空 XML 骨架）/ TagTypeEnum；本地库已执行验证（tag 9 + category 5，中文入库无乱码）。
- **647f77f Task 2**：ITagService/TagServiceImpl——upsertVocabulary（先原子自增、未命中插入、并发首插撞 uk_name_type 捕 DuplicateKeyException 退化自增；空白词跳过去重；纯 MyBatis-Plus API 无自定义 SQL）+ listNamesByType（id 升序稳定输出）；集成测试钉住种子迁移/结构兼容/去重（2 用例）。
- **4ed0025 Task 3**：/tag_category 改调 tagService.listNamesByType（控制器结构未动）；editPicture 补 @Transactional(rollbackFor) + upsert（updateById 成功后清缓存前）；editPictureByBatch 插 upsert（图片列表判空早退之后、重命名之前——没有图被改不计数）；批编接线测试回填红→绿，TagVocabularyIntegrationTest 3 用例全绿。
- **2da0e76 Task 4**：全量回归 65 用例绿（62 存量 + 3 新增）；docs/features/F10-标签词表.md（规则 9 讲解文档）；spec F10 / plan T5 勾选并补实施记录；decisions.md 补 T5 落地口径一条。

## 排障记录（本会话重要教训）

- 批编计数断言曾假红：先疑似连接池旧快照/事务可见性，受控实验（重置计数、单方法运行、事务探针 txActive=false、hex 比对词条字节）后定位为**测试辅助方法 usageCountOf 把词条类型硬编码为 tag**——查 category 类词条恒为 null→0，接线代码从头到尾正确。修复为 usageCountOf(name, type) 后全绿。
- 教训：断言失败先怀疑测试自身 helper 的查询条件，再怀疑环境侧；"写路径命中而读路径不命中"时优先 diff 两条查询的 WHERE 差异。
- 执行偏差：批编接线测试原计划放 Task 2，但其红依赖 Task 3 的接线——按红绿灯纪律挪到 Task 3（先回填测试看红、再接线转绿）；已记入计划文档"执行记录"。

## 进行中 / 下一步

- 无进行中任务。**T6 OpenAI 协议模型接入（ai/agent）可开工**；随后 T7 图库工具集（visionTagger 提示词直接消费 /picture/tag_category 动态词表，T5 是它的地基）→ T8 会话类型 + headless SSE 端点（含仅 SSE 循环收敛）→ T9 MCP client → T10 backend 代理 → T11 前端助手界面 → T12 联调验收。
- 每任务开工前按 writing-plans 写即时实施计划（范本：T4 迁移计划、T5 本计划）。

## 未决问题

- 无阻塞决策。待实施期确认项不变：SSE 转发 SB 2.7 选型（T10）、Pexels key 实测（T9）、视觉模型读 COS 私有空间签名 URL 可用性（T7/T12）。
- dev/main 均未 push（两仓），何时推远程由用户决定。
- 词表管理端点/删除入口、计数回收：一期不做，是否立项留二期（decisions.md 已记口径）。

## spec 偏差（认真轨）

- 越界（做了 spec 没写的）：无——单编 editPicture 补 @Transactional 属"编辑同事务 upsert"要求的必然；建表脚本独立成文件未触碰 create_table.sql。
- 缺口（spec 承诺但没做的）：无——F10 全项交付，picture.tags JSON 列与 LIKE 查询逻辑未动。
- 关键三行检视：目标/不做的事/边界仍成立；列名 usageCount 与设计文档速写 usage_count 的微差已在 spec F10 行内注明。

## 环境备注

- 分支：main；未提交改动：无；本会话产出 be1c006…2da0e76 共 5 笔 + 本 handoff 1 笔。
- 本机：Maven 3.9.11 + JDK 21；MySQL(3306)/Redis(6379) 在跑；本地库 yu_picture.tag 已建——种子 14 行 + 测试残留（tag-vocab-* 用户/空间/图片、T5新词* 词条、热门/海报计数有累计），集成测试共享库、断言用 contains/相对值不受影响，可清理不强制。
- 门禁：pre-commit 跑 backend 纯单测（13 用例）+ ai/agent 纯单测（55 用例）；TagVocabularyIntegrationTest 以 IntegrationTest 结尾不入 CI/门禁（既有约定），全量靠本地手动回归。
- Windows 工具链坑（0006 记录仍有效），本会话新增：cmd 下 `git commit -m ... ; powershell ...` 的 `;` 链接会断链报 129（一律用 `&&`）；mysql CLI 中文结果直出控制台必乱码（GBK 显示问题，数据无损），用 `| Out-File -Encoding utf8` 落文件再读；git log 中文乱码同理，`git log --pretty=%B > 文件` 后按 UTF-8 读即可验证。
