# Handoff 0011 — T6.1 独立 review 完成 + 一期转向"MVP 档位"改锚（2026-09-14）

## 当前状态（一句话）

T6.1 已由独立会话完成规则 13 review（**有条件通过**，结论 `f445d11`），一期随即转向"MVP 优先"档位口径并已获用户确认关键三行（`f6c4d7e`）；**本会话零代码改动**，工作区干净；下一会话从**档 1（T8-lite + T7 三个只读工具）**开工。

## 刚完成（本会话，按时间）

- **T6.1 独立 review（规则 13，只读）**：复核 `e20a5dd`，门禁口径实跑 **68/68 绿**、`OpenAiChatModelsTimeoutTest` 连跑 3 次 6/6 稳定；框架事实全部落在字节码/源码（版本锚点见"环境备注"）。结论 `f445d11`，**全文与逐条证据在 `docs/handoff/0010-…md` 末节"独立 review 结论"**。要点：
  - 两条机制本体成立，"缺一不可"用自建探针独立复现（只挂 connector + 吐两段后静默 → 6s 无信号；完整装配 + 从不响应 → 1020ms 抛 `WebClientRequestException <- HttpTimeoutException`）。
  - 10 条必审项全部有裁定（响应元信息 / filter 作用域 / 重试 / 工具轮次 / 一把尺 / 测试有效性 / 稳定性 / 反射 / 范围 / T6 的 P3）。
  - **R1（P1）客户端取消不关闭上游请求**：根因 Spring `JdkClientHttpResponse.adaptBody` 的 `Flux.cache(0)`（= `replay(0).autoConnect()`，取消不断开上游），裸 JDK `ofPublisher`+cancel 则立刻 FIN；非 T6.1 引入。
  - **R2（P2）** `RetryUtils.DEFAULT_RETRY_TEMPLATE` 实为 `maxAttempts(10)` + retryOn(`TransientAiException`, `ResourceAccessException`) + `exponentialBackoff(2s,×5,cap180s)`，阻塞路径读超时会重试；原"仅 retryOn `TransientAiException`"口径已更正。
  - **R3（P2）** 流式超时到达调用方时是**状态码 200** 的 `WebClientResponseException` 包裹，不是裸 `TimeoutException` —— T8 必须沿 cause 链判定。
- **转向"MVP 优先"改锚**（`f6c4d7e`，用户口径"先做出来能用，再慢慢优化"，回答见该提交）：spec 关键三行的"目标""不做的事"两条各追加一句档位口径（用户已确认）；plan.md 新增"MVP 档位与推进顺序"节 + T7/T8 条目标注档位拆分；decisions.md 追加转向口径行（含放宽/不放宽清单与 R1 延后）。

## 进行中 / 下一步

**下一会话的第一件事：档 1（headless 打通）。** 定义（用户已确认）：

- **T8-lite**：会话类型（普通 / 图库助手，决定工具注册表与系统提示词）+ headless SSE 端点（服务间 API key 鉴权 + `huoshan:<userId>` 身份映射 + satoken 上下文传给工具）。**循环不重构、`run()` 暂不退役**（那部分属档 3 的 T8-hard）。
- **T7-lite**：`listSpaces` / `listPictures` / `getTagCategory` 三个只读工具，prototype 化 per-request 注入透传 token 与图库地址。
- **验收**：一条脚本 / curl 跑出"提问 → 真实调图库 → SSE 步骤事件流"，且工具调用走用户真实 satoken（越权不可达）。
- **档 2（本轮终点，本轮不做）**：T10 backend 代理 + T11 前端助手界面与步骤折叠条 = 设计文档验收标准 1 最小形态；收口时做一次里程碑独立 review（规则 13）+ 一篇合并讲解（规则 9）。

**本会话已摸清的触碰点（都是 file:line 事实，可直接用）**：

- SSE 端点已存在，不用从零写：`AiController` 的 `@GetMapping("/manus/chat")`（`controller/AiController.java:74-75`，实际路径 `/api/ai/manus/chat`），返回 `SseEmitter`，由 `BaseAgent.runStream` 创建（`agent/BaseAgent.java:157-159`）并写出事件（step `:193-199`、answer `:201`、metrics `:225-229`、`[DONE]` `:208,231,239`、信封 `sendEvent` `:288-296`）。
- 每请求新建 agent（**不能做单例**，`agent/MyManus.java:14-16`）：`new MyManus(allTools, dashscopeChatModel, convId, memory)`（`AiController.java:81`），`convId` 来自 `chatId` 或 UUID（`:78`）。
- 工具现为单例数组 `ToolRegistration.allTools()`（`config/ToolRegistration.java:25-36`），经 **`DashScopeChatOptions.withToolCallbacks`** 挂载（`agent/ToolCallAgent.java:108-113`）——见下方风险 1。
- 记忆与会话：`FlowWindowBasedChatMemory`（`chatmemory/FlowWindowBasedChatMemory.java:25-28`，`@Component` 单例）、`ConversationService.getOrCreate`（`service/ConversationService.java:18,26-37`）。
- **ai/agent 目前无任何鉴权**：pom 无 sa-token，无 Filter / Interceptor（唯一 `WebMvcConfigurer` 是 `config/CorsConfig.java:11` 只配 CORS），`/ai/manus/chat` 不校验登录（`AiController.java:47-52` 的登录态只用于侧记书名）。
- 模型：控制器注入容器里那一个 `ChatModel`（`AiController.java:32-33`）；**`OpenAiChatModels` 至今零生产消费者**（只有 config 与测试），档 1 要把它接进图库助手会话类型。

**开工前先验证的 3 个风险**：

1. **工具调用挂载是 DashScope 专用**（`ToolCallAgent.java:108-113`），换成 `OpenAiChatModels.assistant()`（OpenAI 协议）后工具调用能否照常——先花几分钟验证，必要时改成 provider 中立的 `ToolCallingChatOptions`。这可能是档 1 唯一的真未知。
2. **旧端点 `/ai/manus/chat` 无鉴权**：加新端点 API key 时要不要一并锁旧端点，属用户决定（本会话未擅自扩范围），开工前问一声。
3. **工具返回结构要面向前端折叠条友好**（结构化分页，设计文档已要求），档 1 就把返回 DTO 定形，别留到档 2 返工。

**建议顺序**：写 `docs/plans/` 档 1 即时计划（范本 T5/T6 计划文档）→ 验证风险 1 → T7-lite 三个工具（Mock 图库 HTTP 单测）→ T8-lite 端点与鉴权 → 冒烟脚本。

## 未决问题

- **R1（P1）**：用户已拍板**延后到档 2 之后**；T8 只做"超时转显式错误事件"、不承诺关闭上游；期间加超时/取消可观测埋点，接真实云端 provider 后按实测再定"接受+文档 / 换 connector / 补显式终止"。
- **R2**：口径已更正入库；若要让阻塞路径有硬上限，需给 `OpenAiChatModel.builder()` 传更短的重试模板 —— 属新决策，未做。
- **T6 的三个 P3**：③ `@DurationUnit(SECONDS)`、⑥ `OpenAiChatModelsTest` 夹具去 `/v1` 建议并入 T7 首次触碰 `OpenAiChatModels` 时同做（已记 plan.md）；① ② ⑤ ⑦ 继续挂。
- **push / tag**：main 领先 `origin/main` **75 笔**、仓库**无任何 tag**（用户未决，本会话未擅自 push 或打 tag）。
- **门禁稳定余量**：稳态看门用例段间隔 300ms vs `CONTROL_TIMEOUT` 1000ms = 3.3×，2 核 CI 上偏紧（失败是响亮的红）；加固 = 把 `CONTROL_TIMEOUT` 提到 1500~2000ms，未做。
- **ai/agent 无鉴权 & 旧端点未锁**（见风险 2）。
- 档 2 之后仍挂着：visionTagger / 两个批量工具 / T9 MCP 搜图 / T8-hard 循环合并 / T12 联调 / T13 清理。

## spec 偏差（认真轨）

- **越界（做了 spec 没写的）**：本会话零代码改动，无新增越界。T6.1 自身的 `timeout=0/负数=不限制` 越界已由 review 判定合理（规避启动即崩，见 0010）。
- **缺口（spec 承诺但没做的）**：无新增；一期后续项已按档 1/2/3 显式排序，档 2 之前不做项写进 spec.md 第 8 行。
- **关键三行检视**：**成立**——`ai/` 仍是独立 Maven 应用与独立进程，本会话 backend / frontend 零改动；2026-09-14 追加的档位口径已获用户当轮确认。
- 转向属规则 8"小转向"，已走完"改锚 + 记因"（decisions.md 2026-09-14 行）；旧任务一条未删，无需 archive tag。

## 线上状态

- 线上 https://www.lincode.online 仍是 backend / frontend 存量版本；`ai/` 引擎尚未部署（档 2 之后才涉及部署），**无 [线上] 反馈项**。

## 环境备注

- 分支 `main`；本会话提交 `f445d11`（review 结论）、`f6c4d7e`（转向改锚），均为 docs-only；**工作区干净**；ahead `origin/main` **75 笔**未 push；无 tag。门禁 `core.hooksPath=.githooks` 已启用。
- **版本锚点（务必先锚版本再读框架源码）**：spring-ai-openai **1.1.2**、spring-webflux **6.2.17**、reactor-core **3.7.17**、JDK 21（`%JAVA_HOME%\lib\src.zip` 可用）。复核命令：`cd ai/agent && mvn -o dependency:build-classpath -Dmdep.outputFile=target/cp-review.txt`。
- 门禁口径命令（本会话实跑 68/68 绿）：`cd ai/agent && mvn -B -o test -Dtest="!MyManusTest,!MyAiAgentApplicationTests,!ProductScopeTest,!PgVectorVectorStoreConfigTest,!FileOperationToolTest,!PDFGenerationToolTest,!ResourceDownloadToolTest,!TerminalOperationToolTest,!WebScrapingToolTest,!WebSearchToolTest" -DfailIfNoTests=false`；单类 `-Dtest=OpenAiChatModelsTimeoutTest`。
- **review 探针在 `%TEMP%\t61rev\`（仓库外，可删）**：`T61ReviewProbe2`（取消 / FIN / 迟写、两轮间隔、裸 JDK 控制组）、`T61ReviewProbe3`（G1/G2/G3 定位 `cache(0)`）、`T61ReviewProbe4`（H1/H2 两条负向控制）+ 编译产物 `classes`；classpath 复用 `%TEMP%\t6-smoke\cp.txt`；上一轮 T6/T6.1 探针仍在 `%TEMP%\t6rev\`。
- **三个可复用的坑**：① 探针需要包级访问 `OpenAiChatModels.streamingConnector` 时必须 `javac -encoding UTF-8 -cp "target/classes;<cp.txt>" -d classes <probe>.java` 再 `java -cp`，`java Foo.java` 单文件模式用 MemoryClassLoader 会 `IllegalAccessError`；② classpath 27k 超 cmd 限制，跑探针/应用一律走 PowerShell（照 `%TEMP%\t6rev\run-t61-app.ps1` 抄），cmd 里别用 PowerShell cmdlet；③ Spring 源码可用 `-sources.jar`（`spring-webflux-6.2.17-sources.jar` / `spring-web-6.2.17-sources.jar` 在本地仓库，用 `[System.IO.Compression.ZipFile]` 抽特定文件）。
- 本机：Maven 3.9.11 + JDK 21（本地仓库 `D:\develop\Maven\apache-maven-3.9.11\maven_repo`）；MySQL 3306 / Redis 6379 / Ollama 11434（`qwen3:4b`）/ ai/agent 端口 8124 空闲；backend 8123、frontend 5173 档 1 用不到。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**；
2. 档 1 开工前读 `docs/plan.md` 的"MVP 档位与推进顺序"节与 T7/T8 条目、`docs/decisions.md` 最后三行；
3. 如要复核 T6.1 的 review 细节或复现证据，读 `docs/handoff/0010-…md` 末节"独立 review 结论"。
