# Handoff 0027 — 2026-09-15 22:40 — T18 补记 / T21-T24 收口与打标现场验收

> 上一份：`0026-2026-09-15-T20并发降级review收口.md`。本快照覆盖本会话全部工作：T18 补记（时间敏感用例）→ T21（三条 P3）→ T22（MCP 降级可见化 + 前端社交清理）→ T23（R1 接受 + 埋点）→ T24（AI 打标现场验收）。
> ahead 口径沿用 0024-0026：下文的 **185** 不含本快照自身；提交本快照后为 **186**。
> **2026-09-15 追加**：本快照随后并入了同日的**第二个会话**（独立复核 + 流程损耗治理），见文末「会话 2 追加」。按 AGENTS.md 规则 6 的新口径，同一天的多个会话合并进同一份快照、只写增量。文末那节的 ahead 数字已更新；上文正文中的 185/186 保留为当时的原始记录。

## 当前状态（一句话）

**队列里"能自动做"的都做完了**：T18 那个时间敏感用例订正并加固、T8-hard review 的三条 P3 收口、MCP 降级从静默改为用户可见（新增 `notice` 帧）、R1 拍板"接受"并落地超时/取消计数、前端停用的社交残留清干净、AI 打标现场验收由用户在浏览器实测通过并用真库复核；**打标那次验收是用户手点的**（我用手搓 HTTP 没打通管理员门槛，原因未定性，见下），门禁全绿、工作区干净、本机服务已全部停掉。

## 刚完成

1. **T18 补记（时间敏感用例，提交 `8f6cf26` + 一笔 `docs:`）**：`OpenAiChatModelsRetryTest.totalBudgetTruncatesRetriesEvenWhenAttemptsRemain` 原先预算 800ms 与首次退避 500ms 只差 300ms，满载时拦红。实测确认框架语义（`TimeoutRetryPolicy` 从 `open()` 起用**墙钟**计时；预算在每次尝试前后各查一次），**确定性复现**：往重试回调注入 400ms 延迟即退化成 1 次尝试（旧参数 48 次重负载里 15 次为 1、极端负载下还出现 0）。新参数 = 预算 2500ms + 首次退避 1000ms（"必须通过"的那次留 1500ms 余量），注入 1400ms 仍稳定 2 次。**同时订正旧记录的方向**：`plan.md`/`F12` 原记的 `expected: 2 but was: 3` 在单调钟下不可达（需墙钟回拨），真实可复现的偏差是**变少**。
2. **T21（三条 P3，提交 `e4f09a5` + 一笔 `docs:`）**：`runLoop` 上限提示加 `state == RUNNING` 前置条件（最终回答/卡死终止恰好落在第 `maxSteps` 步时不再多补一条回答帧）+ 两条契约测试；`AgentEvent.Error` javadoc 改为"替代回答帧、是本轮**最后**一条业务事件"并补中途失败契约测试；计划 Task 3 两处标注实施偏差（**选"文档对齐代码"**：`state=ERROR` 有承载），"实例只跑一次"不变量整段提到 `BaseAgent`。
3. **T22（MCP 降级可见化 + 前端社交清理，提交 `2da2d67` / `fcd668d` + 一笔 `docs:`）**：新增 `AgentEvent.Notice` 与 `{"event":"notice"}` 帧；`BaseAgent` 增"开场事件"重载（校验失败时不发）；`McpToolCallbackResolver.resolve` 返回 `McpTools(callbacks, degraded)`（**Bean 不存在=配置选择，不提示**）；前端 `onNotice` 可选渲染成回答上方一行弱化文本。前端侧删掉三个停用路由与三个页面、`postController.ts` 整体删除（通知 6 函数先迁入 `api/notificationController.ts`）。
4. **T23（R1 接受 + 埋点，提交 `5da2c9e` + 一笔 `docs:`）**：`AgentRunMetrics` 两个 Micrometer 计数器（`huoshan.agent.run.timeout` / `.cancelled`），引擎已有 actuator、零新增依赖；`BaseAgent` 加可空 `metrics`（不注入=空操作）。**真机**：`curl -m 6` 掐断连接后 `cancelled` 0.0 → 1.0，日志"用户停止生成，Agent 提前终止, steps=2"。
5. **T24（打标现场验收，提交 `0144b3e`、`eba024d`）**：用户浏览器实测"出建议 → 确认入库"**可用**；真库复核 3 张公共图被写入 tags/category、**审核四字段逐字段未变**、词表 upsert；负向证据两条（普通用户 `40400`、零写库；登录态必须 satoken + SESSION 两把同在场）。T15/T16 条目已镜像该结论。
6. **同批验证**：引擎侧真机跑了两轮（admin / 非管理员）——**降级提示是 SSE 首帧**、管理员轮无"非管理员"日志而普通用户轮有、第二轮命中 MCP 冷却（T19 规则在真机可见）。证据 `%TEMP%\t23\acceptance.txt`、`%TEMP%\t24\`。

## 进行中 / 下一步

1. **T17 的浏览器侧人工验收**仍未做（引擎侧装配已真机验收：日志"调用者非管理员，本次会话不挂 visionTagger"）；同一趟可顺带看 T22 的 `notice` 行渲染、以及普通用户问打标时模型的话术（提示词未按角色补话术）。
2. **R5 五条 P3**（`chatId` 可换行注入日志、relay 多行 data 产出半个 JSON 帧、上游缺 `[DONE]` 时两句话同屏、上游非 JSON 错误体原文进气泡、代理 emitter 超时早于引擎）——建议至少修"错误体原文进气泡"。
3. **上线四项**：nginx server 块（`proxy_buffering off` / `proxy_read_timeout`）、旧端点 `/ai/manus/chat` 鉴权或删除、引擎与 MCP 的 prod 配置与环境变量、上线后勾 F11。
4. **零碎**：`frontend/src/api/index.ts`（零引用死文件）、`typings.d.ts` 里已无引用的 `Post*` 类型、`decisions.md` 同一天几行的顺序（T19 → T23 → T22 → T21，纯排版）。

## 未决问题

- **push / tag**：写本快照前 `main` 领先 `origin/main` **185**，仍未 push、仓库仍无 tag。
- **AI 手搓 HTTP 打不通管理员门槛**（未定性）：同一套 satoken+SESSION 打 `/user/get/login` 正常返回 `userRole=admin`、浏览器同账号同入口可用，但打 `/picture/ai_tag/suggest` 仍 `40400`。**差异在我的请求构造**；复现请从 DevTools「Copy as cURL」原样回放。**这条记在 `decisions.md` T24 行**，别当成应用 bug 去查。
- **用户给过的会话凭据已明文出现在对话里**（Sa-Token 有效期 7 天）——建议已提醒用户登出。
- **真机验收的环境口径**：后端要用 `--spring.profiles.active=local,test` 起才免验证码（登录只要账号+密码）；**跑真机前务必 `mvn package`**——本轮踩过"跑的是旧 jar，于是新功能看起来没生效"（`mvn test` 只编译不打包）。

## spec 偏差（认真轨）

- **越界**：无。T22 反转了 T19"静默降级"的**任务级决定**（用户 2026-09-15 拍板"给用户可见提示"），已记 `decisions.md` T22 行；T23 是用户拍板"接受"；T24 只做验收与记录。关键三行未变（三应用边界、鉴权信道、凭据不落库均未动）。
- **缺口**：T17 浏览器验收、R5 五条 P3、上线四项、push/tag 仍未处理，均已在 `plan.md` / `decisions.md` / 本快照留痕。

## 线上状态

- 线上仍是存量版本 https://www.lincode.online；无 `/assistant` 入口，`ai/` 引擎、MCP、AI 打标均未部署。本轮**没有任何线上操作**，也没有新增 `[线上]` 反馈任务。

## 环境备注

- 分支 `main`；工作区干净；ahead **185**（提交本快照后 186）；无 tag。
- **本机服务已全部停掉并核实端口释放**：8123 / 8124 / 8125 / 8127 / 5173 均已释放，无 `my-ai-agent` / `yun-picture-base` java 进程残留。MySQL **3306**、Redis **6379** 常驻。
- 证据目录：`%TEMP%\t23\acceptance.txt`（引擎真机：notice 首帧 / 角色装配 / 冷却 / 计数）、`%TEMP%\t24\`（后端启动与打标验证日志）。
- **没有留下探针数据**：`acc_ai_tag_admin` 这个账号当时**并未真正注册成功**（我的脚本把"没抛异常"当成成功，实为业务错误），库里查无此账号；打标验收用的候选图由用户手点，AI 那次尝试前后态一致、零写库。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**。
2. 细节：T21/T22/T23/T24 看 `docs/plan.md` 对应条目与 `docs/decisions.md` 同日各行；协议看 `docs/features/F13-对话循环与SSE事件协议.md`（含 `notice` 帧）；打标看 `F14-AI打标.md`。
3. 下一件事建议：**先处理部署前必办 D1**（见下节"会话 2 追加"），再 push/tag。

---

# 会话 2 追加（同日第二个会话：独立复核 + 流程损耗治理）

> 按 AGENTS.md 规则 6 的新口径**并入当天同一份**，且**只写增量**（不复述"当前状态/刚完成"）。上文第 3 条的"先补 T17 人工验收、再做 R5 五条 P3"已被本节取代。

## 新产生的决定（细节见 `docs/decisions.md` 2026-09-15 两行 + `docs/decisions/2026-09-15-process-overhead.md`）

1. **一次独立复核**（规则 13）跑了 AI 助手一期全过程，结论：过程扎实、记录诚实（我实跑的门禁数字 **166 / 74 / 124 全部与文档一致**），但发现**一处被低估的危害**与若干流程损耗。
2. **流程损耗治理（用户拍板"按你的建议来做"）**：新增「文档归属（单一事实源）」；规则 3 字据收窄到四类；规则 13 review 输出只剩"现在修 / 判为已知限制并关闭"、**取消挂账**；规则 6 handoff 一天一份只写增量；规则 7 收口记录并入 handoff；新增规则 16 证据落定、规则 17 文档一致性门禁。
3. **实操**：`plan.md` 114 KB → **26 KB**、`decisions.md` 91 KB → **32 KB**（常读的两份合计 205 KB → 58 KB，**−71%**），实施记录与理由外移到 `docs/plans/records/`（25 个文件）与 `docs/decisions/`（33 个文件），**逐字无损**（可复核方式写在详情文件里）。提交 `3008ca7`、`a4b6fa8`。
4. 复核顺带订正 AGENTS.md 两处事实错误：`backend/docs/deploy` **不在版本控制内**；前端门禁口径改为"改动不得净增"+ 基线文件（`frontend/type-check-baseline.txt`，实测 124 例）。
5. 门禁做了**负向控制**：注入"指向不存在的路径"与"悬空指针"各一处 → 两项都被抓红；恢复后转绿。基线分支也实测会真跑 `vue-tsc`（不是静默通过）。

## 未决（取代上文"进行中/下一步"）

**第一优先 = D1（复核升级为硬阻断）**：引擎旧端点 `/ai/manus/chat` **零鉴权**，而它挂的 `FileOperationTool` 对 `fileName` 不做路径清洗、`message`（=提示词）由调用者控制 ⇒ **未授权的任意文件读写**（受进程权限约束）+ 两个抓取类工具的 SSRF 面。删除 `TerminalOperationTool` 不足以覆盖。**部署前必须**：给它加鉴权，或只监听回环。落地方式需用户定（它服务的是冻结的 MyManus 前端，浏览器发不了自定义头，所以不能直接复用 headless 的 key 方案）。
其余部署前置 D2–D5（引擎 CORS 收紧、nginx SSE 配置入库、prod 配置清单、F11 勾选时机）与"待人工验收"两项，全部登记在 `plan.md` 的**「未决与非任务事项」**新节。
**R5 那五条 P3 已按新契约处置**：两条（错误体原文进气泡、取票不判 `data.code`）提升为 P2 待开任务；其余**关闭为已知限制**，不再挂账。

## 环境残留 / 口径

- 分支 `main`；工作区**干净**；ahead **189**（本快照提交后 190）；**仍无 tag、仍未 push**（`origin/main` 本地引用停留在 2026-09-09）。
- 本机服务未启动（复核前状态即如此，本次全部只读 + 文档改动）；`ai/agent` 跑全模块测试会生成 `ai/agent/tmp/`（该目录**未进 .gitignore**），已清除。
- 本次**未改任何业务代码**：两笔提交都是 `docs:`，含 `.githooks` 与前端基线文件。
- **遗留待用户决定**：`docs/spec.md` 的"关键三行"仍是 10 KB 的三条超长行（含被 features/decisions 重复承载的档位推进史）；按规则 3 关键三行是用户合同，缩短未做。

