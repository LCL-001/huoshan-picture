# Handoff 0024 — 2026-09-15 — T17 角色裁剪 + T18 失败时限收口（附两条清单勘误）

> 上一份：`0023`（T8-review 收口 + AI 打标 T15/T16）。**本文件只记 0023 之后的增量**——档 3 全貌、真机命令、更早的坑在 0020/0021/0022/0023，不重复。
> 本会话 7 笔提交：`0616c2b` → `84c9bd1` → `25792a4` → `559597e` → `de134fc` → `34a0181` → `ab6ba2d`（会话起点 HEAD `fde4011`，ahead 158 → **165**）。

## 当前状态（一句话）

三件事：① **T17 落地**（助手侧按角色裁剪看图打标工具，用户拍板的那半边 + 我判定"不复用 T15 接口"并记了理由）；② **两条陈旧清单修掉**（plan 的 T7 漏勾、代理集成测试 javadoc 的 `getAndDelete` 残留）；③ **T18 失败时限收口**（重试改为有界 45s/≤4 次、看图按 T15"层次一"对齐），并**真机复跑验证**：黑洞 provider 下 3.95s 收到 `error` 帧 + `[DONE]`。工作区干净；**四个端口全空**（本会话起的引擎已停）。

## 刚完成（按时间）

1. **T17 助手侧按角色裁剪**（`0616c2b` 代码 + `84c9bd1` docs）：普通用户的助手不再挂 `visionTagger`。新增**角色信道**——backend 代理把 `loginUser.getUserRole()`（空值按 `UserConstant.DEFAULT_ROLE`）放进 `X-User-Role` 头（角色为空**不发**该头），引擎 `HuoshanToolFactory.assistantTools(client, visionModel, canTagPictures, extraTools...)` 只在"配了视觉模型 **且** 管理员"时挂载；判定 `HuoshanAssistantSession.isAdmin` **fail-closed**（缺失/空/未知一律普通用户），角色原文不进日志。**验收**：装配断言 + 身份判定 2 例 + 代理头 2 例 + 控制器角色 2 例 + 集成测试 `AiAssistantProxyIntegrationTest` **8/8**（真登录后桩引擎收到 `X-User-Role: user`）。**负向控制实测**：摘工厂开关 → 装配那条红；`isAdmin` 改恒真 → 判定两条红。**"是否复用 T15 的接口"我判定为不复用**（助手侧服务空间图片、T15 端点服务公共图库管理页，覆盖面不同；新增工具类超出条目文件范围）——**这一半是 AI 判定、用户未单独拍板**，理由与代价在 `decisions.md` 同日行。
2. **两条清单勘误**（用户点名要修）：① `docs/plan.md` 的 **T7 一直是 `[ ]` 但早已完成**（子条目自己写着"T7 全部完成"）——补勾 + 加勘误，并把"以勾选状态为准"这条教训补上"**勾选本身也可能漏**"（`559597e`）；② `AiAssistantProxyIntegrationTest:285` 的 javadoc 仍写 `getAndDelete`，实现是 Lua `GET+DEL`（R5 review 的 P3 ④）——订正为 Lua 口径并写明原因（`GETDEL` 在 Redis 5.0 不存在）（`25792a4`）。
3. **T18 失败时限收口**（字据 `de134fc` → 代码 `34a0181` → 记录 `ab6ba2d`）：用户拍板参数后落地两件事。① **重试有界**：`OpenAiChatModels.boundedRetryTemplate()`（`TimeoutRetryPolicy(45s)` + `SimpleRetryPolicy(4)` 经 `CompositeRetryPolicy(optimistic=false)` 组合 + 退避 500ms→4s 封顶），`.retryTemplate(...)` 接到主脑与看图两个模型——默认模板是 10 次 + 退避到 180s（**约 19 分钟**），远超 emitter 的 300s。② **看图层次一**：`VisionTaggerTool` 改**按波提交**（波宽=并发 4）到每请求独立有界池 + 单张 45s 墙钟预算（超时只降级该张）+ 结果按输入顺序；**提前停止粒度由逐张变逐波**（并发下同波几张已在飞），note 仍报"剩余 N 张未尝试"。**门禁**：ai/agent 140 → **147 例**、backend 74 例不变。**负向控制实测**：摘 `.retryTemplate(...)` → "两个模型都挂上"红；并发度改 1 → 峰值 1≠4、按波跳过 7≠4 两条红。**真机**：黑洞 `http://127.0.0.1:9` 下 **3.95s** 收到 `data:{"content":"助手处理失败，请稍后重试","event":"error"}` + `data:[DONE]`。
4. **R1 事实订正（只改记录，无行为改动）**：R1 的机制（`JdkClientHttpResponse.adaptBody` 的 `Flux.cache(0)` 导致取消不断上游）**只在 WebClient 流式路径上生效**，而引擎**没有任何流式模型调用**（全仓 main 无 `ChatClient.stream()`；主循环 `ToolCallAgent.callLlm` 与看图都是 `.call()` 阻塞），`RetryTemplate.execute` 在 `OpenAiChatModel` 里只出现在 `internalCall` 的 lambda（1.1.2/1.1.8 都核过）⇒ **当前不可达**，当年"按实测再定"可结案；不换 connector。

## 进行中 / 下一步

1. **MCP 不可达时整条对话失败（本会话新发现，未拍板收口，建议优先）**：不关 MCP（默认开）而 8127 没起时，一次对话在**装配工具**那步就抛——栈顶 `McpSyncClient.listTools` ← `HuoshanAssistantController.chat:107`，实测 **20.1s** 后回 HTTP `50000`（JSON、**无 SSE 流**）。与 T9 口径不符（T9 只说"搜图工具会失败"）。**本地起引擎请带 `--spring.ai.mcp.client.enabled=false`**。收口方向已记两条（`getToolCallbacks()` 失败即空数组 + warn；或先健康探测再挂），**需先立字据**。
2. **T17 的剩余口径（用户未拍板）**：管理员侧工具是否改调 T15 接口（现为"不复用"）；提示词未按角色补话术（普通用户问"看图打标签"只能按工具清单自行判断）；两种角色的人工会话验收未做（引擎无控制器单测，"读头→开关"那一行只有代码阅读 + 装配/判定单测覆盖）。
3. **review 的 3 条 P3**（plan.md 第 105 行）：`runLoop` 上限提示未判 `state`（附一例 `maxSteps=1 + finishOnStep` 契约测试即先红后绿）；`AgentEvent.Error` javadoc 措辞；计划 §5 与实现对齐 + 实例只跑一次的不变量提到 `BaseAgent`。
4. **R1 的埋点未做**（超时/取消/重试次数计数）——它是"是否真需要换 connector"的数据来源。
5. **引擎 emitter 仍未动（本轮未拍板）**：引擎 300s 与代理 300s"赛跑"未消；但慢失败现已被压到 ≈60s（主脑）/45s（看图），余量足够。
6. 沿用 0023 的挂账：AI 打标的现场验收最后一步（"点一次出建议"未见证）、R5 的几条 P3（前端取票不判业务码 / `POST /ticket` 无频控 / 无票被拒不记 userId / 集成用例少一条断言）、前端社交残留（未拍板）、前端 `type-check` 138 / `eslint` 73 存量红、上线必办四项（nginx SSE 块 / 旧端点 `/ai/manus/chat` 未鉴权 / 双端密钥一致 / MCP 与引擎同机）、探针清理（0020 记的 t12 空间 9 张图 + `t8-*` 记忆行）、push/tag。

## 未决问题

- **push / tag**：`main` 领先 `origin/main` **165 笔**未 push（本会话新增 7 笔）；仓库**仍无任何 tag**（档 3 + AI 打标 + T17/T18 都已收口，是打第一个 tag 的时机，用户未决）。
- **`docs/decisions.md` 有一行历史遗留多一个 `|`**（本会话核对表格结构时发现，非本次引入，渲染会多出一列）——未动，属可穿插的一行级清理。
- **spec 的 F11 仍未勾选**：一期任务 T4–T18 本轮全部收口，是否勾选（或是否等到上线）待用户确认。

## spec 偏差（认真轨）

- **越界**：**无**。T17 是用户拍板范围内的落地（有一处**已记录的执行偏差**：角色头常量与 `isAdmin` 落在 `HuoshanAssistantSession`——为的是让 fail-closed 判定有单测，放控制器里就只剩 Spring 上下文可测）；T17 的另一半（不复用 T15 接口）**已声明为 AI 判定**并记了理由；T18 是新提需求（用户给了参数）已按规则 3 立字据（plan T18 + decisions）。
- **缺口**：T17 的三条剩余（上方"下一步"2）、MCP 收口（1）、R1 埋点（4）、上位评审的 3 条 P3（3）。
- **关键三行检视**：**成立**——引擎仍是独立 Maven 应用/独立进程（8124）；headless 端点仍只认服务间 API key（本轮只多了一个跟随同一信道透传的角色头，不改鉴权）；凭据与密钥仍只在内存或**不入库**的本地配置里。本轮没有新增任何凭据通道。
- **过程教训**：**"以勾选状态为准"要再加半句——勾选本身也可能漏**（T7 就是例证，与 0022 把已完成的 T13 列成待办是同一类错的两个方向）。已写进 plan.md 的 T7 勘误。

## 线上状态

- 仍是存量版本（同 0020–0023）：线上无 `/assistant` 入口，`ai/` 引擎与 MCP 均未部署，**AI 打标也未上线**。本会话无线上操作，plan.md 无 `[线上]` 任务。

## 环境备注

- 分支 `main`；**工作区干净**；ahead `origin/main` **165 笔**；**无 tag**。
- **本机服务**：MySQL 3306 / Redis 6379 常驻；**8123 / 8124 / 8127 / 5173 全空**（本会话起的引擎已停，交接前实测）。
- **本会话踩的坑（新）**：
  1. **MCP 不可达 ⇒ 整条对话失败**（见"下一步"1），本地起引擎带 `--spring.ai.mcp.client.enabled=false`。
  2. **Maven 本地库不在 `~/.m2`**：这台机器是 `D:\develop\Maven\apache-maven-3.9.11\maven_repo`（`mvn help:evaluate -Dexpression=settings.localRepository -q -DforceStdout` 实测）。`~/.m2` 里躺着一份 **1.1.8** 的 spring-ai jar，照着它 javap 会得出**错的 API 结论**（本会话差点按 1.1.8 开方子）。**生效版本是 spring-ai-openai 1.1.2**（`dependency:tree`），`OpenAiChatModel.Builder.retryTemplate(...)` 在 1.1.2 上存在。
  3. **引擎没有任何流式模型调用**（全仓 main 无 `ChatClient.stream()`）——谈"流式超时/取消"前先记住这条，否则会像 T6.1/R1 那样保护到一条没有调用方的路径。
  4. **Windows 起后台 java 要日志**：`start /b cmd /c "java ... > log"` 在这个 shell 里报"不支持输入重新定向"（且可能已起进程却拿不到日志，本会话因此白跑一轮）；用 PowerShell `Start-Process -RedirectStandardOutput/-RedirectStandardError` 才拿得到。`timeout /t N /nobreak > nul` 同样不支持该重定向。
  5. **`findstr` 打印 UTF-8 中文文档会乱码**（控制台 GBK）——读文档用 Read 工具或 `Get-Content -Encoding utf8`。
  6. **提交信息含中文**：写 UTF-8 文件后 `git commit -F <file>`（命令行 `-m` 会按控制台 GBK 落库）。
  7. 沿用：`mvn -Dtest=!A,!B` 在 cmd 里**不要**带单引号；`npm run openapi` **不要跑**（见 `decisions.md`）；改 `application-local.yaml` 必须**停服务后重打包**（jar 里那份才是运行时读的）。
- **T18 真机验证的复现方式**（下次想复跑照这个走）：起引擎带 `--app.ai.openai.assistant.base-url=http://127.0.0.1:9 --app.huoshan.headless-api-key=smoke-key [--spring.ai.mcp.client.enabled=false]`，然后
  `curl -N -H "X-Internal-Api-Key: smoke-key" -H "satoken: dummy" -H "Cookie: SESSION=dummy" "http://127.0.0.1:8124/api/ai/huoshan/chat?message=hi&userId=1"`
  → 期望 **3.95s 内**收到 `event=error` 帧 + `[DONE]`（headless 端点只校验 key 非空、不校验凭据真伪，故无需登录）。
- **探针与产物**：`%TEMP%\t18-mcp-on.log`（MCP 失败的完整栈）、`%TEMP%\t18-engine.log`、`%TEMP%\t18-pkg.log`、`%TEMP%\t18-*.txt`；引擎 jar 已按 T18 代码重新打包（`ai/agent/target/my-ai-agent-0.0.1-SNAPSHOT.jar`）。沿用 0020 的 `%TEMP%\t12\jar.txt`（**含凭据的 cookie 罐，用完请删**）与 `%TEMP%\t8\*`。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**。T17 细节看 `docs/features/F12`（工具集/角色裁剪）与 `F11`（已知限制 13）；T18 细节看 `F13` 已知限制 1 与 `F12` 已知限制 3/10；AI 打标看 `F14`。
2. 口径全文：`docs/decisions.md` **2026-09-15 各行**（本轮新增 T18 / R1 事实订正 / MCP 挂账三条）。
3. **下一件事建议**：MCP 不可达的降级（新发现、本地验收反复踩）→ 再 review 的 3 条 P3（可穿插）→ 再按用户原话办：T17 剩余口径、R1 埋点、前端社交残留、探针清理、push/tag。
