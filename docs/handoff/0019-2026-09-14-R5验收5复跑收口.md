# Handoff 0019 — R5 验收 ⑤ 复跑收口（2026-09-14）

> 上一份：`0018`（R5 独立 review 完成 + 两处来源白名单收口）。
> 本文件是本会话的快照：按用户一句"复跑一次"**把那条唯一的 P2（验收 ⑤ 口径）实跑收口**。
> **本会话零代码改动**，产出只有 docs（`3ed170a` + 本快照）。

## 当前状态（一句话）

**R5 review 那条唯一的 P2①（浏览器跨站导航探针未复跑）已按用户拍板"补跑"实跑通过——R5 review 结论由"有条件通过"转为"通过"**：原探针 `csrf-page.js` **原样**跑出"跨站顶层导航带齐两把 Cookie → 被判 40300 且桩引擎零请求"，并补了一个**正向对照**（同一跨站形态、只多一张票 → 完整 SSE 流 + 桩引擎恰好 1 次请求）把"40300 不是登录门槛造成的假通过"钉死；顺带把原探针第 1 步（no-cors 子资源请求，响应按设计读不到）换成可观测版本，实测"跨站子资源请求带不上 Cookie（回 40100）"。**下一会话可以直接进档 3**（R5 已完整收口，review 6 条 P3 仍挂着、可穿插）。

## 刚完成（本会话，按时间）

1. **接手（只读）**：AGENTS → spec 关键三行 → handoff 0018 → `git log`/`git status`（工作区干净、HEAD `1825f25`）。摸清探针机制后才动手：`csrf-page.js` 由 `127.0.0.1:8135` 提供页面（与 `localhost` 不同站），先发一个 no-cors 带凭据的子资源请求、2.5 秒后顶层导航到对话端点；`application.yaml` 的 sa-token 是 `http-only: true` + `same-site: lax`，所以顶层导航**会**带上两把凭据（探针前提成立）。

2. **先发现一个会毁掉结论的前提**：`backend/target/*.jar` 是当日 **19:43** 构建的，而 R5 代码提交 `af2995f` 是 **19:50**——**旧 jar 里根本没有票闸门**，拿它复跑会得到"跨站导航跑通 agent"的相反结论。遂 `mvn -B -o package -DskipTests` **按 HEAD 重新打包**（本会话第一个动作，也是这次复跑最关键的一步）。

3. **起服务与造凭据**：`node stub-sse.js 8130`（桩引擎）+ `java -jar`（8131，profile `local,test`，`AI_ENGINE_INTERNAL_API_KEY=smoke-internal-key`、`AI_ENGINE_BASE_URL=http://127.0.0.1:8130/api`、`--management.server.port=8141` 避开 8125）+ `node csrf-page.js`（8135）。`local,test` 关掉了验证码校验，故注册/登录可直接走 API：`POST /api/user/register` 建探针账号 `csrfrerun01`（id `2099486502479810561`）。**浏览器侧不用手工注入 Cookie**——让应用内 Chromium 在 `localhost:8131` 上真登录（同源 `fetch` 打 `/api/user/login`，Cookie 由服务端写出），再用 `/api/user/get/login` 回读 `code=0` 确认登录态成立。

4. **主探针（验收 ⑤ 本体）**：浏览器访问 `http://127.0.0.1:8135/` → 顶层导航落到 `http://localhost:8131/api/ai/assistant/chat?message=s3&chatId=csrf-nav-1` → 响应 `{"code":40300,"data":null,"message":"缺少或已失效的一次性凭据，请刷新页面后重试"}`；**桩引擎零 REQ**、代理日志**零**条"AI 助手对话开始"。40300 是门槛四、代码上排在三个登录门槛之后 ⇒ 这次导航**确实带齐并带对了** satoken 与 SESSION（不是被登录门槛挡下的假通过）。

5. **正向对照（本会话补的，价值高于预期）**：新增 `%TEMP%\t11-rerun\csrf-control-ticketed.js`（同形态页面，导航 URL 里带票；票在同源页面 `POST /api/ai/assistant/ticket` 取得）→ 同一条顶层导航拿到完整 SSE 事件流（`complete-frame` + 合成中断帧 + `[DONE]`），**桩引擎恰好 1 次**请求（`path=/api/ai/huoshan/chat`、`chatId="csrf-nav-ticketed"`、`userId=2099486502479810561`、密钥、satoken 与 `SESSION` 全对），代理"AI 助手对话开始"也只 1 条。两次运行**唯一差别就是票** ⇒ ① 的结论可归因到票本身，且**实测证实"Lax 会在跨站顶层导航上发出 Cookie"这个前提**（review 当时只能推理）。

6. **顺带补上原探针第 1 步的可观测版本**：原页面的子资源请求用 `mode:"no-cors"`，响应按设计读不到，等于**这半步从来没被观测过**。新增 `csrf-cors-read.js`（页面 `127.0.0.1:8137`，改 cors 模式带凭据）→ `POST /ai/assistant/ticket` 与 `GET /user/get/login` **都回 `{"code":40100,"message":"未登录"}`** ⇒ 跨站子资源请求**带不上 Cookie**，跨站页面因此**取不到票**。本地能读到这个 40100 是因为 dev 白名单含 `http://127.0.0.1:*`；生产白名单只剩 www，连读都读不到（与 review 的"承重屏障是 CORS 读限制"一致）。

7. **证据与留档（只改 docs，`3ed170a`）**：证据汇总 `%TEMP%\t11-rerun\accept5-rundown.txt`（含三次观测的原始报文、桩引擎计数、代理侧 5 条 BusinessException 的对应关系）。文档三处同步：`docs/decisions.md` 追加一行（P2 收口 + 边界）、`docs/plan.md`（实施记录的"验收 ⑤ 说明"补记 + review 记录"条件解除" + 原 P2 挂账行改写为"已收口"）、`docs/features/F11-AI助手MVP.md`（头部追加一行 + 「怎么验证」新增"跨站导航探针"条 + 「已知限制」7 的 ⑥ 补实测口径）。

8. **清理（按惯例）**：停掉全部临时服务（8130/8131/8135/8136/8137；8136 那个 node 是孤儿进程，按 PID `taskkill` 掉）、关闭探针标签页；删除**含凭据**的文件（cookie 罐 `jar.txt`、`stub.log`、`proxy.log`、`ticketed-page.log`）；删除探针账号行（`DELETE FROM user WHERE userAccount='csrfrerun01'` → `remaining=0`，因 `getLoginUser` 每轮回库 `getById`，账号一删所有残留会话即失效）；保留 `csrf-page.js` **原样未改**，仓库外新增两个无凭据脚本。

## 进行中 / 下一步

1. **档 3 完整一期**：visionTagger 看图打标、`batchEditPictures` / `batchUploadByUrl`、T9 MCP 搜图、T8-hard 循环合并、T12 两条业务场景联调、T13 清理。R5 已无任何条件地收口，可直接开写类工具。
2. **review 的 6 条 P3**（可穿插，见 plan.md"review 新增挂账（P3）"）：③ 前端取票判 `data.code`、④ `getAndDelete` / javadoc 订正（**必须改代码**，按约定留给实现任务）、⑤ 拒绝路径可观测性、⑥ "票不转发给引擎"补 1 行断言、⑦ `/ticket` 频控与长度校验。
3. **上线前必办与引擎部署前必办**：同 0016 第 3 条（nginx server 块 / SSE 配置、引擎旧端点 `/ai/manus/chat` 未鉴权、双端密钥一致、打包启动方式）。另：**`application-prod.yaml` 的 apex 删除与 WS 来源改动都要靠重新打包才生效**——本会话已按 HEAD 打包过一次 `backend/target` 里的 jar，但生产部署要自己重打。
4. **apex 那条的尾巴**：定唯一主机名（www）+ 另一个 301（nginx，不在仓库）。

## 未决问题

- 沿用 0018 的全部挂账（未消解）：P3 六条（同上）、引擎侧"超时转显式错误事件 + 埋点"、R1（引擎侧取消不关上游）、"用户提问原文进 nginx access log"（要根治得改 POST + fetch 流式，票取代不了它）、前端门禁存量 138/73、门禁余量、T6 遗留 P3 ①②⑤⑦。
- **多实例部署未验**（票在 Redis 共享，理论上跨实例成立，只有单实例实测）；**浏览器兼容未验**（本会话仍只在 Chromium 系，且只有应用内 Chromium 一个实例）；`EventSource` 自动重连在真实 403 下的行为未验。
- **push / tag**：`main` 领先 `origin/main` 未 push（本会话又加 2 笔）；仓库**无任何 tag**（用户未决，同 0016/0018）。

## spec 偏差（认真轨）

- **越界（做了 spec/plan 没写的）**：**无**。复跑用的是 plan.md 验收 ⑤ 立项时就指定的探针资产；两个新脚本是**仓库外**证据工具（`%TEMP%\t11-rerun\`），不是产品代码。`csrf-page.js` **原样未改**（改写一个已 review 的探针会让复跑失去意义）。
- **缺口（spec/plan 承诺但没做的）**：**无**。本会话唯一的产出就是补上 plan.md R5 验收 ⑤ 那个缺口——补完即无缺口；review 的 P2 随之消解。
- **关键三行检视**：**成立**——`git diff` 全程只有 docs（3 个文件，8 增 4 删），`ai/` 与 backend 代码零改动；"引擎端点只接受服务间 API key、不注册登录体系"未变；"凭据只在请求生命周期内存流转、不落库、不进日志"未变——本会话产生的凭据（探针账号的 satoken / 会话 Cookie）只活在浏览器与 curl 的临时文件里，**含凭据的文件已删除**，账号行已从库里删掉。

## 线上状态（已上线项目才填）

- 当前线上版本：https://www.lincode.online 仍是 backend / frontend **存量版本**；档 2 全部改动（含 R4/R6/R5）未上线（线上没有 `/assistant` 入口，R5 这个洞在线上**不可达**），`ai/` 引擎仍未部署。本会话无线上操作。
- 未处理反馈（[线上] 任务）：**无**。

## 环境备注

- 分支 `main`；**工作区干净**；**本会话共 2 笔提交**：`3ed170a` docs（R5 验收 ⑤ 复跑收口）→ 本快照（docs）。ahead `origin/main` 未 push（用户未决）；**无 tag**。
- **本会话实跑命令（可原样复现；均在仓库根 cmd）**：
  - 重新打包（**必须先做**，否则旧 jar 无票闸门）：`cd backend && mvn -B -o package -DskipTests`。
  - 桩引擎：`cd %TEMP%\t11-review && node stub-sse.js 8130 > <log> 2>&1`。
  - 代理：`set "SPRING_PROFILES_ACTIVE=local,test" && set "AI_ENGINE_INTERNAL_API_KEY=smoke-internal-key" && set "AI_ENGINE_BASE_URL=http://127.0.0.1:8130/api" && java -jar backend\target\yun-picture-base-0.0.1-SNAPSHOT.jar --server.port=8131 --management.server.port=8141`（**management 端口也要改**，否则 8125 撞车；`set "VAR=value"` 的引号不能省，见 0016 坑 1）。
  - 注册探针账号（`local,test` 下免验证码）：`curl -s -X POST http://localhost:8131/api/user/register -H "Content-Type: application/json" -d "{\"userAccount\":\"csrfrerun01\",\"userPassword\":\"password123\",\"checkPassword\":\"password123\",\"email\":\"csrfrerun01@example.com\"}"` → `{"code":0,...}`。
  - 删账号：`set "MYSQL_PWD=123456" && "<mysql>" -u root yu_picture -e "DELETE FROM user WHERE userAccount='csrfrerun01';"`。
  - 探针页面：`node csrf-page.js`（8135）、`node csrf-control-ticketed.js 8136 <ticket>`、`node csrf-cors-read.js 8137`。
  - 浏览器侧：应用内 Chromium（`agent.browsers.getForUrl("http://localhost:8131/api/user/get/login")`），登录/取票用 `playwright.evaluate` 里的同源 `fetch`；跨站页面用 `tab.goto("http://127.0.0.1:8135/")` + `waitForTimeout(5000)`，读 `tab.url()` 与 `document.body.innerText`。
- **本会话新踩的坑**：① **`timeout /t N >nul` 与 `start /b cmd /c "... > log"` 在这个 shell 里都会报"不支持输入重新定向"**——要后台跑服务就直接用工具的后台执行，别套 `start`/`timeout`。② 顶层导航会**把标签页带走**：探针页导航后那个 tab 的 URL 已经变成对话端点，再按 `127.0.0.1:8135` 找 tab 会找不到——按当前 URL 重新绑定（或直接用 `tab.url()` 读结果）。③ 桩引擎/代理日志里含**真凭据**（stub 会打印 `satoken=` 与 `cookie=`），用完必须删；我汇总证据时对这两项做了 redact。
- **本机服务状态**：MySQL 3306 / Redis 6379 常驻在跑；**其余端口本会话起过的 8130/8131/8135/8136/8137/8141 全部已停**（`netstat` 复核无监听）。应用内浏览器本会话新建的标签页已关闭，当前无受控标签页、无用户标签页。
- **仓库外新资产（无凭据，可复用）**：`%TEMP%\t11-rerun\` —— `accept5-rundown.txt`（证据汇总）、`csrf-control-ticketed.js`（正向对照页，用法 `node <脚本> <port> <ticket>`）、`csrf-cors-read.js`（跨站可读性探针）、`csrfpage.log` / `corsread.log`（只有页面命中行）。**含凭据的文件（`jar.txt` / `stub.log` / `proxy.log` / `ticketed-page.log`）已删**。
- **探针残留**：探针账号行已删（`remaining=0`）；Redis 里可能残留该账号的 satoken / Spring Session 键（TTL 到期自灭；且 `getLoginUser` 回库 `getById`，账号已删即失效）。**票键**（`huoshantuku:ai:assistant:ticket:*`）60 秒自灭。
- **版本锚点**：backend SB 2.7.6 / Java 17 / sa-token 1.39.0；引擎 SB 3.5.13 / Java 21；后端 context-path `/api`、actuator 8125；**本地 Redis 5.0.14.1**；`backend/target` 里的 jar 现为**本会话按 HEAD 重新打包**的版本（含 R5 + WS 来源收口）。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**（要看 review 全文读 0018 + `docs/decisions.md` 的"R5 收口独立 review"行，要实施细节读 0016）。
2. **R5 已完整收口（含此前那条 P2），review 结论 = 通过**；没有任何东西在等用户拍板才能开工——可直接进档 3。挂账清单在 `docs/plan.md` 的"review 新增挂账（P3）"行。
3. 若还想复现这次复跑：**先重新打包**（旧 jar 无票闸门），再照"环境备注"的命令起 8130/8131/8135，浏览器真登录后访问 `http://127.0.0.1:8135/`；对照脚本与证据格式见 `%TEMP%\t11-rerun\accept5-rundown.txt`。
4. 坑与门禁口径看 0016「环境备注」（`set "VAR=value"` 别留尾随空格、cookie jar 的 `#HttpOnly_`、Redis 5.x 三条）与本快照「环境备注」（`start`/`timeout` 的重定向坑、导航会带走标签页、桩引擎日志含凭据）。
