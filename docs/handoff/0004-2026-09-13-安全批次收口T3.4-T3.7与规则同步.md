# Handoff 0004 — 安全修复批次收口 T3.4-T3.7 与规则同步（2026-09-13）

## 当前状态（一句话）

2026-09-12 功能审查产出的 T3 批次 7 项全部完成（plan.md 全勾、无进行中任务），工作区干净，工作规则已同步至 vibe 最新 12 条。

## 刚完成

- **T3.4 重复上传额度虚增**（ad4dada）：`uploadPicture` 更新分支改为 `GREATEST(totalSize - oldSize, 0) + newSize`、条数不变；事务提交成功才 `cleanupPictureFile` 清旧 COS 对象（新旧 URL 相同不清理）。**顺带修正**：`cleanupPictureFile` 引用计数阈值 `count > 1` → `count > 0`（调用点查询时记录本身已删/已改指向，剩 1 条引用也不能删共享文件）。测试：PictureReplaceQuotaIntegrationTest 5 用例（净差值/缩图回落/超限回滚/共享URL跳过清理/个人图清理）
- **T3.5 删空间/删用户级联**（8c702c1）：`deleteSpace` 重构为单事务（空间行 + 成员记录 + 空间全部图片逻辑删），事务提交后按 URL 引用计数异步清 COS；新增 `ISpaceService.deleteUserCascade`（接口文件超出原定范围已在 plan 注明），`UserController.deleteUser` 先级联名下空间、移除他人空间成员关系、最后删号踢会话；**口径**：用户上传到他人团队的图片保留（团队内容不随账号消失）。测试：SpaceDeleteCascadeIntegrationTest 2 用例
- **T3.6 ShardingSphere**（d000360，用户拍板"不删不启、注释停用"）：排查发现其本就双重休眠（主类 `exclude = {ShardingSphereAutoConfiguration.class}` + DynamicShardingManager 的 @Component 已注释），yaml 分表块无人读取——仅注释 application.yaml 死配置块（附恢复前置：库名不一致/空 range 静默丢数据/动态建表回退三个坑）；pom 依赖与主类 exclude 保留作为停用护栏。application-local/prod.yaml（gitignore 不入库）已同步注释，**线上服务器的 prod yaml 可选同步（不改也无功能影响）**
- **T3.7 社交通知口径**（dde6916，用户拍板"保留通知并改 spec"）：前端核实 GlobalHeader.vue 铃铛正在调用 /notification/* 五接口，SocialController 本体是活的（仅 /timeline 被注释）——spec F7 改"部分停用：通知保留"，AGENTS.md 停用模块注记修正（旧记录"Social 不被 Spring 加载"不实），decisions.md 记入 T3.6/T3.7 两条口径
- **工作规则同步**（6023df4）：AGENTS.md 工作规则从 7 条同步至 vibe 最新 12 条（新增方向转向/功能讲解/编码纪律/命令行纪律/回归测试），规则 7 豁免清单与 pre-commit 脚本对齐；补建 docs/features/（.gitkeep）
- 测试基线：全量 54 绿（13 门禁单测 + 41 集成）；T3.4/T3.5 的测试通过 @MockBean CosManager + 真实 FilePictureUpload 模板绕开 COS（文件头魔数用真 JPEG 前缀、字节数即 picSize），未改 pom、无 mock-maker 配置

## 进行中 / 下一步

- 无进行中任务，plan.md 任务清单全部勾完。接手后等用户指派方向，候选（均需用户点名，未登记 plan）：
  - 低危清单（handoff 0002 的 2026-09-12 审查报告）：IP 限流头伪造（nginx 未强制覆写 X-Real-IP）、picture 索引与 `LIKE '%kw%'` 不匹配、admin 接口返回密码哈希、字段长度口径（url 1024 vs 列 512）、协同编辑双页签互踢、前端 WS 无重连
  - T3.4/T3.5 过程中顺手发现的存量问题（未修）：① `uploadPicture` 开头的空间额度预检（约 :226-231）对"替换图片"场景仍按新增逻辑拦截——空间达上限时替换会被误拒（替换不增条数）；② `getPicture` 更新分支无条件 `setUserId(loginUser)`——admin 替换他人图片会把归属改成 admin

## 未决问题

- 无阻塞决策；T3.6/T3.7 已于 2026-09-13 拍板并记入 decisions.md（防翻案：ShardingSphere 注释停用不删不启；通知接口保留）。Spring Session 改密后不失效为已接受残余（T3.3 时评估，量级低）
- 规则 9（功能讲解）生效后：存量功能不批量补写 docs/features/（vibe-onboard 明确约定），新完成的功能逐篇写，用户点名存量功能时单独补写那一篇

## 环境备注

- 分支：main，未提交改动：无；本会话产出 ad4dada…6023df4 共 9 笔（4 fix/chore + 5 docs）
- 本机环境：Maven 3.9.11 + JDK 21（编译目标 17），本地 MySQL 8（3306）与 Redis（6379）可用；测试口径与 CI 一致：`mvn -B test -Dtest='!*IntegrationTest,!RedisStringTest,!YunPictureBaseApplicationTests'`，全量含集成在本机可跑
- 集成测试夹具坑（0002/0003 仍有效）：① `StpKit.SPACE.login()` 后必须补 session 快照；② viewer 正例用团队空间；③ 绕验证码种子 Redis 键 `huoshantuku:auth:captcha:img:{uuid}`（值 abcd）与 `huoshantuku:auth:captcha:email:{email}`；④ 带 @AuthCheck 的控制器方法可直调（先 bindRequest 再调用）；⑤ mock COS 的新范式见 PictureReplaceQuotaIntegrationTest 类注释
- Windows 工具链坑（规则 10/11 已固化，实操教训）：findstr 对 UTF-8 中文失灵 → 用 PowerShell Select-String；禁止用 PowerShell Set-Content 改含中文的文件（GBK 覆写事故）→ 用工具自带的 Write/Edit；cmd 无 `;` 链接、无 `ls/grep/tail`
- 后端起停命令见 AGENTS.md"命令"节；本会话未启动过后端/前端
