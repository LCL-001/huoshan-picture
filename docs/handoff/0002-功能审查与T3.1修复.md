# Handoff 0002 — 功能审查与 T3.1 修复（2026-09-12）

## 本次做了什么

应"从功能出发深入代码逻辑，找现有功能不合理之处"的要求，做了全项目审查（三个只读子代理分工：用户/空间/权限、图片管线/缓存、协同编辑/分表/配置/前端），**所有重量级结论均经人工读码核验**。随后按认真轨流程完成 T3.1 修复（安全级问题）。

## 审查结论（已核验）

**误报澄清（子代理报过、核验为不成立）：**
- "生产密钥提交进 git"——`application-prod.yaml` 被 backend/.gitignore 忽略、git 全历史无痕、被跟踪的 application.yaml 只有空默认值
- "批量编辑绕过审核"——被 spaceId 必填 + 仅属主校验挡住，仅剩空间内口径不一致

**已修复（T3.1，5768cc9）：空间隔离可被绕过**。根因：`StpInterfaceImpl` 的授权上下文从请求参数/body 嗅探 + 空上下文默认授予 picture:view + spaceUserId 优先级最高，而 `/picture/get/vo` 与两个列表接口恰好只信这套判定。利用方式与完整代码走查见当次会话或 plan.md T3.1。

**待修批次（按优先级登记在 docs/plan.md T3.2-T3.7）：**
- T3.2 `/space/list/page/vo` 匿名枚举全部空间（含他人私有空间的名称/配额/属主）
- T3.3 会话生命周期：改密/删号/降权不踢 Sa-Token 会话（快照 7 天）；getLoginUser 给匿名请求造 7 天 Redis 空会话
- T3.4 重复上传额度重复累计 + 旧 COS 对象不清（`PictureServiceImpl` uploadPicture 事务段）
- T3.5 删空间/删用户无级联、删空间两步操作无事务
- T3.6 ShardingSphere 全链路禁用（主类 exclude + 调用点注释），但 README/配置/依赖都宣称分表；现在启用也跑不通（库名不一致、空 range 静默丢数据）
- T3.7 社交模块停用不彻底：SocialController 的 /notification/* 三接口还活着，与 spec"社交停用"记录冲突

其余低危项（IP 限流头伪造、picture 索引与 LIKE '%kw%' 不匹配、admin 返回密码哈希、字段长度口径、协同编辑双页签互踢/前端 WS 无重连等）见当次会话审查报告，未登记 plan。

## T3.1 修复内容（5768cc9）

1. `PictureController#getPictureVOById`：改为 `checkPictureAuth(loginUser, picture, PICTURE_VIEW)` 目标绑定判定（恢复被注释的显式校验）
2. `PictureServiceImpl#checkSpaceViewPermission(spaceId, request)`：新增 spaceId 参数，按目标空间的落库归属/成员角色判权；两个列表接口同步
3. `StpInterfaceImpl`：pictureId 分支提到 spaceUserId 之前（防走私劫持）；空上下文默认授权加"读接口不得依赖"的警示注释
4. `HttpRequestWrapperFilter` 与 `StpInterfaceImpl` 的 Content-Type 判断同步改为忽略大小写的 startsWith（兼容 charset 变体；两处必须一致，否则 getBody 消费未包装请求的流会破坏 @RequestBody）

## 测试证据

- 单测（进门禁，CI 同口径）：13 个全绿，新增 `HttpRequestWrapperFilterTest` 4 个
- 集成测试：`PictureSpaceViewAuthIntegrationTest` 8 个全绿——三条攻击路径（charset 变体空上下文 / spaceUserId 走私 / 详情越权）全部 40101，属主、viewer 成员、公共图库三条合法路径行为不变
- 存量集成测试 11 个全绿，无回归
- 门禁实跑放行提交（[vibe-gate] PASS）

## 夹具注意（给后续测试的坑）

- 测试里 `StpKit.SPACE.login()` 之后必须 `StpKit.SPACE.getSession().set(USER_LOGIN_STATE, user)`——真实登录会写用户快照，`StpInterfaceImpl` 的身份判定读的就是它，漏写会报"用户未登录"
- viewer 正例必须用**团队空间**：私有空间按设计只认属主/管理员，成员行不参与判权

## 下一步

T3.2-T3.7 待用户逐项排期；一任务一 commit。改动前先读 docs/plan.md 对应条目。
