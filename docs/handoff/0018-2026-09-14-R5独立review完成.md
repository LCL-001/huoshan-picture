# Handoff 0018 — R5 收口独立 review 完成（2026-09-14）

> 上一份：`0016`（R5 收口的实施快照）、`0017`（面向本次 review 的复核交接）。
> 本文件是 **review 会话**的快照：结论、复核证据、未决项。**本轮只改 docs，未动任何代码。**

## 当前状态（一句话）

**R5（跨站 CSRF，一次性 ticket）的规则 13 独立 review 已完成并留档：有条件通过——R5 本体成立、无 P1，1 条 P2 + 6 条 P3 全是加固项**；可独立复核的部分**全部实跑复现**（backend 45 例、集成 58 例 / 14 类、该集成类连跑 4 次 8/8、`type-check` 138 与基线一致、`ai/` 区间零 diff），另**新做了两项原实施没做的实测**（64 路并发消费同一张票 → 恰好 1 个成功；apex 域名的实际行为 → 把一条 P2 降为加固项）；**下一会话第一件事是让用户拍板唯一那条 P2**（验收 ⑤ 的口径）；**apex CORS 白名单与 WebSocket 来源白名单均已按用户拍板收口**（提交 `1c732db` 与 `2b29786`，注意 prod 那份不入库、改动随重新打包的 jar 生效），然后按 0016 的路线进档 3。

## 刚完成（本会话，按时间）

1. **接手（只读）**：AGENTS → spec 关键三行 → handoff 0016/0017 → `git log`/`git status` → 代码本体（`git show af2995f`：`AiAssistantController` / `AiAssistantTicketManager` / 两个测试类 / 三个前端文件）→ `AiAssistantProxyManager` → `ErrorCode` / `CorsConfig` / `application.yaml`（Cookie 与 CORS 配置）/ `GlobalExceptionHandler` / `request.ts` / `AssistantPage.vue` / `assistantSse.ts` → `plan.md` 的 R5 立项行与实施记录、`decisions.md`、`F11`。

2. **独立复跑门禁（不采信自述）**：
   - backend 门禁 **45 例绿**（`AiAssistantControllerTest` 20 / 代理 9 / 守卫 3 / 其余 13）——AGENTS 那条过滤命令，原样。
   - 本地全量集成 **58 例 / 14 类绿**（MySQL 3306 + Redis 6379 当时在跑）。
   - `AiAssistantProxyIntegrationTest` **单独连跑 4 次全绿**（8/8 ×4）——针对 0017 第 7 条"`REQUESTS==0` 假红"的质疑：**未复现**，且判定结构性不成立（两条会转发的用例都在断言前 `awaitEngineRequest()`，测试结束时无在途请求，`@AfterEach` 的复位不会被迟到自增污染）。
   - 前端 `npm run type-check` **138 例**（与基线一致）、改动三个前端文件 **0 命中**。
   - `git diff 36aa8af..HEAD -- ai/` **空** ⇒ "引擎零改动"属实（**未复跑 ai/agent 87 例**：区间零 diff 已足以排除回归，跑一遍是重复劳动）。
   - `redis-cli info server` → **`redis_version:5.0.14.1`**，`GETDEL` 不可用这条约束**实测成立**。

3. **代码层逐条核对**（对照 0017 的 12 个自曝点与 5 条判据清单）：
   - **票不转发给引擎**：`chatUrl` 只拼 `message`/`userId`/`chatId` ⇒ 票只到 backend 代理为止（未加断言，列为 P3⑥）。
   - **门槛顺序**（票在三个登录门槛之后）：判定保留——保住"登录过期仍回 40100"的语义、身份不一致不白烧票；跨站请求多付两次 Redis 读，量级不构成放大器。
   - **40300 vs 401xx**：判定保留——401xx 是"没登录 / 空间登录态失效"的既有语义，本失败是"请求来源不可信"。
   - **票绑 `userId`**：判定保留——两行成本，把"票与凭据同属一人"显式化，与门槛三同构。
   - **`requireTrustedCaller` 是行为保持式重构**：门槛一~三的判定与错误码逐行等价；唯一变化是"AI 助手对话开始"日志前移到票闸之后 ⇒ 无票的 403 只剩 `GlobalExceptionHandler` 的 ERROR 堆栈（少了 `userId`/`chatId`，列为 P3⑤）。
   - **票的安全边界**：真正承载它的是 **CORS 读限制**（`CorsConfig` 白名单 + `allowCredentials(true)`），`SameSite=Lax` 只是第二道；票值 UUIDv4（122bit）不可猜、单次使用、60s、绑签发人、无回显 ⇒ 攻击者要凑票必须同时具备受害者 Cookie 与一次**可读的同站响应**。这条同时解释了"没复跑浏览器探针"为何不构成安全缺口，并让 CORS 白名单成为**承重配置**（见 P2②）。
   - **凭据不进日志**：`GlobalExceptionHandler` 只记异常与堆栈、不记 URL；`TraceIdFilter`/`HttpRequestWrapperFilter` 里无 URI/query 日志 ⇒ 票不进应用日志（只在 nginx access log 的 query 里，已按"60s + 单次"记豁免）。

4. **新做的实测（原实施没做的那一项）**：把 Java 里那份 Lua 脚本原样落成 `%TEMP%\r5rev-consume.lua`，用 `redis-cli --eval` **64 路并发**消费同一张真票 → **恰好 1 个拿到值**，事后 `exists=0`。"取用即删"的原子性从"代码论证"升级为"实测证据"（脚本与 bat 见环境备注，无凭据）。

5. **结论落库（只改 docs）**：
   - `docs/decisions.md` 末行 → R5 review 结论（复核证据 + 1 条 P2 + 6 条 P3 + 判定保留项 + "本会话只改 docs"）。
   - `docs/plan.md` R5 节 → "R5 独立 review 记录" + 新增挂账 P2 两条 / P3 五条。
   - `docs/features/F11-AI助手MVP.md` → 头部加 review 追加说明；「已知限制」7 补 ⑥-⑪（CORS 承重 / 前端不判业务码 / 无频控与长度校验 / 无自动化守护 / 重试语义 / apex CORS 白名单）；「怎么验证」的集成测试行订正 `getAndDelete` 为 Lua 并补 64 路并发与 4 次复跑。

6. **追加补充：apex 域名的实测（用户追问后做，据此把一条 P2 降为加固项）**：走查 `application-prod.yaml` 的 CORS 白名单时发现 `https://lincode.online`（apex，不带子域的主域）与 `www.lincode.online` **同属一个 site**（`SameSite` 按 eTLD+1 判定），而 R5 唯一的屏障是"CORS 读限制"（读不到 `/ticket` 响应体 ⇒ 凑不出票）——白名单放行 apex 恰好把这道屏障对着 apex 让开。随即实测该前提是否成立：`curl -I` 两个 host 都 `200 + nginx + text/html`；抓正文对比 **apex 与 www 逐字相同（1394 字节、`identical=True`）**；`/api/user/get/login` 在两个 host 上都同源可用（匿名均回 `{"code":40100}`）；前端生产用相对 base（`request.ts` 的 `BASE_URL=''`）⇒ **该白名单不是应用所需**。结论：现状不是活漏洞（apex 上目前没有第三方可控页面），但删掉零代价、能把 R5 的屏障收回成只有 www 一个 origin，故**由 P2 降为 P3⑧/⑪（加固项，建议删 + 定唯一主机名 www + 另一个 301）**，三份文档已同步订正。

## 进行中 / 下一步

1. **等用户拍板**（本会话未决的核心）：
   - **P2①**：R5 的**验收 ⑤ 按字面未达成**（浏览器跨站导航探针 `csrf-page.js` 未复跑）。二选一：补跑（资产仍在 `%TEMP%\t11-review\`：`csrf-page.js` / `stub-sse.js` / `README.md`）**或**把验收口径改成"已由 over-the-wire 覆盖"并说明理由。review 的判断：**不构成安全缺口**，属流程/口径问题。
   - **apex CORS 白名单：用户已拍板删除，本会话已执行**（代码提交 `1c732db`，门禁 45 + 87 双绿）——跟踪的 `application.yaml:100` 与不入库的 `application-prod.yaml` 各去一条，只留 `https://www.lincode.online`。**关键提醒**：`application-prod.yaml` 被 `backend/.gitignore:10` 排除，prod 侧的改动**随重新打包的 jar 内嵌生效、不经 git 到服务器**（要用在 prod 得重新 `mvn package` 并部署）。仍待办：定唯一主机名（www）+ 另一个 301（nginx，不在仓库）。

7. **同批收口的第二半：WebSocket 来源白名单（用户拍板"一起做"，代码提交 `2b29786`）**：`WebSocketConfig` 原先手写 `("http://localhost:*", "https://www.lincode.online", "https://lincode.online")`——**比 CORS 那处更宽**（生产还带着 localhost），而 WS 握手不受 CORS 约束、这张列表就是它唯一的来源防线。现改为与 `CorsConfig` 共用 `app.cors.allowed-origins`，生产自动只剩 www。新增守护测试 `CorsOriginAllowlistGuardTest`（2 条不变量：入库默认值里凡 `lincode.online` 必带 `www.`；CORS 与 WS 两处必须取自同一属性——防"两处各写一份"这个 apex 漂移的根因复发）。**负向控制实测**：把 apex 临时加回 `application.yaml` → 测试 1 例红；回退后门禁 **47 例**（45 + 2）全绿，`PictureEditHandlerIdleLockIntegrationTest` 2/2 绿。仍待办：唯一主机名（www）+ 301（nginx）；`application-prod.yaml` 那处同样需要重新打包才生效。
2. **档 3 完整一期**（同 0016）：visionTagger 看图打标、`batchEditPictures` / `batchUploadByUrl`、T9 MCP 搜图、T8-hard 循环合并（`run()` 退役、事件协议固化）、T12 两条业务场景联调、T13 清理。R5 已收口且 review 通过，可直接开写类工具。
3. **P3 五条**（可穿插，见 plan.md"review 新增挂账（P3）"）：前端取票判 `data.code`、`getAndDelete` 文档与 javadoc 订正、"票不转发给引擎"补 1 行断言、拒绝路径的可观测性、`/ticket` 频控与长度校验。**其中④（javadoc 订正）必须改代码，按约定留给了实现任务。**
4. **上线前必办与引擎部署前必办**：同 0016 第 3 条（nginx server 块 / SSE 配置、引擎旧端点 `/ai/manus/chat` 未鉴权、双端密钥一致、打包启动方式）。

## 未决问题

- **那条 P2（验收 ⑤ 口径）**——需用户拍板，故本次 review 记"有条件通过"而非"通过"。**apex CORS 白名单与其同类项（WS 来源列表）已全部收口**（用户拍板 → 提交 `1c732db`、`2b29786`）；唯一还开着的是它们共同的尾巴：定唯一主机名（www）+ 另一个 301（nginx，不在仓库）。
- 沿用 0016 的全部挂账（未消解）：P3 六条（`chatId` 净化 / relay 逐行 emit / emitter 超时对齐 / 错误文案不贴原文 / 拒绝先发可见文案 / 代理 emitter 超时早于引擎）、引擎侧"超时转显式错误事件 + 埋点"、R1（引擎侧取消不关上游）、"用户提问原文进 nginx access log"（要根治得改 POST + fetch 流式，票取代不了它）、前端门禁存量 138/73、门禁余量、T6 遗留 P3 ①②⑤⑦。
- **多实例部署未验**（票在 Redis 共享，理论上跨实例成立，只有单实例实测）；**浏览器兼容未验**（只在 Chromium 系）；**`EventSource` 自动重连在真实 403 下的行为未验**（本次只确认代码在 `onerror`/`[DONE]` 时显式 `close()`）。
- **push / tag**：`main` 领先 `origin/main` 未 push；仓库无任何 tag（用户未决，同 0016）。

## spec 偏差（认真轨）

- **越界（做了 spec/plan 没写的）**：**无**。改动文件与 plan.md 立项时的"文件范围"逐条对得上（1 个新 manager + 1 个 controller + 2 个测试类 + 3 个前端文件）；**本 review 会话同样未越界**——只改 docs（decisions / plan / F11 / 本快照），未动代码。
- **缺口（spec/plan 承诺但没做的）**：**一条**——plan.md R5 验收 ⑤ 的浏览器跨站导航探针未执行（实施记录已透明写入，review 判定不构成安全缺口，但**按字面未达成**，已列为 P2① 待用户定夺）。其余挂账均已同时记在 plan.md / F11 / 本快照。
- **关键三行检视**：**仍成立**——`git diff 36aa8af..HEAD -- ai/` 空（引擎仍是独立 Maven 应用、本轮零改动）；"引擎端点只接受服务间 API key、不注册登录体系"未变；"凭据只在请求生命周期内存流转、不落库、不进日志"**经独立核对成立**（`GlobalExceptionHandler` 不记 URL、两个 filter 不记 URI/query）。**唯一需要解释的边界**（同 0016）：票确实往 Redis 写了一个 `userId`（60s TTL），但它不是图库凭据（非 satoken、非会话标识）、不入 MySQL、不进应用日志；review 判定**不构成对关键三行的突破**。

## 线上状态（已上线项目才填）

- 当前线上版本：https://www.lincode.online 仍是 backend / frontend **存量版本**；档 2 全部改动（含 R4/R6/R5）未上线（线上没有 `/assistant` 入口，R5 这个洞在线上**不可达**），`ai/` 引擎仍未部署。
- 未处理反馈（[线上] 任务）：**无**（本会话无线上反馈项）。

## 环境备注

- 分支 `main`；**本会话开始前工作区干净**；本会话只产生 docs 改动（一笔 `docs:` 提交）。ahead `origin/main` 未 push（用户未决）；**无 tag**。
- **本会话实跑命令（可原样复现；均在仓库根 cmd）**：
  - backend 门禁：`cd backend && mvn -B -o test "-Dtest=!*IntegrationTest,!RedisStringTest,!YunPictureBaseApplicationTests" -DfailIfNoTests=false` → review 时 **45 例绿**，收口后 **47 例绿**（+2 条 `CorsOriginAllowlistGuardTest`）。
  - WS 集成用例（本会话为验证"来源改配置驱动后握手未变"而跑）：`mvn -B -o test -Dtest=PictureEditHandlerIdleLockIntegrationTest -DfailIfNoTests=false` → 2/2 绿（需 MySQL/Redis）。
  - 本地全量集成：`cd backend && mvn -B -o test -Dtest=*IntegrationTest -DfailIfNoTests=false` → **58 例绿**（14 类；需 MySQL/Redis）。
  - 集成类连跑：`mvn -B -o test -Dtest=AiAssistantProxyIntegrationTest -DfailIfNoTests=false` ×4 → 每轮 8/8 绿。
  - 前端：`cd frontend && npm run type-check` → **138**（基线一致；改动三文件 0 命中）。
  - Redis 版本：`"D:\develop\Redis\Redis-x64-5.0.14.1\redis-cli.exe" -h 127.0.0.1 -p 6379 -n 1 info server` → `redis_version:5.0.14.1`。
  - 并发消费探针：`"%TEMP%\r5rev-concurrency.bat"`（内部 `redis-cli --eval %TEMP%\r5rev-consume.lua <key>` ×64）→ 64 个输出文件里 **1 个**为票值、其余空，`exists` 事后为 0。
  - apex 域名实测：`curl -s -o nul -D - --max-time 15 https://lincode.online`（与 `https://www.lincode.online` 对照，均 `HTTP/1.1 200 OK` + `Content-Type: text/html`）；正文对比用 `Invoke-WebRequest` 取两 host 的 `.Content` 比长度与相等性（均 1394、`identical=True`）；`curl -s https://lincode.online/api/user/get/login` 与 www 对照（均 `{"code":40100,"data":null,"message":"未登录"}`）。
- **本机服务状态**：MySQL 3306 / Redis 6379 常驻在跑；其余端口（8123/8124/8125/8127/8130/8131/8132/8135/5173）**本会话一个都没起**（review 用的全是离线单测与集成测试，不需要起服务）。0016 提到的应用内浏览器里那个指向已停 5173 的遗留标签页，本会话未动。
- **本会话新增的仓库外文件（无凭据，可复用）**：`%TEMP%\r5rev-consume.lua`（与 Java 同一份 Lua 脚本）、`%TEMP%\r5rev-concurrency.bat`、`%TEMP%\r5rev-consume\`（64 个结果文件）、`%TEMP%\r5rev-it{1,2,3}.log`、`%TEMP%\r5rev-typecheck.txt`。**含凭据的探针资产（cookie 罐等）同 0016 惯例已在实施会话删除，本会话也没有新建任何凭据文件。**
- **版本锚点**：backend SB 2.7.6 / Java 17 / sa-token 1.39.0；引擎 SB 3.5.13 / Java 21；后端 context-path `/api`、actuator 8125；**本地 Redis 5.0.14.1**。

## 接手方式

1. 读 `AGENTS.md` → `docs/spec.md` 关键三行 → **本快照**（要复盘复核过程再读 0017，要实施细节读 0016）。
2. review 结论全文：`docs/decisions.md` **末行**；功能侧残留边界：`docs/features/F11-AI助手MVP.md`「已知限制」7 的 ⑥-⑩；挂账清单：`docs/plan.md` 的"review 新增挂账（P2/P3）"两行。
3. **先让用户拍板 P2（验收 ⑤ 口径）**，再进档 3；apex CORS 与 WS 来源白名单两项已收口（只剩 nginx 侧"唯一主机名 + 301"）。档 3 要新增"GET 有副作用"或流式端点时，先问用户要不要顺手做全站策略（plan.md"后续候选"）——apex 这条实测表明"CORS / 来源白名单核实"在这套设计里是**承重项**而非可选项。
4. 坑与门禁口径看 0016「环境备注」（`set "VAR=value"` 别留尾随空格、cookie jar 的 `#HttpOnly_`、Redis 5.x 三条）；本快照「环境备注」是本会话实跑过的命令，可直接抄。
