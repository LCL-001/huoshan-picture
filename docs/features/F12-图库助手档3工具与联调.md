# F12 图库助手档 3（整理与入库工具 + 搜图 + 真实联调）

> 覆盖任务：docs/plan.md T7（写类工具 + visionTagger）、T9（MCP 搜图）、T12（一期联调验收）。
> 档 1/档 2 的助手链路（代理 + 界面 + 折叠条）见 `F11-AI助手MVP.md`，本文件只讲**档 3 新增的能力**。
> 口径全文见 `docs/decisions.md` 2026-09-14 的三条（T7 工具集 / DeepSeek 接入 / T12 修复）。

## 一句话

图库助手从"只会读"变成"能整理、能入库"：看得懂图片内容并给出打标建议（`visionTagger`）、按建议批量落库（`batchEditPictures`）、按 URL 批量入库（`batchUploadByUrl`）、还能联网搜图再入库（MCP `searchImage`）——两条业务场景已在真机链路上跑通。

## 怎么用

在助手对话框里直接说要做什么（中文即可）：

- **智能整理**："我的空间叫 X，里面有几张图还没有标签和分类，帮我看看内容并打上标签。"
  → 助手会：列空间 → 拉图片清单 → **逐张看图**给建议（复用词表已有标签）→ 批量落标 → **回读清单复核** → 汇总回答。
- **选图入库**："帮我找 3 张 mountain 主题的图，放进空间 X。"
  → 助手会：搜图（MCP）→ 批量按 URL 入库 → 回读清单确认 → 逐条报成功/失败。

助手**不会**做的事：删除任何东西（一期不注册删除类工具）；用户没明确要求时不动数据。

## 怎么实现

### 关键文件

| 层 | 文件 | 作用 |
|---|---|---|
| 工具（读） | `ai/agent/.../tools/huoshan/{ListSpacesTool,ListPicturesTool,GetTagCategoryTool}.java` | 档 1 的三个只读工具 |
| 工具（写） | `.../tools/huoshan/{BatchEditPicturesTool,BatchUploadByUrlTool}.java` | 批量打标/分类/命名；按 URL 批量入库（工具内部循环单张端点） |
| 工具（看图） | `.../tools/huoshan/VisionTaggerTool.java` | 图片 id → 自己取地址 → 逐张调多模态模型 → 返回建议（不落库） |
| 工具（搜图） | 引擎 `application.yaml` 的 `spring.ai.mcp.client` 段 + 控制器里的 `ToolCallbackProvider` | MCP client 接 `ai/image-search-mcp-server`（SSE, 8127），工具只挂图库助手 |
| 客户端 | `.../tools/huoshan/HuoshanApiClient.java` | 图库 API 封装（per-request 携带 satoken + SESSION），新增 `batchEditPictures` / `uploadByUrl` / `getPicture` |
| 装配 | `.../tools/huoshan/HuoshanToolFactory.java` | `assistantTools(client, visionModel, extraTools...)`——**prototype**，按请求新建 |
| 提示词 | `.../agent/HuoshanAssistantAgent.java` | 写操作纪律（明确要求才改、不得编造 id、如实上报、不支持删除） |
| 模型 | `.../config/OpenAiChatModels.java` + `OpenAiModelProperties.java` | 主脑/vision 两组独立配置；`extra-body` 直通厂商私有参数 |
| 回放 | `.../utils/MessageConverter.java` 的 `toReplayMessages` + `.../chatmemory/FlowWindowBasedChatMemory.java` | 给模型的历史回放（**剥掉未配对的工具痕迹**，T12 修复） |
| 后端 | 无改动 | 两个写端点（`/picture/edit/batch`、`/picture/upload/url`）与词表 upsert 是 T5/T10 就已存在的能力 |

### 核心流程

**智能整理**：`listSpaces` → `getTagCategory` → `listPictures` → **`visionTagger`（一次最多 8 张）** → `batchEditPictures`（按图分别提交）→ `listPictures` 复核 → 回答。
**选图入库**：`listSpaces` → **`searchImage`（MCP）** → `batchUploadByUrl`（一次最多 20 条，逐条上报）→ 回答。

### 关键设计与理由

1. **粗粒度工具（一步一批）**：图库的按 URL 上传是**单张**端点，"批量"在工具内部循环完成；打标也是工具内一次看完 N 张。这样 20 步预算够用，模型不必"一张一步"。
2. **看图不让模型搬 URL**：`visionTagger` 只收图片 id，自己经 `GET /picture/get/vo?id=` 取地址再送多模态模型——复制长 URL 是模型最易出错的环节，且图库那个端点会按图片归属再做一次空间权限校验。
3. **建议与落库分离**：`visionTagger` 只给建议（返回里明说"尚未写入图库"），落库由 `batchEditPictures` 承担且要用户确认（或用户已明确要求）。
4. **写类工具"如实上报"**：图库批编只回一个布尔且按"空间 + id"过滤（不属于该空间的 id **静默跳过**），所以工具返回 `requestedCount`（提交了几张）而**不是**"改成了几张"，并带一句说明——联调里模型据此**自己回读清单复核**，说明这条口径真的被用上了。
5. **没配视觉模型就不挂 `visionTagger`**：宁可让模型看见"手上没有这个工具"，也不给它一个每次调用都报错的工具。
6. **思考模式必须关（DeepSeek）**：带 `tools` 的请求每轮都必须回传 `reasoning_content`，而 Spring AI 的 OpenAI 客户端不认这个私有字段 ⇒ 开着必 400；通过新加的 `extra-body` 配置直通 `{"thinking":{"type":"disabled"}}`。
7. **回放剥掉工具痕迹**（T12 修复）：记忆只存 assistant 的 toolCall 决策行、不存工具响应行，回放必须去掉 toolCalls（否则严格校验的服务端直接 400）；折叠条读的是库里原样的元数据，不受影响。

## 怎么验证

- **门禁**：`ai/agent` 单测 **113 例**绿（含写类工具 13、看图打标 8、装配 3、回放守护 2）。
- **工具 schema 契约**：`pictureIdList` / `fileUrls` 必须发布为 JSON `array`（模型靠 schema 才知道传数组）——有断言。
- **真机冒烟（DeepSeek 直连）**：`/v1/chat/completions` 路径可用；带工具的多轮调用通过（模型确实调用了工具）；图像输入对**图库 COS 图**可用（模型准确描述画面），Pexels CDN 地址被 DeepSeek 拒（不影响本链路）。
- **两条场景端到端（真机链路）**：智能整理 12.6s 内完成，落库核对到 `tags`/`category` 与 `tag.usageCount` 增量正确；选图入库 9.3s 内完成，空间图片 3→6、`totalCount`/`totalSize` 记账正确。
- **复现方式**：起 backend 8123（`local,test`）+ 引擎 8124（`profile local`，需 `HUOSHAN_HEADLESS_API_KEY`/`HUOSHAN_BASE_URL`）+ MCP 8127，然后用"真登录 → 取票 → 带票建流"的脚本读 SSE（探针脚本与证据在 `%TEMP%\t12\`，见 handoff 0020）。

## 已知限制

1. **同一条用户消息会被 Memory Advisor 重复写入**（实测 3 次）：token 浪费，不影响正确性；根治要改记忆写入路径，本轮未动。
2. **MCP 搜图返回的是"逗号拼接的一整串"**，模型要自己拆成多条 URL。工具侧已剥包裹引号，但**不按逗号拆分**（URL 里的逗号合法，启发式拆分风险大于收益）：若模型把整串当一个 URL 传进来，会逐条失败而不是静默出错。
3. **看图是逐张串行**（单次上限 8 张，每张数秒）：一步可能数十秒，折叠条会长时间转圈。要提速可换有界线程池并发（仓内 `ParallelSearchTool` 有先例）。
4. **打标只建议、不自动落库**：用户没确认时助手应当先讲建议；若用户明确要求"打上去"，会直接落库（联调即此形态）。
5. **前端工具摘要认不出新载荷**：`assistantFormat.ts` 的 `summarizeToolResult` 覆盖的是档 1 三类只读载荷，新工具（看图/批编/入库）会退化成"已完成"（设计上 fail-soft，不报错）。要好看需在前端补摘要分支。
6. **批编的逐张结果不可知**：图库不回逐张结果，工具只能报"提交成功"；确认要靠再拉一次清单（模型在联调里自发做了）。
7. **`/v1` 路径依赖**：引擎按 T6 口径把 base-url 当作"协议根"再拼 `/v1/chat/completions`；DeepSeek 实测可用，但换厂商时这条必须重测（配错是 404）。
8. **未做的档 3 尾项**：T8-hard（统一 `run`/`runStream`、`run()` 退役、事件协议固化为显式契约）。当前 `run()` 仍在，事件协议靠现有测试与前端语义约束，尚未固化为显式契约测试。
