# Plan — 火山图库（huoshan-picture）

> 任务粒度：一个任务 = 一次可测试的改动 = 一个 commit。每个任务必须标注允许触碰的文件范围。
>
> 2026-09-13 **AI 助手一期立项**（设计文档 docs/designs/2026-09-13-图库智能体一期设计.md）：**同日 R2——MyManus 引擎与 image-search-mcp-server 整体迁入本仓库（ai/ 独立应用），一期全部任务在本仓执行（T4-T12，另 T13 可选）**。

## 任务清单（AI 助手一期，2026-09-13 立项，R2 单仓化）

- [x] T4 AI 引擎迁入（2026-09-13 完成）：ai/agent（端口 8124）与 ai/image-search-mcp-server（8127）原样迁入；顺带修复 ImageSearchTool.java UTF-8 BOM 编译错误（源仓遗留）；ai/agent 55 用例基线在新位置全绿；门禁已纳入 ai/agent 单测（负向探针验证会拦）；双服务冒烟通过（8124 /api/health → ok、8127 SSE 启动正常）；源仓库 yu-ai-agent 已归档（handoff 2026-09-13-引擎迁出归档.md + tag archive/engine-migrated-to-huoshan；用户 README 手改已代提交 8a5f450）。实施计划：docs/plans/2026-09-13-T4-AI引擎迁入实施计划.md（迁移提交 88b50bf / f1c4a1a / 4560fd2）
- [x] T5 标签词表（2026-09-14 完成）：新表 `tag`（id/name/type(tag|category)/usageCount/is_delete/create_time/update_time，全局词表不按空间拆）+ Tag entity/mapper/service + /tag_category 查表动态化（返回结构逐字段不变，9 标签 + 5 分类种子迁移）+ 编辑接口（单编/批编）同事务 upsert 词表并累加 usageCount
  - 文件范围：backend/sql 新建表脚本、domain（Tag）、mapper、service、PictureController.listPictureTagCategory、PictureServiceImpl 编辑两处
  - 验收：集成测试（动态词表返回/种子迁移/upsert 计数/返回结构兼容）+ 门禁绿 + 全量回归
  - 实施记录：提交 be1c006（计划）/ a68d8cc（建表脚本+域模型）/ 647f77f（词表服务+集成测试）/ 4ed0025（动态化+编辑接线）；实施计划 docs/plans/2026-09-13-T5-标签词表实施计划.md；功能讲解 docs/features/F10-标签词表.md；全量回归 65 用例绿（62 存量 + 新增 TagVocabularyIntegrationTest 3 用例）；实现备注——upsert 采用"先原子自增、未命中插入、并发首插撞 uk_name_type 退化自增"（无自定义 SQL）；单编 editPicture 因"同事务"要求补加 @Transactional；列名 usageCount 与设计文档速写 usage_count 的微差随库内 camelCase 列约定（features 文档已记）；执行偏差——批编接线测试因先红后绿的正确顺序由 Task 2 调整至 Task 3（计划文档已补执行记录）
- [x] T6 OpenAI 协议模型接入（2026-09-14 完成）：新增 `spring-ai-starter-model-openai:1.1.2` 依赖；两套独立配置 `app.ai.openai.assistant`（主脑）/ `.vision`（visionTagger），装配为 `OpenAiChatModels` 持有器（**刻意不注册 ChatModel Bean**，避免与 profile 默认模型及 ChatClient.Builder 自动装配按类型冲突）；`application.yaml` 显式关掉 OpenAI 其余模型类型自动装配（六条自动装配均 matchIfMissing=true）；普通会话 Ollama 默认链路零改动
  - 文件范围：ai/agent 的 pom、application.yaml（+example）、模型装配 config
  - 验收：单测（装配/配置绑定）+ 本地冒烟
  - 实施记录：提交 4b0d527（计划）/ b946a42（依赖 + 守卫 + 配置骨架）/ 5730e7d（装配三件套 + 7 单测）/ b5edf74（base-url 示例修正 + 装配日志）；实施计划 docs/plans/2026-09-14-T6-OpenAI协议模型接入实施计划.md；门禁口径 62 用例绿（55 存量 + 7 新增），守卫测试做过负向探针（删属性 / 置 openai 均红）；冒烟实测——只加依赖不加守卫时启动即崩（`openAiAudioSpeechModel` 创建失败：OpenAI API key must be set），加守卫后六条 OpenAI 自动装配全落 Negative matches 且 /api/health=ok；用本机 Ollama 的 OpenAI 兼容端点（base-url `http://localhost:11434` + `qwen3:4b`）真实调用返回「收到」，默认会话 `/api/ai/manus/chat` SSE 正常（answer + [DONE]）；关键坑——base-url **不能带尾部 /v1**（Spring AI 请求路径自带 `/v1/chat/completions`，实测带 /v1 会 404），已写进配置注释与 .env.example
  - 独立 review（规则 13，2026-09-14，独立会话只读复核，证据见该会话报告）：**有条件通过**——交付本体成立（① 六条 OpenAI 自动装配的条件反查为 `@ConditionalOnProperty(spring.ai.model.<type>=openai, matchIfMissing=true)`，与守卫一一对应，运行时条件报告六条全 Negative；② 持有器不注册 ChatModel Bean 的依据反查为真：`ChatClientAutoConfiguration#chatClientBuilder` 的 `ChatModel` 形参按类型取单个，容器实有 `ollamaChatModel`（ollama profile），再注册即不唯一；③ base-url 带/不带 `/v1` 的请求路径实测为 `/v1/chat/completions` 与 `/v1/v1/chat/completions`；④ 半配置在真实上下文启动即失败且消息清晰；⑤ 装配进真实上下文（传参启动）后 `OpenAI 协议模型就绪` 日志与 /api/health=ok 同时成立，默认链路零扰动；⑥ 门禁口径 62 用例含新增 7 条；⑦ T6 三笔代码提交无任何密钥入内容，`.env`/`application-local.yaml` 仍被 .gitignore 覆盖）
  - review 发现（P1/P2/P3 全量口径）：**P1** 流式路径无任何超时（见 T6.1，2026-09-14 已收口）；**P2** "超时已生效"无测试守护——7 条单测无一断言超时真的接到 HTTP 客户端上，这正是 P1 漏网的原因，随 T6.1 补反射断言（已补）；**P3**（可接受，记已知限制）① 守卫测试只读 `application.yaml`，看不到 profile 覆盖（若未来某 profile 把 `spring.ai.model.chat` 置回 `openai`，测试仍绿；但因实际后果是启动即崩，属响亮失败）；② 守卫是"按上游当前六类型"的清单式，升级 Spring AI 时上游新增第七类模型类型会静默回到 `matchIfMissing=true` 启用（已核 1.1.8 与 1.1.2 的 imports 清单相同，均为六条；建议把"比对 `META-INF/spring/...AutoConfiguration.imports`"写进 Spring AI 升级检查项）；③ `timeout` 裸数字按毫秒绑定（实测 `timeout: 60` → `PT0.06S`，`60s` → `PT1M`），建议加 `@DurationUnit(SECONDS)` 或注释写明单位；④ 只配 base-url（无 api-key/model）被静默忽略，与"半配置拒绝"不对称（实测 `configured=false partial=false`，不报错也不生效），建议文档写明；⑤ `spring.ai.model.{image,audio.speech,audio.transcription}` 与 DashScope starter 同名共享，这四条守卫同时也关掉 DashScope 对应装配（ollama profile 下本已被 `spring.ai.dashscope.enabled=false` 关掉、且无代码消费这些 Bean，无实际影响；yaml 注释"OpenAI starter 的这四类"宜补一句共享说明）；⑥ `OpenAiChatModelsTest` 夹具里的 base-url 仍带 `/v1`（该文件 :21、:26），与 b5edf74 修正后的口径相反，建议顺手改成不带 `/v1` 的形态；⑦ 半配置 fail-fast 会拒掉"全局只注入一半变量"的部署姿势（已实测：`BeanCreationException ... app.ai.openai.vision 配置不完整`，属有意设计，记为已知限制）
- [x] T6.1（P1，T7/T8 开工前必做，2026-09-14 完成）流式路径超时保护（**两件事，缺一不可**）：① `OpenAiChatModels` 装配 `OpenAiApi` 时补 `webClientBuilder(...)`，给 `WebClient` 的 connector 设连接与响应头超时——覆盖"连不上 / 首包不来"；② 在流上补**空闲超时**（`Flux.timeout(Duration)` 逐信号空闲超时，或 T8 事件循环里的看门狗）——覆盖"吐了几段之后静默"。并补单测钉住"阻塞与流式两条路径的超时均已接到 HTTP 客户端上，且流侧有空闲超时"
  - 现象：`OpenAiApi.Builder` 无参构造默认 `webClientBuilder = WebClient.builder()`（字节码），而 `OpenAiChatModels#build` 只调 `restClientBuilder(...)`
  - 证据（T6 review 实测，本机探针）：非流式 `chatCompletionEntity` 走 restClient——`timeout=3s` 时 3046 ms 抛 `java.net.SocketTimeoutException: Read timed out`；流式 `chatCompletionStream` 走 webClient——同一端点、同一 3s 配置，25 s 后仍 pending（无超时、无报错）。反射实测 `JdkClientHttpConnector.readTimeout=null`、底层 `java.net.http.HttpClient.connectTimeout=Optional.empty`（本项目 classpath 无 reactor-netty/jetty/HC5，`WebClient.builder()` 的 connector 探测按 `DefaultWebClientBuilder#initConnector` 字节码顺序落到 JdkClientHttpConnector）
  - 为什么只给 webClient 接超时不够（review 二次实测，connector `readTimeout=3s` 三形态）：**稳态吐字的长流不被误杀**（1 chunk/s、6 段的流 3.4 s 正常 onComplete 全 6 段 → 它不是"整响应死线"）；**完全不响应的端点 3020 ms 抛** `WebClientRequestException: java.net.http.HttpTimeoutException: request timed out`（它等价于 JDK `HttpRequest.timeout`，只覆盖到**响应头到达**）；**吐 2 段后静默的流 20 s 仍 pending、无任何异常**——故"流中途卡死"这一类必须由流侧空闲超时兜住
  - 影响：T8 把对话循环收敛为"仅 SSE 流式"后，上游一次卡死即永久挂住 SSE 连接与请求线程（TCP 不断开、无字节）；且不产生异常，Spring AI 重试模板（仅 retryOn `TransientAiException`）无从介入
  - 验收：单测（阻塞路径 requestFactory 超时 + 流式 connector 超时 + 流空闲超时三处断言）+ 冒烟（无响应 / 吐一半静默两种形态均在超时内失败，且稳态长流不被误杀）
  - 文件范围：ai/agent 的 config/OpenAiChatModels.java + 对应测试（②若落在 T8 事件循环实现，文件范围随之加 T8 的循环文件）
  - 口径备注（2026-09-14 追加）：**不要用"保留 `BaseAgent.run()` 阻塞路径"当作本缺口的兜底**——两条路径的差异在 HTTP 客户端（restClient 有超时 / webClient 没有），不在同步与否；SSE 是产品必需（前端折叠条 + headless 端点），保留 run() 不改变生产流量走哪条路，只是把 T8 要消灭的第二套循环长期留下（与用户 2026-09-13 R2 拍板相反）。若将来确实要"非流式入口"，按设计文档既有口径在事件流之上做收集实现，而非保留 run() 本体
  - 实施记录（2026-09-14 完成）：提交 e20a5dd（含 6 条新单测）；门禁口径 68 用例绿（62 存量 + 新增 OpenAiChatModelsTimeoutTest 6 条）；改动只在 `config/OpenAiChatModels.java` + 新测试类，未动 yaml/pom。实现三条取舍：① `OpenAiApi.Builder.webClientBuilder(...)` 显式接入 JDK connector（`HttpClient.connectTimeout` + `JdkClientHttpConnector.setReadTimeout`，JDK 客户端这两项默认皆不限制）；② 空闲超时落在 WebClient 的 `ExchangeFilterFunction` 上（`response.mutate().body(body -> body.timeout(timeout))`，对响应体流逐信号计时），**刻意不用 ChatModel 装饰器**——装饰器在顶层计时会把"工具执行 + 等下一轮响应"也算进静默，逐请求计时则不会误伤 T7/T8 的工具调用长流；③ 复用既有 `app.ai.openai.*.timeout` 一把尺（连接 / 响应头 / 流空闲共用，未新增配置键），0 与负数视作"不限制"（与 SimpleClientHttpRequestFactory 的 0=无限一致，同时避开 JDK `HttpClient.connectTimeout(0)` 的 IllegalArgumentException）
  - 红绿与负向探针（规则 12）：测试桩为进程内 JDK `HttpServer`（三形态：稳态吐字段 / 吐两段后静默 / 连响应头都不发），不依赖外部服务、随测试类启停。**先红**——只接 connector 超时时"流中途静默"形态实测「等待 5s 仍未收到任何信号」，即 P1 本体；**转绿后两个负向探针**——摘 `.filter(streamIdleTimeout)` → 只有"流中途静默"转红（5.986s 失败），摘 `.clientConnector(...)` → 只有"首包不来"转红（5.028s 失败），证明两件事各自不可省（对应本条目"缺一不可"）
  - 冒烟实测（2026-09-14）：**真模型**——本机 Ollama `/v1` 兼容端点单次流式 **24.6s / 1373 段** 正常 onComplete 并返回全文（稳态长流不被误杀，也是"connector 读超时不是整响应死线"的实机证据）；**装配层 dump**——`connector=JdkClientHttpConnector readTimeout=PT2M`、底层 `HttpClient.connectTimeout=Optional[PT2M]`（超时确实落到 HTTP 客户端上，不只存在于单测断言里）；`timeout=0` 装配不抛错（回退 `WebClient.builder()`，readTimeout=null）；**应用级**——带 OpenAI 配置启动 6s 内 `/api/health=ok`、启动日志 `OpenAI 协议模型就绪 … timeout=PT2M`、默认会话 `/api/ai/manus/chat` SSE 仍 `answer + [DONE]`（默认 ollama 链路零扰动）。探针与日志在 `%TEMP%\t6rev\`（`T61SmokeProbe.java` / `run-t61-app.ps1` / `t61-run.log`），仓库外不入库
  - 已知限制（P1/P2 由本任务收口，以下为残留）：① 空闲超时以 `java.util.concurrent.TimeoutException` 结束这条流，不属 Spring AI 重试模板的 retry 范围（仅 retryOn `TransientAiException`），**T8 的 SSE 端点必须把它转成显式错误事件**，否则前端只看到连接断开；② `timeout` 仍是一把尺（连接 / 响应头 / 流空闲共用），日后若要"首包慢"与"吐字间隔慢"两个不同口径再拆键；③ 单测用反射读 `DefaultRestClient.clientRequestFactory`、`JdkClientHttpConnector.{readTimeout,httpClient}` 三个框架内部字段，Spring 升级后若结构变化会响亮失败（失败消息已写明"反射路径需要同步更新"）
  - 独立 review 追加（规则 13，2026-09-14，只读会话；结论与全部证据见 docs/handoff/0010 末节）：**R1（P1，待用户拍板）** 取消不关闭上游请求——空闲超时触发或下游 `dispose()` 之后，桩服务器仍能对同一 socket 连续迟写成功（2.4s / 20s 两轮实测），裸 JDK `ofPublisher`+cancel 则立刻 FIN；根因是 Spring `JdkClientHttpResponse.adaptBody` 的 `Flux.cache(0)`（= `replay(0).autoConnect()`，取消不断开上游），非本任务引入，但意味着"卡死一次 = 泄漏一条上游连接 + 一个在途 provider 请求"，T7/T8 上线前须在"接受+文档 / 换 connector spike / 写进 T8 验收"中选一；**R2（P2）更正**：`RetryUtils.DEFAULT_RETRY_TEMPLATE` 是 `maxAttempts(10)` + retryOn(`TransientAiException`, **`ResourceAccessException`**) + `exponentialBackoff(2s,×5,cap180s)`，故上方现象行与本条①里的"仅 retryOn `TransientAiException`"不准确、阻塞路径读超时会重试（默认 `PT2M` 下最坏分钟级），"一次超时即失败"不成立（**流式路径无重试这一点不变**）；**R3（P2）** T8 拿到的不是裸 `TimeoutException`，而是**状态码 200** 的 `WebClientResponseException` 包裹（`... 200 OK from POST <uri>, but response failed with cause: TimeoutException ...`），T8 须沿 cause 链判超时；**P3** 稳态看门用例段间隔 300ms vs `CONTROL_TIMEOUT` 1000ms 余量 3.3×（2 核 CI 上偏紧，失败是响亮的红；加固即把 `CONTROL_TIMEOUT` 提到 1500~2000ms）；负数 `timeout` 在两条路径处理不对称（`restClientBuilder` 只挡 null）；T6 的 ③⑥ 一行级 P3 建议并入 T7 首次触碰 `OpenAiChatModels` 时同做——**R1 处置（2026-09-14 用户拍板）：延后到档 2 之后**（T8 只做"超时转显式错误事件"、不承诺关闭上游；期间记为已知限制并加超时/取消可观测埋点，接真实云端 provider 后按实测再定"接受+文档"或"换 connector / 补显式终止"）
- [ ] **MVP 档位与推进顺序（2026-09-14 用户拍板"先做出来能用，再慢慢优化"）**——一期范围不变，只调顺序与颗粒度：
  - **档 1 headless 打通（先做）**：T8 的"会话类型 + headless SSE 端点 + 服务间 API key + `huoshan:<userId>` 身份映射"（**循环不重构、`run()` 暂不退役**）+ T7 的三个只读工具（`listSpaces` / `listPictures` / `getTagCategory`，prototype 化 per-request 透传 token，是 T7 里最便宜的部分）。验收：一条脚本 / curl 跑出"提问 → 真实调图库 → SSE 步骤事件流"，且工具调用走用户真实 satoken（越权不可达）。**2026-09-14 完成**（实施记录见下一条）。
  - **档 1 实施记录（2026-09-14 完成）**：提交链 `01379be`（计划）→ `775d1e6`（T7 工具集与客户端）→ `c995c88`（会话类型/身份映射/agent）→ `fbcfe2e`（风险 1 钉桩测试）→ `74921ec`（headless 端点与鉴权）；计划文档 docs/plans/2026-09-14-档1-headless打通实施计划.md。门禁口径实测 **86 用例全绿**。**风险 1（工具挂载能否用于 OpenAI 协议模型）结论：不成立、无需改代码**——字节码（`OpenAiChatModel.buildRequestPrompt` 对 `ToolCallingChatOptions` 走接口拷贝并回设 `internalToolExecutionEnabled`）+ 桩测试（含负向控制）＋ Ollama 真机探针（真模型+真工具，`listSpaces_http_hits=1` 证明无双跑）三层证据。**冒烟**：后端对无效 satoken 回 40100、工具转结构化错误 `{"error":true,"code":40100}`、SSE `step/answer/[DONE]` 正常，越权不可达达成；`getTagCategory` 读到真库词表（9 标签+5 分类）。**环境坑（记下备用）**：本地 backend jar 曾陈旧于源码（09-12 构建 vs 09-13 的 T3.2 收权提交），导致首轮冒烟出现"匿名可枚举空间/图片元数据与属主"的修复前行为——真图库冒烟前必须先按当前源码重建后端。**偏差**：agent 构造参数改用 `ChatMemory` 接口（便于无上下文单测）；风险 1 探针改为"真工具+桩图库"（不依赖用户 token）。**挂账**：① 带真实 satoken 的正样本冒烟（需用户浏览器登录取 token，档 2 联调前补跑）；② 旧端点 `/ai/manus/chat` 未锁（引擎部署前必办）；③ 超时转显式错误事件与埋点顺延档 2。口径见 docs/decisions.md（2026-09-14 三条）。**同日追加：方案 B 落地与正样本达成**——用户以 `application-test.yaml`（叠加 `local,test` 关验证码）提示可自取真 token，冒烟由此暴露"空间列表不吃 sa-token"（详见下一行前置项），当日拍板 **B（透传凭据组）**：提交 `a2d0f41`（引擎侧凭据组：`HuoshanApiClient` 带 `Cookie: SESSION=…`、端点从 `SESSION` cookie 取会话标识、缺任一把入口即拒 40000）+ `1f28ae0`（顺带修掉冒烟暴露的真 bug：工具把 id 收成 Long 再吐回 JSON 数字，模型读 19 位雪花 id 后写回被截断 …745→…700 导致 `40400`，改为 id 一律字符串透传并加断言钉住）。**正样本实测**：探针账号真登录（`tier1probe0914140822`）下 `listSpaces` 读到真实空间 `tier1-probe-space`、空间维度 `listPictures` 回 `code=0`（通过 `checkSpaceViewPermission` 的两把一致校验）、仅带 satoken 则回 40000；门禁 87 用例全绿。
  - **档 2 前置项（2026-09-14 档 1 冒烟发现 → 同日已决策 B）**：实测 `/space/list/page/vo` 的登录态只认 **Spring Session**（`SpaceController.java:142` → `UserServiceImpl.getLoginUser` 读 `USER_LOGIN_STATE`），**不吃 sa-token**（新签发的有效 satoken 单独调用仍 `40100`）；空间维度图片读更严——`checkSpaceViewPermission`（`PictureServiceImpl:768-786`）要求 Spring Session 用户与 sa-token loginId 一致。**用户 2026-09-14 拍板方案 B**：代理与工具透传**一组凭据**（satoken + Spring Session 会话标识），引擎侧已落地（`HuoshanApiClient` 带 `Cookie: SESSION=…`、端点从 `SESSION` cookie 取会话标识、缺任一把入口即拒 40000）。**T10 必须一并实现会话 Cookie 透传**；口径见 docs/decisions.md 2026-09-14 两条。
  - **档 2 用户可见 MVP（本轮终点）**：T10（backend 代理：服务间 API key + 凭据组透传（satoken + 会话 Cookie，B 口径）+ SSE 转发）+ T11 最小形态（助手入口 + 对话面板 + SSE 步骤折叠条）。验收：登录后在界面里对话、折叠条逐步可见；收口时做一次里程碑独立 review（规则 13）+ 一篇合并的功能讲解（规则 9）。
  - **档 2 实施记录（2026-09-14 完成）**：提交链 `46d266d`（计划）→ `68ded79`（T10 代理端点+转发+配置，含 19 条新单测）→ `e329a48`（凭据链集成测试）→ `6d6e4f2`（T11 前端界面）→ `7988542`（fix：会话 Cookie 原样值）。实施计划 docs/plans/2026-09-14-档2-用户可见MVP实施计划.md；功能讲解 docs/features/F11-AI助手MVP.md。门禁 backend **32 例**全绿（13 存量 + 19 新增：`AiAssistantControllerTest` 10 + `AiAssistantProxyManagerTest` 9），ai/agent 87 例零回归。**真图库冒烟（正+负）**：探针账号经代理提问 → `listSpaces` 读到真实空间 `tier1-probe-space`、SSE `step/answer/[DONE]` 正常；停掉引擎再打 → 收到合成的"引擎暂时不可用"+`[DONE]`，客户端不挂死。**浏览器端到端**：起 backend(`local,test`)+引擎+前端 dev，登录后进 `/assistant` 发送，界面依次呈现用户气泡 → 折叠条「正在执行（0 步）」+转圈+输入禁用+停止按钮 → 「执行步骤（1 步）」含 `工具 · listSpaces` 与真实返回 → 回答气泡；真实界面截图存 `%TEMP%\t11-smoke\ui-answer.png`。**冒烟暴露并修掉一个真 bug**：代理原先把 `session.getId()` 当会话 Cookie 转发，而 Spring Session 的 Cookie 值是 Base64 形态、`getId()` 是解码后的 id，实测用解码值打 `/space/list/page/vo` 回 `40100`（原样 Cookie 值回 `code=0`）；现固定按 Cookie 名取原样值转发，并留回归测试（单测 + 集成测试各一条）。**执行偏差**：① 设计文档 L110 留的"SSE 转发选型"定为 `SseEmitter` + JDK `HttpURLConnection`（零新增依赖）；② `AiAssistantController` 用显式构造注入（4 参）而非 `@RequiredArgsConstructor`，`:81-88` 传 `sessionCookieName`；③ 转发线程池上界 64、被拒时 `completeWithError` 响亮失败。**挂账（新增）**：前端 `npm run type-check` 138 例 / `eslint .` 73 例**既有**错误（本次净增 0，AGENTS 与 handoff 0001 记的"前端以 type-check + lint 为准"目前不成立，需单独立任务清理）；回答按纯文本渲染、模型输出的 Markdown 会字面显示星号；错误以 `answer` 形式呈现、无独立错误事件（超时转显式错误事件仍按 decisions.md 挂账）。
  - **档 2 独立 review 记录（2026-09-14 完成，规则 13）**：结论**有条件通过**（无 P1；4 条 P2、7 条 P3），提交 `625f294`（只改 docs）。**我独立复跑证实**（非采信自述）：门禁 backend 32/32、ai/agent 87/87（`.githooks` 排除名单口径）＋集成测试 3/3；`type-check` 138 例且 assistant 相关文件 0 命中；`git diff 46d266d^..HEAD -- ai/` 空；自建桩引擎收到与浏览器逐字一致的凭据组；三个探针实例日志 + 引擎日志对真凭据 **0 命中**（含异常与拒绝路径）；引擎不可达合成 `answer`+`[DONE]` 不挂死；线程池 70 并发实测 64 通 6 拒；真浏览器跑真实 `assistantSse.ts` 断流后 15s 零重连；全仓 `v-html`/`innerHTML` 0 处。**注意门禁口径**：AGENTS 那条通用过滤命令直接套 `ai/agent` 会红（`MyManusTest` 注入不存在的 `MyManus` bean，遗留教学测试），`ai/agent` 用的是 `.githooks/pre-commit:54` 的排除名单。
  - **review 新增挂账（待用户拍板 / 上线前必办）**：**R4（P2）** 代理"只判存在性"实测**放行任意非空白 satoken**（真会话 + 假 token 仍读到真实空间），decisions 2026-09-14 那条"不一致时回 `SPACE_NOT_LOGIN`"只对空间维度读图成立 → 建议补一次身份一致性校验（`StpKit.SPACE` 现成 API）或先更正记录表述；**R5（P2，档 3 写类工具上线前必办，届时 P1）** `GET /api/ai/assistant/chat` 只认 Cookie + 有 LLM 副作用 + 无 nonce，实测跨站顶层导航即带 Cookie 跑完一次 agent → 加一次性 ticket 或改 POST 流式；**R6（P2）** `/ai/**` 不在 `SaTokenConfigure` 且 Sa-Token 注解在未注册路径会静默失效 → 补一条反射守护测试；**上线前必办（P2-部署）** 仓库缺 nginx server 块，`proxy_buffering off` / `proxy_read_timeout` 未能证实，SSE 一旦被缓冲折叠条就不会逐步显示；**P3** `chatId` 可换行注入后端日志、relay 多行 `data:` 会产出半个 JSON 帧（前端静默丢弃）、上游缺 `[DONE]` 时"完整答案 + 响应意外中断"同屏、上游非 JSON 错误体（网关 502 的 HTML）原文进回答气泡、线程池拒绝只给空体 HTTP 500、代理 emitter 超时早于引擎数十毫秒使超时形态退化成裸断连。
  - **R4/R6 收口记录（2026-09-14，用户拍板"R4 同意加校验、R6 做"）**：**R4** `AiAssistantController` 新增门槛三——`StpKit.SPACE.getLoginIdByToken(satoken)` 必须非空且与 Spring Session 用户同 id（无效/过期 40100、不同人 40102，零上游请求），**推翻**原"只判存在性"口径并更正 2026-09-14"B 落地"条中判据的适用范围（该条只对空间维度读图成立）；权限仍只由图库服务端判。**R6** 新增 `AiPathSaTokenGuardTest`（3 条不变量），`SaTokenConfigure` 的拦截路径提取为 `ANNOTATION_GUARDED_PATTERNS` 常量。**红绿（规则 12）**：先写复现测试——`AiAssistantControllerTest` 2 条新用例与 `AiAssistantProxyIntegrationTest` 2 条新用例在修复前**实测红**（12 跑 2 失败 / 5 跑 2 失败，且修复前那个版本连 `rejectsWhenNotLoggedIn` 的 `REQUESTS==0` 断言都被异步残留带红，正是"请求真的打到引擎"的旁证）；修复后单测 12/12、该集成类 5/5 绿。**守护测试负向控制**：给助手端点挂 `@SaCheckLogin` → 2 条不变量红并报出 `AiAssistantController#chat`；把 `/ai/**` 加进拦截路径 → 第 3 条红（证明不是空转）。**回归**：门禁 **37 例**全绿（原 32 + 5 新增）；本地全量集成套件 **55 例**全绿（14 个类）。**仍未做**：R5（跨站 CSRF，档 3 写类工具上线前必办，届时 P1）、nginx SSE 配置必办项、以及"两把不一致时前端只显示连接中断"（`EventSource` 读不到 JSON 错误体，已记 F11 已知限制 8）。
  - **R5 收口任务（2026-09-14 用户拍板"采用一次性 ticket"）**：`GET /ai/assistant/chat` 新增**必需参数 `ticket`**——跨站顶层导航能带齐两把 Cookie 但**读不到响应体**，因此拿不到票，盲打必被拒（这正是选它的理由）。新增 `manager/ai/AiAssistantTicketManager`（`StringRedisTemplate`，键 `huoshantuku:ai:assistant:ticket:<value>`，值为签发用户 id，TTL 60s，原子取用即删 = 单次使用，实施用 Lua 脚本而非 `getAndDelete`——见实施记录）+ `POST /ai/assistant/ticket` 签发端点（复用与 chat 同一套登录门槛）+ `chat` 新增**门槛四**（票必须存在、未被用过、且属于会话用户，否则 40300、零上游请求）。
    - 未采用：**改 POST + fetch 流式**（治本、且顺带解决"用户提问进访问日志"，但要在前端手写 SSE 解析，而前端无测试脚本、存量 type-check 138 / eslint 73，风险不划算）；**sa-token 自带 `SaSameUtil`**（1.39.0 源码已核：全站共享单值、默认 24h、只从 header 取，EventSource 只能放 query → 落进访问日志即全站长效共享密钥）。选型理由入库 decisions.md。
    - 文件范围：backend 新建 `manager/ai/AiAssistantTicketManager.java`、改 `controller/AiAssistantController.java`、改 `AiAssistantControllerTest` / `AiAssistantProxyIntegrationTest`；frontend 改 `api/assistantController.ts` / `pages/AssistantPage.vue`、`utils/assistantSse.ts`（仅补注释）
    - 验收（规则 12 先红后绿）：① 复现测试——不带 `ticket` 直调 `chat` 在修复前**通过**（即洞），修复后 40300 且零上游；② 单测覆盖"票缺失/未知/已用过/属别人 → 拒，有效 → 转发"，签发端点的门槛与 chat 同口径；③ 集成测试覆盖真实 Redis 单据（取用即失效）；④ 门禁绿（backend 无 MySQL/Redis 单测）+ ai/agent 87 例零回归 + 本地全量集成套件绿；⑤ **跨站探针 `%TEMP%\t11-review\csrf-page.js` 复验**：修复前顶层导航跑完一次 agent，修复后 403 且引擎零请求（浏览器 Cookie 语义只有探针能证）
    - **实施记录（2026-09-14 完成）**：提交 `af2995f`（代码一笔，走 pre-commit：backend 45 例 + ai/agent 87 例双绿）。新增 `AiAssistantTicketManager`（键 `huoshantuku:ai:assistant:ticket:<value>`、值=签发用户 id、TTL 60s）、`POST /ai/assistant/ticket`、`chat` 门槛四（40300、排在三个登录门槛之后）、前端"取票后建流"。**先红后绿（规则 12）**：门槛未加时单测 20 跑 **4 失败**（无票/编造票/别人的票/用过的票全被**照常转发**——失败信息一律 `Expecting code to raise a throwable`，这就是洞本身）；补门槛后 20/20。**集成套件 58 例绿**（14 个类；新增 3 例含"真 Redis 单次使用"与"别人的真票"，既有的 5 例也全部改为带票调用）。**over-the-wire 复验**（真 HTTP + 桩引擎计数）：无票 → `40300` 且桩引擎**零请求**；取票 → `code=0`；带票对话 → HTTP 200 + SSE 且桩引擎**恰好 1 次**（路径/密钥/satoken/`SESSION` 全对）；**同一张票复用** → `40300` 且仍 1 次。**浏览器端到端**同样复跑通过（前端 dev 指 8131，发送后正常出气泡，桩引擎记录到该请求；截图 `%TEMP%\r5-ui-answer.png`）。**一处实现取舍**：取用即删用 Lua（GET+DEL）而非 `ValueOperations.getAndDelete`——后者下发 Redis 6.2 才有的 `GETDEL`，本机 Redis **5.0.14.1** 直接报 `ERR unknown command`（**集成测试抓到、替身单测抓不到**，口径已记 decisions）。前端 `type-check` 仍 138（净增 0）、改动文件 eslint 0 错。**本轮只做单点收口**，通用方案（下面那条）仍待拍板。**验收 ⑤ 的说明**：没复跑 `csrf-page.js` 那个浏览器跨站导航探针——本次修复不改浏览器侧语义（Lax 放行顶层导航这件事没变），缺票必拒已由 over-the-wire 探针覆盖；浏览器侧验的是"正常用户这条路仍然通"（取票 → 建流 → 出回答）。**2026-09-14 补记**：该验收 ⑤ 已按用户拍板补跑并通过（本次复跑把当时那个边界补上了），见下方"验收 ⑤ / review P2① 已收口"行。
    - **R5 独立 review 记录（2026-09-14 完成，规则 13，只读会话）：有条件通过**（无 P1；1 条 P2、6 条 P3；全文见 docs/decisions.md 末行与 F11「已知限制」7 的 ⑥-⑪）。可独立复核的部分**已逐条实跑**：门禁 backend **45 例**绿、本地全量集成 **58 例 / 14 类**绿、`AiAssistantProxyIntegrationTest` **连跑 4 次 8/8** 绿（"`REQUESTS==0` 假红"未复现且结构性不成立——两条会转发的用例都在断言前 `awaitEngineRequest()`，测试结束时无在途请求）、`type-check` 138 与基线一致且改动文件 0 命中、`git diff 36aa8af..HEAD -- ai/` 空、本机 `redis_version:5.0.14.1` 实测。**新增实测**：用与 Java 同一份 Lua 脚本（`redis-cli --eval`）**64 路并发**消费同一张票 → **恰好 1 个拿到值**、事后 `exists=0`（"取用即删"的原子性不再只有代码论证）。**判定保留**：门槛四排在三个登录门槛之后、错误码用 40300、票绑 `userId`（理由见 decisions 该行）。**该条件（P2①）已于同日复跑解除**（见下行），故 R5 review 结论现为**通过**。
    - **验收 ⑤ / review P2① 已收口（2026-09-14，用户拍板"补跑探针"，实测通过）**：跨站导航探针**已补跑**，前提是**按当前 HEAD 重新打包**（当日 19:43 那个 jar 早于 R5 代码 `af2995f`，拿它复跑会得到相反结论）。① **原探针 `csrf-page.js` 原样**（跨站页面由 `127.0.0.1:8135` 提供、浏览器在 `localhost:8131` 真登录、Cookie 为服务端写出的 HttpOnly+`SameSite=Lax`）：顶层导航落到 `/api/ai/assistant/chat?message=s3&chatId=csrf-nav-1` → `{"code":40300,...}` 且**桩引擎零请求**；40300 是门槛四、排在三个登录门槛之后 ⇒ 该导航确实带齐并带对了 satoken 与 SESSION，只缺票（不是被登录门槛挡下的假通过）。② **正向对照**（同一跨站形态、只多一张票，新脚本 `csrf-control-ticketed.js`）：同一条顶层导航拿到完整 SSE 事件流、桩引擎**恰好 1 次**请求（`chatId="csrf-nav-ticketed"`、`userId`、服务间密钥、satoken 与 `SESSION` Cookie 全对），代理日志"AI 助手对话开始"也只 1 条——两次运行**唯一差别就是票**，故①的结论可归因到票，且实测证实"Lax 会在跨站顶层导航上发出 Cookie"这个前提。③ 原第 1 步（no-cors 子资源请求，响应按设计读不到）换成可观测版本（新脚本 `csrf-cors-read.js`，页面 `127.0.0.1:8137`、cors 模式带凭据）：`POST /ai/assistant/ticket` 与 `GET /user/get/login` 都回 `{"code":40100,"message":"未登录"}` ⇒ **跨站子资源请求带不上 Cookie**，跨站页面因此取不到票；本地能读到这个 40100 是 dev 白名单含 `http://127.0.0.1:*` 之故（生产白名单只有 www，连读都读不到）。证据汇总 `%TEMP%\t11-rerun\accept5-rundown.txt` 与同目录两个新脚本；含凭据的 cookie 罐、桩引擎与代理日志已按惯例删除。**故 review 的"有条件"解除：R5 review 结论 = 通过**；review 的 6 条 P3 仍挂着（见下行，其中④必须改代码、按约定留给实现任务）。
    - **review 新增挂账（P3，可穿插）**：③ 前端 `fetchAssistantTicket` 未判 `data.code`（本仓其他调用点都判）——取票遇非 40100 业务失败（如 Redis 不可用回 50000、HTTP 仍 200）拿到 `null`，URL 成 `ticket=null`，用户看到"连接中断"而非"未能取得凭据"，且白打一次 chat；④ `getAndDelete` / `GETDEL` 口径残留（集成用例 javadoc `:279` 与 decisions 上一行仍写它，实现已改 Lua）——**review 会话按约定未动代码**，订正留给实现任务；⑤ 无票被拒后 `log.info` 不再记 `userId`/`chatId`（只剩 `GlobalExceptionHandler` 的 ERROR 堆栈），攻击取证少了"谁被打了"这一维；⑥ 建议给集成用例加 1 行"`receivedUri` 不含 ticket"断言（`receivedUri` 已在夹具里），把"票不污染引擎日志"钉住；⑦ `POST /ticket` 无频控（已登录用户可无限造 60s 键）、`ticket`/`message` 无长度校验；⑧ **生产 CORS 白名单含 apex `https://lincode.online`**（`application-prod.yaml:49`，**原按 P2 记，review 实测后降为加固项**）：`SameSite` 按 eTLD+1 判定，apex 与 www **同站**、白名单又放行 apex ⇒ 一旦那个 origin 上出现非本应用的可控页面，就能既读 `/ticket` 又读 chat 响应、**绕过 R5**；**实测** apex 与 www 当前返回逐字相同的 `index.html`（1394 字节、`identical=True`）、`/api` 在两个 host 上同源可用（匿名 `{"code":40100}`）、前端生产用相对 base（`request.ts` 的 `BASE_URL=''`）⇒ 该白名单**不是应用所需**。**已按用户拍板于同日删除**（提交 `1c732db`：跟踪的 `application.yaml` + 不入库的 `application-prod.yaml` 各去一条；**prod 那份不入库，改动随重新打包的 jar 生效、不经 git 到服务器**）；仍待办：定唯一主机名（www）+ 另一个 301（nginx，不在仓库）。⑨ **删这条时新发现（不在 review 那六条内）**：`WebSocketConfig:25` 的 `setAllowedOriginPatterns` 是**硬编码**列表，含 `http://localhost:*` 与 `https://lincode.online`、生产同样生效——WS 握手没有 CORS 可挡，来源校验是它唯一的 CSRF 防线。**已按用户拍板"一起做"于同日收口**（提交 `2b29786`：改为与 `CorsConfig` 共用 `app.cors.allowed-origins`，生产自动不含 localhost 与 apex；新增 `CorsOriginAllowlistGuardTest` 两条不变量，负向控制实测"加回 apex → 1 例红"，门禁 47 例绿、WS 集成用例 2/2 绿）。
  - **后续候选（未拍板，通用方案）**：全站 CSRF 策略——跨站请求拦截过滤器（`Sec-Fetch-Site: cross-site` 拒 `/api/**`，缺失头不拦以免误伤脚本）+ "GET 无副作用"守护测试（清单式，与 R6 同款）+ CORS 白名单按环境核实（`CorsConfig` 默认含 `http://localhost:*`）。用途是覆盖将来新增的端点，避免每加一个流式接口重设计一次 nonce；本轮按用户拍板只做单点收口。
  - **档 3 完整一期**：T7 的 visionTagger / `batchEditPictures` / `batchUploadByUrl`、T9（MCP 搜图）、T8 的循环合并（T8-hard：统一 run/runStream、`run()` 退役、事件协议显式契约）、T12 两条业务场景联调、T13 清理。
  - **MVP 阶段放宽**：规则 13 独立 review 与规则 9 讲解文档降为里程碑（档 2 收口）一次；重集成测试留到 T12；上轮 review 的 P3（反射断言精简、`CONTROL_TIMEOUT` 余量、一把尺拆分、T6 的 ③⑥）继续挂着。**不放宽**：密钥不入库 / 凭据（satoken 与会话标识）只在请求生命周期不落库、不进日志；一期不注册删除类工具、工具调用走用户真实凭据（B 口径：satoken + Spring Session）由图库服务端判 RBAC、门禁红绿灯、AGENTS/docs 改动用 `docs:` 前缀单独提交。
  - **R1（取消不关闭上游请求）处置（2026-09-14 用户拍板）**：**延后到档 2 之后**——T8 只做"超时转显式错误事件"（沿 cause 链判定，见其下方 R3），**不承诺关闭上游连接**；期间记为已知限制并加可观测埋点（超时 / 取消次数），接上真实云端 provider 后按实测决定"接受+文档"还是"换 connector / 补显式终止"。
  - **转向原因与口径记入 decisions.md（2026-09-14）；旧任务一条不删，档 3 照旧挂在清单里。**
- [ ] T7 图库工具集（ai/agent）：`tools/huoshan/` 新增——listSpaces / listPictures / getTagCategory / batchEditPictures / batchUploadByUrl（RestClient 封装图库 API，prototype 化 per-request 注入透传 token 与图库地址）+ visionTagger（OpenAI 协议多模态批量看图，提示词注入词表）；粗粒度原则，一期不注册删除类工具
  - 档位拆分（见上"MVP 档位"）：前三个只读工具属**档 1**；visionTagger 与两个批量工具属**档 3**。**档 1 部分已于 2026-09-14 完成**（`tools/huoshan/` 三工具 + `HuoshanApiClient` + `HuoshanToolFactory`，桩 HTTP 单测进门禁），visionTagger 与两个批量工具仍挂档 3
  - **档 3 进度（2026-09-14）**：**T7 全部完成**——两个批量工具（`BatchEditPicturesTool` / `BatchUploadByUrlTool` + 三个结果 DTO + 客户端 `batchEditPictures` / `uploadByUrl`）+ **看图打标 `VisionTaggerTool`**（图片 id → 工具自己经 `/picture/get/vo` 取地址 → 逐张调多模态模型，提示词注入当前词表）+ `HuoshanToolFactory.assistantTools`（取代 `readOnlyTools`；**未配视觉模型时不挂 visionTagger**）+ 助手系统提示词放开写操作（写明"只在用户明确要求时改、不得编造 id、如实上报、不支持删除、建议需用户确认后才落库"）。新增 **20 条单测**（写类工具 11 + 看图打标 8 + 装配 1，含**工具 schema 数组契约**：`pictureIdList` / `fileUrls` 必须发布为 `array`，否则模型只会当字符串传），门禁 **107 例**绿。实施计划、口径与阻塞项见 `docs/plans/2026-09-14-档3-完整一期实施计划.md`；剩余：T9 MCP 搜图、T8-hard 循环收敛、T13 清理、T12 联调（T12 需用户提供 `AI_OPENAI_*` 与 Pexels 密钥）
  - 文件范围：ai/agent 的 tools 新增包、必要 config/DTO
  - 验收：单测（Mock 图库 HTTP + visionTagger 打标解析）进门禁绿
- [ ] T8 会话类型机制 + headless 端点（ai/agent）：会话类型（普通/图库助手）决定工具注册表与系统提示词；图库助手 SSE 端点——服务间 API key 鉴权、`huoshan:<userId>` 外部身份映射、satoken 上下文传递给工具集。**随任务执行对话循环收敛（用户 2026-09-13 拍板"对话保留 SSE 模式即可"）：抽事件消费者接口统一 run/runStream 双循环，同步 run() 退役（存量调用方迁移到事件流收集口径），lastStepKind 等受保护字段通道收编进事件载荷，SSE 事件协议（step/answer/metrics/[DONE]）固化为显式契约**
  - 文件范围：ai/agent 的 BaseAgent/ReActAgent/ToolCallAgent 循环重构、agent 装配类、controller 新增端点、config、受影响测试迁移
  - 档位拆分（见上"MVP 档位"）：会话类型 + headless SSE 端点 + 鉴权与身份映射属**档 1**；循环收敛（统一 run/runStream、`run()` 退役、受保护字段收编、事件协议固化为显式契约）属**档 3（T8-hard）**。**档 1 部分已于 2026-09-14 完成**（`ConversationType` + `HuoshanAssistantAgent` + `HuoshanAssistantSession` + `HuoshanAssistantController` + `HeadlessApiKeyInterceptor`，循环零改动）；原属 T8 的"超时转显式错误事件 + 埋点"**顺延档 2**（口径见 docs/decisions.md 2026-09-14 条）
  - 验收：单测（鉴权/身份映射/工具装配/事件协议契约）+ SSE 端点冒烟；门禁绿
- [x] T9 MCP client 接入（ai/agent）（2026-09-14 完成）：spring-ai-starter-mcp-client（已在 pom）连接 ai/image-search-mcp-server（**传输方式定为 SSE**）；Pexels 搜图工具进图库助手工具集；MCP 服务本体零改动
  - 文件范围：ai/agent 的 application.yaml、MCP client 配置/装配
  - 验收：单测 + 本地双进程冒烟（搜图返回 URL 列表）
  - 实施记录：**零新增依赖**（starter 已在 pom），改动 5 处——① `application.yaml` 开启 `spring.ai.mcp.client`（`sse.connections.image-search.url` 取 `${AI_MCP_IMAGE_SEARCH_URL:http://localhost:8127}`）；② `spring.ai.mcp.client.initialized: false`——**MCP 服务没起也不该拖垮引擎启动**（首次调用时才连），另留 `AI_MCP_CLIENT_ENABLED` 总开关；③ `HuoshanToolFactory.assistantTools(client, visionModel, extraTools...)` 支持并入外部工具回调，控制器经 `ObjectProvider<ToolCallbackProvider>` 取 MCP 工具（**Bean 不存在时图库助手少一个工具、照常开对话**），**只挂图库助手、MyManus 的 `ToolRegistration` 一字未动**；④ 助手提示词补一句"搜图工具返回逗号分隔的图片 URL，交给 `batchUploadByUrl` 入库，不要自己编 URL"；⑤ 新增 1 条装配单测（门禁 **108 例**）。**双进程冒烟实测**：MCP 服务（8127，profile `sse`，Pexels 密钥在服务侧 `application-local.yaml`）——`tools/list` 回 `["searchImage"]`、`tools/call`（query=sunset）回 **15 个** `images.pexels.com` 地址；**引擎侧**用一次性 Spring 上下文冒烟（跑完即删，不入库）确认引擎自己的配置能装配出 `searchImage` 并调用成功（`MCP TOOLS [searchImage]`、调用结果含 Pexels 地址）。**部署耦合（必记）**：MCP 服务需与引擎同机/内网可达，且它没起时图库助手的搜图工具会调用失败（引擎本身能正常启动）；不部署就设 `AI_MCP_CLIENT_ENABLED=false`。冒烟脚本与步骤见 docs/decisions.md 同日"MCP 口径"行
- [x] T10 图库后端 AI 代理模块（2026-09-14 完成）：新增代理端点（转发对话请求到 ai/agent headless SSE 端点）——服务间 API key 认证（配置化，密钥不入库）、**当前用户凭据组透传（satoken 头 + `SESSION` 会话 Cookie，B 口径 2026-09-14 拍板；缺任一把引擎侧入口即拒 40000）**、用户标识（huoshan:<userId>）传递、SSE 流转发（SB 2.7 实现选型实施时定：SseEmitter/OkHttp EventSource）
  - 文件范围：backend 的 controller 新增代理端点、service/manager 新增代理与转发组件、config、application-local.yaml.example
  - 验收：集成测试（API key 校验/凭据组透传，含"只带 satoken 不带会话 Cookie 时引擎拒绝"的负向用例）+ 与 ai/agent 冒烟联调
  - 注意：凭据只在请求生命周期内存流转、不落库、不进日志（与引擎侧同口径）
  - 实施记录：提交 68ded79（端点+转发+配置+19 单测）/ e329a48（凭据链集成测试）/ 7988542（fix 会话 Cookie 原样值）。实现选型 `SseEmitter` + JDK `HttpURLConnection` + 专用有界线程池（上界 64，零新增依赖）；端点 `GET /api/ai/assistant/chat?message=&chatId=`（`/ai/**` 不进 SaTokenConfigure，登录门槛在 controller 显式判）；「只带 satoken 不带会话 Cookie」的负向在**代理入口**即拒（40100，零上游请求），引擎侧的存在性拒绝不变。门禁 32 例绿；集成测试 3 例本地绿；真图库正/负冒烟与浏览器端到端见"档 2 实施记录"
- [x] T11.1 助手界面可用性打磨（2026-09-14 用户实机使用后当场提出并当日完成，属 T11 的返工打磨）：① **回答渲染 Markdown 子集**——模型回答里的 `**加粗**`、`- `/`1. ` 列表、`` `行内代码` ``、http(s) 链接、`> ` 引用目前字面显示成星号/横杠；② **工具步骤不再平铺原始 JSON 与参数**——那是给开发看的，普通用户只需要"干了什么、结果几条"，改为「工具 · 中文别名 + 一句话摘要（共 N 个空间 / 共 N 张图片 / 标签 N 个·分类 M 个 / 失败：原因）」，原始返回收进该行「详情」折叠、默认收起
  - 文件范围：frontend/src 的 components/assistant/*、utils/assistantFormat.ts、pages/AssistantPage.vue
  - 验收：浏览器实测——回答里加粗/列表/行内代码/链接正常渲染，工具行默认只见摘要、点「详情」才出原始 JSON；`npm run type-check` 净增 0（既有 138 例存量不动）
  - 实现口径：**不引 markdown 库、不用 `v-html`**——自写子集解析 + Vue 模板插值（转义交给 Vue），从根上不留 XSS 面（回答文本可被工具数据/提问影响，绝不能当 HTML 渲染）；摘要解析失败一律退化成"已完成"，不抛错、不空屏
  - 实施记录：提交 5fac9d2。新增 `utils/assistantFormat.ts`（`parseAssistantText` / `summarizeToolResult` / `toolLabel`，纯函数）+ `components/assistant/{AssistantText,AssistantInline,ToolStepRow}.vue`，改 `StepTimeline.vue`（工具步走 ToolStepRow、思考步走 AssistantText）与 `AssistantPage.vue`（回答气泡用 AssistantText，`white-space: normal`）。**验证**：无前端测试框架，改用 esbuild 把真实 `assistantFormat.ts` 转成 mjs 后在 node 里跑 19 条断言（载荷取自真机 SSE 原文与回答原文，覆盖摘要四类 + 兜底、块解析五种 + URL 尾随标点 + `3 > 2` 不误判引用），19/19 通过（脚本 `%TEMP%\t11-smoke\format-check.mjs`，仓库外）；改动的前端文件 eslint 干净、`vue-tsc --build --force` 计数仍 138（净增 0）；浏览器实机复跑：工具行显示「工具 · 读取标签词表 / 标签 13 个 · 分类 5 个 / 详情」，点「详情」展开原始 JSON 且按钮变「收起」，回答里 `**加粗**` 渲染为真粗体、反引号渲染成代码片（截图 `%TEMP%\t11-smoke\ui-polish-{default,detail}.png`）。**顺带观察**：摘要数比模型口播更准（同一轮工具返回 13 个标签，模型口播"10个标签"），因为摘要直接读工具载荷
- [x] T11 前端助手界面（2026-09-14 完成）：AI 助手入口 + 对话面板 + SSE 步骤折叠条（消费 ai/agent 步骤事件协议语义，AntD 组件渲染）；复用现有登录态
  - 文件范围：frontend/src 新增助手页面/组件、路由入口、openapi 生成代码如需
  - 验收：npm run type-check + lint 过；对话与折叠条人工验收
  - 实施记录：提交 6d6e4f2。新增 `pages/AssistantPage.vue` + `components/assistant/StepTimeline.vue` + `utils/assistantSse.ts` + `api/assistantController.ts`，改 `router/index.ts`（`/assistant`）、`GlobalHeader.vue` 与 `BasicLayout.vue`（两处菜单入口）、`request.ts`（导出 `BASE_URL`）。客户端用原生 `EventSource`（GET + `withCredentials`；不能用 axios——无流式形态且 10s 超时会掐断），**收到 `[DONE]` 与出错都必须显式 `close()`**（`EventSource` 会自动重连，不关就重发同一条 message = 重跑一次 agent）。未登录前置判后重定向登录页（`EventSource` 读不到 40100）。人工验收：真浏览器端到端跑通（截图 `%TEMP%\t11-smoke\ui-answer.png`）；**npm run type-check 与 eslint 均有既有存量错误（138 / 73 例，全在本次未触碰文件里，本次净增 0）**——"type-check + lint 过"这条验收按现状无法达成，已挂账见"档 2 实施记录"
- [ ] T12 一期联调验收：跑通两条场景（智能整理/选图入库）；按规则 9 写功能讲解文档 docs/features/
  - 文件范围：联调发现的必要小修 + docs/features/
  - 验收：设计文档"验收标准"4 条全过；门禁绿 + 全量回归
- [x] T13 引擎教学遗留清理（承接原 yu-ai-agent T1）（2026-09-14 完成）：AuthAdvisor 假实现、demo 包、空壳控制器（ChatMessage/ChatSummary）、FileBasedChatMemory 死代码、空目录
  - 文件范围：ai/agent 内上述文件与目录
  - 验收：编译 + 门禁绿
  - 实施记录：提交 `05db19e`，删 8 个文件（`advisors/AuthAdvisor.java` 328 行、`chatmemory/FileBasedChatMemory.java` 147 行、两个 **20 行空壳** `@RestController`（`ChatMessageController` / `ChatSummaryController`，类体是空的）、`demo/invoke/` 四个教学类）+ 空掉的 `demo/` 目录。**删除前逐条核实引用**：全部零引用（`AuthAdvisor` 只有 `@Slf4j`、不是 Spring Bean；`FileBasedChatMemory` 无注解、无人 new；两份 yaml/json/properties 零命中）。**顺带纠正一处计划描述**：`ChatMessage` / `ChatSummary` 的 **PO / Mapper / Repository / Service 不是死代码**——它们是 `FlowWindowBasedChatMemory`（现行记忆实现）的落地表，只删那两个**空壳 Controller**。门禁 **108 例**绿

## 任务清单（存量修复，已完成）

以下为 2026-09-12 功能审查（三个只读子代理 + 逐条人工核验，详见 handoff/0002）产出的修复批次，按优先级排列：

- [x] T1 工程化收尾：端口统一 8123、README 启动步骤修正、Redis 键前缀统一 `huoshantuku:` 并迁移存量数据（5c0ed24 / 5faf41f / fe71e78 / 596cb8f）
  - 文件范围：backend 配置与缓存相关类、README
  - 验收：已提交，CI 绿
- [x] T2 CI 门禁：GitHub Actions 跑编译 + 无 MySQL/Redis 单测（4f4cd5b）
  - 文件范围：.github/workflows/ci.yml
  - 验收：CI 绿
- [x] T3.1 图片读接口权限绑定目标资源，封堵"请求嗅探"越权
  - 文件范围：manager/auth/StpInterfaceImpl.java、config/HttpRequestWrapperFilter.java、controller/PictureController.java、service/impl/PictureServiceImpl.java、backend/src/test/（新增 HttpRequestWrapperFilterTest、PictureSpaceViewAuthIntegrationTest）
  - 验收：单测进门禁全绿；集成测试钉住"charset 变体令上下文为空 / spaceUserId 走私劫持 / 详情接口越权读"三条攻击路径全部 40101，属主与 viewer 成员的合法路径不受影响
- [x] T3.2 `/space/list/page/vo` 匿名枚举收紧（仅返回本人空间与已加入团队空间，admin 除外）；`/space/get/vo` 同步收紧为"与空间有归属关系才可查"
  - 文件范围：controller/SpaceController.java、service/impl/SpaceServiceImpl.java、service/ISpaceService.java
  - 验收：SpaceListAuthIntegrationTest 9 用例全绿（匿名 40100、无关用户不可见他人空间、viewer 可见已加入团队空间、属主/管理员正常）；已提交 c2f61e6
- [x] T3.3 会话生命周期：改密/删号/降权踢 Sa-Token 会话；getLoginUser 匿名请求不建会话
  - 文件范围：service/impl/UserServiceImpl.java、controller/UserController.java
  - 验收：UserSessionLifecycleIntegrationTest 4 用例全绿（resetPassword 后旧 satoken 失效且新密码可登录、删号踢会话、降权踢会话、匿名请求零会话）；已提交 5501642。已知残余：Spring Session（非 Sa-Token）在改密后仍有效，但因空间接口全部依赖 Sa-Token 登录态，残余影响限于个人资料编辑等自持操作，量级低
- [x] T3.4 重复上传额度重复累计修复（更新分支改为 `totalSize - oldSize + newSize`、count 不变）+ 旧 COS 对象清理
  - 文件范围：service/impl/PictureServiceImpl.java（uploadPicture 事务段）+ backend/src/test/（新增 PictureReplaceQuotaIntegrationTest）
  - 验收：PictureReplaceQuotaIntegrationTest 5 用例全绿（替换后额度净增量 = 新旧差值、缩图替换回落、超限替换整笔回滚且不清理旧文件、旧 URL 仍被其它记录引用时跳过清理、个人图库无空间也清理旧文件）；已提交 ad4dada
  - 实现备注：扣减用 `GREATEST(totalSize - oldSize, 0) + newSize`（与 deletePicture 同款防负数）；`cleanupPictureFile` 引用计数阈值由 `count > 1` 改为 `count > 0`（调用点查询时记录本身已删/已改指向，剩 1 条引用也不能删共享文件，原阈值会在恰好剩一条引用时误删）
- [x] T3.5 删空间级联（事务内逻辑删图片 + 异步清 COS + 包事务）、删用户级联
  - 文件范围：service/impl/SpaceServiceImpl.java、controller/UserController.java、service/ISpaceService.java（新增 deleteUserCascade 声明，接口变更超出原定范围已在此说明）+ backend/src/test/（新增 SpaceDeleteCascadeIntegrationTest）
  - 验收：SpaceDeleteCascadeIntegrationTest 2 用例全绿（删空间后图片逻辑删除、URL 仍被他人空间引用的图跳过清理、成员记录清理、他人空间不受影响；删号后名下空间级联、他人空间成员关系移除、其上传到他人空间的图保留）；全量 54 测试绿；已提交 8c702c1
  - 口径备注：删空间为单事务（空间行 + 成员 + 图片），COS 清理在事务提交后按 URL 引用计数异步执行；图片列表缓存不在级联中失效（空间删除后其缓存条目已不可达，TTL 兜底）；删号保留其上传到他人团队空间的图片（团队内容不随账号消失），如需一并删除另行立任务
- [x] T3.6 ShardingSphere 二选一 → 用户拍板第三选项：**注释停用（不删不启）**
  - 实际改动：application.yaml 分表死配置块注释停用（application-local/prod.yaml 属 gitignore 本地同步同步注释）；主类 exclude、pom 依赖、manager/sharding 两类保持原状作为停用护栏与恢复基础；README 本无分表表述，不改
  - 验收：全量 54 测试绿（上下文启动正常）；口径记入 decisions.md；已提交 d000360
- [x] T3.7 社交通知口径二选一 → 用户拍板：**保留通知接口，修正 spec/AGENTS 记录**
  - 实际改动：仅文档（docs/spec.md F7、AGENTS.md 技术栈与停用模块注记、decisions.md 两条口径）；前端核实 GlobalHeader.vue 正在调用 /notification/* 五接口，SocialController 未改动
  - 验收：spec F7 改为"部分停用：通知保留"；decisions.md 记入 T3.6/T3.7 两条口径
- [x] T3.8 空间额度预检误拒替换图片（T3.4 顺手发现的存量问题，用户 2026-09-13 点名）：预检移到 oldPicture 解析之后，仅对新增图片按新增口径预检；替换不增条数、大小按净差值在事务内原子校验，满员空间替换（缩图/等量）不再被"空间条数不足/大小不足"误拒
  - 验收：PictureReplacePrecheckOwnershipIntegrationTest——满员空间替换成功且额度记净差值（1000-500+200=700、条数不变）；满员空间新增仍被拒（守卫用例）；已提交 4d37b45
- [x] T3.9 admin 替换他人图片归属被改（T3.5 顺手发现的存量问题，用户 2026-09-13 点名）：getPicture 更新分支保留原归属 userId，仅新增图片归属上传人；管理员替换不再把图片改成自己的
  - 验收：admin 替换后 userId 不变、URL 更新；PictureReplacePrecheckOwnershipIntegrationTest 3 用例全绿；全量 57 测试绿；已提交 4d37b45
  - 独立 review（规则 13，2026-09-13）：**通过**（8 用例实测绿，静态推演确认回退即红）。P2 既有问题（非本次引入）已立 T3.10 收口。P3 已知限制：满员空间超额替换在 COS 上传后于事务内被拒、留孤儿对象（与新增路径既有失败面一致）；回归测试属 IntegrationTest 口径不入 CI/门禁（既有约定）
- [x] T3.10 替换图片 spaceId 反推路径缺空间上传权限校验（T3.8/T3.9 独立 review 发现的既有 P2，用户 2026-09-13 拍板收口）：uploadPicture 在 spaceId 由原图反推时补查目标空间并校验 PICTURE_UPLOAD（抽 checkSpaceUploadPermission 私有方法复用），与显式传 spaceId 路径、checkPictureAuth（deletePicture）判权同源
  - 口径后果（有意收紧）：站点管理员若非团队空间成员，替换/上传该空间图片将被拒（与显式传参路径及 deletePicture 现状一致）；私有空间属主/站点管理员不受影响，团队空间属主由 createSpace 自动建 admin 成员行、不受影响
  - 验收：PictureReplaceSpaceAuthIntegrationTest 4 用例全绿（被移出成员替换被拒、降权 viewer 替换被拒、非成员站点管理员替换被拒、editor 成员正常替换且归属不变）；全量 61 测试绿；已提交 8b29f46
- [x] T3.11 上传事务失败后补偿删除刚上传的 COS 文件（P3 孤儿对象收口，用户 2026-09-13 点名选项①）：transactionTemplate.execute 包 try-catch，事务回滚后按引用计数（count == 0 才删）异步清理刚上传的新文件；原异常照常抛出。覆盖替换超额与"过预检但事务内原子校验失败"的新增两条失败路径
  - 口径：COS 上传在事务前这一时序不变（picSize 只有上传后才知道）；cleanupPictureFile javadoc 补"事务回滚后清理孤儿文件"第三类调用前提
  - 验收：PictureReplaceQuotaIntegrationTest 6 用例全绿（超额替换：旧文件绝不动 + 新文件被补偿清理；新增超额：补偿清理；另 4 用例回归不变）；全量 62 测试绿；已提交 5f44ecc
