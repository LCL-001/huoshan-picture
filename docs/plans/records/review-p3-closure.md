> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] **同批 review 的 3 条 P3（2026-09-15 T21 全部收口，提交 `e4f09a5`；本行原先标注"未做，可穿插到任何一次会碰这些文件的任务"）**：① `BaseAgent.runLoop` 的上限提示未判 `state`——最终回答恰好落在第 `maxSteps` 步时会多补一条"执行结束：达到最大步骤 (N)"回答帧（改成 `if (state == RUNNING && currentStep >= maxSteps)` + 补一例 `maxSteps=1 + finishOnStep` 的契约测试即会先红后绿）；② `AgentEvent.Error` javadoc 的"且是本轮唯一的事件"措辞过强（中途失败时此前已有 step/answer 帧发出）；③ 计划 §5 的设计（新增 `AgentExecutionException`、失败置 `state=FINISHED`）与实现（不新增异常类、置 `state=ERROR`）不一致，"一个 agent 实例只跑一次"的不变量宜从 `HuoshanAssistantAgent` 类注释提到 `BaseAgent`。全文见 `decisions.md` 2026-09-15 review 行与 F13「已知限制」7/8/9。**收口记录（T21）**：① 上限判断加 `state == RUNNING` 前置条件，并补两条契约测试（回答落在最后一步 / 卡死落在最后一步，修复前各红一次，摘掉 guard 的负向控制同样是这两条红）；② javadoc 改为"替代回答帧、是本轮最后一条业务事件"并写明边界，补一例中途失败契约测试；③ 计划 Task 3 两处标注实施偏差（**选"文档对齐代码"**），不变量整段提到 `BaseAgent` 类注释。门禁 `ai/agent` **159** + backend **74** 绿。
① `BaseAgent.runLoop` 的上限提示未判 `state`——最终回答恰好落在第 `maxSteps` 步时会多补一条"执行结束：达到最大步骤 (N)"回答帧（改成 `if (state == RUNNING && currentStep >= maxSteps)` + 补一例 `maxSteps=1 + finishOnStep` 的契约测试即会先红后绿）；② `AgentEvent.Error` javadoc 的"且是本轮唯一的事件"措辞过强（中途失败时此前已有 step/answer 帧发出）；③ 计划 §5 的设计（新增 `AgentExecutionException`、失败置 `state=FINISHED`）与实现（不新增异常类、置 `state=ERROR`）不一致，"一个 agent 实例只跑一次"的不变量宜从 `HuoshanAssistantAgent` 类注释提到 `BaseAgent`。全文见 `decisions.md` 2026-09-15 review 行与 F13「已知限制」7/8/9。**收口记录（T21）**：① 上限判断加 `state == RUNNING` 前置条件，并补两条契约测试（回答落在最后一步 / 卡死落在最后一步，修复前各红一次，摘掉 guard 的负向控制同样是这两条红）；② javadoc 改为"替代回答帧、是本轮最后一条业务事件"并写明边界，补一例中途失败契约测试；③ 计划 Task 3 两处标注实施偏差（**选"文档对齐代码"**），不变量整段提到 `BaseAgent` 类注释。门禁 `ai/agent` **159** + backend **74** 绿。
