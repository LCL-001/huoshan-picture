> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T23（2026-09-15，用户拍板 **R1 = 接受**）**R1 收口：接受"取消/超时不关闭上游连接" + 落地可观测埋点**：把 2026-09-14 review 的 R1（空闲超时触发或下游 `dispose()` 后，上游 provider 的 socket 仍能继续被写成功——Spring `Flux.cache(0)` 语义，非本项目引入）从"延后、先记已知限制"推进到**明确接受**，并补上当时就要求的"超时 / 取消次数"埋点，供接真实云端 provider 后按数据复核。
接受"取消/超时不关闭上游连接" + 落地可观测埋点**：把 2026-09-14 review 的 R1（空闲超时触发或下游 `dispose()` 后，上游 provider 的 socket 仍能继续被写成功——Spring `Flux.cache(0)` 语义，非本项目引入）从"延后、先记已知限制"推进到**明确接受**，并补上当时就要求的"超时 / 取消次数"埋点，供接真实云端 provider 后按数据复核。
  - 文件范围：`ai/agent` 新增 `agent/AgentRunMetrics.java`、改 `agent/BaseAgent.java`（两处计数调用点 + 可空的 `metrics` 字段）与 `controller/HuoshanAssistantController.java`（注入）、`BaseAgentTest` 新增 2 例。**不动** 重试预算、connector、`request-timeout`、emitter 超时。
  - **实施记录（2026-09-15 完成，提交 `5da2c9e`）**：`AgentRunMetrics`（`@Component`，构造期注册两个 Micrometer `Counter`：`huoshan.agent.run.timeout` / `huoshan.agent.run.cancelled`；引擎已有 `spring-boot-starter-actuator` 且 `management.endpoints` 含 `metrics`，**零新增依赖**）；`BaseAgent` 加可空字段 `metrics`（不注入=空操作，MyManus 旧链路与既有测试不受影响），在①`catch` 里 `isTimeout(e)` 为真时计超时、②`stopped` 分支出循环时计取消；控制器在新建 agent 后 `setMetrics(...)`。**测试**：`BaseAgentTest` 新增"超时与提前终止各计一次，互不串味"（用真 `SimpleMeterRegistry` 断言计数，等价于负向控制——不调用就恒为 0）与"不注入计数器时循环照常跑" ⇒ `ai/agent` 164 → **166 例**。
  - **真机验收（2026-09-15，本机引擎 8124 + 真模型 MiMo；证据 `%TEMP%\t23\acceptance.txt` 与引擎日志）**：两个计数器在 `/api/actuator/metrics/...` 上可读、起始 COUNT=0.0；**`curl -m 6` 掐断连接**后 `cancelled` 由 **0.0 → 1.0**，同时日志出现"用户停止生成，Agent 提前终止, steps=2"（即"客户端断开 → stopped 置位 → 计数"这条链在真机上通了）。超时计数当晚未在真机上触发（黑洞 provider 那一形态 T18 已有真机记录：3.95s 出 `error` 帧），由上述单测钉住。
- **已拍板（2026-09-15）**：① 结果**两步式**（先建议、管理员确认后落库）——公共图库是访客门面，写错影响面比空间图大，且与设计文档 L58 的"建议-确认"口径一致，不改既有口径；② 普通用户助手里的只读 `visionTagger` **摘掉**（对齐"只有管理员能调用"），落地在 T17。**默认参数**（已在 T15 落地）：单次上限 8 张、并发 4、单张超时 45s、模型复用 vision 那组（当前 MiMo `mimo-v2.5`）。
