> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T17（助手侧收口，2026-09-15 用户拍板"普通用户助手里的只读 visionTagger 摘掉"）**按角色裁剪打标工具 + 是否复用 T15 的接口**：普通用户的助手不再挂 `visionTagger`；管理员若保留助手里的打标能力，工具应改为调 T15 的出建议/应用接口，避免提示词与词表注入两处维护
  - **实施记录（2026-09-15 完成，提交 `0616c2b`）**：① **角色信道**——backend 代理新增 `X-User-Role` 头（`AiAssistantProxyManager.chat(...)` 多一个 `userRole` 入参；角色为空**干脆不发该头**，让引擎按非同管理员处理），`AiAssistantController` 用 `loginUser.getUserRole()`（空值按 `UserConstant.DEFAULT_ROLE`，不默认给管理员）如实转述；② **引擎裁剪**——`HuoshanToolFactory.assistantTools(client, visionModel, canTagPictures, extraTools...)` 新增开关，`visionTagger` 只在"配了视觉模型 **且** 调用者是管理员"时挂载，其余五个工具不分角色；`HuoshanAssistantController` 读头得 `canTagPictures`，非管理员时记一条 info（**不记角色原文**——与 `chatId` 同属"可换行注入"的请求头输入）；③ **判定 fail-closed**——`HuoshanAssistantSession.isAdmin(role)` 只认 `admin`（忽略大小写与两侧空白），缺失/空/未知一律按普通用户。**验收达成**：装配断言（`HuoshanToolFactoryTest` 新增"普通用户在配了视觉模型时仍拿不到 visionTagger"，并断言工具名里不含 `vision*`）+ 身份判定 2 例（`HuoshanAssistantSessionTest`）+ 代理头 2 例与控制器角色 2 例（backend 侧）+ 集成测试真登录后桩引擎收到 `X-User-Role: user`。**门禁**：backend 70 → **74 例**、ai/agent 137 → **140 例**全绿；`AiAssistantProxyIntegrationTest` 8/8（11.18s）。**负向控制实测**：摘掉工厂里的 `canTagPictures` → "普通用户不含 visionTagger"红（报出多挂 `visionTagger`）；`isAdmin` 改成恒真 → 角色两条判定红。**执行偏差**：角色头常量 `USER_ROLE_HEADER` / `ADMIN_ROLE` 与 `isAdmin` 落在 `HuoshanAssistantSession`（超出上面写的三个文件，属"谁在调用"这一类的自然归属）；放在控制器里就只能靠 Spring 上下文测，而引擎侧没有控制器单测，会丢掉 fail-closed 那两条断言。④ **"是否复用 T15 的接口"结论：不复用**（理由与代价见 `docs/decisions.md` 同日行）——助手侧 `visionTagger` 服务空间图片、T15 端点服务公共图库管理页，覆盖面不同；真要合一另立条目。
  - **现场验收（2026-09-15，引擎侧已完成；证据 `%TEMP%\t23\acceptance.txt` + 引擎日志）**：本机起引擎 8124 + 真模型，用 headless 端点按角色各打一轮（`X-User-Role: admin` / 缺省或 `user`，凭据为假——本轮验的是**装配**而非工具执行）：**admin 轮日志无角色行**（`visionTagger` 挂上了），**user 轮日志出现"调用者非管理员，本次会话不挂 visionTagger（看图打标仅管理员可用）"**，与 T17 的装配断言一致。**顺带复验**：第二次会话的 MCP 日志是"处于降级冷却期"（距上次失败 <60s ⇒ T19 冷却在真机可见）；两轮 SSE **首帧都是 T22 的 `notice`**（8127 未启动）⇒ 降级提示与角色装配在同一次真机会话里同时成立。**仍未做**：① **浏览器**侧两种角色的人工验收（本页只验到引擎装配，UI 表现未看）；② 普通用户"要求打标"时的模型话术（提示词未按角色补话术）；③ 管理员侧真凭据下 `visionTagger` 的实际执行（本轮凭据是假的，工具会失败）。

> **后续（2026-09-15）**：本节「仍未做」里的浏览器侧两种角色人工验收**已补做并通过**（管理员有 `visionTagger`、普通用户没有；真凭据下 `visionTagger` 实际执行了 2 次）；② 普通用户问打标时的话术也顺带看到了（措辞正常，未暴露内部信息）；③ 管理员侧真凭据下的实际执行已验证。过程与证据见 `docs/plans/records/acceptance-2026-09-15-T17-T22.md`。
