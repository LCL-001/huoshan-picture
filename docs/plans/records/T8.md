> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T8 会话类型机制 + headless 端点（ai/agent）：会话类型（普通/图库助手）决定工具注册表与系统提示词；图库助手 SSE 端点——服务间 API key 鉴权、`huoshan:<userId>` 外部身份映射、satoken 上下文传递给工具集。**随任务执行对话循环收敛（用户 2026-09-13 拍板"对话保留 SSE 模式即可"）：抽事件消费者接口统一 run/runStream 双循环，同步 run() 退役（存量调用方迁移到事件流收集口径），lastStepKind 等受保护字段通道收编进事件载荷，SSE 事件协议（step/answer/metrics/[DONE]）固化为显式契约**
会话类型（普通/图库助手）决定工具注册表与系统提示词；图库助手 SSE 端点——服务间 API key 鉴权、`huoshan:<userId>` 外部身份映射、satoken 上下文传递给工具集。**随任务执行对话循环收敛（用户 2026-09-13 拍板"对话保留 SSE 模式即可"）：抽事件消费者接口统一 run/runStream 双循环，同步 run() 退役（存量调用方迁移到事件流收集口径），lastStepKind 等受保护字段通道收编进事件载荷，SSE 事件协议（step/answer/metrics/[DONE]）固化为显式契约**
  - 档位拆分（见上"MVP 档位"）：会话类型 + headless SSE 端点 + 鉴权与身份映射属**档 1**；循环收敛（统一 run/runStream、`run()` 退役、受保护字段收编、事件协议固化为显式契约）属**档 3（T8-hard）**。**档 1 部分已于 2026-09-14 完成**（`ConversationType` + `HuoshanAssistantAgent` + `HuoshanAssistantSession` + `HuoshanAssistantController` + `HeadlessApiKeyInterceptor`，循环零改动）；原属 T8 的"超时转显式错误事件 + 埋点"**顺延档 2**（口径见 docs/decisions.md 2026-09-14 条）
  - **实施计划（2026-09-15）**：`docs/plans/2026-09-15-T8hard-循环收敛实施计划.md`——含现状四处缺口（受保护字段旁路 / 卡死路径双 `[DONE]` / 校验失败流不以 `[DONE]` 收尾 / 失败文本多轮进气泡且重复）、事件模型与单一循环设计、四个任务与三个拍板项（**用户 2026-09-15 拍板：T8-c 纳入本轮、`MyManusTest` 直接删、`runSummary()` 保留**）。开工基线 `ai/agent` 门禁 **118 例**绿（HEAD `9daa286`）。
  - **实施记录（2026-09-15 完成，T8-hard + T8-c）**：提交链 `fb46859`（计划）→ `97079e6`（统一循环：`step()` 产出 `List<AgentEvent>`、`run()` 退役、`agent/event` 包与事件消费者接口、`createEmitter()` 缝、`MyManusTest` 删除）→ `5e11c7b`（协议契约 16 例 + 三种负向控制实测）→ `da61141`（T8-c：`error` 事件 + 重试无关的失败收尾 + 前端两处 + 代理透传守护）。**门禁**：`ai/agent` 118 → **143 例**绿，backend 47 → **48 例**绿；前端 `type-check` 138（净增 0）、改动文件 eslint 0 错。**三处有意的行为修正**：卡死路径不再双发 `[DONE]`、校验失败流补 `[DONE]`、失败/超时改发**一条** `error` 事件并终止（原文只进日志）。**真机**：改造前（`9daa286` worktree 单独打包）与改造后逐帧比对，帧形态与字段集合**逐个一致**、终止帧各 1 个；故障注入（provider 400）对照——改造前 6 条相同厂商原文 vs 改造后 1 条 `error`。**功能讲解**：`docs/features/F13-对话循环与SSE事件协议.md`；口径：`docs/decisions.md` 2026-09-15 三条（其中一条为挂账：provider 不可达时重试预算 > emitter 300s 超时）。**未做**：T13 其余教学遗留清理、浏览器端 `error` 渲染人工验收（记 F13「已知限制」）。
  - **独立 review（规则 13，2026-09-15 另开只读会话）：有条件通过**——本体成立（三处缺口读旧代码确认为真缺口；测试迁移是加强不是削弱；越界为零；`ai/agent` 全模块独立复跑 **152 例全绿** ⇒ 门禁 143 口径一致；前端 `type-check` 138 与基线持平且改动文件 0 命中）。四条待办（1 条 P2 + 3 条 P3，均只记录不修）：**P2** F11 讲解文档与 `/assistant` 页面仍宣称"只读不改"而写工具已可用；P3 上限提示未判 `state` 会多补一条回答帧；P3 `AgentEvent.Error` javadoc 的"本轮唯一"措辞过强；P3 计划 §5 设计（`AgentExecutionException` + `state=FINISHED`）与实现（`state=ERROR`）未同步、实例只跑一次的不变量未提到 `BaseAgent`。全文见 `docs/decisions.md` 2026-09-15「T8-hard 独立 review」行与 F13／F11「已知限制」。**四条待办均已收口**：P2 于 T14（提交 `29e8ee6`）；3 条 P3 于 **T21**（提交 `e4f09a5`）。
