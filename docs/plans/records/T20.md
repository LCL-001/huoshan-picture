> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T20（2026-09-15，用户要求修复本轮 review）**T19 并发首探测与文档口径收口**：MCP 故障窗口内只允许一个会话执行工具列表探测，其余并发会话立即按“少一个工具”降级，避免多个请求依次支付约 20s 超时；同时订正 T19 的 HTTP/业务码与自愈时间表述。
  - **实施记录（2026-09-15 完成）**：提交链 `ff492a4`（字据）→ `f14a7c9`（代码）。先新增并发回归：首个 provider 探测用 latch 固定在途，第二个会话同时进入；旧实现稳定红（9 例中 1 例失败，`AtomicInteger(2)` 而期望 1）。修复用非阻塞 `ReentrantLock#tryLock` 做 single-flight：只有一个会话取得探测资格，其余并发会话立即返回空工具数组；获锁后再次检查冷却，覆盖“前一个探测刚失败并写入截止时间”的竞争；全部持锁路径由 `finally` 解锁。测试清理使用 5s 调度窗口与有界 `awaitTermination`，避免制造新的时间敏感红。**验证**：目标类 9/9 绿；提交门禁 backend **74/74**、ai/agent **156/156** 绿；独立只读 review 未发现 P0-P2。
