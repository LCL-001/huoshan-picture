> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T15（P0，backend）**AI 打标服务（管理员手动触发，2026-09-15 用户拍板：不做定时/阈值自动触发）**：管理员在管理端选图 → 服务端自查词表、调多模态模型出**标签/分类建议** → 管理员确认后落库。**为什么必须在图库侧**：公共图库的图 `spaceId IS NULL`，助手的 `batchEditPictures` 走的 `/picture/edit/batch` 按 `spaceId` 查图且强制空间属主（`PictureServiceImpl.java:1145-1154`）**覆盖不到**；而单张 `editPicture`（`:921`）会调 `fillReviewParams`（`:587-599`）——非管理员打标会把图**打回待审核**、管理员打标会**覆盖 `reviewerId`/`reviewMessage`**，两条都不可接受 ⇒ 需要专用的"只改标签"服务端方法。**打标提示词照抄引擎 `VisionTaggerTool.PROMPT_TEMPLATE`（含词表注入与"优先复用已有词"规则）**，词表直接读本库 `tag`
不做定时/阈值自动触发）**：管理员在管理端选图 → 服务端自查词表、调多模态模型出**标签/分类建议** → 管理员确认后落库。**为什么必须在图库侧**：公共图库的图 `spaceId IS NULL`，助手的 `batchEditPictures` 走的 `/picture/edit/batch` 按 `spaceId` 查图且强制空间属主（`PictureServiceImpl.java:1145-1154`）**覆盖不到**；而单张 `editPicture`（`:921`）会调 `fillReviewParams`（`:587-599`）——非管理员打标会把图**打回待审核**、管理员打标会**覆盖 `reviewerId`/`reviewMessage`**，两条都不可接受 ⇒ 需要专用的"只改标签"服务端方法。**打标提示词照抄引擎 `VisionTaggerTool.PROMPT_TEMPLATE`（含词表注入与"优先复用已有词"规则）**，词表直接读本库 `tag`
  - 文件范围：backend 新增 `manager/ai/PictureAiTagManager.java`（或 `service/impl` 内方法）、`IPictureService`/`PictureServiceImpl` 新增"只改 tags/category"方法（**不碰 reviewStatus 与三个审核字段**）、`PictureController` 新增两个管理员接口（出建议 / 应用建议，均 `@AuthCheck(mustRole=ADMIN)`，先例同 `/picture/review`）、`application.yaml` + `.example` 新增 `app.ai.vision.*`（base-url/model/api-key/timeout，**密钥不入库**）、对应单测
  - 验收：① 普通用户调用两个接口均被拒（`@AuthCheck` 生效）；② **出建议接口零写库**（断言库内 tags/category 不变）；③ 应用接口只写 tags/category 并累加 `tag.usageCount`（口径同 `editPicture`：每词条每次提交 +1），**`reviewStatus`/`reviewerId`/`reviewMessage`/`reviewTime` 逐字段断言不变**；④ 落库后清公共图库列表缓存（断言 `clearPictureListCache` 被调用）；⑤ **并发=已定的"层次一"**（有界池 3~4 上限、单张超时降级、单张失败只影响该张、结果按输入顺序）——规则 12 先红后绿，桩 `AliYunAiApi` 式 HTTP 桩或 Mockito 计数型多模态桩，不连网、进门禁；⑥ 单次选图上限（建议 20 张）超限即拒；⑦ 门禁绿
  - **实施记录（2026-09-15 完成，用户拍板：两步式 + 普通用户助手摘掉只读打标）**：新增 `api/vision/AiVisionTagApi`（OpenAI 兼容看图：协议根 + `/v1/chat/completions`、文本+`image_url` 两段 content、出错文案只带状态码）、`manager/ai/PictureAiTagManager`（提示词 / **有界并发** / 单张独立计时 / 容错解析 / 清洗 / `applyTo` 组装"只改 tags+category"的更新条件）、`PictureServiceImpl#suggestAiTags/#applyAiTags`、`PictureController` 两个 `@AuthCheck(mustRole=ADMIN)` 端点、DTO/VO 三个、配置 `app.ai.vision.*`（base-url/api-key/model/timeout-ms=45000/concurrency=4/max-per-request=8，密钥不入库）。**验收①-⑦ 全过**：普通用户被 `@AuthCheck` 拦（**证据边界**：注解 + 既有 `AuthInterceptor` AOP，加一条反射守护测试钉住注解与路径；端到端 403 用例留给 T16 的前端调用链自然覆盖）；出建议零写库（真库核对）；应用只写 tags/category 且 **reviewStatus/reviewerId/reviewMessage/reviewTime 逐字段不变**（单测断言 `getSqlSet()` 不含审核列 + 集成测试真库核对）；`usageCount` 本批去重各 +1；并发四条契约（峰值并发恰为并发度、保序、单张超时降级、单张失败不连坐）；超限即拒；门禁 **48 → 70 例**绿。**负向控制**：并发池改 1 → "峰值并发恰为 3"红；摘 `@AuthCheck` → 守护测试红。**集成测试** `PictureAiTagIntegrationTest`（本机 MySQL + 桩模型，11.8s）自建唯一词条并硬删收尾，不留探针数据。**功能讲解**：`docs/features/F14-AI打标.md`。**未做**：前端入口（T16，含"该接口单独放宽超时 + 分批提示"）、助手侧工具裁剪（T17）。
  - **现场验收（2026-09-15 T24，用户浏览器实测通过）**：管理端图库页选**无标签公共图** → 出建议 → 确认入库**可用**。AI 事后真库只读复核（21:58:18 那一批 3 张）：`["风景","雪山","自然","高清","背景"]`（未给分类）、`["高清","艺术","背景"]`/`素材`、`["高清","艺术","风景"]`/`素材`；**审核四字段逐字段保持原值**（`reviewStatus=1`、`reviewMessage` 仍"管理员自动过审"、`reviewTime` 仍 2026-09-10）⇒"只改 tags/category"在真机成立；词表同步 upsert（`雪山/风景/自然` 作新词 `usageCount=1`）。**负向（AI 用真实会话实测）**：普通用户会话打两个端点均 **40400 拒绝**、候选图前后态一致（**该路径零写库**）；该链路登录态**必须 satoken + SESSION 两把同时在**（只带 satoken 回 40100，与"方案 B"口径一致）。**口径**：真机验收后端用 `--spring.profiles.active=local,test` 起（test profile 免验证码，登录只要账号+密码）。**观察（未定性）**：AI 手搓 HTTP 带齐两把凭据打 `suggest` 仍被 40400 拒，而同凭据打 `/user/get/login` 正常返回 `userRole=admin`、浏览器同一入口可用 ⇒ 差异在请求构造，复现请从 DevTools「Copy as cURL」原样回放。全文见 `decisions.md` T24 行。
