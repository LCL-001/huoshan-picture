> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T16（frontend）**管理端 AI 打标入口**：公共图库管理页给管理员加"AI 打标"入口——选图 → 跑建议（可编辑，仿 `BatchEditPictureModal` 的形状）→ 确认落库；普通用户完全看不到入口
  - **实施记录（2026-09-15 完成）**：新增 `components/AiTagPictureModal.vue`（勾选 → 出建议（标签/分类均可改，选项复用 `/picture/tag_category` 词表）→ 确认写入；超过 8 张在页面上拦住并提示分批；看图请求单独放宽 axios 超时到 180s、写入 60s——全局默认 10s 会掐断）；`pages/admin/PictureManagePage.vue` 加勾选列 + 「AI 打标（n）」按钮（无勾选时禁用）+ 成功后刷新列表并清空勾选；`api/pictureController.ts` 与 `api/typings.d.ts` **手写**两个接口与类型。**入口的管理员可见性**由既有全局守卫保证（`access.ts:22-28`：`/admin/**` 非 admin 直接跳登录页），后端 `@AuthCheck` 兜底。**检查**：`type-check` **138 → 138（净增 0）**——改动文件里的 4 处命中经 stash 基线对比确认为**既有**错误（行号仅因新增代码位移）；新组件 eslint 干净（另两处为生成器产物）；vite HMR 编译无报错、dev server 200。**未做**：~~浏览器人工点选验收（需要管理员登录态，本轮未持有）~~ **2026-09-15 T24 已补：用户浏览器实测"出建议 → 确认入库"通过**（真库核对见 T15 的现场验收行）。**重要发现**：`npm run openapi` 现在会把 `api/` 全量改名并删掉手写文件，**不可用**（见 `decisions.md` 2026-09-15 行），新接口一律按现有风格手写。
  - **现场 bug 与修复（2026-09-15，用户实测报"看图失败：系统错误"）**：根因＝管理页的 `a-table` **没配 `row-key`**（ant-design-vue 默认 `rowKey='key'`，图库记录没有该字段）⇒ 勾选给出的 key 是 `undefined` ⇒ `String(undefined)` 得到**字面量 `"undefined"`** ⇒ 请求体 `{"pictureIdList":["undefined"]}` ⇒ 后端 Jackson 解析 `List<Long>` 失败（日志原文 `Cannot deserialize value of type java.lang.Long from String "undefined"`）⇒ `HttpMessageNotReadableException` 落到 `GlobalExceptionHandler` 的泛 `RuntimeException` 兜底 ⇒ **`50000 系统错误`**——**与模型配置无关**（请求根本没走到调模型那一步）。**修复**：表格补 `row-key="id"`；新增纯函数 `frontend/src/utils/pictureSelection.ts`（`toPictureIdList` 规范化勾选 key：丢弃 `undefined`/空串/超出安全整数范围的 number；`isPictureIdList` 校验），页面与弹窗两侧接入——拿不到合法 id 就在前端拦住并提示，不再发脏请求。**验证（规则 12 先红后绿）**：仓外回归脚本 `%TEMP%\t16-selection\check.mjs`（esbuild 转译真实纯函数后 node 断言，10 例）修复前**红**（模块不存在；旧写法 `[undefined].map(String)` 实测产出 `["undefined"]`，与后端日志逐字一致），修复后 **10/10 绿**；**后端契约双向实测**（无需登录态即可区分）——数字字符串 id → `40100 未登录`（解析通过、走到鉴权门槛），字面量 `"undefined"` → `50000`（解析即失败）；前端 `type-check` 仍 **138**（新文件 0 错）、新代码 eslint 干净。**遗留**：请求体解析失败与真正的服务端故障共用同一句"系统错误"，已记 F14 已知限制 9。
