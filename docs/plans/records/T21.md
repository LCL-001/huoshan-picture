> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T21（2026-09-15，用户"执行"T8-hard review 遗留的三条 P3）**循环收尾与文档口径收口**：① 修掉"最终回答恰好落在第 `maxSteps` 步时多补一条上限提示"；② 订正 `AgentEvent.Error` javadoc 的"本轮唯一"措辞；③ 把计划 Task 3 的设计偏差入档，并把"一个 agent 实例只跑一次"的不变量从 `HuoshanAssistantAgent` 提到 `BaseAgent`。
① 修掉"最终回答恰好落在第 `maxSteps` 步时多补一条上限提示"；② 订正 `AgentEvent.Error` javadoc 的"本轮唯一"措辞；③ 把计划 Task 3 的设计偏差入档，并把"一个 agent 实例只跑一次"的不变量从 `HuoshanAssistantAgent` 提到 `BaseAgent`。
  - 文件范围：`ai/agent` 的 `agent/BaseAgent.java`、`agent/HuoshanAssistantAgent.java`、`agent/event/AgentEvent.java` + 对应测试（`BaseAgentTest`、`AgentFailureEventTest`）；文档 `docs/plans/2026-09-15-T8hard-循环收敛实施计划.md`、`docs/decisions.md`、`docs/plan.md`、`F13`、`F12`。**不动**事件协议字段与帧序、前端、backend、任何配置。
  - 验收：① 先红后绿（新契约测试修复前必红，红输出逐字抄进实施记录）+ 负向控制（摘掉 guard → 恰好这两条红）；② ②③ 零行为改动；③ `ai/agent` 156 → 159 例、backend 74 例绿；④ 门禁绿。
  - **实施记录（2026-09-15 完成，提交 `e4f09a5`）**：① `runLoop` 尾部的上限判断加前置条件 `this.state == AgentState.RUNNING`——循环的两个退出条件（`state != FINISHED` 与 `currentStep < maxSteps`）会**同时成立**，所以"到步数"不等于"没答完"；② `AgentEvent.Error` javadoc 改为"**替代回答帧**、是本轮**最后**一条业务事件"，写明"只有第一步就失败时才是唯一事件"与"全仓产出点仅 `BaseAgent.runLoop` 的 catch 一处"；③ `BaseAgent` 类注释补"实例生命周期不变量"整段（写明它**不被入口守卫强制**；真正让实例不可复用的是 `ERROR` 与 `stopped` 两处状态粘连），`HuoshanAssistantAgent` 那句收敛为指回父类；计划 Task 3 的 `AgentExecutionException.java`（新）与"状态 `FINISHED`"两处**标注实施偏差**，原设计正文不改写（选"文档对齐代码"：`state=ERROR` 有承载）。
  - **红绿实测（规则 12）**：`shouldNotAppendMaxStepsNoticeWhenAnswerLandsOnTheLastStep` 红输出 `expected: "final-answer"` / `but was: "final-answer` + 换行 + `执行结束：达到最大步骤 (1)"`；`shouldNotAppendMaxStepsNoticeWhenStuckTerminationLandsOnTheLastStep` 红在 `doesNotContain("达到最大步骤")`（实际串 = "检测到循环，智能体已终止" + 换行 + "执行结束：达到最大步骤 (3)"）；修复前同类其余 13 例保持绿。**负向控制**：修复后把 `state == AgentState.RUNNING` 摘掉重跑，恰好这两条红、同类其余 13 例绿。**新增 3 例**：`BaseAgentTest` 两条（回答落在最后一步 / 卡死落在最后一步）+ `AgentFailureEventTest` 一条（中途失败：error 之前已有回答帧，故它不是本轮唯一事件）。**验证**：目标类 22/22；`ai/agent` 全量 **159/159**；提交门禁 backend **74/74** + ai/agent **159/159** 绿。
