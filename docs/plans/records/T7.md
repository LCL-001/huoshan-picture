> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T7 图库工具集（ai/agent）：`tools/huoshan/` 新增——listSpaces / listPictures / getTagCategory / batchEditPictures / batchUploadByUrl（RestClient 封装图库 API，prototype 化 per-request 注入透传 token 与图库地址）+ visionTagger（OpenAI 协议多模态批量看图，提示词注入词表）；粗粒度原则，一期不注册删除类工具
`tools/huoshan/` 新增——listSpaces / listPictures / getTagCategory / batchEditPictures / batchUploadByUrl（RestClient 封装图库 API，prototype 化 per-request 注入透传 token 与图库地址）+ visionTagger（OpenAI 协议多模态批量看图，提示词注入词表）；粗粒度原则，一期不注册删除类工具
  - **档 3 进度（2026-09-14）**：**T7 全部完成**——两个批量工具（`BatchEditPicturesTool` / `BatchUploadByUrlTool` + 三个结果 DTO + 客户端 `batchEditPictures` / `uploadByUrl`）+ **看图打标 `VisionTaggerTool`**（图片 id → 工具自己经 `/picture/get/vo` 取地址 → 逐张调多模态模型，提示词注入当前词表）+ `HuoshanToolFactory.assistantTools`（取代 `readOnlyTools`；**未配视觉模型时不挂 visionTagger**）+ 助手系统提示词放开写操作（写明"只在用户明确要求时改、不得编造 id、如实上报、不支持删除、建议需用户确认后才落库"）。新增 **20 条单测**（写类工具 11 + 看图打标 8 + 装配 1，含**工具 schema 数组契约**：`pictureIdList` / `fileUrls` 必须发布为 `array`，否则模型只会当字符串传），门禁 **107 例**绿。实施计划、口径与阻塞项见 `docs/plans/2026-09-14-档3-完整一期实施计划.md`；剩余：T9 MCP 搜图、T8-hard 循环收敛、T13 清理、T12 联调（T12 需用户提供 `AI_OPENAI_*` 与 Pexels 密钥）
  - **勘误（2026-09-15，T17 会话核清单时发现）**：本条此前一直是 `[ ]`，但其下两个档位记录早已写明"档 1 部分已于 2026-09-14 完成"与"**T7 全部完成**"——**是漏勾，不是未做**（子条目含实施记录的那部分才是准的）。现只把勾选补上，正文一字未改。**教训**：0022/0023 记的"以 plan 勾选状态为准，不要照抄上一份快照的待办"要再加半句——**勾选本身也可能漏**，逐条核实时以"该条目下有没有实施记录"为准，勾选只是辅助。同批核实结果：本节"MVP 档位与推进顺序"那条容器仍**有意保持未勾**（它承载的 R1 处置"延后到档 2 之后"已过期、待用户决策）；清单其余未勾项（review 3 条 P3）确为未做。
