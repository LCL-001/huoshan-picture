> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T19（2026-09-15 用户拍板"做 MCP 降级"）**搜图 MCP 不可达时的降级**：把"8127 没起 ⇒ 每次对话在装配工具那步整条抛、HTTP 200 + 业务码 50000（JSON、无 SSE 流）、前端只看到连接中断"改成"少一个工具照常开对话"，并且不让服务长时间不可用时每次对话都白等一次连接超时。
  - **背景与证据**：`HuoshanAssistantController.chat:107` 的 `mcpToolCallbackProvider.getToolCallbacks()` 在 MCP 服务不可达时抛异常（栈顶 `McpSyncClient.listTools`，T18 会话实测 20.1s 后回 50000）；20.1s ≈ `spring.ai.mcp.client.request-timeout` 的默认值 20s（已从 spring-ai-autoconfigure-mcp-client-common **1.1.2** 的 `spring-configuration-metadata.json` 核实）。T9 的 `initialized: false` 只覆盖"MCP 没起不拖垮引擎**启动**"，没覆盖"Bean 在、服务不可达"这一形态。
  - 文件范围：ai/agent 新增 `tools/mcp/McpToolCallbackResolver.java`；改 `controller/HuoshanAssistantController.java`（第 106-107 行的解析改为走 resolver）；新增 `tools/mcp/McpToolCallbackResolverTest.java`；`application.yaml` 仅补注释（写明 request-timeout 的实际作用），不动键值
  - 验收：单测——provider 抛异常 ⇒ 空数组且不抛出；冷却期内不重复尝试（provider 调用计数不再增长）；冷却到期重试，成功即清冷却；provider 为 null 时不进冷却（T9 口径不变）；正常路径原样返回工具。**先红后绿**（规则 12：先跑一次现状基线取证）＋门禁绿＋负向控制（摘 try/catch ⇒ 降级断言红；冷却置 0 ⇒"不重复尝试"红）＋**真机复跑**：不启 8127、且**不带** `--spring.ai.mcp.client.enabled=false` 打 headless 端点，期望不再 50000、对话正常出事件流，且第二次对话明显快于第一次（冷却生效）；反向对照：起 8127 后搜图工具仍能挂上（降级不误伤正常路径）。
  - **实施记录（2026-09-15 完成）**：提交链 `731a481`（本字据）→ `669794a`（代码）。新增 `tools/mcp/McpToolCallbackResolver`（`@Component`，唯一的 `resolve(Supplier<ToolCallbackProvider>)` 不抛异常：Bean 不存在直接空数组且**不进冷却**，取 Bean/列工具任一步失败 ⇒ 空数组 + warn + 60s 冷却，成功 ⇒ 清冷却；时间用 `LongSupplier` 注入、生产走 `System::nanoTime`），控制器第 106-107 行改为一行 `resolve(mcpToolCallbackProvider::getIfAvailable)`；`application.yaml` 只补注释（写明 request-timeout 的实际作用），键值未动。**门禁**：ai/agent 147 → **155 例**（新增 `McpToolCallbackResolverTest` 8 例）、backend 74 例不变。**负向控制实测会红**：在 catch 里强制重抛（等价于摘掉降级）→ 4 例红（两条降级用例 + 冷却用例两条，因它们都以"首次解析失败"开头）；把 `isCoolingDown()` 改成恒 false（等价于摘掉冷却）→ 恰好 1 例红，失败信息 `Expecting AtomicInteger(2) to have value: 1`（正是"冷却期内不该再尝试"这条）。**红基线（先红后绿的第一手证据）**：修复前同形态实测 HTTP 200 + `{"code":50000,...}` + 20.13s + 无 SSE，栈顶 `HuoshanAssistantController.java:107`，异常 `CompletionException(ConnectException)`；20s 上界经核实来自 `spring.ai.mcp.client.request-timeout` 默认值（1.1.2 元数据）。**真机复跑（修复后，8127 不启）**：首次对话 24.1s 收到 `event=error` + `[DONE]`（无 50000）、紧接着第二次 3.62s（冷却生效），日志依次为一条 WARN（`RuntimeException: Client failed to initialize listing tools`）与一条 INFO（冷却期）。**反向对照**：8127 起来后同形态 4.35s、零降级日志；真模型（MiMo）下 `searchImage` 照常被调用并返回 Pexels 地址（15.15s，`tool`/`answer`/`[DONE]`）。证据与脚本在 `%TEMP%\t19\`。
  - **顺带实测发现（2026-09-15，与 T19 无关、未修、待用户拍板）**：`OpenAiChatModelsRetryTest.totalBudgetTruncatesRetriesEvenWhenAttemptsRemain`（T18 的用例）是**时间敏感**的——预算 800ms、首次退避 500ms，两者太近。本轮全量跑里偶发红一次（`expected: 2 but was: 3`，该用例自身耗时 2.399s），而**单类隔离跑 4 次全绿**（每次 9.4s）⇒ 只在满载（并行跑全模块）时触发，**门禁会随机拦提交**。收口二选一：把断言放宽成"远小于 10 次"（如 `isLessThanOrEqualTo(3)`），或用可控时钟/更宽的预算-退避比把时序钉死；属 T18 的文件范围，按规则 1 不在 T19 内顺手改。**已收口（2026-09-15）：见上方 T18 补记——选了"更宽的预算-退避比"这一支，并订正了本条记的偏差方向（确定性可复现的是"退化成 1 次"，"3 次"需墙钟回拨才能解释）。**
