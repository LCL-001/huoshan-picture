> 实施记录（自 docs/plan.md 外移，2026-09-15；**内容逐字保留**，下面第一行是该条目的完整原文）

- [x] T22（2026-09-15，用户拍板"给用户可见提示"）**MCP 降级可见化 + 前端社交残留清理**：① 搜图 MCP 降级不再是静默的——引擎在本轮开头发一条 `notice` 帧，前端渲染成一行提示；② 删掉停用的帖子/社交前端残留，只保留通知。
  - ① 文件范围：`ai/agent` 的 `agent/event/AgentEvent.java`（新增 `Notice`）、`agent/event/SseAgentEventListener.java`、`agent/BaseAgent.java`（`runStream`/`runLoop` 的开场事件重载）、`controller/HuoshanAssistantController.java`、`tools/mcp/McpToolCallbackResolver.java`（`resolve` 返回 `McpTools(callbacks, degraded)`）+ 对应测试；`frontend/src/utils/assistantSse.ts`、`frontend/src/pages/AssistantPage.vue`。**不动** AgentEvent 既有字段与帧序、后端代理（`relay()` 原样转发未知 `event`，零改动）、`request-timeout`、冷却参数与鉴权。
  - ② 文件范围：`frontend/src/api/notificationController.ts`（新，通知 6 函数迁入）、`components/GlobalHeader.vue`（改 import + 删孤儿 `goToMyProfile` 与"论坛/我的主页"注释块）、`router/index.ts`（删 3 条路由与静态 import）、`components/PageTransition.vue`（`/post/` 分支）、删 `pages/SquarePage.vue`、`pages/PostDetailPage.vue`、`pages/UserProfilePage.vue`、`api/postController.ts`。
  - **实施记录（2026-09-15 完成，提交 `2da2d67`（MCP 提示）、`fcd668d`（前端清理）+ 一笔 `docs:`）**：① 新增 `AgentEvent.Notice` + `{"event":"notice","content":...}` 帧；`BaseAgent` 增 `runStream(prompt, openingEvents)` / `runLoop(prompt, listener, openingEvents)` 重载（**校验失败时不发开场事件**，其余路径一字未改，原两参版本委托空列表）；`McpToolCallbackResolver.resolve` 返回值由 `ToolCallback[]` 改为 `McpTools(callbacks, degraded)`——**Bean 不存在是 `degraded=false`**（配置选择，不提示用户），服务不可达/冷却期内/其它会话探测在途是 `true`；控制器按 `degraded` 决定是否带提示。文案"图片搜索服务暂时不可用，本轮对话无法帮你搜图（其它能力不受影响）"（不提 "MCP"，用户认不出这个词）。前端 `onNotice` 为**可选** handler（老消费者不受影响）。**测试**：`BaseAgentTest` 新增 2 例（开场事件先于循环产出 / 校验失败不发）、`McpToolCallbackResolverTest` 新增 3 例（正常路径不降级、Bean 不存在不降级、失败与冷却期降级且自愈后清除），既有 13 处 `resolve` 调用点改用 `toolsOf(...)` 读法保持原断言不变 ⇒ `ai/agent` 159 → **164 例**。② 走"通知迁出再删"的顺序：`postController.ts` 是**混装文件**（生成器产物 + 手写通知函数），先把 6 个通知函数迁到新文件 `notificationController.ts`、改 `GlobalHeader.vue` 的 import，再整文件删除；三个页面只被 `router/index.ts` 静态 import，随路由一并删；顺带清掉两处注释残留与孤儿函数 `goToMyProfile`（其目标路由已删）。**验证**：`type-check` 124（改动前亦 124，删除的三个页面自身带 14 条存量错误所以净减；改动文件里只有 `GlobalHeader.vue` 的 8 条**存量**错误——`ReadOutlined` 未使用、`afterEach` 形参未使用、5 处 `any`，已核实 HEAD 版本同形）、改动文件 eslint 0 新增错、门禁 backend 74 + ai/agent 164 绿。
  - **未做 / 边界**：提示只在"本轮开头"发（通用机制是 `openingEvents`，"中途才发现降级"覆盖不到，见 F13 已知限制 10）；**没让模型知道**降级（提示词未改，模型坚持搜图仍会拿到工具错误）；降级提示的浏览器人工验收未做（只过 `type-check`/`eslint` 与帧级契约）；`api/index.ts`（零引用死文件）与 `typings.d.ts` 里的 `Post*` 类型**未删**（前者是另一个挂账、后者是生成产物，均不在本次范围内）。
