# T4 AI 引擎迁入实施计划

> **执行方式说明：** 本计划按任务逐条执行，步骤用 `- [ ]` 勾选跟踪。可在本会话内联执行，也可派发子代理逐任务执行（每个任务一个新上下文，任务间人工审查）。T5-T12 的实施计划在各任务开工前即时编写（本文件不含），plan.md 保持总索引。
>
> 对应：plan.md T4 ｜ 设计文档 docs/designs/2026-09-13-图库智能体一期设计.md R2 修订

**Goal:** 把 MyManus 引擎与 image-search-mcp-server 原样迁入 huoshan-picture 仓库 `ai/` 目录，门禁纳入 ai/agent 单测，双服务冒烟，源仓库 yu-ai-agent 冻结归档。

**Architecture:** 同仓不同应用——`ai/agent/`（SB 3.5/Java 21，独立进程，端口 8124）+ `ai/image-search-mcp-server/`（独立进程，端口 8127）；不并 Maven reactor。运行时架构（代理 + token 透传）不在本计划范围（T10）。

**Tech Stack:** Spring Boot 3.5 / Spring AI Alibaba 1.1.2.0（迁入物）、系统 Maven 3.9 + JDK 21、cmd 命令行（Windows）。

---

## 前置事实（执行者必读）

- 源仓库：`D:\develop\codes_claudecode\yu-ai-agent`（dev 分支）。**其工作区的 README.md 有用户自己的未提交改动，任何操作不许碰它**。
- 目标仓库：`D:\develop\idea_java_projects\huoshan-picture`（main 分支）。
- 端口占用：8123 = 图库 backend；5173 = 图库前端；8127 = image-search-mcp-server（其 application.yaml 现值）；**agent 由 8123 改为 8124**。
- 迁移清单（源仓 git 跟踪项，已核实）：`pom.xml`、`src/`、`mvnw`、`mvnw.cmd`、`.mvn/`、`create_sql/`、`docker-compose.yml`、`.env.example`。**不迁**：`Dockerfile`、`nginx.conf`（部署遗留留归档）、`.githooks`/`.gitattributes`（目标仓有自己的）、`my-ai-agent-frontend/`（冻结）、`doc/`、`docs/`、`AGENTS.md`、`CLAUDE.md`、`target/`、`.idea/`、`data/`（未跟踪）。
- 本地密钥件（手动复制、绝不入库）：`src/main/resources/application-local.yaml`、`.env`。
- 门禁现状：`.githooks/pre-commit` 对 docs-only 提交跳过；对代码提交跑 `backend` 无 MySQL/Redis 单测。本计划 Task 3 追加 `ai/agent` 独立检查块（沿用源仓排除清单：`!MyManusTest,!MyAiAgentApplicationTests,!ProductScopeTest,!PgVectorVectorStoreConfigTest,!FileOperationToolTest,!PDFGenerationToolTest,!ResourceDownloadToolTest,!TerminalOperationToolTest,!WebScrapingToolTest,!WebSearchToolTest`，55 用例基线）。
- shell 纪律（cmd）：命令一律单行；`robocopy` 退出码 0-7 均为成功；含空格路径加引号。
- 本机 MySQL(3306)/Redis(6379) 在跑；agent 的本地库 `yu-ai-agent` 已存在（源仓日常使用中），冒烟起服务直接可用。

---

### Task 1: 迁入 ai/agent 并保持构建与单测绿

**Files:**
- Create: `ai/agent/**`（自源仓复制）
- Modify: `.gitignore`（根，追加 ai/ 忽略规则）
- Modify: `ai/agent/src/main/resources/application.yaml:94-97`（端口 8123→8124）

- [ ] **Step 1.1 建目录并复制迁移清单**（逐条单行执行）

```cmd
robocopy "D:\develop\codes_claudecode\yu-ai-agent\src" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\src" /E /XD target
copy /Y "D:\develop\codes_claudecode\yu-ai-agent\pom.xml" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\pom.xml"
copy /Y "D:\develop\codes_claudecode\yu-ai-agent\mvnw" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\mvnw"
copy /Y "D:\develop\codes_claudecode\yu-ai-agent\mvnw.cmd" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\mvnw.cmd"
robocopy "D:\develop\codes_claudecode\yu-ai-agent\.mvn" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\.mvn" /E
robocopy "D:\develop\codes_claudecode\yu-ai-agent\create_sql" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\create_sql" /E
copy /Y "D:\develop\codes_claudecode\yu-ai-agent\docker-compose.yml" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\docker-compose.yml"
copy /Y "D:\develop\codes_claudecode\yu-ai-agent\.env.example" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\.env.example"
```

- [ ] **Step 1.2 复制本地密钥件（不入库）**

```cmd
copy /Y "D:\develop\codes_claudecode\yu-ai-agent\src\main\resources\application-local.yaml" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\src\main\resources\application-local.yaml"
copy /Y "D:\develop\codes_claudecode\yu-ai-agent\.env" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\.env"
```

- [ ] **Step 1.3 改端口**：读 `ai/agent/src/main/resources/application.yaml`，将第 95 行 `port: 8123` 改为 `port: 8124`（`context-path: /api` 不动）。核对 `application-local.yaml` / `application-ollama.yaml` / `application-prod.yaml` 无端口覆写（已核实无，改完再 findstr 一次确认）：

```cmd
findstr /n /s "port:" "D:\develop\idea_java_projects\huoshan-picture\ai\agent\src\main\resources\application*.yaml"
```

预期：仅 application.yaml 一处 8124。

- [ ] **Step 1.4 根 `.gitignore` 追加**（文件末尾）：

```gitignore
# AI 模块（ai/agent、ai/image-search-mcp-server）本地件
ai/**/target/
ai/**/.env
ai/**/application-local.yaml
ai/**/application-prod.yaml
ai/**/application-test.yaml
```

- [ ] **Step 1.5 构建 + 门禁口径单测**：

```cmd
cd /d "D:\develop\idea_java_projects\huoshan-picture\ai\agent"
mvn -B test "-Dtest=!MyManusTest,!MyAiAgentApplicationTests,!ProductScopeTest,!PgVectorVectorStoreConfigTest,!FileOperationToolTest,!PDFGenerationToolTest,!ResourceDownloadToolTest,!TerminalOperationToolTest,!WebScrapingToolTest,!WebSearchToolTest" -DfailIfNoTests=false
```

预期：`Tests run: 55, Failures: 0, Errors: 0`（55 用例基线）。

- [ ] **Step 1.6 提交**（明确路径 add，禁用 `git add -A`）：

```cmd
cd /d "D:\develop\idea_java_projects\huoshan-picture"
git add ai/agent .gitignore
git commit -m "chore: 迁入 MyManus 引擎至 ai/agent（原样迁移，端口 8124）"
```

预期门禁：跑 backend 单测 → 绿 → 放行。提交后 `git status` 确认 application-local.yaml 与 .env 未被跟踪。

### Task 2: 迁入 ai/image-search-mcp-server

**Files:**
- Create: `ai/image-search-mcp-server/**`

- [ ] **Step 2.1 复制**（先核实源仓该子项目的跟踪清单）：

```cmd
cd /d "D:\develop\codes_claudecode\yu-ai-agent\image-search-mcp-server"
git ls-files
```

预期输出为 pom.xml + src/** （若有 .mvn/mvnw 一并复制）：

```cmd
robocopy "D:\develop\codes_claudecode\yu-ai-agent\image-search-mcp-server\src" "D:\develop\idea_java_projects\huoshan-picture\ai\image-search-mcp-server\src" /E /XD target
copy /Y "D:\develop\codes_claudecode\yu-ai-agent\image-search-mcp-server\pom.xml" "D:\develop\idea_java_projects\huoshan-picture\ai\image-search-mcp-server\pom.xml"
```

- [ ] **Step 2.2 构建**：

```cmd
cd /d "D:\develop\idea_java_projects\huoshan-picture\ai\image-search-mcp-server"
mvn -B package -DskipTests
```

预期：`BUILD SUCCESS`。

- [ ] **Step 2.3 提交**：

```cmd
cd /d "D:\develop\idea_java_projects\huoshan-picture"
git add ai/image-search-mcp-server
git commit -m "chore: 迁入 image-search-mcp-server 至 ai/"
```

### Task 3: 门禁纳入 ai/agent 单测

**Files:**
- Modify: `.githooks/pre-commit`（主 if/elif 链之后、"PASS" 输出之前插入独立块；保持 LF）

- [ ] **Step 3.1 插入检查块**（在现有 `fi` 之后、`echo "[vibe-gate] PASS: tests green, commit allowed."` 之前）：

```sh
# AI 引擎（ai/agent）：沿用源仓 yu-ai-agent 的纯单测排除口径
if [ -f ai/agent/pom.xml ]; then
  (cd ai/agent && mvn -B test -Dtest='!MyManusTest,!MyAiAgentApplicationTests,!ProductScopeTest,!PgVectorVectorStoreConfigTest,!FileOperationToolTest,!PDFGenerationToolTest,!ResourceDownloadToolTest,!TerminalOperationToolTest,!WebScrapingToolTest,!WebSearchToolTest' -DfailIfNoTests=false) || fail
fi
```

- [ ] **Step 3.2 正向验证**：对一个含代码外文件的提交观察门禁放行（如 `git commit` 一处 `.gitignore` 级改动），预期先跑 backend 测试再跑 ai/agent 测试后 PASS。

- [ ] **Step 3.3 负向验证（门禁真的会咬人）**：临时在 `ai/agent/src/test` 加一个必然失败的测试类 → 提交任一代码文件 → 预期 `[vibe-gate] FAIL` 拦截 → 删除临时测试并确认工作区干净。

- [ ] **Step 3.4 提交**：

```cmd
git add .githooks/pre-commit
git commit -m "ci: 门禁纳入 ai/agent 纯单测（沿用源仓排除口径）"
```

### Task 4: 双服务冒烟（无代码改动，验证任务）

- [ ] **Step 4.1 agent 冒烟**：

```cmd
cd /d "D:\develop\idea_java_projects\huoshan-picture\ai\agent"
mvn -B package -DskipTests
dir /b target\*.jar
java -jar target\<上一步实际 jar 名>
```

新开窗口：`curl http://localhost:8124/api/health` 预期 200 与健康 JSON。确认日志无端口冲突。

- [ ] **Step 4.2 MCP 服务冒烟**：

```cmd
cd /d "D:\develop\idea_java_projects\huoshan-picture\ai\image-search-mcp-server"
java -jar target\*.jar
```

预期日志 `Started ImageSearchMcpServerApplication` 且监听 8127（PEXELS_API_KEY 可暂用 .env 既有值）。

- [ ] **Step 4.3（可选）** Pexels 真调用验证：有有效 key 时对 MCP SSE 端点做一次工具调用或直接 curl Pexels API 确认 key 有效；无效则记录"待 T9 联调"。

### Task 5: 归档源仓库 yu-ai-agent

- [ ] **Step 5.1【停下问用户】** 其工作区 README.md 未提交改动的处置（用户自行提交 / 放弃 / 先摘走）。在用户答复前不执行本任务后续步骤。
- [ ] **Step 5.2 写归档交接快照** `docs/handoff/2026-09-13-引擎迁出归档.md`：迁移去向（huoshan-picture ai/agent、ai/image-search-mcp-server）、本次 tag 名、任务移交映射（原 T4-T8 → 图库 plan T6-T9；原 T1 → 图库 T13；T2 作废）、门禁/标签现状。提交：`docs: handoff 2026-09-13 引擎迁出归档`。
- [ ] **Step 5.3 打归档标签**：

```cmd
git tag archive/engine-migrated-to-huoshan
```

- [ ] **Step 5.4 汇报归档完成；此后源仓库只读，后续开发全部在 huoshan-picture。**

### Task 6: 图库项目速览与 README 同步

- [ ] **Step 6.1** `AGENTS.md` 项目速览追加一条："`ai/`（2026-09-13 自 yu-ai-agent 迁入）：ai/agent = MyManus 智能体引擎（SB 3.5/Java 21，端口 8124，独立进程；一期任务 T6-T9 改造点见 docs/plans/）；ai/image-search-mcp-server = Pexels 搜图 MCP 服务（端口 8127）"；`README.md` 目录结构补 `ai/` 两行。
- [ ] **Step 6.2 提交**：`docs: 项目速览与 README 补 ai/ 模块说明`。

---

## 自检记录（写计划时已核对）

- spec 覆盖：plan.md T4 的四项验收（迁入+原样、门禁绿、双服务冒烟、速览同步）分别对应 Task 1/3/4/6；归档（decisions R2 承诺）对应 Task 5。
- 类型/名称一致性：任务编号 T4 与 plan.md 一致；排除清单与源仓 AGENTS.md 门禁命令逐字一致；端口口径 8123/8124/8127/5173 全文一致。
- 无占位符：唯一的运行期取值为 `<上一步实际 jar 名>`（Step 4.1，由 `dir /b target\*.jar` 现场确定）与 Step 2.1 的 ls-files 核实（防源仓有未列出的跟踪文件），均为执行期现场读取动作，非设计缺口。
