> 自 docs/decisions.md 外移（2026-09-15）；**口径以 decisions.md 表内那一行为准**，本文件保留完整原文。

## 原始表格行

| 2026-09-15 | **T24：AI 打标"现场验收"完成（用户浏览器实测：出建议 → 确认入库可用）+ 真库核对佐证 + 两条负向证据** | **正向（用户实测）**：管理端图库页选无标签公共图 → 出建议 → 确认入库，流程走通。**真库核对（AI 事后只读复核，2026-09-15 21:58:18 那一批）**：3 张公共图被写入——`["风景","雪山","自然","高清","背景"]`（无分类）、`["高清","艺术","背景"]`/`素材`、`["高清","艺术","风景"]`/`素材`；**审核四字段全部保持原值**（`reviewStatus=1`、`reviewMessage` 仍是"管理员自动过审"、`reviewTime` 仍是 2026-09-10，未被 apply 覆盖）；词表对应条目同步 upsert（`雪山/风景/自然` 新词 `usageCount=1`，`高清/艺术/素材` 基数 19/13/19 且 `updateTime` 同为 21:58:18）。**负向（AI 用真实会话实测）**：① 普通用户会话打两个端点均回 **40400 无权限**（fail-closed 生效），且两张候选图前后态完全一致 ⇒ **该路径零写库**；② 该链路的登录态**必须 satoken + SESSION cookie 两把同时在**（只带 satoken 回 40100，与"方案 B"口径一致）。 | **口径补充（下次别踩）**：真机验收要用 `--spring.profiles.active=local,test` 起后端——test profile 关掉图形/邮箱验证码校验，登录只需账号+密码（见 `application-test.yaml` 头注释）。**观察（未定性，留给下次）**：AI 用"手搓 HTTP"（SESSION+satoken 双凭据都已带上）打 `ai_tag/suggest` 仍被 `40400` 拒，而同一套凭据打 `/user/get/login` 正常返回 `userRole=admin`、浏览器里同一账号点同一入口可用 ⇒ **差异在我的请求构造，不在数据或应用逻辑**；要复现请从浏览器 DevTools 取 "Copy as cURL" 原样回放（比继续猜请求头快）。**打标验收记录尚未镜像进 `plan.md` T15 条目**（本轮上下文用尽，仅此一处补记待办）。 | 生效 |

## 决定

**T24：AI 打标"现场验收"完成（用户浏览器实测：出建议 → 确认入库可用）+ 真库核对佐证 + 两条负向证据**

## 理由

**正向（用户实测）**：管理端图库页选无标签公共图 → 出建议 → 确认入库，流程走通。**真库核对（AI 事后只读复核，2026-09-15 21:58:18 那一批）**：3 张公共图被写入——`["风景","雪山","自然","高清","背景"]`（无分类）、`["高清","艺术","背景"]`/`素材`、`["高清","艺术","风景"]`/`素材`；**审核四字段全部保持原值**（`reviewStatus=1`、`reviewMessage` 仍是"管理员自动过审"、`reviewTime` 仍是 2026-09-10，未被 apply 覆盖）；词表对应条目同步 upsert（`雪山/风景/自然` 新词 `usageCount=1`，`高清/艺术/素材` 基数 19/13/19 且 `updateTime` 同为 21:58:18）。**负向（AI 用真实会话实测）**：① 普通用户会话打两个端点均回 **40400 无权限**（fail-closed 生效），且两张候选图前后态完全一致 ⇒ **该路径零写库**；② 该链路的登录态**必须 satoken + SESSION cookie 两把同时在**（只带 satoken 回 40100，与"方案 B"口径一致）。

## 状态

**口径补充（下次别踩）**：真机验收要用 `--spring.profiles.active=local,test` 起后端——test profile 关掉图形/邮箱验证码校验，登录只需账号+密码（见 `application-test.yaml` 头注释）。**观察（未定性，留给下次）**：AI 用"手搓 HTTP"（SESSION+satoken 双凭据都已带上）打 `ai_tag/suggest` 仍被 `40400` 拒，而同一套凭据打 `/user/get/login` 正常返回 `userRole=admin`、浏览器里同一账号点同一入口可用 ⇒ **差异在我的请求构造，不在数据或应用逻辑**；要复现请从浏览器 DevTools 取 "Copy as cURL" 原样回放（比继续猜请求头快）。**打标验收记录尚未镜像进 `plan.md` T15 条目**（本轮上下文用尽，仅此一处补记待办）。
