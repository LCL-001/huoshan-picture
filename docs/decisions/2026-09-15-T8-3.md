> 自 docs/decisions.md 外移（2026-09-15）；**口径以 decisions.md 表内那一行为准**，本文件保留完整原文。

## 原始表格行

| 2026-09-15 | **T8-hard 独立 review（规则 13，另开只读会话）：有条件通过** | **复核范围**：提交链 `fb46859`→`0dd8e56`（引擎循环收敛 + 协议契约 + T8-c error 事件）。**本体成立**：① 三处协议缺口读旧代码逐一确认为真缺口、修法正确（卡死路径旧 `:208`+`:231` 双发 `[DONE]`；校验失败流发 `answer` 后直接 `complete()`；`ToolCallAgent.think()` 吞异常写 `messageList` 却不置 `FINISHED` ⇒ 同段厂商原文重复多轮，"失败当轮终止"在修复前确实要跑 5 步）；② 契约测试与失败收尾测试是真钉住（三种负向控制已实测）；③ 测试迁移是**加强**不是削弱——`ReActAgentTest` 两条"吞异常返回错误文本"用例改成"必须上抛"，`BaseAgentTest` 的 `run()` 用例全部等价改写为事件流断言，覆盖面无缺失（非 IDLE／空／null 提示词、上限、卡死、`currentStep` 归零、生命周期都在），不构成"删测试换绿灯"；④ 越界为零（改动文件 = `ai/agent` + 用户拍板的 frontend 两处 + backend 1 例测试 + docs + `.githooks`，无任何配置文件）；⑤ 安全面改善确认——失败文案固定、原文只进日志，且有断言保证不进事件流与 `messageList`。**独立复跑**：`ai/agent` 全模块 `mvn -B -o test` **152 例全绿**（门禁排除名单去掉 9 类 ⇒ 143，与文档口径一致）；前端 `npm run type-check` **138 例**（与基线持平、两个改动文件 0 命中）。**关键三行仍成立**（引擎独立进程 8124／headless 只认服务间 API key／凭据不入库——本次未动任何配置与鉴权代码）。**四条待办见右栏，已登记到 F13／F11「已知限制」，本轮只记录不修。** | ① **P2 `F11` 讲解文档与 `/assistant` 页面文案仍宣称"只读不改"**，而档 3 的写工具（`batchEditPictures`／`batchUploadByUrl`）早已可用：`AssistantPage.vue` 的 subtitle（"当前只读不改"）与空状态提示（"不会改动任何数据"）是**用户可见的错误声明**，F11 作为规则 9 的入口文档又仍停留在档 2 口径 ⇒ 需一次"前端文案 + F11 回填"的小任务。**✅ 已于 2026-09-15 收口（T14，提交 `29e8ee6`）**：页面两处文案改为如实描述（可读可改、改动须用户明确要求、不支持删除），F11 的「一句话」「怎么用」「核心流程图」回填档 3 能力、已知限制 12 改写为收口说明；前端 `type-check` 138（净增 0）、改动文件 eslint 干净。口径与验收见 `docs/plan.md` T14。② P3 `runLoop` 的上限提示未判 `state`：最终回答**恰好落在第 `maxSteps` 步**时会再多一条"执行结束：达到最大步骤 (N)"回答帧（旧代码同形、非本次引入；`if (state == RUNNING && currentStep >= maxSteps)` + 1 例 `maxSteps=1 + finishOnStep` 契约测试即可收口）。③ P3 `AgentEvent.Error` 的 javadoc 写"且是本轮唯一的事件"，中途失败时不成立（此前已有 step/answer 帧）。④ P3 计划 §5 的设计（新增 `AgentExecutionException`、失败置 `state=FINISHED`）与实现（不新增异常类、置 `state=ERROR`）不一致：实现更合理，但计划正文未同步；且"一个 agent 实例只跑一次"（`ERROR` 状态粘连 + `stopped` 不复位）只在 `HuoshanAssistantAgent` 类注释写了一半，宜提到 `BaseAgent`。**✅ 已于 2026-09-15 收口（T21，提交 `e4f09a5`）**：① 上限判断加 `state == RUNNING` 前置条件 + 两条契约测试；② javadoc 订正为"替代回答帧、是本轮最后一条业务事件"并补中途失败契约测试；③ 计划 Task 3 两处标注实施偏差（**选"文档对齐代码"**），不变量整段提到 `BaseAgent` 类注释。**观察（非本次改动，与计划里已挂账的"代理侧错误语义统一"同批）**：代理自身合成的失败仍是 `answer` 帧、文案取自引擎错误体原文（"内部接口未授权：缺少或错误的 X-Internal-Api-Key"这类内部细节会进用户气泡），而前端只在 `event=error` 时才走错误分支 ⇒ 两类失败在 UI 上无法区分。 | 挂账 |

## 决定

**T8-hard 独立 review（规则 13，另开只读会话）：有条件通过**

## 理由

**复核范围**：提交链 `fb46859`→`0dd8e56`（引擎循环收敛 + 协议契约 + T8-c error 事件）。**本体成立**：① 三处协议缺口读旧代码逐一确认为真缺口、修法正确（卡死路径旧 `:208`+`:231` 双发 `[DONE]`；校验失败流发 `answer` 后直接 `complete()`；`ToolCallAgent.think()` 吞异常写 `messageList` 却不置 `FINISHED` ⇒ 同段厂商原文重复多轮，"失败当轮终止"在修复前确实要跑 5 步）；② 契约测试与失败收尾测试是真钉住（三种负向控制已实测）；③ 测试迁移是**加强**不是削弱——`ReActAgentTest` 两条"吞异常返回错误文本"用例改成"必须上抛"，`BaseAgentTest` 的 `run()` 用例全部等价改写为事件流断言，覆盖面无缺失（非 IDLE／空／null 提示词、上限、卡死、`currentStep` 归零、生命周期都在），不构成"删测试换绿灯"；④ 越界为零（改动文件 = `ai/agent` + 用户拍板的 frontend 两处 + backend 1 例测试 + docs + `.githooks`，无任何配置文件）；⑤ 安全面改善确认——失败文案固定、原文只进日志，且有断言保证不进事件流与 `messageList`。**独立复跑**：`ai/agent` 全模块 `mvn -B -o test` **152 例全绿**（门禁排除名单去掉 9 类 ⇒ 143，与文档口径一致）；前端 `npm run type-check` **138 例**（与基线持平、两个改动文件 0 命中）。**关键三行仍成立**（引擎独立进程 8124／headless 只认服务间 API key／凭据不入库——本次未动任何配置与鉴权代码）。**四条待办见右栏，已登记到 F13／F11「已知限制」，本轮只记录不修。**

## 状态

① **P2 `F11` 讲解文档与 `/assistant` 页面文案仍宣称"只读不改"**，而档 3 的写工具（`batchEditPictures`／`batchUploadByUrl`）早已可用：`AssistantPage.vue` 的 subtitle（"当前只读不改"）与空状态提示（"不会改动任何数据"）是**用户可见的错误声明**，F11 作为规则 9 的入口文档又仍停留在档 2 口径 ⇒ 需一次"前端文案 + F11 回填"的小任务。**✅ 已于 2026-09-15 收口（T14，提交 `29e8ee6`）**：页面两处文案改为如实描述（可读可改、改动须用户明确要求、不支持删除），F11 的「一句话」「怎么用」「核心流程图」回填档 3 能力、已知限制 12 改写为收口说明；前端 `type-check` 138（净增 0）、改动文件 eslint 干净。口径与验收见 `docs/plan.md` T14。② P3 `runLoop` 的上限提示未判 `state`：最终回答**恰好落在第 `maxSteps` 步**时会再多一条"执行结束：达到最大步骤 (N)"回答帧（旧代码同形、非本次引入；`if (state == RUNNING && currentStep >= maxSteps)` + 1 例 `maxSteps=1 + finishOnStep` 契约测试即可收口）。③ P3 `AgentEvent.Error` 的 javadoc 写"且是本轮唯一的事件"，中途失败时不成立（此前已有 step/answer 帧）。④ P3 计划 §5 的设计（新增 `AgentExecutionException`、失败置 `state=FINISHED`）与实现（不新增异常类、置 `state=ERROR`）不一致：实现更合理，但计划正文未同步；且"一个 agent 实例只跑一次"（`ERROR` 状态粘连 + `stopped` 不复位）只在 `HuoshanAssistantAgent` 类注释写了一半，宜提到 `BaseAgent`。**✅ 已于 2026-09-15 收口（T21，提交 `e4f09a5`）**：① 上限判断加 `state == RUNNING` 前置条件 + 两条契约测试；② javadoc 订正为"替代回答帧、是本轮最后一条业务事件"并补中途失败契约测试；③ 计划 Task 3 两处标注实施偏差（**选"文档对齐代码"**），不变量整段提到 `BaseAgent` 类注释。**观察（非本次改动，与计划里已挂账的"代理侧错误语义统一"同批）**：代理自身合成的失败仍是 `answer` 帧、文案取自引擎错误体原文（"内部接口未授权：缺少或错误的 X-Internal-Api-Key"这类内部细节会进用户气泡），而前端只在 `event=error` 时才走错误分支 ⇒ 两类失败在 UI 上无法区分。
