# 2026-09-15 — spec「关键三行」缩短（用户确认）

> 详情文件：自 `docs/decisions.md` 引用。口径以 `decisions.md` 表内那一行为准。

## 决定

用户 2026-09-15 审阅 AI 起草的缩短稿后答复"落地"，据此重写 `docs/spec.md` 的**关键三行**与 **F11** 一行：只保留合同内容，把混进去的立项史、档位推进史与逐任务进度流水账移出（内容已在别处，逐字未丢）。

按 AGENTS.md 规则 3，本改动属需要字据的四类之一（**② 改动 spec 关键三行**），故先落本字据再改文件。

## 触发：关键三行被进度流水账侵蚀

spec.md 原有 10,329 B，其中**四行占了 7,514 B（73%）**：

| 位置 | 字节 | 性质 |
|---|---|---|
| 目标 | 2,581 | 合同 + 混进的立项过程与档位推进史 |
| **F11** | **2,998** | **纯进度流水账**（全文件最大一行，且不在关键三行里） |
| 不做的事 | 1,413 | 合同 + 一条已过期的档位排除项 |
| 边界 | 522 | 基本是纯合同 |

这不是"文件大"的问题，而是**合同文件里塞了过程**：接手会话读它时要花时间分辨"哪句是要我遵守的口径、哪句是当时的进度"。这与同日「文档归属」规则要治的是同一件事。

## 改动

spec.md **10,329 B → 5,680 B（−45%）**；被改的四行 **7,514 B → 2,865 B（−62%）**。

三行现在只装合同，各带出处标注（哪一年由谁确认）：

- **目标**：产品一句话 + 已上线 URL + AI 助手一期（界面/代理/引擎位置/两个场景/词表）+ AI 打标（管理员两步式、只改 `tags` 与 `category`）+ 档位三档已完成。存量功能清单**指向下方 F1–F9**，不再复述。
- **不做的事**：存量原始排界未记录（保留"待用户补充原话"标记）+ 三条耐久排除项 + 一期四项不做 + 打标四项不做。
- **边界**：三应用（`backend/` 8123、`frontend/` 5173、`ai/` 两个独立进程）+ 建表脚本位置 + **密钥不入库** + 源仓库已归档。
- **F11**：从进度流水账改为「已到什么程度 + 未上线 + 详情指向 F11/F12/F13 与各任务记录」。

## 被移走的内容去哪了（逐条核实存在，未销毁）

| 移走的内容 | 现在住在哪 |
|---|---|
| 档 1/2/3 推进史与转向口径 | `docs/plans/records/mvp-phases.md`；`docs/decisions/2026-09-14-topic04.md` |
| "档 2 之前不做 visionTagger/批量工具/搜图/循环合并"（**已过期**，档 3 已完成） | 同上 |
| AI 打标"为什么必须落在图库侧"（`fillReviewParams` 会把图打回待审核、`checkSpaceViewPermission` 覆盖不到等） | `docs/decisions/2026-09-15-T15.md`、`docs/features/F14-AI打标.md`、`docs/plans/records/T15.md` |
| 词表建表依据（标签原为 Controller 硬编码 9 词，AI 自由打标需受治理词表） | `docs/decisions.md` 2026-09-13 立项行 |
| F11 里的 T5/T6/档 1/档 2/T22 逐项进度 | `docs/plan.md` 对应条目 + `docs/plans/records/` 各任务记录 + `docs/features/F11-AI助手MVP.md` |
| 凭据组方案 B、id 字符串透传、超时口径等 | `docs/decisions/2026-09-14-*` 多条 |

**替代方案（未采纳）**：只把 F11 那一行拆走、三行不动。否决理由——三行里的档位推进史与"为什么落在图库侧"论证同样是过程内容，只治 F11 等于半途。

## 配套：把"边界"这条合同变成机器可查

`.githooks/check-docs-consistency.mjs` 新增第 2b 项检查：**spec.md 的「边界」行里每个反引号路径必须真实存在**（`backend/`、`frontend/`、`ai/`、`backend/sql`、`ai/agent`、`ai/image-search-mcp-server`）。

理由：这条行是用户合同，而"改目录名时忘了改合同"正是本门禁要抓的漂移类。此前它只靠人记得，现在改了目录就会红。

## 未动 / 已知边界

- **F7（751 B）与 F10（502 B）未动**：F7 描述的是"停用模块的现状 + 前端残留已清理"，属容易踩坑的事实而非进度流水；F10 体量已小。
- **不改任何规则条文**：本次只改合同正文，AGENTS.md 一字未动（规则 3 要求的是"等用户确认关键三行"，已确认）。
- **门禁抓不到散文里的数字**：本次报告的所有体积数字仍是人工测量，第 4 项检查只覆盖前端 type-check 基线。这是已知边界。
