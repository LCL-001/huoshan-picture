> 自 docs/decisions.md 外移（2026-09-15）；**口径以 decisions.md 表内那一行为准**，本文件保留完整原文。

## 原始表格行

| 2026-09-14 | **档 2 里程碑独立 review（规则 13，只读会话）：有条件通过**。交付本体逐条实测成立（证据见 `%TEMP%\t11-review\`：自建桩引擎 `stub-sse.js`、并发 `saturate.js`、跨站 `csrf-page.js`）：① 凭据组原样透传（桩收到 `satoken` + `Cookie: SESSION=…` + `X-Internal-Api-Key` + `userId`/`chatId`，与浏览器真实 Cookie 逐字一致）；② 凭据零落日志（三个探针实例日志对真 token/会话值 **0 命中**，含引擎不可达与线程池拒绝路径）；③ 引擎不可达 → `answer`"引擎暂时不可用" + `[DONE]`（不挂死）；④ 线程池上界 64（70 并发实测：64 通、6 拒）；⑤ 前端 `EventSource` 断流后 15s 零重连（真浏览器跑真实 `assistantSse.ts`，桩侧每 chatId 恰好 1 次请求）；⑥ XSS 面无 sink（全仓 `v-html`/`innerHTML` 0 处，链接只认 http(s)）；⑦ 门禁实跑绿（backend 32 例；ai/agent 87 例，`.githooks` 口径）；⑧ `git diff 46d266d^..HEAD -- ai/` 空（引擎侧零改动属实）。**R4（P2，待用户拍板，涉推翻"只判存在性"）**：代理放行**任意非空白 satoken**——实测真会话 + 假 token `review-bogus-token` 仍读到真实空间；decisions 2026-09-14 那条"不一致时回 SPACE_NOT_LOGIN"只对空间维度读图（`checkSpaceViewPermission`）成立，`listSpaces` 实际只由 Spring Session 授权，**该条记录需补适用边界**；最小加固 = 代理调 `StpKit.SPACE` 现成 API 校验两把同属一人（只判身份一致性，不判权限）。**R5（P2，档 3 前必办 → 届时的 P1）**：`GET /api/ai/assistant/chat` 只认 Cookie 且有 LLM 副作用、无 nonce，实测跨站（`127.0.0.1:8135` → `localhost:8131`）**顶层导航即带齐两把 Cookie 并跑完一次 agent**（同页 no-cors 子资源请求被 Lax 拦下、回 40100）；当前工具全只读 ⇒ 危害=替受害者烧 token + 污染其会话记忆（无外泄：CORS 白名单 + Lax），**档 3 上写类工具即成跨站写操作**。**R6（P2）**：`/ai/**` 不在 `SaTokenConfigure` 拦截路径、注解会静默失效，现仅有注释/文档，建议补廉价守护测试（反射扫 `/ai/**` 控制器不得出现 Sa-Token 注解 + 断言拦截路径不含 `/ai/**`）。**P3**：`chatId` 可换行注入后端日志（实测伪造出整行）；relay 多行 `data:` 单帧发出会产出半个 JSON 帧（前端静默丢弃）；上游缺 `[DONE]` 时"完整答案 + 响应意外中断"同屏；上游非 JSON 错误体（如网关 502 的 HTML）原文进回答气泡；线程池拒绝只给空体 HTTP 500；代理 emitter 超时早于引擎数十毫秒使超时形态退化成裸断连 | review 全部判据与反证见 F11「已知限制」7-11 与最终报告；R4/R5/R6 需用户拍板后另立任务（规则 1/2 下本会话未动实现） | 生效（有条件通过） |

## 决定

**档 2 里程碑独立 review（规则 13，只读会话）：有条件通过**。交付本体逐条实测成立（证据见 `%TEMP%\t11-review\`：自建桩引擎 `stub-sse.js`、并发 `saturate.js`、跨站 `csrf-page.js`）：① 凭据组原样透传（桩收到 `satoken` + `Cookie: SESSION=…` + `X-Internal-Api-Key` + `userId`/`chatId`，与浏览器真实 Cookie 逐字一致）；② 凭据零落日志（三个探针实例日志对真 token/会话值 **0 命中**，含引擎不可达与线程池拒绝路径）；③ 引擎不可达 → `answer`"引擎暂时不可用" + `[DONE]`（不挂死）；④ 线程池上界 64（70 并发实测：64 通、6 拒）；⑤ 前端 `EventSource` 断流后 15s 零重连（真浏览器跑真实 `assistantSse.ts`，桩侧每 chatId 恰好 1 次请求）；⑥ XSS 面无 sink（全仓 `v-html`/`innerHTML` 0 处，链接只认 http(s)）；⑦ 门禁实跑绿（backend 32 例；ai/agent 87 例，`.githooks` 口径）；⑧ `git diff 46d266d^..HEAD -- ai/` 空（引擎侧零改动属实）。**R4（P2，待用户拍板，涉推翻"只判存在性"）**：代理放行**任意非空白 satoken**——实测真会话 + 假 token `review-bogus-token` 仍读到真实空间；decisions 2026-09-14 那条"不一致时回 SPACE_NOT_LOGIN"只对空间维度读图（`checkSpaceViewPermission`）成立，`listSpaces` 实际只由 Spring Session 授权，**该条记录需补适用边界**；最小加固 = 代理调 `StpKit.SPACE` 现成 API 校验两把同属一人（只判身份一致性，不判权限）。**R5（P2，档 3 前必办 → 届时的 P1）**：`GET /api/ai/assistant/chat` 只认 Cookie 且有 LLM 副作用、无 nonce，实测跨站（`127.0.0.1:8135` → `localhost:8131`）**顶层导航即带齐两把 Cookie 并跑完一次 agent**（同页 no-cors 子资源请求被 Lax 拦下、回 40100）；当前工具全只读 ⇒ 危害=替受害者烧 token + 污染其会话记忆（无外泄：CORS 白名单 + Lax），**档 3 上写类工具即成跨站写操作**。**R6（P2）**：`/ai/**` 不在 `SaTokenConfigure` 拦截路径、注解会静默失效，现仅有注释/文档，建议补廉价守护测试（反射扫 `/ai/**` 控制器不得出现 Sa-Token 注解 + 断言拦截路径不含 `/ai/**`）。**P3**：`chatId` 可换行注入后端日志（实测伪造出整行）；relay 多行 `data:` 单帧发出会产出半个 JSON 帧（前端静默丢弃）；上游缺 `[DONE]` 时"完整答案 + 响应意外中断"同屏；上游非 JSON 错误体（如网关 502 的 HTML）原文进回答气泡；线程池拒绝只给空体 HTTP 500；代理 emitter 超时早于引擎数十毫秒使超时形态退化成裸断连

## 理由

review 全部判据与反证见 F11「已知限制」7-11 与最终报告；R4/R5/R6 需用户拍板后另立任务（规则 1/2 下本会话未动实现）

## 状态

生效（有条件通过）
